---
title: "Landscape analysis for shallow neural networks: Complete classification of critical points for cubic activation and affine target functions"
title_zh: 浅层神经网络的景观分析：三次激活和仿射目标函数的临界点完全分类
authors: "Shokhrukh Ibragimov, Ilkhom Mukhammadiev, Diyora Salimova"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15173v1"
tags: ["query:ml-theory"]
score: 8.0
evidence: 三次激活浅层神经网络临界点的完整分类
tldr: 针对一维输入输出、单项式激活的浅层多项式神经网络，在仿射线性目标函数下，本文首先证明了损失下确界为零，且全局最小化存在的充要条件是隐层神经元数不小于激活度数。其次，对于三次激活函数，完整分类了所有临界点：无局部最大值，临界点不能恰好有两个不同pivots，全局最小化需至少三个不同pivots，非全局局部最小化和非平凡鞍点仅出现于所有pivots重合时。这些结果揭示了浅层多项式网络的损失景观结构，为优化理解提供了理论基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15173v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1007, \"height\": 711, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15173v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 652, \"height\": 591, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15173v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1304, \"height\": 892, \"label\": \"Figure\"}]"
motivation: 理解浅层多项式神经网络在仿射目标下的优化景观，揭示损失函数临界点的结构特性。
method: 通过解析损失函数的多项式结构，导出全局最小化存在性判据，并对三次激活情形进行临界点完全分类。
result: 给出了全局最小化存在的充要条件，以及三次激活下所有临界点的完整分类，包括pivot数目与临界点类型的关系。
conclusion: 证明了损失景观中全局最小化依赖于隐层神经元数与激活度数的匹配，以及三次激活下非平凡鞍点与局部最小化的结构约束。
---

## 摘要
本文研究了浅层多项式神经网络（PNN）在真实损失下的优化景观，其中隐藏层有$\mathfrak{h} \in \mathbb{N}$个神经元，输入和输出层是一维的，激活函数为次数$d \in \mathbb{N}$的单项式，训练目标函数为非恒定仿射线性函数。我们的第一个主要结果为任意激活次数$d$提供了\textit{全局最小化器}的尖锐存在/不存在判据及其必要的结构条件。我们证明损失的下确界总是零，并且可以通过至少$d$个活跃且可见的隐藏神经元（即具有非零内权和外权的隐藏神经元）且具有两两不同的枢轴来实现。相反，如果$\mathfrak{h} < d$，则下确界无法达到，任何最小化参数序列必然发散到无穷。在第二个主要结果中，我们提供了三次激活下损失函数所有临界点的完整分类。我们证明损失景观不存在\textit{局部最大化器}，临界点不能恰好有两个不同的枢轴，全局最小化器至少需要三个不同的枢轴，没有活跃隐藏神经元的临界点只对应\textit{鞍点}，因此\textit{非全局局部最小化器}和非平凡鞍点仅出现在所有枢轴重合的网络中。此外，非全局局部最小化器要求所有隐藏神经元都是活跃且可见的，并且恰好有一个隐藏神经元的斜率符号与目标函数匹配。我们的第二个主要结果还保证，不是全局最小化器的临界点中的每个隐藏神经元对其对应的实现函数要么有输入依赖贡献，要么贡献为零，但绝无非零的输入无关贡献。

## Abstract
In this paper, we study the optimization landscape induced by the true loss for shallow polynomial neural networks (PNNs) with $\mathfrak{h} \in \mathbb{N}$ neurons on the hidden layer, one-dimensional input and output layers, and a monomial activation of degree $d \in \mathbb{N}$, trained against a non-constant affine linear target function. Our first main result provides for arbitrary activation degree $d$ a sharp existence/non-existence criterion for \emph{global minimizers} with necessary structural conditions. We show that the infimum of the loss is always zero and achievable with at least $d$ active and visible hidden neurons -- that is, hidden neurons with non-zero inner and outer weights -- with pairwise distinct pivots. In contrast, if $\mathfrak{h} < d$, then the infimum cannot be attained and any minimizing sequence of parameters necessarily diverges to infinity. In the second main result, we provide a complete classification of all critical points of the loss function for the cubic activation. We show that the loss landscape admits no \emph{local maximizers}, critical points cannot have exactly two distinct pivots, global minimizers require at least three distinct pivots, critical points with no active hidden neurons correspond to \emph{saddle points} only, and consequently, \emph{non-global local minimizers} and non-trivial saddle points arise only in networks where all pivots coincide. Moreover, non-global local minimizers require all hidden neurons to be active and visible with exactly one hidden neuron having a slope sign matching that of the target function. Our second main result also guarantees that each hidden neuron of a critical point that is not a global minimizer has either input-dependent or zero contribution, but has no nonzero input-independent contribution, to its corresponding realization function.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：理解浅层多项式神经网络（PNN）在仿射线性目标函数下的优化景观几何。深度学习中损失函数的非凸性导致训练陷入局部极小或鞍点，而单项式激活函数使损失成为参数的多项式，从而允许精确分析。
- **核心问题**：确定全局最小化存在的充要条件，并对三次激活情形下所有临界点（全局最小、非全局局部最小、鞍点）进行完整分类，揭示激活度数与网络宽度、pivot 数目与临界点类型之间的关系。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（文字说明）
- **核心思想**：利用损失函数的多项式结构推导梯度与 Hessian 的显式表达式，结合多项式表示论（Vandermonde 行列式、移位单项式表示）建立全局最小化存在性判据；对三次激活，通过分析临界点方程并构造扰动方向（上升/下降方向）完成分类。
- **关键技术细节**：
  - 将网络参数分为内权、内偏置、外权、外偏置，并定义 pivot = -b/w（当 w≠0）。
  - 通过 Lemma 2.2 和 2.3 给出梯度分量和 Hessian 条目公式。
  - 全局最小化判据（Theorem 1.1/Proposition 3.8）：损失下确界为零，全局最小化存在当且仅当隐层神经元数 h ≥ 激活度 d，且需要至少 d 个活跃且可见的隐藏神经元具有两两不同的 pivot。
  - 三次激活分类（Theorem 1.2/Theorem 5.19）：
    - 损失函数无局部极大值。
    - 临界点不能恰好有两个不同 pivot。
    - 全局最小化：至少三个不同 pivot 且损失为零。
    - 非全局局部最小化：所有 pivot 重合（pivot = (a+b)/2），且恰好一个隐藏神经元的斜率符号与目标函数匹配（η=1），实现函数为特定三次函数。
    - 鞍点：所有 pivot 重合且 η>1，或 pivot 为另外两个固定值，或无活跃神经元（常数实现）。
- **公式示例**：损失函数 L(θ) = ∫_a^b |N_θ(x)-f(x)|² dx，N_θ(x)=c+∑v_i (w_i x+b_i)^d。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- 本文为纯数学理论分析，没有设计任何数值实验或使用数据集。
- 无 benchmark 对比。
- 仅证明了理论结果（如 Lemma 4.8、Proposition 5.16–5.18），并用 Wolfram Mathematica 验证了部分符号计算。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点
- 论文未提及任何 GPU、计算集群或训练时长。所有结果均通过数学推导得到，仅“部分符号计算使用 Wolfram Mathematica 验证”，未报告具体硬件。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平
- 无实验。理论证明本身是充分且严谨的，覆盖了所有可能临界点的分类。但缺乏数值实验验证理论在实际训练中的体现，不过这是理论论文的正常特征。

### 6. 论文的主要结论与发现
- **对于任意单项式激活 d**：损失下确界为零；全局最小化存在当且仅当 h ≥ d；窄网络（h<d）中任何最小化序列必须发散。
- **对于三次激活**：
  - 无局部极大值。
  - 临界点不可能正好有两个不同 pivot。
  - 全局最小化：至少三个不同 pivot，损失为零。
  - 非全局局部最小化：所有 pivot 重合于区间中点，恰好一个神经元斜率与目标函数同号，实现函数为特定三次多项式。
  - 鞍点：两种情况：① 所有 pivot 重合但 η>1；② pivot 为另外两个固定值（区间中点 ± (b-a)/(2√7)）；③ 无活跃神经元且实现恒等于 f((a+b)/2)。

### 7. 优点：方法或实验设计上有哪些亮点
- **数学严格性**：利用多项式表示论和 Vandermonde 行列式给出精确的表示性判据，不依赖任何近似或概率假设。
- **完整分类**：对三次激活实现了所有临界点的完全刻画，包括鞍点与非全局局部极小的精确结构。
- **工具通用性**：提出的梯度/ Hessian 显式公式、扰动构造方法可推广到更高次单项式激活。
- **揭示结构**：清晰地揭示了 pivot 数目、神经元活跃性、斜率符号与临界点类型之间的约束关系，特别是非平凡鞍点和局部极小仅在所有 pivot 重合时出现。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **应用限制**：仅考虑一维输入输出、仿射线性目标、单项式激活。真实世界网络通常使用 ReLU、高维输入、复杂目标，本文结论不能直接推广。
- **激活限制**：完整分类仅针对三次激活（d=3），更高次的情形留待未来工作。
- **无数值实验**：缺少对理论预测在实际训练中是否成立的验证。
- **忽略采样效应**：研究的是真实损失（population loss），而非经验损失，未考虑有限样本下的随机性。

（完）
