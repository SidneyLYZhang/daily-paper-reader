---
title: Fast approximation and learning of binary classification tasks in o-minimal structures using ReLU neural networks
title_zh: 使用ReLU神经网络在o-极小结构中的二分类任务的快速逼近与学习
authors: "Clemens Kinn, Philipp Petersen"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2607.01266v1"
tags: ["query:ml-theory"]
score: 8.0
evidence: 代数几何（o-minimal）用于神经网络逼近
tldr: "针对o-minimal结构中定义集的二分类问题，引入可迹集作为决策区域代理。利用ReLU神经网络逼近可迹集的特征函数，在边界函数具有一致连通分量界和C^m光滑延拓条件下，获得了L^p逼近误差为O(ε^{-p(n-1)/m})的速率，且网络深度与ε无关。结合网络类的熵估计，证明了基于铰链损失的经验风险最小化分类器的期望误分类误差阶为N^{-m/(m+pn-p)}。该工作建立了模型定量逼近与学习理论率的联系。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01266v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 455, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01266v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 724, \"label\": \"Figure\"}]"
motivation: o-minimal结构中的定义集决策边界复杂，需量化神经网络逼近可迹集的精度并推导学习误差率。
method: 利用胞分解引入可迹集，构造ReLU神经网络逼近其特征函数，并基于熵估计分析经验风险最小化。
result: "逼近速率O(ε^{-p(n-1)/m})，学习误差率N^{-m/(m+pn-p)}（多项式因子忽略）。"
conclusion: 建立了非光滑决策区域由神经网络逼近并泛化的定量理论基础。
---

## 摘要
我们研究决策集由实数域的o-极小扩张中的可定义集给出的二分类问题。受可定义集胞分解的启发，我们引入可追踪集作为可定义决策区域的经典代理，并分析其通过ReLU神经网络的逼近。在连通分量数量的一致界和边界函数的适当C^m扩展条件下，我们证明[-1/2,1/2]^n的可追踪子集的特征函数可以通过大小为O(ε^{-p(n-1)/m})的ReLU神经网络在L^p中以精度ε>0逼近，其中深度与ε无关且权重多项式有界。这为使用ReLU神经网络逼近o-极小结构中的某些可定义集合族建立了定量逼近率。相同方法还可为可定义映射[-1/2,1/2]^n→R的子类得到所述逼近率。然后，我们将逼近能力与ReLU神经网络类的熵估计相结合，获得使用铰链损失的经验风险最小化的统计学习率。对于N个均匀分布的样本，所得分类器的期望误分类误差阶为N^{-m/(m+pn-p)}，直至任意小的多项式损失。

## Abstract
We study binary classification problems whose decision sets are given by definable sets in o-minimal expansions of the real field. Motivated by cell decomposition of definable sets, we introduce traceable sets as a classical proxy for definable decision regions and analyze their approximation by ReLU neural networks. Under uniform bounds on the number of connected components and suitable $C^m$ extensions for the boundary functions, we prove that characteristic functions of traceable subsets of $[-1/2,1/2]^n$ can be approximated in $L^p$ to accuracy $\varepsilon>0$ by ReLU neural networks of size $\mathcal{O}(\varepsilon^{-p(n-1)/m})$, with depth independent of $\varepsilon$ and polynomially bounded weights. This establishes quantitative approximation rates for certain definable collections in o-minimal structures using ReLU neural networks. The same approach also yields the stated approximation rates for a subclass of definable maps $[-1/2,1/2]^n \to \mathbb{R}$. We then combine the approximation capabilities with entropy estimates for ReLU neural network classes to obtain statistical learning rates for empirical risk minimization with hinge loss. For $N$ uniformly distributed samples, the resulting classifiers achieve expected misclassification error of order $N^{-m/(m+pn-p)}$ up to an arbitrarily small polynomial loss.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：针对二分类问题，当决策区域由**o‑极小结构**（o‑minimal structures）中的可定义集（definable sets）给出时，如何利用 ReLU 神经网络实现高效的**逼近**与**统计学习**，并获得定量的收敛速率。

- **研究动机**：
  - 实际分类问题中决策边界往往具有几何规律性，传统的光滑性假设（如边界为 $C^m$ 或 Barron 类）虽然有效，但可定义集框架能涵盖更广泛的非光滑、但仍是“驯顺”的集合（如半代数集、Pfaffian 函数图等），其具有均匀的有限性质（如一致有界的连通分量数、有限 VC 维）。
  - o‑极小结构中的胞分解定理为描述此类复杂边界提供了结构化的工具，且神经网络中的 ReLU 激活函数本身是半代数的，因此神经网络与 o‑极小结构有自然的兼容性。
  - 此前工作已证明 o‑极小结构中的可定义集具有 PAC 可学习性，但缺乏**定量的逼近率和学习率**。本文旨在填补这一空白，建立从逼近到学习的完整理论。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  1. **引入“可追踪集”（traceable sets）**：模仿 o‑极小结构中 $C^m$‑胞的形状，将可定义决策区域抽象为有限个可追踪集的不交并。可追踪集通过一族光滑边界函数递归定义，并具有“扩展性质”（extension property），允许将边界函数延拓为整个 $Q^{i-1}$ 上的 $C^m$ 函数。
  2. **逼近可追踪集的特征函数**：利用可追踪集边界函数的延拓，构造其“地平函数”（horizon functions），这些函数属于 $HF_{B}^{m,n}$ 类。再借助已有结果（[PV18] Theorem 3.4）用 ReLU 神经网络逼近地平函数。然后通过取最小值和最大值操作，组合出原特征函数的逼近网络。
  3. **统计学习率的推导**：基于上述逼近结果，设置一个具有有界深度、大小和权重的 ReLU 网络类。使用**经验风险最小化**（ERM）与**铰链损失**，结合网络类的**熵估计**（covering entropy）与 [PV21] 中的通用学习定理，获得泛化误差上界。

- **关键技术细节**：
  - **可追踪集的定义**（Definition 3.1）：$A\subseteq\mathbb{R}^n$ 称为 $m$‑可追踪，若存在 $A_0,\dots, A_n$ 和有界 $C^m$ 函数 $f_i,g_i:A_{i-1}\to\mathbb{R}$，使得 $A_i=(f_i,g_i)_{A_{i-1}}$，且 $A_n=A$。这实际上是对定向 $C^m$‑胞的精确模拟，但允许边界函数定义在闭包上并具有光滑延拓。
  - **$(m,B)$‑扩展性质**：每个 $f_i,g_i$ 能延拓为整个 $Q^{i-1}$ 上的 $C^m$ 函数，且 Sobolev 范数 $\le B$。这保证了地平函数属于标准的光滑类。
  - **逼近构造**：
    - 对每个 $A\in K_{B}^{m,n}$，构造地平函数 $\underline{F}_i,\overline{G}_i$（分别为 $H_i(x_i-F_i(x_{<i}))$ 和 $H_i(G_i(x_{<i})-x_i)$）。
    - 用 ReLU 网络 $\Phi_{\varepsilon}^{\underline{F}_i}$ 逼近 $\underline{F}_i$，误差 $\varepsilon$；类似地逼近 $\overline{G}_i$。
    - 通过求最小值网络 $\Phi_{\min}^{2n}$ 组合 $2n$ 个网络得到 $\Phi_{\varepsilon}^{A}$，误差 $2n\varepsilon$。
    - 对有限并 $\Omega = \cup A_j$，再用最大值网络组合得 $\Phi_{\varepsilon}^{\Omega}$。
  - **学习率推导**：设置网络类 $\mathcal{F}_N$ 尺寸为 $\mathcal{O}(N^{p(n-1)/(m+pn-p)})$，最大权重 $\mathcal{O}(N^{sm/(m+pn-p)})$。应用 [PV21] Theorem 5.6 的条件，通过熵界验证，得到期望误分类误差 $\lesssim N^{\kappa - m/(m+pn-p)}$（$\kappa>0$ 任意小）。

- **算法流程（文字说明）**：
  1. 输入：目标集 $\Omega \in K_{B,\ell}^{m,n}$，精度 $\varepsilon$。
  2. 对每个 $A_j$（$j=1,\dots,\ell$）：
     - 选择 $C^m$ 延拓 $F_{ji},G_{ji}$；
     - 计算地平函数 $\underline{F}_{ji},\overline{G}_{ji}$；
     - 调用 [PV18] 构造逼近网络 $\Phi_{\varepsilon}^{\underline{F}_{ji}},\Phi_{\varepsilon}^{\overline{G}_{ji}}$；
     - 用 $\Phi_{\min}^{2n}$ 组合为 $\Phi_{\varepsilon}^{A_j}$。
  3. 用 $\Phi_{\max}^{\ell}$ 组合所有 $\Phi_{\varepsilon}^{A_j}$ 得到 $\Phi_{\varepsilon}^{\Omega}$。
  4. 逼近误差：$\|\chi_{\Omega}-\Phi_{\varepsilon}^{\Omega}\|_{L^p}<\varepsilon$（通过适当调整 $\varepsilon$ 的因子实现）。
  5. 学习阶段：对样本量 $N$，取 $\varepsilon = N^{-m/(m+pn-p)}$，定义网络类 $\mathcal{F}_N$，执行经验风险最小化（铰链损失），输出分类器 $\chi_{[1/2,\infty)}\circ\hat{\Phi}_S$。

## 3. 实验设计

- **实验**：论文**未包含任何数值实验或模拟**。这是一篇纯理论论文，所有结果均为数学定理及证明。
- **数据集/基准/对比方法**：不存在。理论分析中未涉及具体数据集，也未与现有方法进行实证比较。
- **替代说明**：论文在“Final remarks”中简要讨论了 $m=0$（Lipschitz）情形，但同样未进行数值验证。

## 4. 资源与算力

- **未报告**：由于是纯理论工作，未涉及任何 GPU 训练、计算资源或算力需求。所有结果均为解析推导。

## 5. 实验数量与充分性

- **实验数量为零**。因此无法评价实验的充分性、客观性或公平性。
- **理论充分性**：从理论角度，论文给出了完整的逼近构造和泛化上界证明，逻辑链条清晰，定理的条件（可追踪性、扩展性质、边界光滑性）均有明确定义，并联系了 o‑极小结构中的可定义性。但**缺乏实证验证**，无法确认这些理论界在实际问题中的紧致性或可达到性。

## 6. 论文的主要结论与发现

- **逼近定理（Theorem 3.3/7.8）**：对任意 $\Omega \in K_{B,\ell}^{m,n}$ 和 $0<\varepsilon<1/2$，存在 ReLU 神经网络 $\Phi_{\varepsilon}^{\Omega}$，其深度 $\le \lceil\log_2((\ell+1)n)\rceil + (3+\lceil\log_2 m\rceil)(14+2m/n)$，大小 $\le c\cdot\varepsilon^{-p(n-1)/m}$，权重 $\le \varepsilon^{-s}$，使得 $\|\Phi_{\varepsilon}^{\Omega}-\chi_{\Omega}\|_{L^p(Q^n)}<\varepsilon$。
- **学习率定理（Theorem 3.5/8.2）**：基于上述逼近类进行 ERM，对于 $N$ 个均匀独立样本，期望误分类误差 $\lesssim N^{\kappa - m/(m+pn-p)}$，其中 $\kappa>0$ 可任意小。
- **应用意义**：o‑极小结构中的可定义集合族（如半代数集、Pfaffian 集等）在满足 $m$‑可容许性时，上述逼近和学习率成立。这为**将模型论中的驯顺性作为分类问题的新正则假设**提供了理论依据。

## 7. 优点

- **理论创新**：将 o‑极小结构的胞分解与神经网络逼近理论有机结合，首次给出了**可定义集合的定量逼近率和学习率**，拓展了分类问题中“边界正则性”的概念范畴。
- **方法优雅**：通过引入“可追踪集”和“扩展性质”，将复杂的可定义集化简为类似定向胞的结构，并利用地平函数与已知的光滑函数逼近结果，实现了从光滑到非光滑（可定义）的跨越。
- **结构清晰**：论文自包含性强，从定义、引理到主定理的证明层层递进；附录（或正文）给出了所有必要的神经网路构造引理（并行化、求极值、相乘等）。
- **统一框架**：采用 $(m,B,\ell)$‑扩展性质统一处理连通分量数量与边界光滑度，学习率的上界自然体现了维数 $n$、光滑度 $m$ 和 $p$ 的相互作用，指数 $m/(m+pn-p)$ 在特殊情况下可退化至已有结果。

## 8. 不足与局限

- **缺乏实验验证**：所有结果都是理论性的，未在合成或真实数据集上验证所提出方法的实际性能。理论界可能并非紧致（最优），且实际训练中 ERM 的实现（非凸优化）可能无法达到理论保证。
- **假设强**：需要决策区域是 $(m,\ell)$‑可追踪且具有 $(m,B,\ell)$‑扩展性质。虽然 Lemma 6.14 表明任何有界可定义集可以任意精度被具有扩展性质的可追踪集逼近，但逼近过程会改变边界的光滑性，且所需的 $B$ 可能很大。这限制了直接应用。
- **仅限均匀分布**：学习率假设样本来自 $Q^n$ 上的均匀分布。对于一般分布，结论不一定成立，需要额外的密度条件。
- **网络深度恒定但大小依赖指数**：逼近定理中深度与 $\varepsilon$ 无关，但网络大小以 $\varepsilon^{-p(n-1)/m}$ 增长。在高维情形（$n$ 大），该指数可能很大，导致实际计算代价高昂。
- **未覆盖 $m=0$ 情形**：虽然“Final remarks”简要提到了 Lipschitz 情况，但全文主要定理均要求 $m\ge 1$。对于仅仅 Lipschitz 的边界，逼近率会退化，且该情况未作为正式定理给出。
- **静态边界**：所有结果针对固定的决策区域，没有考虑在线学习或动态环境。

（完）
