---
title: A Tractable Pseudo-Metric on Non-Parametric Exponential Statistical Manifolds via SPD Geometry
title_zh: 基于SPD几何的非参数指数统计流形上的可计算伪度量
authors: "Amit Vishwakarma, K. S. Subrahamanian Moosath"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11092v1"
tags: ["query:npl-bdl-saig"]
score: 9.0
evidence: 指数流形上的信息几何与SPD几何
tldr: 非参数统计流形上的距离计算因无限维函数空间而不可解。本文提出两阶段框架：先通过矩匹配将分布投影到有限维指数族，再将其嵌入对称正定矩阵流形。该框架导出可计算伪度量，其拉回度量与Fisher-Rao相差一个三阶累积量校正项。关键结果：伪度量下界可从样本矩闭式计算，且仿射不变。应用于双样本检验，无需带宽调参。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.11092v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1206, \"height\": 861, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11092v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1330, \"height\": 844, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11092v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1410, \"height\": 686, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11092v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1363, \"height\": 480, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.11092v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 767, \"height\": 279, \"label\": \"Table\"}]"
motivation: 解决非参数统计流形上距离计算不可行问题，实现可计算伪度量。
method: 两阶段框架：投影到有限维指数族，再嵌入SPD流形，利用仿射不变度量给出伪度量下界。
result: 得到闭式伪度量下界，仿射不变，无需调参，通过置换检验实现双样本检验。
conclusion: 提供了可计算的非参数统计流形伪度量，拓展了SPD几何的应用。
---

## 摘要
在非参数统计流形上计算概率分布之间的距离本质上是难以处理的。测地线方程存在于无限维函数空间中，且没有通用的闭式解。我们开发了一个两阶段框架，在Pistone-Sempi指数流形上生成一个可计算的伪度量，并将其应用于两样本假设检验。在第一阶段，通过矩匹配将任意分布投影到选定的有限维参数指数族上。该投影是多对一的，因此结果是伪度量而非真正度量。在第二阶段，通过增广充分统计量向量的期望外积，将参数族嵌入到对称正定矩阵流形中。该嵌入是光滑微分同胚。诱导的后拉度量与Fisher-Rao度量相差一个显式修正项，该修正项涉及充分统计量的三阶联合累积量；对于高斯族，修正项消失，恢复Calvo-Oller嵌入作为特例。环境矩阵流形上的仿射不变黎曼度量随后为伪度量提供了一个闭式下界，可直接从样本矩计算。应用于两样本检验时，该框架产生一个仿射不变的检验统计量，且无需连续调优参数（如带宽）。目标指数族的选择决定了比较哪些矩。临界值通过置换获得。

## Abstract
Computing distances between probability distributions on non-parametric statistical manifolds is fundamentally intractable. The geodesicequations live in infinite-dimensional function spaces and admit no general closed-form solution. We develop a two-stage framework that produces a computable pseudo-metric on the Pistone--Sempi exponential manifold and apply it to two-sample hypothesis testing. In the first stage, an arbitrary distribution is projected onto a chosen finite-dimensional parametric exponential family via moment-matching. This projection is many-to-one, so the resulting object is a pseudo-metric rather than a true metric. In the second stage, the parametric family is embedded into the manifold of symmetric positive definite matrices via the expected outer product of the augmented sufficient statistics vector. The embedding is a smooth diffeomorphism. The induced pullback metric differs from the Fisher--Rao metric by an explicit correction involving third-order joint cumulants of the sufficient statistics; the correction vanishes for the Gaussian family, recovering the Calvo--Oller embedding as a special case. The affine-invariant Riemannian metric on the ambient matrix manifold then provides a closed-form lower bound for the pseudo-metric, computable directly from sample moments. Applied to two-sample testing, the framework produces a test statistic that is affine-invariant and requires no continuous tuning parameters such as a bandwidth. The choice of target exponential family determines which moments are compared. Critical values are obtained by permutation.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：在非参数统计流形（Pistone–Sempi 指数流形）上，概率分布之间的距离计算是本质困难的，因为测地线方程需要求解无限维函数空间中的两点边值问题，无法获得闭式解。
- **动机**：经典散度（如 KL 散度）不提供可计算的距离；Fisher–Rao 距离虽具有几何意义，但在非参数情形下不可解；现有方法（如核均值嵌入、平方根嵌入）要么需要调参、要么缺乏与流形几何的直接联系。
- **整体含义**：本文提出一个两阶段框架，将无限维问题约化为有限维参数指数族上的可计算伪度量，并通过 SPD 几何提供闭式下界，为信息几何在统计推断（尤其两样本检验）中提供实用工具。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：通过组合两个映射——① m-投影（矩匹配）将无限维分布投影到有限维参数指数族；② 将参数族嵌入对称正定（SPD）矩阵流形，并拉回仿射不变黎曼度量（AIRM）——在原始流形上诱导出一个可计算的伪度量。
- **第一阶段：m-投影**  
  - 对于任意分布 \( q \in E_p \)，最小化 KL 散度 \( D_{\text{KL}}(q \| p_\theta) \) 得到投影 \( \pi^{(m)}(q) = p_{\theta^*} \)。  
  - 最优条件：矩匹配方程 \( E_{p_{\theta^*}}[u_j] = E_q[u_j] \)，其中 \( u_j \) 是所选指数族的充分统计量。该投影是多对一的，因此结果只是伪度量。
- **第二阶段：SPD 嵌入**  
  - 定义增广向量 \( V(x) = (u_1(x), \dots, u_d(x), 1)^\top \)，嵌入映射 \( F(p_\theta) = E_{p_\theta}[V V^\top] \)，得到块结构矩阵 \( S(\theta) = \begin{pmatrix} A(\theta) & \eta(\theta) \\ \eta(\theta)^\top & 1 \end{pmatrix} \)，其中 \( A \) 是二阶矩矩阵，\( \eta \) 是均值参数。  
  - 证明了 \( F \) 是光滑微分同胚到像集 \( M \subset \text{SPD}(d+1) \)。  
  - 拉回 AIRM 得到 \( N \) 上的黎曼度量 \( g^F \)，其线元与 Fisher–Rao 度量的差异由三阶累积量修正（式 (65)–(74)）；当三阶累积量为零时（如高斯族），两者相等。
- **距离下界**：由 AIRM 距离 \( d_{\text{AIRM}}(F(\theta_1), F(\theta_2)) \) 可闭式计算（式 (39)），且满足 \( d_{\text{AIRM}} \le d_F \)，等号成立当且仅当 AIRM 测地线完全落在 \( M \) 上。
- **部分嵌入与多项式判据**：若忽略的统计量可表示为保留统计量的至多二次多项式，则部分嵌入仍保持单射（如高斯族线性统计量二次项可恢复）；否则需全嵌入（如 Gamma 族）。
- **两样本检验应用**：对样本计算经验 SPD 嵌入矩阵 \( \hat S_X, \hat S_Y \)，以 \( d_{\text{AIRM}} \) 为检验统计量，通过置换获得临界值（Algorithm 1）。检验仿射不变、无需调参，仅需选择目标指数族以指定比较的矩。

## 3. 实验设计：数据集、场景、对比方法
- **数据集/场景**：使用合成数据。  
  - 例1：非参数分布对比：Laplace(0,1) vs Logistic(0.5, √(4.5/π))，样本量 \( m=n=200 \)。  
  - 例2：纯方差偏移：两个 Logistic 分布均值均为 0，方差比 \( r = \text{Var}(q_2)/\text{Var}(q_1) \) 从 1 到 3，样本量 \( m=n=100 \)。
- **基准方法**：  
  - Hotelling’s \( T^2 \)（仅比较均值）。  
  - 最大均值差异（MMD）使用高斯核、中位数带宽（每轮置换重算）。  
- **对比方式**：三种方法均通过置换检验（B=2000 或 B=200）得到 p 值，显著性水平 α=0.05。例2中每个方差比重复 200 次独立试验，计算经验拒绝率（功效）。

## 4. 资源与算力
- 论文中未明确提及所使用的硬件资源（如 GPU 型号、数量、训练时长）。所有实验均为合成数据上的小规模模拟，计算量很小，不需要特别的高性能计算资源。

## 5. 实验数量与充分性
- **实验数量**：两个主要场景。例1仅展示一次检验结果，例2系统考察了 r=1.0, 1.5, 2.0, 3.0 四种方差比，每种进行 200 次独立试验，统计拒绝率。
- **充分性**：例2的功效比较覆盖了效果量从零到大的范围，并与 MMD 和 Hotelling T² 进行了公平对比（均使用置换检验）。但缺乏对不同样本量（如 n=50, 500）、非高斯指数族（如 Gamma 族）下的检验行为、以及更多元分布偏移类型（如偏度、峰度差异）的实验。此外，仅对比了高斯核 MMD（带宽中位数法则），未与其他核或自动调参方法比较。总体上实验广度有限，但核心比较（方差偏移）有说服力。
- **公平性**：三种检验均采用相同的置换机制，比较方式公正。AIRM 检验无需调参，MMD 需每轮重算带宽，略有计算优势，但不影响公平性。

## 6. 主要结论与发现
- 提出并理论证明了两阶段框架：通过 m-投影将非参数分布压缩到有限维指数族，再嵌入 SPD 流形，诱导出可计算的伪度量。
- 后拉度量 \( g^F \) 与 Fisher–Rao 度量之差由三阶联合累积量显式给出；对高斯族两者一致。
- 获得关键不等式 \( d_{\text{AIRM}} \le d_F \)，等号成立当且仅当 AIRM 测地线位于嵌入子流形内；固定均值切片是完全测地子流形（如 Calvo–Oller 嵌入）。
- 应用于两样本检验时，所提 AIRM 检验统计量仿射不变、无需连续调参，在纯方差偏移场景下功效显著高于 MMD 和 Hotelling's T²（表1）。
- 伪度量有局限性：无法区分矩匹配相等的分布（即仅比较所选的充分统计量期望）。

## 7. 优点
- **理论严谨性**：在 Pistone–Sempi 非参数流形上建立了完整的几何框架，证明了嵌入微分同胚、度量差显式公式、距离不等式等核心性质。
- **可计算性**：伪度量的下界（AIRM 距离）可从样本矩闭式计算，无需求解测地线或优化。
- **仿射不变性**：检验统计量自然具有仿射不变性（AIRM 的齐性），这是 MMD 不具备的性质。
- **无调参**：除选择目标指数族外，无带宽、核函数等连续调参参数，简化了使用。
- **可解释性**：指数族的选择直接对应比较哪些矩（如均值、方差、高阶矩），比核方法的黑箱调参更直观。

## 8. 不足与局限
- **多对一投影导致信息损失**：伪度量只保留目标指数族的矩信息，分布在高阶矩上的差异完全被忽略（如 Laplace 与 Logistic 同矩时无法区分）。
- **必须预选指数族**：应用者需要根据问题选择合适的指数族，这本身也是一种选择（类似于核方法选核），且错误选择可能导致检验无功效。
- **实验覆盖有限**：仅比较了高斯族作为投影目标，未展示 Gamma 族或其他指数族下的性能。仅用合成数据，未使用真实数据集。
- **理论假设较强**：非参数分布需属于 Pistone–Sempi 最大指数模型 \( E_p \)，且 m-投影的矩向量须落在均值参数空间内部（边界情况不保证存在性）。
- **无法给出精确小样本性质**：临界值依赖置换，非解析，且计算消耗随样本量和置换次数线性增长。
- **与 Fisher–Rao 距离的关系**：虽然给出度量差公式，但真正的 Fisher–Rao 距离不可计算，只能以下界近似；当子流形非完全测地时，下界可能很松。

（完）
