Title: Local large deviations for linear-region growth in random piecewise-linear networks

URL Source: https://arxiv.org/pdf/2607.07014v1

Published Time: Thu, 09 Jul 2026 00:28:16 GMT

Number of Pages: 23

Markdown Content:
LOCAL LARGE DEVIATIONS FOR LINEAR-REGION GROWTH IN RANDOM PIECEWISE-LINEAR NETWORKS 

RECEP ¨OZKAN AND CHRISTIAN HIRSCH 

Abstract. We study a random compositional model for the growth of affine regions in deep piecewise-linear networks. The model is generated by i.i.d. perturbations of the symmetric height-one tent map, and the main observable is the number Nn of affine pieces after n layers. We prove the existence of a submultiplicative pressure for Nn, yielding exponential upper bounds for both tails of n−1 log Nn. The same argument applies to abstract submultiplicative complexity observables and gives higher-dimensional extensions for convex-polytopal affine-cover counts and worst-line affine-piece counts. Since the true branch count has no matching supermultiplicative inequality, lower bounds require a separate certified construction. We introduce a finite-state defect process that records branches whose future splitting can be guaranteed, and use bridge words to obtain constructive upper-tail lower bounds. In a uniformly favorable small-noise regime, this process is governed by a companion matrix whose Perron root tends to 2, implying eventual exclusion of lower tails below log 2 − ξ.

1. Introduction 

The geometry of linear regions is a standard proxy for complexity in deep piecewise-linear net-works. For ReLU and related activations, the input space is partitioned into polyhedral cells on each of which the network computes an affine map, and a substantial literature studies how the number and arrangement of these cells depend on architecture and depth [3, 16, 13, 18, 20]. At one end of this literature lie explicit depth-separation constructions, most notably Telgarsky’s sawtooth construction, where repeated composition of a one-dimensional folding map creates exponentially many oscillations or regions [21, 22]. At the other end lie results showing that, for standard ran-dom initialization, this worst-case mechanism is highly nongeneric [15, 7]. Figure 1 illustrates the type of geometric complexity measured by linear-region counts in a simple two-dimensional ReLU network. The present paper studies a one-dimensional random compositional model in which the same region-counting problem can be analyzed through submultiplicative pressure and certified lower processes. The purpose of the present paper is to study a model that sits between these two viewpoints. We keep the compositional folding mechanism of a one-dimensional depth-separation construction, but we randomize the layer parameters. The resulting process is no longer deterministic, and the 

Middle East Technical University, Ankara, Turkey Department of Mathematics, Aarhus University, Aarhus, Denmark 

E-mail addresses : rozkan@metu.edu.tr, hirsch@math.au.dk .2020 Mathematics Subject Classification. Primary 60F10; Secondary 68T07, 37H12, 37E05. 

Key words and phrases. Large deviations, affine regions, linear regions, piecewise-linear neural networks, random compositions, tent maps, submultiplicative pressure.      

> 1arXiv:2607.07014v1 [math.PR] 8 Jul 2026 2LARGE DEVIATIONS FOR LINEAR REGIONS 101
> x1
> 1
> 0
> 1
> x2
> 2 hidden layers, 97 observed patterns
> 101
> x1
> 1
> 0
> 1
> x2
> 5 hidden layers, 518 observed patterns

Figure 1. Linear regions of randomly initialized ReLU networks on the input square [ −1, 1] 2. Colors indicate different activation patterns, and hence different affine regions of the realized piecewise-affine map. The left panel uses two hidden layers, while the right panel uses five hidden layers. The figure is illustrative only; the results of this paper concern the exact one-dimensional branch-count process generated by random tent-map compositions. natural observable becomes the exponential growth rate of the number of affine pieces. Our goal is to understand this growth from a large-deviation point of view. Large-deviation principles (LDPs) for random growth observables often arise from subadditive, superadditive, or approximately additive structures [19]. The present model exhibits a related but asymmetric structure. The branch-count admits a submultiplicative inequality, which yields pressure bounds and exponential large-deviation upper estimates. However, no corresponding su-permultiplicative inequality is available for the true branch count. This asymmetry is the reason that the lower-bound theory below requires additional geometric structure. There are substantial LDPs for Galton–Watson processes, branching processes in random environment, and branching random walks; see, for example, [4, 14, 5, 2]. This theory, however, does not apply directly to the affine-region count studied here. The affine pieces are not particles with conditionally independent reproduction. They are cells of a random piecewise-affine partition, and their future splitting de-pends on their geometric position and on the common future layers shared by all cells. Thus, the term “branch” is used below in a geometric sense, not in the probabilistic sense of a Galton–Watson or branching-random-walk genealogy. Recent work has developed LDPs for a broad range of neural-network observables and asymptotic regimes, including outputs and covariance processes of Gaussian deep networks, deep networks with ReLU or other linearly growing activations, functional output processes in wide or deep limits, and empirical weight dynamics under stochastic-gradient training of shallow networks; see, for instance, [12, 1, 23, 10, 11, 8]. The observable studied here is of a different nature: we focus on the geometric growth of affine pieces under random composition. LARGE DEVIATIONS FOR LINEAR REGIONS 3

The model is simple. Each layer is a random tent map Tωn , and the depth-n composition is 

Fn = Tωn ◦ · · · ◦ Tω1 . The quantity of interest is Nn, the number of affine pieces of Fn on [0 , 1]. Thus, Nn is a one-dimensional linear-region count for a random deep piecewise-linear network. Since each tent map has an explicit width-three ReLU representation, this model is directly embedded in the standard ReLU formalism. The deterministic choice ωn ≡ (2 , 2) recovers the symmetric height-one tent map underlying Telgarsky’s construction and hence the classical exponential doubling mechanism. The map T2,2 is the deterministic sawtooth block underlying the 1D depth-separation construc-tions of Telgarsky [21, 22]; see also [17, Sec. 6] for a textbook treatment of the same doubling mechanism in terms of piecewise-linear regions. In this deterministic case, each composition dou-bles the affine-piece count, so Nn = 2 n. The contribution of the present paper is not a new deterministic doubling construction, but the large-deviation analysis of Nn when the layers are random perturbations of this block. The random perturbation of this construction is natural both theoretically and practically. In any realistic network, layer parameters are not tuned to the exact Telgarsky configuration ωn ≡ (2 , 2); rather, they deviate due to initialization, finite-precision arithmetic, or training dynamics. The probabilistic question therefore arises as: how stable is exponential linear-region growth under such deviations? Hanin and Rolnick [7] showed that, along any fixed one-dimensional input trajectory, the average number of linear regions grows only linearly with the total number of neurons, far below the exponential worst-case bound. The present model occupies the intermediate regime: the layer parameters are random, but concentrated near T2,2 rather than drawn from a standard initialization. This is precisely the regime where large-deviation questions become meaningful. One can rigorously quantify how likely it is that near-maximal expressivity is achieved or lost under small perturbations of the Telgarsky configuration. Figure 2 illustrates the corresponding one-dimensional mechanism. Small perturbations of the Telgarsky block may preserve the full affine-piece count at moderate depth, whereas more lossy perturbations can cause some branches to miss later peaks and eliminate potential teeth. The main results are stated in Section 2. For the upper bound, a pathwise submultiplicative inequality for Nn yields a bilateral pressure Λ( q) = lim n→∞ n−1 log E[N qn], q ∈ R, and exponential upper bounds for both tails of n−1 log Nn. Because this argument uses only submultiplicativity and independent environment blocks, we also record an abstract pressure principle and two higher-dimensional consequences, one for convex-polytopal affine-cover counts and one for worst-line affine-piece counts. The lower-bound theory is local and constructive. Its purpose is to compensate for the absence of a supermultiplicative inequality for the true branch count. Near the deterministic Telgarsky regime, branches may lose future splitting potential when their image intervals approach the end-points of [0 , 1]. We therefore introduce a coarse defect dynamics that tracks only enough endpoint information to certify future splits. This gives a favorable finite-state subsystem and a bridge word that restores quasi-multiplicativity for a certified lower count. At fixed coarse parameters 4 LARGE DEVIATIONS FOR LINEAR REGIONS 

Figure 2. Perturbed Telgarsky compositions at moderate depth. In each panel, the pale curve shows the deterministic reference n-fold composition T2,2 ◦ · · · ◦ T2,2,while the darker curve shows one realization of the random composition Fn. Small perturbations may preserve the full affine-piece count, whereas moderate lossy per-turbations can cause some branches to miss later peaks and eliminate potential teeth. The figure is illustrative only; the results below concern the exact branch-count process Nn, not numerical sampling of the graph. (r, m ), the bridge construction gives upper-tail lower bounds for the true observable Nn along bridge-compatible subsequences. In a uniformly favorable small-noise regime, the lower subsystem becomes deterministic and is governed by a companion matrix Am, whose Perron root ρm satisfies 

ρm ↑ 2. Consequently, for every ξ > 0, if the support of the noise is sufficiently small, then the lower tail below log 2 − ξ is eventually impossible. The bridge construction is intentionally one-sided. It gives a sufficient mechanism for high upper-tail growth, not a sharp characterization of all high-growth trajectories. The certified lower count may be much smaller than the true number of affine pieces. The remaining obstruction to a full sharp large-deviation principle is therefore an asymptotic-exactness theorem comparing the true branch count to the certified coarse transfer process on the relevant rare-event scale. The paper is organized as follows. Section 2 introduces the model and states the main theorems, first the pressure upper bounds and then the certified lower-bound mechanisms. Section 3 proves the pressure theorem and the resulting exponential upper bounds. Section 4 constructs the favorable subsystem, defines the coarse transfer matrices, and proves the bridge lemma. Section 5 proves the bridged lower bound for the upper tail of the original branch-count observable. Section 6 analyzes the uniformly favorable regime, proves the Perron-root asymptotics, and proves the local theorem near log 2. 2. Model and main results 

This section has three parts. Section 2.1 defines the random tent model and the branch-count observable. Section 2.2 states the large-deviation upper bounds, which are global consequences of LARGE DEVIATIONS FOR LINEAR REGIONS 5

submultiplicativity. Section 2.3 states the large-deviation lower bounds, which require a certified lower process based on coarse endpoint defects. 2.1. Model. Fix 0 < ε < 1 and h(a, b ) := ab/ (a + b). Let με be a probability measure supported on 

Uε := (a, b ) ∈ (0 , ∞)2 : |a − 2| ≤ ε, |b − 2| ≤ ε, h (a, b ) ≤ 1 ,

Let ( ωn)n≥1 be i.i.d. with law με, and write ωn = ( An, B n). For ω = ( a, b ) ∈ Uε, define 

Tω(x) := (min( ax, b (1 − x))) +,

The map Tω is continuous and piecewise affine, with peak location c(ω) := b/ (a + b) and peak height h(ω) = ab/ (a + b) ≤ 1. Thus, Tω([0 , 1]) ⊆ [0 , 1]; see Figure 3 for an illustration. 

x

Tω(x)  c(ω), h (ω)

0 1

h(ω)

u c(ω) v

J = [ u, v ]L(J) = u R(J) = 1 − v      

> slope aslope −bu < c (ω)< v Jcrosses the peak
> the branch is guaranteed to split at the next layer

Figure 3. Endpoint defects and guaranteed splitting. A branch with image J =[u, v ] has left defect L(J) = u and right defect R(J) = 1 − v. If u < c (ω) < v , then 

J crosses the peak of the next tent map Tω, so the branch splits into two child branches. The observable studied in this paper is the number Nn of affine pieces of Fn on [0 , 1], where 

Fn := Tωn ◦· · ·◦ Tω1 . Equivalently, if A(f ; J) denotes the smallest number of subintervals partitioning 

J on each of which f is affine, then 

Nn := A(Fn; [0 , 1]) .

Since each layer has at most two affine pieces on [0 , 1], one always has 1 ≤ Nn ≤ 2n. In the deterministic Telgarsky case ωn ≡ (2 , 2), one has Nn = 2 n. The model is a scalar width-three ReLU network. Indeed, for c = b/ (a + b), 

Tω(x) = a ReLU( x) − (a + b) ReLU( x − c) + b ReLU( x − 1) .

Thus, the random tent model is directly embedded in the standard ReLU formalism. 6 LARGE DEVIATIONS FOR LINEAR REGIONS 

2.2. Large deviation upper bounds. We first state the upper-bound results. These are global and use only submultiplicativity of the true branch count. 2.2.1. Pressure bounds for the true branch count. The first result concerns only the true branch-count observable Nn. Let Xn := n−1 log Nn.

Theorem 2.1 (Pressure and exponential upper bounds) . For every q ∈ R, the limit 

Λ( q) := lim  

> n→∞

n−1 log E[N qn]

exists. For q ≥ 0, one has Λ( q) = inf n≥1 n−1 log E[N qn], while for q < 0, one has Λ( q) =sup n≥1 n−1 log E[N qn].Moreover, Λ is finite and convex on R, and min {0, q log 2 } ≤ Λ( q) ≤ max {0, q log 2 }. If I+(x) := sup q≥0{qx − Λ( q)} and I−(x) := sup q≤0{qx − Λ( q)}, then, for every x ∈ R,

lim sup 

> n→∞

n−1 log P(Xn ≥ x) ≤ − I+(x) and lim sup 

> n→∞

n−1 log P(Xn ≤ x) ≤ − I−(x).

The proof uses only the pathwise submultiplicativity, Fekete’s lemma, and exponential Markov bounds. Thus, no coarse graining, favorable subsystem, or choice of m is involved in Theorem 2.1. The result gives exponential upper bounds for both tails of the true observable Nn, but it does not give matching lower bounds. The argument is more robust than the one-dimensional tent model suggests. Section 3 first isolates an abstract pressure principle for submultiplicative complexity observables. Theorem 2.1 is then obtained as its first application. Two further consequences are recorded next. 2.2.2. Robust extensions of the pressure argument. The first extension concerns higher-dimensional compositional models. Let K ⊆ Rd be a compact convex polytope. We use the convention that a map is affine on a subset of K if it agrees there with the restriction of an affine map on Rd.For a map f : K → K, let Rcov (f ; K) denote the smallest integer M ≥ 1 for which there exist compact convex polytopes P1, . . . , P M ⊆ K whose union is K and such that f is affine on each 

Pi. This is a convex-polytopal affine-cover count. It is the higher-dimensional analogue of the one-dimensional affine-piece count that is most directly compatible with composition. In the next two results, piecewise affine means that such a finite convex-polytopal affine cover exists. The uniform bound below is an architecture-level assumption: in standard piecewise-affine classes, such as ReLU networks with fixed widths and depth, the constant B can be bounded explicitly in terms of the architecture. 

Theorem 2.2 (Higher-dimensional affine-cover pressure) . Let K ⊆ Rd be a compact convex poly-tope, and let (Φ ωn )n≥1 be i.i.d. random piecewise affine self-maps of K. Set Φn := Φ ωn ◦ · · · ◦ Φω1

and Rn := Rcov (Φ n; K). Assume that Rn is measurable for every n ≥ 1, and that 

sup 

> ω

Rcov (Φ ω; K) ≤ B

for some deterministic B ≥ 1. Then the conclusions of Theorem 2.1 remain valid after replacing 

Nn, Xn, Λ, I±, and log 2 by Rn, Yn := n−1 log Rn, Λcov , Icov ,±, and log B, respectively, where 

Λcov (q) := lim  

> n→∞

n−1 log E[Rqn]LARGE DEVIATIONS FOR LINEAR REGIONS 7

and Icov ,± are defined from Λcov exactly as in Theorem 2.1. 

Example 2.1 (A computable affine-cover bound) . Let K ⊆ Rd be a compact convex polytope. Fix a ReLU architecture with L hidden layers of widths m1, . . . , m L, input dimension d, and output dimension d. Suppose that each random map Φ ω : K → K is the restriction to K of a network with this architecture. Then the assumption in Theorem 2.2 holds with 

B =

> L

Y

> ℓ=1

Hd(mℓ), Hd(m) := 

> d

X

> j=0

mj



.

Indeed, on each affine cell created before layer ℓ, the mℓ preactivation functions of layer ℓ are affine functions of the original input. Their zero sets cut the cell into at most Hd(mℓ) convex polytopes. Iterating this over the layers gives Rcov 

 Φω; K ≤ QLℓ=1 Hd(mℓ) uniformly in the weights and biases. Thus, the constant B is deterministic and computable from the architecture alone. In the one-dimensional case, H1(m) = m + 1; in particular, a single one-kink layer gives B = 2, matching the two-piece tent-map case. The second extension concerns one-dimensional slices through higher-dimensional compositions. Let S(K) denote the family of affine maps γ : [0 , 1] → K, including degenerate affine segments. For a map f : K → K, define its worst-line affine-piece count by L(f ; K) := sup γ∈S (K) A(f ◦ γ; [0 , 1]). 

Theorem 2.3 (Worst-line pressure in higher dimension) . Let K ⊆ Rd be a compact convex polytope, and let (Φ ωn )n≥1 be i.i.d. random piecewise affine self-maps of K. Set Φn := Φ ωn ◦ · · · ◦ Φω1 and 

Ln := L(Φ n; K). Assume that Ln is measurable for every n ≥ 1, and that 

sup 

> ω

L(Φ ω; K) ≤ B

for some deterministic B ≥ 1. Then the conclusions of Theorem 2.1 remain valid after replacing 

Nn, Xn, Λ, I±, and log 2 by Ln, Zn := n−1 log Ln, Λline , Iline ,±, and log B, respectively, where 

Λline (q) := lim  

> n→∞

n−1 log E[Lqn]

and Iline ,± are defined from Λline exactly as in Theorem 2.1. 

The supremum over all affine segments in Theorem 2.3 is important. For a fixed deterministic segment γ, the image Φ n ◦ γ is generally a random piecewise affine curve rather than the original segment γ. Consequently, one obtains a comparison with the worst-line complexity of the future block, not a closed submultiplicative inequality for the fixed-line observable itself. 2.3. Large deviation lower bounds. The pressure argument gives exponential upper bounds for the tails of the true observable Nn. The lower-bound problem is more delicate. There is no corresponding supermultiplicative inequality for the true branch count, and therefore no direct lower-bound analogue of the pressure argument. 2.3.1. Geometric obstruction and endpoint defects. The geometric obstruction is that branch growth is no longer automatic once the layers are random. In the deterministic map, every affine branch crosses the peak of the next layer and splits into two descendants. Under perturbations, this can fail as illustrated in Figure 4. If the image interval of a branch lies too close to one endpoint of 8 LARGE DEVIATIONS FOR LINEAR REGIONS                     

> (a) c(ω)∈J
> c(ω)
> 01
> J= [ u, v ]
> uv
> L(J) = uR(J) = 1 −v
> (b) c(ω)/∈J
> c(ω)
> 01
> J= [ u, v ]
> uv
> L(J) = uR(J) = 1 −v

Figure 4. In panel (a), the image interval crosses the peak of the next layer, so a split is certified. In panel (b), the image interval misses the peak, so the certified lower process does not record a split. [0 , 1], it may miss the next peak and produce only one descendant. In this sense, one side of the interval has lost its safety margin, and the branch has lost some of its future splitting potential. Thus, the total number of branches alone does not contain enough information to propagate lower bounds. One must also record how close branch images are to losing the ability to split. For a branch whose image is J = [ u, v ], the relevant quantities are the endpoint defects 

L(J) = u, R(J) = 1 − v. 

Small endpoint defects guarantee that the interval crosses the peak of the next layer and hence must split. The lower-bound construction below keeps only a coarse record of these defects. 2.3.2. Coarse graining. We now turn the endpoint-defect observation into a finite-state lower pro-cess. Set δε := 12 inf ω∈Uε min {c(ω), 1 − c(ω)}, Kε := 2 + ε, and η(ω) := 1 − h(ω). Since 

a, b ∈ [2 − ε, 2 + ε], one has δε = (2 − ε)/8. For a fixed integer m ≥ 1, define 

ri := δεK−iε , i = 0 , . . . , m. 

A layer is called m-favorable if η(ω) ≤ rm.The thresholds ri form a finite defect scale. A larger value of i means a smaller certified defect and therefore a longer remaining defect budget. One step of the tent recursion can multiply an old endpoint defect by at most Kε, so a defect controlled by ri is transformed into one controlled by 

ri−1. Increasing m improves the certified exponent available from the favorable chain, but it also makes the strongest favorable condition η(ω) ≤ rm more restrictive. For fixed m, define the coarse alphabet Am := {0, 1, . . . , m, ∂ } and the coarse symbol κm(ω)by κm(ω) := max {ℓ ∈ { 0, . . . , m } : η(ω) ≤ rℓ} if η(ω) ≤ r0, and κm(ω) := ∂ otherwise. Thus, 

κm(ω) = m is exactly the event that the layer is m-favorable. We write π = πm,ε for the law of 

κm(ω1), and pm := π(m) = P(κm(ω1) = m). The distribution of the layer does not choose m, but it determines whether a given choice of m is useful. In particular, if pm = 0, then the all-favorable lower-bound mechanism at level m is unavailable. A certified token in state ( i, j ) attached to a branch with image J means that L(J) ≤ ri, and R(J) ≤ rj . The indices i and j are the remaining LARGE DEVIATIONS FOR LINEAR REGIONS 9

defect budgets for the left and right endpoints. This state information is not meant to be an exact description of all branches; it is only the certificate needed to propagate a lower count. For fixed m, the favorable subsystem has states si = ( i, m ), i = 0 , . . . , m − 1. These are the states whose right endpoint has the strongest certificate. On this chain, one m-favorable layer acts by the 

m × m matrix Am with first row consisting of ones, subdiagonal entries equal to one, and all other entries equal to zero. Let ρm be its Perron root, that is, its spectral radius. 

Example 2.2 (The Fibonacci mechanism at level m = 2) . At defect budget m = 2, the favorable chain has two states, 

s1 = (1 , 2) , s0 = (0 , 2) .

Under a 2-favorable layer, a certified branch in state s1 produces two certified favorable descendants: one in state s1 and one in state s0. A certified branch in state s0 produces only one certified favorable descendant, in state s1. Thus, in the ordered basis ( s1, s 0), the certified population evolves according to 

A2 =

1 11 0



.

Starting with one top-state token, the total certified populations are 1 , 2, 3, 5, . . . , and the expo-nential growth rate is the Perron root ρ2 = (1 + √5) /2. The non-integer rate ρ2 is not a fractional one-step branching factor. Each certified branch still has either one or two certified descendants. The Perron root is the asymptotic growth rate of the deterministic mixture of these two branch types. We now provide two results illustrating the use of the favorable concept to obtain lower bounds. 

Theorem 2.4 (Perron growth of the favorable chain) . One has ρ1 = 1 , while ρm ∈ (1 , 2) for every 

m ≥ 2. Moreover, 2 − ρm = ρ−mm , ρm ↑ 2, 2 − ρm ∼ 2−m, and log 2 − log ρm ∼ 2−m−1. If the first 

n layers are all m-favorable, then Nnρ−nm > 0.

Theorem 2.5 (All-favorable upper-tail lower bound) . If pm > 0, then, for every δ > 0, one has 

lim inf n→∞ n−1 log P(n−1 log Nn ≥ log ρm − δ) ≥ log pm. Moreover, E[Nn] ≥ cm(pmρm)n for all 

n ≥ 1. In particular, if pmρm > 1, then E[Nn] grows exponentially. 

Theorem 2.4 explains the role of large m: the deterministic certified exponent log ρm can be made arbitrarily close to log 2. But this does not mean that m should always be taken large. For a fixed law με, the probability pm = P(η(ω1) ≤ rm) may decrease with m, or even vanish. Theorem 2.5 is a statement about the true branch count Nn. Its proof simply forces the event that all layers are m-favorable and then uses the deterministic Perron growth from Theorem 2.4. It should not be read as favoring large m in general. For a prescribed target exponent x < log 2, the all-favorable bound is most naturally invoked with the smallest m for which log ρm > x , up to the arbitrarily small slack allowed by δ, because larger m typically lowers pm and therefore worsens the forcing cost log pm. Likewise, the expectation bound is controlled by the product pmρm, whose maximizing value of m need not be large. 2.3.3. Bridged lower bounds for the upper tail. The all-favorable event is useful but restrictive, because it requires every layer to have the maximally favorable symbol m. We now provide a different construction that weakens this requirement. Instead of forcing all layers to be favorable, 10 LARGE DEVIATIONS FOR LINEAR REGIONS 

we divide the word into long free blocks and short single-symbol slots that we henceforth refer to as bridges . The free blocks are not required to be all-favorable; they may contain arbitrary coarse symbols, provided that they are productive for the certified lower transfer process introduced below. Only the bridge slots are forced to be maximally favorable. We now make this construction more precise. Each symbol a ∈ A m determines its own linear update map M (a), and the update along a word is obtained by composing these symbol-dependent maps. Let Σm := {(i, j ) : 0 ≤ i, j ≤ m}.

We work on the vector space with basis {e(i,j ) : ( i, j ) ∈ Σm}. A basis vector e(i,j ) represents one certified token whose two remaining endpoint-defect budgets are i and j. The purpose of the auxiliary transfer process is to keep only those descendants whose certificates can be verified by the coarse symbols. For a ∈ A m, define a linear map M (a) on this vector space as follows. If 

a ∈ { 0, . . . , m }, set 

M (a)e(i,j ) := 1{i≥1}e(i−1,a ) + 1{j≥1}e(j−1,a ).

If a = ∂, set M (∂)e(i,j ) := 0. The rule should be read as a bookkeeping rule for certified continu-ations. A token in state e(i,j ) may be continued from any endpoint whose budget is still positive. Continuing from the endpoint with budget i consumes one unit of that budget and produces e(i−1,a ).Continuing from the endpoint with budget j consumes one unit of that budget and produces e(j−1,a ).In both cases, the new state is oriented so that the remaining budget of the endpoint just used appears in the first coordinate, while the newly read symbol a becomes the refreshed second coor-dinate. If a = ∂, no certified continuation is allowed, and all mass is killed. For example, if m ≥ 3, then M (3) e(2 ,1) = e(1 ,3) + e(0 ,3) . The two summands correspond to the two possible endpoint con-tinuations. In contrast, M (1) e(0 ,2) = e(1 ,1) , because the endpoint with budget 0 cannot be used. Thus, M (a) records all certified continuations through the symbol a, while discarding uncertified descendants. The favorable chain is the subspace 

Fm := span {esi : si = ( i, m ), i = 0 , . . . , m − 1}.

These are the auxiliary states whose second coordinate is the strongest certificate value m. In this sense, Fm is the part of the auxiliary state space that remains well positioned for later bridging. Let PF denote the coordinate projection onto Fm. Thus, PF keeps the coordinates belonging to the favorable chain and kills all other coordinates. Let 1F be the vector on Fm whose coordinates in the basis {esi : 0 ≤ i ≤ m − 1} are all equal to one. Consequently, for any nonnegative vector x, the scalar 1⊤ 

> F

PF x is the total mass of x lying on the favorable chain. Finally, let e⋆ := esm−1 = e(m−1,m )

be the top favorable-chain basis vector. For a word w = ( a1, . . . , a ℓ), read from left to right, write 

M (w) := M (aℓ) · · · M (a1).

We define the certified favorable block score by Γ( w) := 1⊤ 

> F

PF M (w)e⋆.LARGE DEVIATIONS FOR LINEAR REGIONS 11 

In words, Γ( w) is the total mass of certified tokens that lie on the favorable chain after the block 

w has been read, starting from one token in state e⋆. It is not the total number of certified tokens produced by the block. It counts only those tokens ending in favorable-chain states and thus available for the subsequent bridge construction. The bridge word is the one-letter word 

b(m) := ( m) ∈ A m.

Its role is to return to the top favorable state e⋆ = e(m−1,m ), from which the next free block can be started. Indeed, every favorable-chain state si = ( i, m ), 0 ≤ i ≤ m − 1, produces at least one copy of e⋆ after reading the single favorable symbol m. Consequently, for all words u, v ,Γ vb (m)u ≥ Γ( v)Γ( u).

Thus, inserting one maximally favorable layer between two free blocks makes the certified favorable score supermultiplicative. 

Example 2.3. It may be useful to see this bridge in the smallest nontrivial case. When m = 2, the favorable chain is 

F2 = span {es1 , e s0 }, s1 = (1 , 2) , s0 = (0 , 2) ,

and e⋆ = es1 . Reading the favorable symbol 2 gives 

M (2) es1 = es1 + es0 , M (2) es0 = es1 .

Thus, in the ordered basis ( es1 , e s0 ), the favorable-chain transition matrix is 

A2 =

1 11 0



.

Suppose that the favorable part after reading a word u is PF M (u)e⋆ = x1es1 + x0es0 . Then, Γ( u) = x1 + x0. After one bridge symbol 2, the coefficient of the top state e⋆ = es1 is exactly 

x1 +x0. Each such copy of e⋆ can be used as a fresh starting token for the next word v, contributing Γ( v) favorable tokens. This gives Γ vb (2) u ≥ Γ( v)Γ( u).

The general case is the same restart mechanism with m possible budget levels instead of two. The reason the bridge helps is deterministic. The certified score Γ( w) is not multiplicative in 

w: after a block u, the surviving certified tokens may end in different favorable-chain states. The one-symbol bridge b(m) = ( m) refreshes every such favorable token by creating a copy of the common restart state e⋆. Thus, the bridge converts many individually productive free blocks into one productive long word. Probabilistically, the cost is paid only for the bridge slots, not for every layer. If the free blocks have length r, the forced favorable fraction is roughly 1 /(r + 1), and this overhead becomes small when r is large. The lower bound in Theorem 2.6 below is exactly this tradeoff: a relative-entropy cost for choosing productive free blocks, plus the explicit cost of inserting favorable bridges. Fix a free-block length r ≥ 1. Let Wr := Arm, πr := π⊗r, Gr := {w ∈ W r : Γ( w) > 0}, and 

gr(w) := log Γ( w) for w ∈ G r. The probability of one bridge word is pb := π(b(m)) = pm. Assuming 12 LARGE DEVIATIONS FOR LINEAR REGIONS 

pb > 0, define the bridge construction rate J+

> r,m

(x) as the infimum of ( H(ν | πr) − log pb)/(r + 1) over all ν ∈ P (Wr) such that ν(Gr) = 1 and R gr dν > (r +1) x. Here P(Wr) denotes the probability simplex on Wr, and H(ν | πr) denotes relative entropy. The infimum over the empty set is + ∞.The bridge construction is a sufficient lower-bound mechanism. It counts only certified branches and does not characterize all branch configurations that can lead to large values of Nn.

Theorem 2.6 (Bridged lower bound for the upper tail) . Fix m, r ≥ 1, and assume pb > 0. Let 

nk := 1 + kr + ( k − 1) . Then, for every x ∈ R, one has 

lim inf  

> k→∞

n−1 

> k

log P(n−1 

> k

log Nnk > x ) ≥ − J+

> r,m

(x).

The initial +1 in nk = 1 + kr + ( k − 1) is only a seed layer. On the event that the first coarse symbol is m, the initial branch produces certified tokens in the top favorable state e⋆. This allows the later block construction to start from the vector used in Γ. The seed cost is negligible on the exponential scale. The parameters m and r have different roles. The parameter m sets the defect resolution and the favorable symbol used for bridging. The parameter r is a blocking parameter for the empirical law of the free blocks. For fixed m, larger r reduces the bridge overhead per unit depth, but it does not remove the possible gap between the certified count Γ and the true branch count Nn.

Remark 2.7 (Regenerative interpretation) . The bridge construction has a regenerative interpreta-tion. Once certified mass lies in the favorable chain, a single favorable symbol m creates at least one copy of the common restart state e⋆ = e(m−1,m ) from every favorable-chain state. Thus, favor-able symbols serve as restart symbols for the certified transfer process. One can make this analogy literal by cutting the coarse-symbol sequence at non-overlapping first occurrences of the word mm :after each cut, one waits until the first adjacent occurrence of mm , consumes that terminal word, and then cuts again. The resulting cycles are i.i.d., and each cycle carries a length and a certified reward, namely the logarithm of the number of regenerated e⋆-tokens produced by the correspond-ing matrix product. This leads to a renewal-reward version of the same lower-bound mechanism, with a variational cost of the form inf 

> ν

H(ν | Q)

R s dν ,

where Q is the law of one regeneration cycle, s is its length, and the infimum is restricted to cycle laws whose average certified reward exceeds the desired linear growth rate. This viewpoint is analogous to large-deviation results for regenerative and renewal-reward processes [9, 6, 24]. We do not use the regenerative formulation below. The fixed-block statement in Theorem 2.6 is deliberately more elementary: it works on the finite alphabet Wr, uses only the method of types, and keeps the bridge cost explicit. The regenerative viewpoint is nevertheless a useful way to understand why favorable symbols restore concatenability. 2.3.4. Local lower-tail exclusion near log 2 . Finally, for fixed m, the following condition says that the random environment is so close to the Telgarsky map that every layer is favorable at level m.We say that the model is uniformly favorable at level m if η∗(ε) := sup ω∈Uε η(ω) ≤ rm. In this case, every layer is m-favorable almost surely. The deterministic mechanism behind the next theorem is the favorable-chain growth illustrated in Example 2.2. At general level m, the same mechanism has a longer defect budget and Perron rate ρm ↑ 2. LARGE DEVIATIONS FOR LINEAR REGIONS 13 

The order of choices is important. For a prescribed gap ξ, one first chooses m so that log ρm is close enough to log 2. Only after m is fixed does one choose the noise level ε small enough that all admissible layers are m-favorable. 

Theorem 2.8 (Local lower-tail exclusion near log 2) . For every ξ > 0, there exist m(ξ) ≥ 1,

ε∗(ξ) > 0, and n0(ξ) ≥ 1 such that, whenever 0 < ε ≤ ε∗(ξ) and n ≥ n0(ξ), one has 

P(n−1 log Nn ≤ log 2 − ξ) = 0 .

3. Proofs of Theorems 2.1, 2.2 and 2.3 –Subadditive pressure and exponential upper bounds 

This section proves Theorem 2.1 and its higher-dimensional extensions, Theorems 2.2 and 2.3. The argument is organized in three steps. First, we isolate an abstract pressure theorem for any positive submultiplicative complexity observable with independent increments. Second, we verify its hypotheses for the one-dimensional branch-count observable Nn. Third, we verify the same hypotheses for higher-dimensional affine-cover counts and worst-line affine-piece counts. First, we record the elementary facts about the layer maps and their branch-count observable. 3.1. Layer maps. The first lemma identifies the peak and image of a single random tent layer. 

Lemma 3.1. For every ω = ( a, b ) ∈ Uε, the map Tω is continuous and piecewise affine with unique peak location c(ω) = b/ (a + b) and peak height h(ω) = ab/ (a + b) ≤ 1. In particular, 

Tω([0 , 1]) = [0 , h (ω)] ⊆ [0 , 1] .

Proof. The affine functions x 7 → ax and x 7 → b(1 − x) intersect at x = c(ω), and their common value there is h(ω). Since Tω is their minimum clipped below by 0, it is continuous and piecewise affine. The condition h(ω) ≤ 1 gives Tω([0 , 1]) ⊆ [0 , 1]. □

The next proposition embeds every tent layer into the scalar ReLU formalism. Thus, as mentioned before. 

Proposition 3.2 (ReLU representation) . Let ReLU( t) := max {t, 0}. For ω = ( a, b ) ∈ Uε, with 

c = b/ (a + b), one has 

Tω(x) = a ReLU( x) − (a + b) ReLU( x − c) + b ReLU( x − 1) .

Proof. The right-hand side vanishes for x < 0. For 0 ≤ x ≤ c, only the first ReLU is active, and the value is ax . For c ≤ x ≤ 1, the first two ReLUs are active, and the value is ax − (a + b)( x − c) = 

b(1 − x). For x ≥ 1, all three ReLUs are active, and the value is ax − (a + b)( x − c) + b(x − 1) = 0. This agrees with Tω on all four intervals. □

3.2. An abstract submultiplicative pressure principle. The next proposition is the common mechanism behind all our pressure upper bounds. 

Proposition 3.3 (Submultiplicative pressure principle) . Let (Cn)n≥1 be random variables such that (1) 1 ≤ Cn ≤ Bn almost surely for a deterministic constant B ≥ 1, and (2) for every n, ℓ ≥ 1, there exists a random variable C′ 

> ℓ

with the same law as Cℓ, independent of Cn, and satisfying Cn+ℓ ≤ CnC′ 

> ℓ

almost surely. 14 LARGE DEVIATIONS FOR LINEAR REGIONS 

Then for every q ∈ R, the limit ΛC (q) := lim n→∞ n−1 log E[Cqn] exists. For q ≥ 0, one has ΛC (q) = inf n≥1 n−1 log E[Cqn], while for q < 0, one has ΛC (q) = sup n≥1 n−1 log E[Cqn]. Moreover, ΛC is finite and convex on R, and min {0, q log B} ≤ ΛC (q) ≤ max {0, q log B}. If IC, +(x) := sup q≥0{qx −ΛC (q)}

and IC, −(x) := sup q≤0{qx − ΛC (q)}, then, for every x ∈ R,

lim sup 

> n→∞

n−1 log P(Yn ≥ x) ≤ − IC, +(x) and lim sup 

> n→∞

n−1 log P(Yn ≤ x) ≤ − IC, −(x),

where Yn := n−1 log Cn.Proof. For q ∈ R, define an(q) := log E[Cqn]. Since 1 ≤ Cn ≤ Bn, every moment E[Cqn] is finite and strictly positive, so an(q) is well defined. We treat the cases q ≥ 0 and q < 0 separately. 

q ≥ 0. The map t 7 → tq is increasing on (0 , ∞), hence Cqn+ℓ ≤ Cqn(C′

> ℓ

)q. Taking expectations and using independence of Cn and C′ 

> ℓ

gives E[Cqn+ℓ] ≤ E[Cqn]E[Cqℓ ]. Thus, an+ℓ(q) ≤ an(q) + aℓ(q), so Fekete’s lemma yields existence of lim n→∞ n−1an(q) together with the infimum formula. 

q < 0. The map t 7 → tq is decreasing on (0 , ∞), hence Cqn+ℓ ≥ Cqn(C′

> ℓ

)q. Taking expectations and using independence gives E[Cqn+ℓ] ≥ E[Cqn]E[Cqℓ ]. Thus, an+ℓ(q) ≥ an(q)+ aℓ(q), so the superadditive version of Fekete’s lemma yields existence of lim n→∞ n−1an(q) together with the supremum formula. The deterministic bound 1 ≤ Cn ≤ Bn implies 1 ≤ Cqn ≤ Bnq for q ≥ 0, and Bnq ≤ Cqn ≤ 1for q < 0. After taking expectations, logarithms, dividing by n, and passing to the limit, one obtains min {0, q log B} ≤ ΛC (q) ≤ max {0, q log B}. Hence, Λ C is finite on R. For each fixed n,the map q 7 → n−1 log E[Cqn] is convex. Indeed, it is n−1 times the log-Laplace transform of the bounded random variable log Cn. Since Λ C is the pointwise finite limit of these convex functions, the defining convexity inequality passes to the limit, and Λ C is convex. It remains to prove the tail bounds. Since enqY n = Cqn, exponential Markov inequalities give 

P(Yn ≥ x) ≤ e−nqx E[Cqn] (q ≥ 0) , P(Yn ≤ x) ≤ e−nqx E[Cqn] (q ≤ 0) .

Taking logarithms, dividing by n, passing to lim sup, and then optimizing over q ≥ 0 in the first inequality and q ≤ 0 in the second proves the claimed bounds. □

3.3. Application to branch counts in the random tent model. We now specialize Proposi-tion 3.3 to the true branch count Nn.The deterministic counting inequality behind the next lemma is standard for compositions of piecewise-affine maps; compare [17, Sec. 6.1]. We include the proof because the shifted-block variable and its independence from the past are the probabilistic input needed for Theorem 2.1. 

Lemma 3.4. For all n, ℓ ≥ 1, if Fn,ℓ := Tωn+ℓ ◦ · · · ◦ Tωn+1 , then 

Nn+ℓ ≤ NnN ′

> ℓ

,

where N ′ 

> ℓ

:= A(Fn,ℓ ; [0 , 1]) has the same law as Nℓ and is independent of Nn.Proof. Write Fn+ℓ = H ◦ G with G := Fn and H := Fn,ℓ . By definition of Nn = A(G; [0 , 1]), there exists a partition of [0 , 1] into Nn intervals J1, . . . , J Nn such that G is affine on every Ji. Since 

G([0 , 1]) ⊆ [0 , 1], the image G(Ji) is a compact subinterval of [0 , 1]. By definition of N ′ 

> ℓ

= A(H; [0 , 1]), the map H has an affine partition of [0 , 1] with N ′ 

> ℓ

intervals. Restricting this partition to the subinterval G(Ji) shows that H has at most N ′ 

> ℓ

affine pieces on 

G(Ji). Since G is affine on Ji, the composition H ◦ G therefore has at most N ′ 

> ℓ

affine pieces on Ji.Summing this bound over the Nn intervals Ji gives Nn+ℓ ≤ NnN ′

> ℓ

.LARGE DEVIATIONS FOR LINEAR REGIONS 15 

The variable Nn is determined by ( ω1, . . . , ω n), while N ′ 

> ℓ

is determined by ( ωn+1 , . . . , ω n+ℓ). These environment blocks are independent because the layers are i.i.d. Hence, Nn and N ′ 

> ℓ

are independent. Since the block ( ωn+1 , . . . , ω n+ℓ) has the same law as ( ω1, . . . , ω ℓ), the variable N ′ 

> ℓ

has the same law as Nℓ. □

We now prove the first pressure theorem. It gives exponential upper bounds for both tails of the true observable Xn = n−1 log Nn. It does not by itself prove a full large-deviation principle, because it does not provide matching lower bounds. The later bridge construction supplies one lower-bound mechanism for the upper tail, but not a complete converse to the pressure upper bound. 

Proof of Theorem 2.1. Lemma 3.4 gives, for every n, ℓ ≥ 1, a shifted-block variable N ′ 

> ℓ

with the same law as Nℓ, independent of Nn, and satisfying Nn+ℓ ≤ NnN ′ 

> ℓ

almost surely. Thus, all hypotheses of Proposition 3.3 hold with Cn := Nn and B := 2. The conclusion of Proposition 3.3 is exactly the statement of Theorem 2.1. □

3.4. Higher-dimensional affine-cover complexity. We next prove Theorem 2.2. The main point is that convex-polytopal affine covers are exactly multiplicative under composition. Let K ⊆ Rd be a compact convex polytope. For a map f : K → K, recall that Rcov (f ; K) is the smallest integer M ≥ 1 for which K can be covered by compact convex polytopes P1, . . . , P M ⊆ K

such that f agrees on each Pi with the restriction of an affine map on Rd.

Lemma 3.5 (Submultiplicativity of affine-cover counts) . For piecewise affine G, H : K → K,

Rcov (H ◦ G; K) ≤ Rcov (G; K) Rcov (H; K).

Proof. Set r := Rcov (G; K) and s := Rcov (H; K). By definition of Rcov , there exist compact convex polytopes P1, . . . , P r ⊆ K covering K such that G is affine on each Pi, and compact convex polytopes Q1, . . . , Q s ⊆ K covering K such that H is affine on each Qj . For every i ∈ { 1, . . . , r },choose an affine map Li : Rd → Rd whose restriction to Pi agrees with G. For every j ∈ { 1, . . . , s },choose an affine map Mj : Rd → Rd whose restriction to Qj agrees with H. For 1 ≤ i ≤ r and 1 ≤ j ≤ s, define 

Pij := Pi ∩ L−1 

> i

(Qj ).

Each set Pij is either empty or a compact convex polytope. Indeed, Qj is a compact convex polytope, its inverse image under the affine map Li is a closed convex polyhedron, and intersecting with the compact convex polytope Pi yields a compact convex polytope. The nonempty sets Pij cover K. To see this, fix x ∈ K. Since the Pi cover K, one may choose 

i such that x ∈ Pi. Since G(x) = Li(x) ∈ K and the Qj cover K, one may choose j such that 

G(x) ∈ Qj . Then x ∈ Pi ∩ L−1 

> i

(Qj ) = Pij . Finally, on Pij , one has G = Li and Li(Pij ) ⊆ Qj , so 

H ◦ G = Mj ◦ Li, which is affine. Thus, H ◦ G is affine on every nonempty Pij . There are at most 

rs such sets, and therefore Rcov (H ◦ G; K) ≤ rs . □

We now derive the pressure theorem for Rn.

Proof of Theorem 2.2. For n ≥ 1, set Φ n := Φ ωn ◦ · · · ◦ Φω1 and Rn := Rcov (Φ n; K). For n, ℓ ≥ 1, define the shifted composition Φ n,ℓ := Φ ωn+ℓ ◦ · · · ◦ Φωn+1 and R′ 

> ℓ

:= Rcov (Φ n,ℓ ; K). Since Φ n+ℓ =Φn,ℓ ◦ Φn, Lemma 3.5 gives Rn+ℓ ≤ RnR′

> ℓ

. The variable Rn is determined by ( ω1, . . . , ω n), while 

R′ 

> ℓ

is determined by ( ωn+1 , . . . , ω n+ℓ). The layer sequence is i.i.d., hence these two variables are independent, and R′ 

> ℓ

has the same law as Rℓ.16 LARGE DEVIATIONS FOR LINEAR REGIONS 

It remains to verify the deterministic exponential bound. By hypothesis, Rcov (Φ ωj ; K) ≤ B

almost surely for every j. Repeated application of Lemma 3.5 gives Rn ≤ Bn almost surely. Since Rn ≥ 1, all hypotheses of Proposition 3.3 hold with Cn := Rn. Its conclusion is precisely Theorem 2.2. □

Remark 3.6 . The affine-cover count is deliberately formulated as a cover number rather than as the number of maximal affine regions. This choice eliminates boundary and degeneracy issues and makes the multiplicative inequality exact. In one dimension, the corresponding cover count coincides with the usual affine-piece count on an interval. 3.5. Worst-line affine-piece complexity. We finally prove Theorem 2.3. This extension is close in spirit to the 1D tent observable, because the complexity is still measured by numbers of affine pieces on intervals. The only difference is that one takes the worst affine segment in the higher-dimensional state space. Let K ⊆ Rd be a compact convex polytope, and let S(K) be the family of affine maps γ : [0 , 1] → K, including degenerate segments. For f : K → K, recall that 

L(f ; K) := sup γ∈S (K) A(f ◦ γ; [0 , 1]). 

Lemma 3.7. For piecewise affine maps G, H : K → K,

L(H ◦ G; K) ≤ L(G; K) L(H; K).

Proof. Fix an affine segment γ ∈ S (K). Let m := A(G ◦ γ; [0 , 1]). By definition of A, there exists a partition 0 = t0 < t 1 < · · · < t m = 1 such that G ◦ γ is affine on each interval [ ti−1, t i]. Fix i ∈ { 1, . . . , m }. Since G ◦ γ is affine on [ ti−1, t i], its restriction to this interval is an affine parametrization of a line segment in K, possibly degenerate. After affine reparametrization of [ti−1, t i] onto [0 , 1], this restriction belongs to S(K). Therefore, by definition of L(H; K), the composition H ◦ G ◦ γ has at most L(H; K) affine pieces on [ ti−1, t i]. Summing over the m intervals in the chosen partition gives A(H ◦ G ◦ γ; [0 , 1]) ≤ m L (H; K). Since m = A(G ◦ γ; [0 , 1]) ≤ L(G; K), one obtains A(H ◦ G ◦ γ; [0 , 1]) ≤ L(G; K)L(H; K). Taking the supremum over γ ∈ S (K) proves the claim. □

We now derive the pressure theorem for Ln.

Proof of Theorem 2.3. For n ≥ 1, set Φ n := Φ ωn ◦ · · · ◦ Φω1 and Ln := L(Φ n; K). For n, ℓ ≥ 1, define the shifted composition Φ n,ℓ := Φ ωn+ℓ ◦ · · · ◦ Φωn+1 and L′ 

> ℓ

:= L(Φ n,ℓ ; K). Since Φ n+ℓ =Φn,ℓ ◦ Φn, Lemma 3.7 gives Ln+ℓ ≤ LnL′

> ℓ

. The variable Ln is determined by ( ω1, . . . , ω n), while 

L′ 

> ℓ

is determined by ( ωn+1 , . . . , ω n+ℓ). Since the layers are i.i.d., Ln and L′ 

> ℓ

are independent, and 

L′ 

> ℓ

has the same law as Lℓ. By hypothesis, L(Φ ωj ; K) ≤ B almost surely for every j. Repeated application of Lemma 3.7 gives Ln ≤ Bn almost surely. Since Ln ≥ 1, Proposition 3.3 applies with 

Cn := Ln. Its conclusion is exactly Theorem 2.3. □

Remark 3.8 . The same argument does not close for a fixed deterministic segment γ. One always has a one-step comparison between the affine-piece count of ( H ◦ G) ◦ γ and the worst-line complexity of H, but the future block is evaluated along the random piecewise affine image G ◦ γ, not along the original γ. Thus, the natural closed observable is the supremum L(f ; K), rather than the fixed-line count A(f ◦ γ; [0 , 1]). 4. The favorable subsystem and the bridge lemma 

This section turns the geometric obstruction described in Section 2.3 into a finite-state lower process. A branch is useful for future growth only if its image interval remains sufficiently far from LARGE DEVIATIONS FOR LINEAR REGIONS 17 

both endpoints of [0 , 1], so that it is forced to cross the next peak. We therefore track only certified endpoint information. The resulting process is not an exact branch-count process; it is a lower transfer process counting selected branches whose future splitting can be guaranteed. For an affine branch J of a partial composition with image [ u, v ], recall that L(J) := u, and 

R(J) := 1 − v. The thresholds ri = δεK−iε , i = 0 , . . . , m , were defined in Section 2.3.2. We note that the number δε is a strict uniform safety margin: it is half of the minimum possible distance from the peak location c(ω) to the endpoints 0 and 1. Hence, if a branch image J = [ u, v ] satisfies 

L(J) ≤ δε and R(J) ≤ δε, then u < c (ω) < v for every admissible next layer, and the branch is forced to split. The geometric sequence ri = δεK−iε records a finite defect budget. Since one application of a tent map can multiply an old endpoint defect by at most Kε = 2 + ε, a defect controlled at level i is guaranteed to remain controlled at level i − 1 after one step. Thus, the index 

i measures how many future layers of worst-case expansion the corresponding endpoint can still tolerate. We now show that the matrices M (a) defined in Section 2.3.3 propagate certified branch tokens. A token in state ( i, j ) ∈ Σm attached to a branch J means that L(J) ≤ ri, and R(J) ≤ rj . The indices i and j are defect budgets for the left and right endpoints. A larger index means a smaller defect and hence more certified future splitting potential. The same branch may satisfy many such inequalities. Thus, Σ m is not intended as a disjoint state partition. It is a finite set of lower-control labels. The auxiliary process uses one propagated token per selected branch; the token label records one certificate that is sufficient for the next step and need not be the strongest certificate available for that branch. The next proposition gives the certified transfer process for the true branch count. 

Proposition 4.1. Let an := κm(ωn) ∈ A m, and define Z0 := e(m,m ) and Zn := M (an) · · · M (a1)e(m,m ).Then Zn(i, j ) is a lower bound on the number of depth-n branches carrying a propagated token in state (i, j ), and 

Nn ≥ 1⊤Zn.

Proof. At time n = 0, there is one branch, namely [0 , 1], and its defects are zero. Hence it carries the strongest certified state ( m, m ), so Z0 = e(m,m ) is valid. Assume the statement holds at time n. Each token counted by Zn(i, j ) is attached to a se-lected depth-n branch carrying the certificate ( i, j ), and no selected branch carries more than one propagated token. Let the next layer be Tωn+1 , and write q := κm(ωn+1 ). We first verify directly the one-step transition encoded by M (q). If q = ∂, then no certified continuation is recorded by the auxiliary process, in agreement with M (∂) = 0. Suppose therefore that q ∈ { 0, . . . , m }. Let a selected branch J carry a token in state ( i, j ), and write its image interval as J = [ u, v ]. The certificate means that L(J) ≤ ri and R(J) ≤ rj . In particular, L(J) ≤ δε and 

R(J) ≤ δε. By the definition of δε, one has δε < c (ωn+1 ) and δε < 1 − c(ωn+1 ) for every admissible layer. Hence u < c (ωn+1 ) < v , so the next layer splits J into a left child and a right child. Let aωn+1 and bωn+1 denote the left and right slopes of Tωn+1 . On the left of the peak the map is x 7 → aωn+1 x, while on the right of the peak it is x 7 → bωn+1 (1 − x). Thus the endpoint defects of the two children are (LL, R L) =  aωn+1 L(J), η (ωn+1 ), (LR, R R) =  bωn+1 R(J), η (ωn+1 ).18 LARGE DEVIATIONS FOR LINEAR REGIONS 

Since q = κm(ωn+1 ), the definition of the coarse symbol gives η(ωn+1 ) ≤ rq. Also aωn+1 , b ωn+1 ≤ Kε.Therefore, if i ≥ 1, then LL ≤ Kεri = ri−1 and RL ≤ rq, so the left child carries the certified state (i − 1, q ). Similarly, if j ≥ 1, then LR ≤ Kεrj = rj−1 and RR ≤ rq, so the right child carries the certified state ( j − 1, q ). This is exactly the one-step rule recorded by M (q)e(i,j ). We now apply this verification to every selected token at time n. The two child tokens arising from one parent token, when both are present, are attached to the left and right child branches of that parent and hence to distinct branches. Tokens arising from different parent branches are also attached to distinct child branches. Thus no depth-n + 1 branch receives more than one propagated token. Summing over all parent tokens gives Zn+1 = M (q)Zn = M (an+1 )Zn, and the total number of propagated tokens at depth n + 1 cannot exceed the number of actual depth-n + 1 branches. This closes the induction and proves the claim. □

The favorable chain consists of the states si = ( i, m ), i = 0 , . . . , m − 1, whose right defect is controlled at the strongest threshold rm. Also, if the next layer is m-favorable, then the certified dynamics on the favorable chain are given by Am in the ordered basis ( sm−1, s m−2, . . . , s 0). Equiv-alently, the restriction of M (m) to Fm is Am. Indeed, let the parent token be si = ( i, m ). Since the layer is m-favorable, the new right defect is controlled by rm. If i ≥ 1, the left child carries (i − 1, m ) = si−1, while the right child carries ( m − 1, m ) = sm−1. If i = 0, only the right child is guaranteed to remain in the chain, and it again carries sm−1. Writing these transitions column by column gives Am.Example: the case m = 2. When m = 2, the favorable chain has states s1 = (1 , 2) and s0 = (0 , 2). For the most favorable symbol a = 2, one has M (2) es1 = es0 + es1 and M (2) es0 = es1 . Hence, in the ordered basis ( s1, s 0), the restricted matrix is 

A2 = 1 11 0

!

.

Thus, when m = 2, the favorable chain is the Fibonacci transfer rule. The bridge lemma below is the key structural input for lower bounds. It does not make the true branch count multiplicative. Rather, it makes the certified lower count quasi-multiplicative after the explicit insertion of one favorable bridge symbol. 

Lemma 4.2. For all words u, v in the coarse alphabet, we have Γ vb (m)u ≥ Γ( v)Γ( u), with b(m) =(m).

Proof. Write U = M (u) and V = M (v), and set x = PFU⋆ . First note the elementary restart property of the bridge symbol: for every favorable-chain state si = ( i, m ), the vector M (m)esi

contains at least one copy of e⋆ = esm−1 . Indeed, if i ≥ 1, the two certified descendants include (m − 1, m ) = sm−1, while for i = 0 the only certified favorable descendant is again sm−1. By this restart property and nonnegativity, M (m)x ≥ (1⊤ 

> F

x)e⋆ = Γ( u)e⋆. The part of U e ⋆ outside F m can only add nonnegative mass, and therefore M (m)U e ⋆ ≥ Γ( u)e⋆. Applying PF and then 1⊤ 

> F

after V

gives Γ vb (m)u = 1⊤ 

> F

PFV M (m)U e ⋆ ≥ Γ( u)1⊤ 

> F

PFV e ⋆ = Γ( u)Γ( v).

□LARGE DEVIATIONS FOR LINEAR REGIONS 19 

5. Proof of Theorem 2.6 – A bridge lower bound 

This section proves Theorem 2.6. The proof uses the bridge lemma as a sufficient mechanism for forcing large values of the true branch count Nn. The objects introduced below refer only to the certified lower process. They should not be read as exact descriptions of all branches. Throughout this section we fix integers m, r ≥ 1 and assume pm > 0, equivalently pb = pm > 0. The parameter m fixes the defect resolution and the favorable bridge symbol, while r fixes the length of the free blocks. We recall that Wr := Arm, and πr := π⊗r. A block w ∈ W r is called productive if it leaves at least one certified favorable token: 

Gr := {w ∈ W r : Γ( w) > 0}.

For productive blocks we write gr(w) := log Γ( w). Thus, gr is the logarithmic certified output of one free block. The construction begins with one maximally favorable seed layer. This aligns the actual initial state e(m,m ) with the favorable-chain starting state e⋆ used in the definition of Γ. For k ≥ 1, define 

nk := 1 + kr + ( k − 1) .

The first symbol is a seed symbol. After that, the remaining nk − 1 coarse symbols are read as k

free r-blocks W1, . . . , W k, separated by k − 1 copies of the fixed bridge symbol b(m), in the product order Wkb(m)Wk−1b(m) · · · b(m)W1. Let 

L(r) 

> k

:= k−1

> k

X

> i=1

δWi

be the empirical law of the free blocks. Thus, L(r) 

> k

records how often each productive or nonpro-ductive r-block appears in the construction. Let Ek be the event that the seed symbol is m and all 

k − 1 bridge slots equal the bridge word b(m).The probability cost of the forced seed and bridge slots is explicit. Note that for every k ≥ 1, the event Ek is independent of L(r) 

> k

, and P(Ek) = pmpk−1 

> b

. Indeed, the seed, free blocks, and bridge slots depend on disjoint groups of i.i.d. coarse symbols. Hence, the forced seed and bridge slots are independent of the free-block empirical law. The seed has probability pm, and each bridge slot has probability pb. There are k − 1 one-symbol bridge slots, so P(Ek) = pmpk−1 

> b

.We now prove the upper-tail lower bound for Nn. Recall that J+

> r,m

(x) is the optimal cost of choosing a free-block law ν whose average certified output is large enough to exceed the target level x, after accounting for the bridge cost pb. Here, Theorem 2.6 is a lower bound for the true observable Nn, but it is obtained through the certified lower process. It may therefore be non-sharp if many true branches are not counted by Γ. The parameter r controls only the blocking of the free part of the word. For fixed m, increasing r reduces the bridge overhead per unit depth, but it does not remove the possible gap between the certified count and the true branch count. 20 LARGE DEVIATIONS FOR LINEAR REGIONS 

Proof of Theorem 2.6. If J+

> r,m

(x) = ∞, there is nothing to prove. Otherwise, fix ε0 > 0. Choose 

ν ∈ P (Wr), supported on Gr, such that R gr dν > (r + 1) x and ( H(ν | πr) − log pb)/(r + 1) ≤

J+

> r,m

(x) + ε0. Choose α > 0 such that R gr dν > (r + 1) x + 2 α.Since Wr is finite and ν is supported on Gr, we may choose types τk ∈ P (Wr), with denominator 

k, such that τk → ν, τk(Gr) = 1, and R gr dτ k > (r + 1) x + α for all sufficiently large k.On the event Ek ∩ { L(r) 

> k

= τk}, the seed event creates two certified tokens in the top favorable state e⋆. Moreover, all free blocks belong to Gr. Hence repeated application of Theorem 4.2 to the forced word Wkb(m)Wk−1b(m) · · · b(m)W1 gives 

Nnk ≥ 2

> k

Y

> i=1

Γ( Wi), n−1 

> k

log Nnk ≥ n−1 

> k

log 2 + kn −1

> k

Z

gr dτ k.

Since nk/k → r + 1, the right-hand side is larger than x for all sufficiently large k. Thus 

P(n−1 

> k

log Nnk > x ) ≥ P(Ek ∩ { L(r) 

> k

= τk}) for all sufficiently large k.By independence of the seed event, the bridge events, and the free blocks, and by the standard method-of-types estimate on the finite alphabet Wr,

P Ek ∩ { L(r) 

> k

= τk} = pmpk−1 

> b

P L(r) 

> k

= τk

, lim  

> k→∞

k−1 log P L(r) 

> k

= τk

 = −H(ν | πr).

Using also nk/k → r+1, the fixed seed cost log pm and the missing single bridge factor are negligible after division by nk. Hence lim inf  

> k→∞

n−1 

> k

log P n−1 

> k

log Nnk > x  ≥ log pb − H(ν | πr)

r + 1 ≥ − J+

> r,m

(x) − ε0.

Letting ε0 ↓ 0 gives the claim. □

6. Proofs of Theorems 2.4, 2.5, and 2.8 – The uniformly favorable regime and local Telgarsky stability 

This section proves Theorems 2.4, 2.5, and 2.8. The role of the uniformly favorable regime is to remove all probabilistic losses from the certified lower process: every layer is favorable, so the favorable chain evolves deterministically. Example 2.2 is the first instance of this mechanism; the general level-m case has Perron rate ρm ↑ 2. First, the next deterministic estimate is the lower growth bound on the all-favorable event. 

Proposition 6.1. If the first n layers are all maximally favorable, then Nn ≥ cmρnm for some 

cm > 0.Proof. On the event that all first n coarse symbols are m, the favorable chain evolves determinis-tically by Am. Starting from the initial interval, the first layer creates two favorable tokens in the top state, and afterwards the chain count is bounded below by 1⊤An−1 

> m

(2 e⋆). By Perron–Frobenius theory for the primitive matrix Am, this quantity is at least cmρnm after decreasing cm, if necessary, to handle finitely many small n. Since these tokens correspond to actual branches, the same lower bound holds for Nn. □

We now prove the all-favorable mechanism stated at the beginning of the paper. LARGE DEVIATIONS FOR LINEAR REGIONS 21 

Proof of Theorem 2.5. Let E(m) 

> n

be the event that the first n layers are all maximally favorable. Then P(E(m) 

> n

) = pnm. On this event, Proposition 6.1 gives Nn ≥ cmρnm, and hence n−1 log Nn ≥

log ρm + n−1 log cm. For all sufficiently large n, the right-hand side is at least log ρm − δ, so lim inf  

> n→∞

n−1 log P n−1 log Nn ≥ log ρm − δ ≥ log pm.

Moreover, E[Nn] ≥ E[Nn1E(m)

> n

] ≥ cmρnmpnm = cm(pmρm)n. The final claim is immediate. □

The next proposition quantifies how close the deterministic certified growth rate can be to the ideal Telgarsky rate 2. The parameter m is fixed throughout the proposition. 

Proposition 6.2 (Perron-root asymptotics) . The Perron root ρm of Am satisfies ρ1 = 1 , ρm ∈

(1 , 2) for every m ≥ 2, 2 − ρm = ρ−mm , ρm ↑ 2, 2 − ρm ∼ 2−m, and log 2 − log ρm ∼ 2−m−1.Proof. The matrix Am is primitive: from any favorable-chain state one can reach the top state in one step, and from the top state one can reach any target state in at most m−1 further steps. Hence, 

Amm is strictly positive. Its characteristic polynomial is λm − λm−1 − · · · − λ − 1, so its Perron root is the unique positive solution of ρmm = ρm−1 

> m

+ · · · + ρm + 1. Equivalently 1 = Pmj=1 ρ−jm . The case 

m = 1 gives ρ1 = 1. For m ≥ 2, the function λ 7 → Pmj=1 λ−j is strictly decreasing on (0 , ∞), takes the value m > 1 at λ = 1, and takes a value strictly smaller than 1 at λ = 2. Thus, ρm ∈ (1 , 2). The same strict-monotonicity argument shows that ρm+1 > ρ m, because Pm+1  

> j=1

ρ−jm = 1 + ρ−(m+1)  

> m

> 1. For m = 1, the identity 2 − ρm = ρ−mm is immediate. For m ≥ 2, multiplying the characteristic equation by ρm − 1 gives ρm+1  

> m

− 2ρmm + 1 = 0, which is equivalent to 2 − ρm = ρ−mm . Since ρm is increasing and bounded above by 2, it has a limit. Moreover, ρm ≥ ρ2 > 1 for m ≥ 2, so ρ−mm → 0. The identity 2 − ρm = ρ−mm therefore forces ρm ↑ 2. Write δm := 2 − ρm. Since δm = ρ−mm ≤ ρ−m 

> 2

,one has mδ m → 0. Furthermore, δm = ρ−mm = 2 −m(1 − δm/2) −m. Since mδ m → 0, it follows that (1 − δm/2) −m → 1, and hence δm ∼ 2−m. Finally, log 2 − log ρm = − log(1 − δm/2) ∼ δm/2 ∼

2−m−1. □

We now prove the favorable-chain theorem stated in Section 2.3 and the local lower-tail exclusion theorem. The latter is obtained by choosing the defect resolution first and the noise level second. This order is important. For a given tolerance ξ > 0, we first choose m so that the certified Perron rate ρm is within ξ of 2. Only after m is fixed do we choose ε small enough that every admissible layer is m-favorable. 

Proof of Theorem 2.4. The assertions about the Perron root ρm are exactly Proposition 6.2. The deterministic lower bound for Nn on the all-favorable event is Proposition 6.1. Combining these two propositions proves the theorem. □

Proof of Theorem 2.8. First choose m = m(ξ) so large that log ρm > log 2 − ξ/ 2, which is possible by Proposition 6.2. Say that the environment is m-favorable if η∗(ε) := sup ω∈Uε η(ω) ≤ rm. This condition says that the random environment is so close to the Telgarsky map that every layer is favorable at level m. With this m fixed, choose ε∗(ξ) > 0 so small that η∗(ε) ≤ rm whenever 0 < ε ≤ ε∗(ξ). This is possible because η∗(ε) → 0 as ε ↓ 0, while rm > 0 is now fixed. Under this condition every layer is m-favorable, so Proposition 6.1 gives Nn ≥ cmρnm almost surely. Finally, choose n0 = n0(ξ) so large that n−1 log cm ≥ − ξ/ 2 for all n ≥ n0. Then, for all such n we have 

n−1 log Nn ≥ log ρm + n−1 log cm > log 2 − ξ almost surely. Hence, P n−1 log Nn ≤ log 2 − ξ = 0 ,

for all n ≥ n0. □22 LARGE DEVIATIONS FOR LINEAR REGIONS 

Declarations 

Acknowledgements. The authors used OpenAI’s ChatGPT during the preparation of this man-uscript. All mathematical statements, proofs, computations, references, and conclusions were in-dependently checked and verified by the authors, who take full responsibility for the content of the manuscript. 

Funding. This research was supported by the Scientific and Technological Research Council of T¨ urkiye (T ¨UB ˙ITAK) under the T ¨UB ˙ITAK-B ˙IDEB 2219 International Postdoctoral Research Fel-lowship Programme, Grant No. 1059B192501091. 

Conflict of Interest On behalf of all authors, the corresponding author states that there is no conflict of interest. 

Data Availability The data will be available on reasonable request. 

References 

[1] L. Andreis, F. Bassetti, and C. Hirsch. LDP for the covariance process in fully connected Gaussian neural networks. Electron. J. Probab. , 31:1–35, 2026. Article 22. [2] V. Bansaye and J. Berestycki. Large deviations for branching processes in random environment. Markov Process. Relat. Fields , 15(4):493–524, 2009. [3] M. Bianchini and F. Scarselli. On the complexity of neural network classifiers: A comparison between shallow and deep architectures. IEEE Trans. Neural Netw. Learn. Syst. , 25(8):1553–1565, 2014. [4] J. D. Biggins and N. H. Bingham. Large deviations in the supercritical branching process. Adv. Appl. Probab. ,25(4):757–772, 1993. [5] K. Fleischmann and V. Wachtel. Lower deviation probabilities for supercritical Galton–Watson processes. Ann. Inst. Henri Poincar´ e Probab. Stat. , 43(2):233–255, 2007. [6] P. W. Glynn and W. Whitt. Large deviations behavior of counting processes and their inverses. Queueing Syst. ,17:107–128, 1994. [7] B. Hanin and D. Rolnick. Complexity of linear regions in deep networks. In Proceedings of the 36th International Conference on Machine Learning , volume 97 of Proceedings of Machine Learning Research , pages 2596–2604, 2019. [8] C. Hirsch and D. Willhalm. Large deviations of one-hidden-layer neural networks. Sto.Dyn. , 24:25502, 2024. [9] T. Kuczek and K. N. Crank. A large-deviation result for regenerative processes. J. Theor. Probab. , 4(3):551–561, 1991. [10] S. D. Lillo, C. Macci, and B. Pacchiarotti. Large deviation principles and functional limit theorems in the deep limit of wide random neural networks, 2026. [11] C. Macci, B. Pacchiarotti, K. Papagiannouli, G. L. Torrisi, and D. Trevisan. Functional large deviations for wide deep neural networks with Gaussian initialization and Lipschitz activations. arXiv preprint arXiv:2601.18276 ,2026. [12] C. Macci, B. Pacchiarotti, and G. L. Torrisi. Large and moderate deviations for Gaussian neural networks. J. Appl. Probab. , 63(1):96–115, 2026. [13] G. F. Mont´ ufar, R. Pascanu, K. Cho, and Y. Bengio. On the number of linear regions of deep neural networks. In Advances in Neural Information Processing Systems 27 , pages 2924–2932, 2014. [14] P. E. Ney and A. N. Vidyashankar. Local limit theory and large deviations for supercritical branching processes. 

Ann. Appl. Probab. , 14(3):1135–1166, 2004. [15] R. Novak, Y. Bahri, D. A. Abolafia, J. Pennington, and J. Sohl-Dickstein. Sensitivity and generalization in neural networks: An empirical study. In International Conference on Learning Representations , 2018. [16] R. Pascanu, G. F. Mont´ ufar, and Y. Bengio. On the number of response regions of deep feed forward networks with piece-wise linear activations. In International Conference on Learning Representations , 2014. [17] P. Petersen and J. Zech. Mathematical theory of deep learning. arXiv preprint arXiv:2407.18384 , 2024. LARGE DEVIATIONS FOR LINEAR REGIONS 23 

[18] M. Raghu, B. Poole, J. Kleinberg, S. Ganguli, and J. Sohl-Dickstein. On the expressive power of deep neural networks. In Proceedings of the 34th International Conference on Machine Learning , volume 70 of Proceedings of Machine Learning Research , pages 2847–2854, 2017. [19] T. Sepp¨ al¨ ainen and J. E. Yukich. Large deviation principles for Euclidean functionals and other nearly additive processes. Probab. Theory Related Fields , 120(3):309–345, 2001. [20] T. Serra, C. Tjandraatmadja, and S. Ramalingam. Bounding and counting linear regions of deep neural networks. In Proceedings of the 35th International Conference on Machine Learning , volume 80 of Proceedings of Machine Learning Research , pages 4558–4566, 2018. [21] M. Telgarsky. Representation benefits of deep feedforward networks. arXiv preprint arXiv:1509.08101 , 2015. [22] M. Telgarsky. Benefits of depth in neural networks. In Proceedings of the 29th Conference on Learning Theory ,volume 49 of Proceedings of Machine Learning Research , pages 1517–1539, 2016. [23] Q. Vogel. Large deviations of Gaussian neural networks with ReLu activation. Statist. Probab. Lett. , 230:110611, 2026. [24] M. Zamparo. Large deviation principles for renewal–reward processes. Stochastic Process. Appl. , 156:226–245, 2023.