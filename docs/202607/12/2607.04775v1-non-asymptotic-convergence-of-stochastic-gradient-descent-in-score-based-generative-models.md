---
title: Non-asymptotic Convergence of Stochastic Gradient Descent in Score-based Generative Models
title_zh: 基于分数的生成模型中随机梯度下降的非渐近收敛性
authors: "Stanislas Strasman, Sobihan Surendran, Sylvain Le Corff"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04775v1"
tags: ["query:ml-theory"]
score: 8.0
evidence: 基于分数生成模型中SGD非凸收敛分析
tldr: 基于分数的生成模型训练依赖随机梯度下降，但其优化动力学理论尚不完善。本文首次给出一般参数化下SGD在加权去噪分数匹配目标上的非凸收敛率，并针对过参数化两层ReLU网络进行NTK分析，得到沿SGD轨迹的分数逼近误差界。研究量化了重加权因子对误差的影响，为实际权重选择提供理论依据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04775v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 798, \"height\": 589, \"label\": \"Figure\"}]"
motivation: SGMs训练依赖随机梯度下降，但缺乏优化收敛性的理论保证，尤其是非凸和随机梯度情形。
method: 分别对一般参数化建立非凸收敛率，对过参数化两层ReLU网络进行NTK分析，并结合扩散训练随机梯度特性。
result: 得到SGD在加权去噪分数匹配目标上的非凸收敛率，以及沿轨迹的分数逼近误差界，并量化重加权因子的作用。
conclusion: 首次提供SGD训练SGMs的非渐近收敛保证，为实践中权重选择提供理论指导。
---

## 摘要
基于分数的生成模型（SGMs）在广泛的应用中实现了令人印象深刻的数据生成性能。尽管其采样过程的统计特性日益清晰，但其底层训练的优化动态仍较少被探索。SGMs通常通过最小化加权去噪分数匹配目标来训练，但随机梯度的优化保证仍然有限。在这项工作中，我们研究了SGMs的随机梯度下降（SGD），在两个互补的领域中贡献了结果。首先，对于一般分数参数化，我们建立了SGD在加权去噪分数匹配目标上的非凸收敛速率，明确依赖于时间表相关的权重因子。其次，对于过参数化的两层ReLU网络，我们开发了适用于随机梯度扩散训练的神经正切核分析，得出了沿SGD轨迹的分数逼近误差界。最后，我们的分析量化了重加权因子在分数逼近误差中的作用，为实际中使用的权重选择提供了理论指导。

## Abstract
Score-based Generative Models (SGMs) have achieved impressive performance in data generation across a wide range of applications. While the statistical properties of their sampling procedures are increasingly well understood, the optimization dynamics underlying their training remain less explored. SGMs are typically trained by minimizing a weighted denoising scorematching objective, yet optimization guarantees with stochastic gradients remain limited. In this work, we study Stochastic Gradient Descent (SGD) for SGMs, contributing results in two complementary regimes. First, for general score parameterizations, we establish a non-convex convergence rate for SGD on the weighted denoising score-matching objective, with explicit dependence on the schedule-dependent weighting factors. Second, for overparameterized two-layer ReLU networks, we develop a Neural Tangent Kernel analysis tailored to diffusion training with stochastic gradients, yielding score-approximation error bounds along the SGD trajectory. Finally, our analysis quantifies the role of the reweighting factor in the score approximation error, providing theoretical guidance for weighting choices used in practice.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：基于分数的生成模型（SGMs）通过最小化加权去噪分数匹配目标进行训练，常用的优化器是随机梯度下降（SGD）及其变体。尽管SGMs的采样理论（如采样误差分解、泛化界）已较为成熟，但**训练过程中的优化动态**（即SGD如何收敛、分数逼近误差如何控制）仍缺乏理论保证。现有工作要么假设梯度下降（而非SGD），要么依赖于有界数据或高斯目标等强假设。
- **核心问题**：本文旨在建立**SGD训练SGMs的非渐近收敛保证**，包括：
  - 对于一般（平滑）分数参数化，SGD在加权去噪分数匹配目标上的非凸收敛速率。
  - 对于过参数化两层ReLU网络，沿SGD轨迹的分数逼近误差界。
  - 量化重加权因子（损失权重 λ(t)）对优化和采样误差的影响。
- **整体含义**：首次从优化理论角度解释了SGMs训练中广泛使用的`λ(t)=σ_t²`权重策略的合理性，为实际训练提供理论指导。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将SGD训练SGMs的分析分为两个互补层次：
  1. **一般参数化（平滑网络）**：利用非凸随机优化中的经典技术（L-平滑性、方差界）推导SGD收敛率。
  2. **过参数化两层ReLU网络**：采用神经正切核（NTK）分析，结合扩散训练中随机梯度的特性，得到沿SGD轨迹的分数逼近误差上界。
- **关键技术细节**：
  - **正向扩散过程**：
    \[
    d\overrightarrow{X}_t = -\alpha\beta_t \overrightarrow{X}_t dt + \sqrt{2\beta_t} dB_t,\quad X_0\sim \pi_{\text{data}}
    \]
    边缘分布 $\overrightarrow{X}_t \overset{L}{=} m_t X_0 + \sigma_t Z$。
  - **去噪分数匹配目标**：
    \[
    L_{\text{DSM}}(\theta) = \frac12 \mathbb{E}\left[\frac{\lambda(\tau)}{q(\tau)}\left\| s_\theta(\overrightarrow{X}_\tau,\tau) - \frac{X_0 - \overrightarrow{X}_\tau}{\sigma_\tau^2} \right\|^2\right]
    \]
    其中 $\tau\sim q$。
  - **SGD更新**（算法1）：从 $\theta^{(0)}$ 开始，每一步使用B个样本的mini-batch计算随机梯度：
    \[
    \theta^{(k+1)} = \theta^{(k)} - \gamma_{k+1} \hat{\nabla}_\theta L_{\text{DSM}}(\theta^{(k)}; \mathcal{D}^{(k+1)})
    \]
  - **一般参数化的收敛定理（Theorem 3.1）**：在假设1（数据相关Lipschitz和光滑性）和假设2（多项式增长）下，以步长 $\gamma_n = C_\gamma n^{-1/2}$，得到
    \[
    \mathbb{E}[\|\nabla L_{\text{DSM}}(\theta^{(J)})\|^2] \leq O\left(\frac{\log n}{\sqrt{n}}\right)
    \]
    并显式依赖于权重因子 $\lambda(t)/(\sigma_t^2 q(t))$。
  - **过参数化两层ReLU网络的NTK分析（Theorem 4.1）**：
    - 网络形式：$f(u;W) = \frac{1}{\sqrt{m}} A D_{u,W} W u$，只训练第一层权重 $W$，输出层 $A$ 固定为Rademacher初始化。
    - 局部化技巧：将分析限制在半径 $R$ 的球内，控制无界输入带来的尾概率。
    - 误差分解：预测误差 $\Delta_k$ 满足递归 $\Delta_{k+1} = (I-\gamma_k \mathbf{K}_k)\Delta_k - v_k + \varepsilon_k$，其中 $\mathbf{K}_k$ 是经验NTK矩阵。
    - 通过比较 $\mathbf{K}_k$ 与极限核 $\mathbf{K}_\infty$，得到投影DSM损失的上界，由四项组成：谱收缩、随机梯度噪声、有限宽度效应、局部化尾误差。
- **算法流程**：见Algorithm 1（SGD for Denoising Score Matching）——详述了每一步采样 $X_0, t, Z$、计算扰动样本、随机梯度、参数更新。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集**：Leeds Butterfly数据集（Wang et al., 2009），这是一个中等规模的图像数据集（约700张蝴蝶图片），常用于图像生成评估。
- **场景**：评估损失加权函数 $\lambda(t)$ 对生成质量的影响。
- **Benchmark & 对比**：固定其他训练和采样设置（NCSN++架构、Euler-Maruyama离散化1000步、200,000次迭代），对比五种权重调度：
  \[
  \lambda(t) \in \{1,\ \sigma_t,\ \sigma_t^2,\ \sigma_t^3,\ \sigma_t^4\}
  \]
  采用均匀时间采样 $q(t)=1$。没有对比其他方法（如不同优化器、不同网络架构），仅评估不同 $\lambda$ 下的FID。
- **评价指标**：Fréchet Inception Distance (FID)（Heusel et al., 2017），越低表示生成质量越好。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点

- 文中明确提到：实验在 **NVIDIA RTX 6000 GPUs with 48 GB of VRAM** 上进行。
- **未明确说明**：使用的GPU数量、训练总时长、每个模型训练的具体时间。仅提到训练200,000次迭代，未给出小时数。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平

- **实验数量**：仅包含**一组实验**，即五种 $\lambda(t)$ 调度在各5次独立运行下的FID对比（图1）。没有进行其他数据集验证、不同网络架构、不同优化器（如Adam）、不同采样步数或不同时间采样策略的消融。
- **充分性**：该实验主要作为**理论分析的经验佐证**，并非全面实证评估。作者明确指出“This experiment is intended as an empirical illustration”。因此从理论验证角度是充分的，但作为实际应用指南则覆盖不足。
- **客观性与公平性**：除 $\lambda$ 外其他超参数固定，运行五次取平均和标准差，方法客观。但未与现有SOTA权重策略（如Min-SNR）或更简单的基线（如固定权重）对比，公平性只能说是有限。

### 6. 论文的主要结论与发现

- **理论结论**：
  - 一般参数化下，SGD收敛到近稳定点，速率 $O(\log n/\sqrt{n})$，且优化误差依赖于 $\lambda(t)/(\sigma_t^2 q(t))$。
  - 对于过参数化两层ReLU网络，投影DSM损失沿SGD轨迹的上界由谱收缩、随机噪声、有限宽度和局部化尾项组成，在适当的宽度条件下可控。
  - **重加权因子的影响**：理论提示 $\lambda(t)=\sigma_t^2$ 平衡了不同噪声区间的优化难度，是自然选择。
- **实验发现**：
  - $\lambda(t)=\sigma_t^2$ 达到最低FID（约40），显著优于 $\lambda=1,\sigma_t,\sigma_t^3,\sigma_t^4$。
  - $\lambda=1$ 和 $\lambda=\sigma_t$ 在低噪声区权重不足，而 $\lambda=\sigma_t^3$ 和 $\lambda=\sigma_t^4$ 在高噪声区过重，均导致性能下降。
- **整体结论**：首次为SGD训练SGMs提供非渐近优化保证，从优化角度解释了实践中广泛使用的信号-噪声比加权（$\lambda(t)=\sigma_t^2$）的理论基础。

### 7. 优点：方法或实验设计上有哪些亮点

- **理论贡献全面**：同时覆盖了两种互补的设定——一般平滑参数化（适合sigmoid/tanh/softplus等）和过参数化ReLU网络（更贴合实际），给出了非渐近界，而非渐近或渐近分析。
- **NTK分析适配扩散训练**：考虑了无界输入、随机梯度和时间变量，改进了以往需要数据有界或仅梯度下降的限制。
- **清晰解释了权重选择**：定理3.1和4.1均揭示了 $\lambda(t)$ 与 $\sigma_t^2$ 的关系，并未使用 $\lambda(t)=\sigma_t^4$（对应不加权MSE）作为对比，而是指出后者在实际中表现更差，这为理论提供支撑。
- **实验设计简洁但有针对性**：虽然只有一组实验，但直接验证了理论预测的权重策略，且结果与理论高度一致，增强了可信度。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验覆盖不足**：仅在一个小数据集（Leeds Butterfly）上进行，未在ImageNet、CIFAR-10等标准图像生成benchmark上验证；仅测试了NCSN++架构，未测试其他常见架构（如DDPM U-Net、Latent Diffusion）。
- **理论限制**：
  - 一般参数化分析**排除了ReLU**（需要光滑激活函数）。
  - NTK分析限于**两层全连接网络**，而实际SGM使用深度U-Net，两者差距大，推广性存疑。
  - 假设了子高斯数据分布（Assumption 3），实际数据可能更复杂（如重尾或多模态）。
- **未对比现有优化策略**：如Adam、学习率调度、梯度裁剪等在实际SGM训练中发挥重要作用，本文未涉及。
- **仅分析了SGD基本形式**，未覆盖动量、权重衰减、EMA等常见trick。
- **实验未包含完整的采样误差分解**：仅给出了FID，未展示优化误差、泛化误差、离散化误差分别如何贡献。
- **可能存在的偏差**：实验中仅改变 $\lambda$，但不同 $\lambda$ 可能导致损失尺度变化，可能影响学习率有效性，而文中未调整学习率或其他超参数。
- **应用限制**：理论结果依赖于有限宽度条件（式19），要求宽度随迭代次数和维度指数增长，实际中往往不满足，因此定理在有限宽度下是否成立需要进一步验证。

（完）
