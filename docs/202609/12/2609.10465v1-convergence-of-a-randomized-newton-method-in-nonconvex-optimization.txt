Title: Convergence of a Randomized Newton Method in Nonconvex Optimization

URL Source: https://arxiv.org/pdf/2609.10465v1

Published Time: Thu, 10 Sep 2026 01:32:50 GMT

Number of Pages: 10

Markdown Content:
CONVERGENCE OF A RANDOMIZED NEWTON METHOD IN NONCONVEX OPTIMIZATION ∗       

> EDWARD HUYNH †AND BJORN ENGQUIST ‡
> Abstract. We analyze a stochastic Newton optimization scheme for locating the unique global minimizer of a general nonconvex objective function. The method couples a Newton algorithm to additive Gaussian noise with state-dependent variance. In the bounded domain setting, we prove global almost sure convergence. The proof is based on two features of the algorithm: a nondegenerate exploratory property that ensures entrance into a neighborhood of the minimizer after a finite number of steps, and a decaying-noise property that yields contraction with high probability and prevents infinitely many exits from the neighborhood of the minimum.
> Keywords. randomized Newton’s method, stochastic optimization, quadratic convergence
> AMS subject classifications. 90C26; 90C15; 65K05

1. Introduction Newton’s method is one of the most common optimization algo-rithms and is well-known for its fast local quadratic convergence. Global optimization of non-convex objective functions is of increasing importance throughout the fields of science and engineering and is a major component in machine learning. This gener-ates interest in randomized methods where the noise helps to avoid trapping in local minima and saddle points. The randomness typically comes from added noise or from randomized sub-sampling of the objective or loss function. Properties of stochastic Newton with variants have been studied previously, for instance, in [1, 6, 9, 10]. The proofs of convergence for many of these ideas rely on classical ideas from Robbins-Monro [8] or on applying the Robbins-Siegmund theorem for “almost” supermartingale type processes. In [5], a general framework for a wide class of stochastic optimization methods is presented. Many of these results rely on the underlying cost function being convex or they prove almost sure convergence to a stationary point rather than to the global minimizer. Convergence naturally requires the random term to vanish in the limit and for a global minimization the term cannot vanish too fast to avoid trapping in local minima. The classical papers [2, 4] prove slow convergence rate with the random term decaying logarithmically with time or number of iterations for stochastic gradient descent in the general nonconvex case. The paper [2] also argues that almost sure convergence is not possible for such decay mechanism. To achieve almost sure convergence, we assume the unique optimal value of the objective function to be known so the random term can be scaled by the value of the objective function rather than by time. Knowing this value is quite common, for instance, in the classic problem of fitting a high-degree polynomial against a low number of datapoints. Similarly, the assumption holds true for optimization problems in machine learning involving overparameterized neural networks. The objective function or loss minimum is 0, although the minimum may be obtained at various minimizers. In [3,12] convergence to the global minimizer in a nonconvex setting was shown for stochastic gradient descent based on a known or estimated minimal value and state dependent variance of the noise. 

> ∗Received date, and accepted date (The correct dates will be entered by the editor).
> †edhuynh@utexas.edu
> ‡engquist@oden.utexas.edu 1arXiv:2609.10465v1 [math.OC] 9 Sep 2026

2 A RANDOMIZED NEWTON METHOD IN NONCONVEX OPTIMIZATION 

The rest of the paper contains the precise formulation of the convergence theorem and its proof. The proof is divided into two parts. In the exploration part we show that with any starting value the iterates will enter a small neighborhood of the global minimum after a finite number of steps with probability 1. In the so-called exploitation phase we show that starting in the neighborhood of the global minimum the iterates converge quadratically to the minimum with positive probability. Combining these properties finalizes the proof of almost sure quadratic convergence. 

2. Statement of Results and Proofs We consider the periodic setting Ω = Td.The goal is to prove global almost sure convergence to the unique minimizer. The proof below is organized around two separate effects. Away from the minimizer, the noise is nondegenerate and therefore gives a uniform probability of entering any prescribed neighborhood of x∗. Near the minimizer, the deterministic part is the Newton map, and the state-dependent noise is small enough that there is a positive probability of remaining in a shrinking sequence of balls. Repeated entrances into the local basin then force one successful entrance almost surely. 

Assumptions. We assume: 

A1 f ∈ C2(Td) and f has a unique global minimizer x∗ ∈ Td. We normalize so that 

fmin = f (x∗) = 0. 

A2 There exist constants r0,m,L > 0 such that, on Br0 (x∗), one has 

mI d ⪯ H(x), ∥H(x) − H(y)∥ ≤ L∥x − y∥, x,y ∈ Br0 (x∗).

Assumption A1 is the global nonconvex assumption and implies that for any x̸ = x∗

such that ∇f (x) = 0, we have f (x) − f (x∗) > 0. Assumption A2 says that there is a sufficiently nice neighborhood of the minimizer. Throughout this section, we choose 

r0 smaller if necessary so that the ball Br0 (x∗) may be identified with an ordinary Euclidean ball in a single coordinate chart of the torus. All additions below are understood modulo the periodic identification of Td.

Remark 2.1. The assumptions of bounded domain and periodic boundary conditions are used explicitly in the exploration phase of the proofs. The boundary conditions could potentially be replaced by reflection. This will only affect the proof slightly, but the algorithm itself will be much more complex to accommodate multiple reflections. It would also be possible to handle an infinite domain but then the objective function and noise bound should be of a specific form in the far field such that the iterates will enter a fixed finite domain after a finite number of steps. 

Proposition 2.1. Under A1 –A2 , if we define 

M0 := sup  

> y∈Br0(x∗)

∥H(y)∥,

then M0 < ∞ and 

m

2 ∥x − x∗∥2 ≤ f (x) ≤ M0

2 ∥x − x∗∥2 , ∀x ∈ Br0 (x∗). (2.1) 

Proof . Since H is continuous on Br0 (x∗), the quantity M0 is finite. Also, ∇f (x∗) = 0 because x∗ is a minimizer. Taylor’s theorem with integral remainder gives 

f (x) = 

Z 10

(1 − t)( x − x∗)T H(x∗ + t(x − x∗))( x − x∗)dt, x ∈ Br0 (x∗).HUYNH, ENGQUIST 3The segment x∗ + t(x − x∗) remains inside Br0 (x∗). Using mI d ⪯ H and ∥H∥ ≤ M0 on this ball, we obtain 

m∥x − x∗∥2 ≤ (x − x∗)T H(x∗ + t(x − x∗))( x − x∗) ≤ M0 ∥x − x∗∥2 .

Integrating in t and using R 10 (1 − t)dt = 1 /2 gives the claim. We consider the stochastic iterative scheme 

Xn+1 =

(

Xn − H(Xn)−1∇f (Xn) + f (Xn)αZn, H(Xn) invertible ,Xn + f (Xn)αZn, otherwise , (2.2) where α > 12 and Zn ∼ N (0 ,I d) are independent. Here the definition of the second branch of the scheme is necessary as the function f may have a degenerate Hessian at (possibly) several points in its domain. However, it will turn out that this definition will only be necessary for proving entrance into a “nice enough” neighborhood on which the Hessian will be non-singular. Note that on the torus, after the Euclidean update we take the periodic representative of the resulting point. As an aside, the scheme (2.2) is reminiscent of stochastic gradient descent schemes of the form [8] 

Xn+1 = Xn − η(t)G(Xn−1;ξn) (2.3) where {ξi} are independent random variables, η(t) denotes a step-size, and G is some unbiased approximation of the gradient ∇f . These schemes generally converge under decaying step sizes, e.g. η = η0/n [11] with convergence rate O(1 /n ) in expectation when 

f is strongly convex. Unlike this result, we will prove Theorem 2.1 which gives a path-wise type quadratic convergence rate. Moreover, our scheme (2.2) will converge to the global minimizer of the (possibly) nonconvex function f , unlike (2.3) which generally converges to stationary points ˜ x (i.e. ∇f (˜ x) = 0). Before proving Theorem 2.1, we will need to prove a sequence of lemmas. We first prove an estimate on the “deterministic” step. 

Lemma 2.1 ( Local Newton estimate ). Define the deterministic Newton map 

T (x) := x − H(x)−1∇f (x)

on Br0 (x∗). Then for every x ∈ Br0 (x∗),

∥T (x) − x∗∥ ≤ L

2m ∥x − x∗∥2 . (2.4) 

Consequently, for every q ∈ (0 ,1) there exists r ∈ (0 ,r 0) such that 

∥T (x) − x∗∥ ≤ q ∥x − x∗∥, ∀x ∈ Br (x∗), (2.5) 

and hence T (Br (x∗)) ⊂ Br (x∗).Proof . The proof of (2.4) follows from classical proofs in optimization texts, for instance [7]. If r > 0 is chosen so that ( L/ (2 m)) r ≤ q, then (2.5) follows immediately. The inclusion T (Br (x∗)) ⊂ Br (x∗) follows because q < 1. We now choose, once and for all, a radius r ∈ (0 ,r 0) small enough so that 

L

2m r ≤ 14 , f (x) ≤ 12 , ∀x ∈ Br (x∗). (2.6) 4 A RANDOMIZED NEWTON METHOD IN NONCONVEX OPTIMIZATION 

The first condition gives a strict Newton contraction on Br (x∗), while the second con-dition is only a convenient normalization of the size of the objective in the local basin. 

Lemma 2.2 ( Entrance into the local basin ). Let 

τr := inf {n ≥ 0 : Xn ∈ Br (x∗)}.

Then 

P(τr < ∞) = 1 .

In particular, there exists p∗ > 0 such that 

P(τr > n ) ≤ (1 − p∗)n, n ≥ 0. (2.7) 

Proof . By continuity of f and compactness of Td, for every 0 < r < r 0 we have 

br := min {f (x) : x ∈ Td \ Br (x∗)} > 0. (2.8) The strict positivity follows from the uniqueness of the global minimizer and the nor-malization f (x∗) = 0. On Td \ Br (x∗), the objective is bounded below by br > 0 from (2.8). Hence the noise amplitude satisfies 

f (y)α ≥ bαr , y ∈ Td \ Br (x∗).

For each fixed y / ∈ Br (x∗), the one-step transition law has a periodized Gaussian density on Td. Moreover, since f is bounded above on Td and bounded below by br on Td \

Br (x∗), the variance of the Gaussian perturbation is bounded above and below whenever the current state lies outside Br (x∗). Hence the corresponding periodized Gaussian density admits a strictly positive lower bound on Td, uniformly over all such current states. Therefore there exists p∗ > 0 such that inf  

> y∈Td\Br(x∗)

P(Xn+1 ∈ Br (x∗) | Xn = y) ≥ p∗.

Now 

{τr > n + 1 } = {τr > n } ∩ { Xn+1 /∈ Br (x∗)}.

Using the tower property and the Markov property, 

P(τr > n + 1) = E1{τr >n }P(Xn+1 /∈ Br (x∗) | F n)

= E1{τr >n }P(Xn+1 /∈ Br (x∗) | Xn)

≤ (1 − p∗)P(τr > n ).

Iteration gives (2.7), and therefore P(τr < ∞) = 1. The next lemma is the precise shrinking-ball mechanism. It says that once the process enters the local Newton basin, there is a uniformly positive probability that it remains in a deterministic sequence of smaller and smaller balls. Before we do this, we first prove a proposition that will be needed in the proof. 

Proposition 2.2 ( Tail bound on Gaussian Steps ). Let {Yi}di=1 be independent and identically distributed standard Gaussian random variables (i.e. μ = 0 and σ = 1 ). Then letting Z = Pdi=1 Y 2 

> i

we have for any ε > 0

P(Z > ε ) ≤ 2d/ 2e− ε 

> 4

.HUYNH, ENGQUIST 5

Proof . Since Yi ∼ N (0 ,1), then Zi = Y 2 

> i

is a chi-squared random variable with 1 degree of freedom. By independence, then Z = Pdi=1 Zi is a chi-squared random variable with d degrees of freedom. Then by Chernoff’s inequality 

P(Z > ε ) = P eλZ > e λε  ≤ 1

eλε (1 − 2λ)−d/ 2.

We may choose any λ < 12 so we choose λ = 14 to obtain the bound 

P(Z > ε ) ≤ 2d/ 2e− ε 

> 4

.

Lemma 2.3 ( Quadratic Newton tube ). Suppose α > 1. There exist constants Q > L

> 2m

and ps > 0 with the following property: For every deterministic time N and every state 

XN ∈ Br (x∗),

P(∥XN +k − x∗∥ ≤ ak for all k ≥ 0 | F N ) ≥ ps. (2.9) 

where a0 = r and ak+1 = Qa 2 

> k

for k ≥ 1. Consequently, with conditional probability at least ps, one has 

XN +k → x∗

without leaving Br (x∗).Proof . Fix N ≥ 1. Let 

C0 := L

2m , K0 := 

 M0

2

α

.

Choose any Q > C 0 and set a0 = r and ak+1 = Qa 2

> k

. We shrink r < r 0 such that 

Qr ≤ 1 and f (x) ≤ 12 , ∀x ∈ Br (x∗).

Consequently, a1 = Qa 20 ≤ a0 ≤ r. Applying induction yields that for all k ≥ 0 we have 

ak ≤ ak 

> 0

.Thus, if 

K0a2αk ∥ZN +k∥ ≤ ak+1 − C0a2

> k

, (2.10) then on the event {∥ XN +k − x∗∥ ≤ ak} the inequalities (2.4) and (2.10) imply 

∥XN +k+1 − x∗∥ ≤ ∥ T (XN +k) − x∗∥ + f (XN +k)α ∥ZN +k∥≤ C0a2 

> k

+ K0a2αk ∥ZN +k∥≤ C0a2 

> k

+ ak+1 − C0a2 

> k

= ak+1 .

To guarantee (2.10), we require 

∥ZN +k∥ ≤ Q − C0

K0

a2−2αk .

Since α > 1, the exponent 2 − 2α is negative, and hence the right-hand side tends to ∞

as k → ∞ . Set Rk =: Q−C0 

> K0

a2−2αk . By Proposition 2.2 there exist constants C1,C 2 > 0such that 

P(∥ZN +k∥ > R k) = P ∥ZN +k∥2 > R 2

> k

 ≤ C1e−C2a2−2αk .6 A RANDOMIZED NEWTON METHOD IN NONCONVEX OPTIMIZATION 

It follows that 

> ∞

X

> k=0

P(∥ZN +k∥ > R k) < ∞.

Consequently, there exists I0 ∈ N such that for all i ≥ I0

P(∥Zi∥ ≥ Ri) < 12 , ∀i ≥ I0.

Using the inequality log(1 − x) ≥ − 2x for all 0 < x < 12 gives 

> ∞

X

> k=I0

log P(∥Zk∥ ≤ Rk) ≥ − 2

> ∞

X

> k=I0

P(∥Zk∥ > R k) > −∞ .

Thus, adding back in the first I0 − 1 terms and exponentiating the sum shows that 

ps := 

> ∞

Y

> k=0

P(∥Zk∥ ≤ Rk) > 0.

The independence of the Zn then implies that, conditionally on FN , the event 

SN := 

> ∞

\

> k=0

{∥ ZN +k∥ ≤ Rk}

has probability at least ps, i.e. on the event {XN ∈ Br (x∗)} we have 

P(SN | F N ) ≥ ps, ∀N ≥ 1.

On SN , we apply induction to obtain 

∥XN +k − x∗∥ ≤ ak, k ≥ 0.

Since ak → 0, this proves the lemma. 

Remark 2.2. We note that α > 1 appears quite naturally in the proof where we require the noise to generally be bounded, but at the cost of increasing the noise level, i.e. α > 1.It is tempting to use a similar proof for α = 1 as this implies the noise is on the same level as the Newton term. Unfortunately, the same proof strategy does not work for 

α = 1 as this would require the Zk to stay within a bounded ball that is not shrinking. 

We shall also use the following simple observation. It handles the case where the process enters Br (x∗) and never leaves, even if it is not inside the particular shrinking tube constructed above. 

Lemma 2.4 ( No exit implies local convergence ). Let α > 12 and N be a deterministic or almost surely finite stopping time such that XN ∈ Br (x∗). On the event 

EN := {XN +k ∈ Br (x∗) for all k ≥ 0},

one has 

XN +k → x∗ almost surely on EN .HUYNH, ENGQUIST 7

Proof . Fix ε ∈ (0 ,r ). We claim that the process cannot visit the annulus 

Aε := {x ∈ Br (x∗) : ∥x − x∗∥ ≥ ε}

infinitely many times while staying forever inside Br (x∗). Indeed, on Aε the function f is bounded above and also bounded below by a positive constant. Thus the periodized Gaussian density of the next iterate has a strictly positive lower bound on Td, uniformly over all current states in Aε. Since Td \ Br (x∗) has positive Lebesgue measure, there exists δε > 0 such that inf 

> x∈Aε

P(Xn+1 /∈ Br (x∗) | Xn = x) ≥ δε. (2.11) Therefore, each time the process lies in Aε, it has conditional probability at least δε of leaving Br (x∗) at the next step. Define the stopping times for j > 1: 

σ1 =: inf {k ≥ 0 : XN +k ∈ Aε},σj =: inf {k > σ j−1 : XN +k ∈ Aε}.

It follows from the strong Markov property and (2.11), we have for each j

P XN +σj +1 ∈ Br (x∗) | F τj

 ≤ 1 − δε.

Since EN ∩ { σ1 < ∞,...,σ J < ∞} ⊂ TJj=1 {σj < ∞ : XN +σj +1 ∈ Br (x∗)} then 

P(EN ∩ { σ1 < ∞,...,σ J < ∞} ) ≤ (1 − δε)J .

Therefore, since EN ∩ { XN +k ∈ Br (x∗) i.o. } = EN ∩ T

> J≥1

{σ1 < ∞,...,σ J < ∞} then by continuity of probability we have 

P(EN ∩ { XN +k ∈ Br (x∗) i.o. }) = lim  

> J→∞

P(EN ∩ { σ1 < ∞,...,σ J < ∞} )

≤ lim 

> J→∞

(1 − δε)J

= 0 .

Thus, on EN , for every rational ε ∈ (0 ,r ), the iterates eventually lie outside Aε. Equiv-alently, limsup 

> k→∞

∥XN +k − x∗∥ ≤ ε

for every rational ε > 0. Hence ∥XN +k − x∗∥ → 0 almost surely on EN .We can now combine the entrance mechanism with the local shrinking-tube mech-anism. 

Theorem 2.1 ( Global almost sure convergence on Td). Assume A1 –A2 , and let {Xn}

be defined by (2.2) with α > 1. Then for every initial condition X0 ∈ Td,

Xn → x∗ almost surely. Additionally, for almost every sample path ω there exists a random time N such that the process {XN +k(ω)}∞ 

> k=0

converges to x∗ quadratically. 8 A RANDOMIZED NEWTON METHOD IN NONCONVEX OPTIMIZATION 

Proof . Let r be the radius chosen in (2.6). By Lemma 2.2, the process enters Br (x∗)almost surely. Define the successive entrance and exit times by 

ρ1 := inf {n ≥ 0 : Xn ∈ Br (x∗)},η1 := inf {n > ρ 1 : Xn /∈ Br (x∗)},

and for j ≥ 1, 

ρj+1 := inf {n > η j : Xn ∈ Br (x∗)},ηj+1 := inf {n > ρ j+1 : Xn /∈ Br (x∗)}.

If ηj < ∞, then the strong Markov property and Lemma 2.2 imply that ρj+1 < ∞ almost surely. Thus every exit is followed almost surely by another entrance. On the event S

> K≥1

{ηK = ∞} , then Lemma 2.4 yields the desired result. If not, then at each entrance time ρj , Lemma 2.3 gives a conditional probability at least ps > 0 that the process remains in the shrinking tube 

Xρj +k − x∗ ≤ ak, k ≥ 0.

where ak is as in the statement of the Lemma. Define the event 

Gj =: {∥ Xρj +k − x∗∥ > a k, ∀k}.

On Gcj , by Lemma 2.3 we have Xn → x∗. On the other hand, the probability that the first J entrances all fail to be successful is 

P



> J

\

> j=1

Gj

 ≤ (1 − ps)J .

Letting J → ∞ we have 

P({Xn never enters tube }) = lim  

> J→∞

P



> J

\

> j=1

Gj

 ≤ lim 

> J→∞

(1 − ps)J = 0 .

Hence, there exists a (random) index K such that ηK = ∞ and P(GK ) = 1. Thus convergence follows directly from Lemma 2.3. 

Remark 2.3. The shrinking-ball argument also gives a pathwise rate on the successful entrance event. Namely, on the event constructed in Lemma 2.3 we obtain quadratic convergence 

∥XN +k − x∗∥ ≤ ak, k ≥ 0,

where ak is as in the hypotheses of Lemma 2.3. Note that this is not the same as a deterministic rate for every sample path from time zero. The entrance time N is random, and the proof only asserts that one of the random entrances is eventually successful almost surely. 

Observing the proof of Lemma 2.3, we can also generalize for α > 12 at the cost of obtaining linear (or superlinear) convergence in the tube. This implies the result of Theorem 2.1 still holds even in this case. HUYNH, ENGQUIST 9

Lemma 2.5 ( A shrinking Newton tube ). There exist constants θ ∈ (0 ,1) and ps > 0 with the following property. For every deterministic time N and every state XN ∈ Br (x∗),

P ∥XN +k − x∗∥ ≤ rθ k for all k ≥ 0 | F N

 ≥ ps. (2.12) 

Consequently, with conditional probability at least ps, one has 

XN +k → x∗

without leaving Br (x∗).Proof . Let 

C0 := L

2m , K0 := 

 M0

2

α

.

Choose θ ∈ (0 ,1) and, by decreasing r if necessary, assume 

C0r ≤ θ

2 . (2.13) Set 

ak := rθ k, k ≥ 0.

Suppose that ∥XN +k − x∗∥ ≤ ak. Since ak ≤ r, Proposition 2.1 gives 

f (XN +k)α ≤

 M0

2

α

∥XN +k − x∗∥2α ≤ K0a2αk . (2.14) Also, by Lemma 2.1, 

∥T (XN +k) − x∗∥ ≤ L

2m ∥XN +k − x∗∥2 ≤ C0a2

> k

. (2.15) Thus, if 

K0a2αk ∥ZN +k∥ ≤ ak+1 − C0a2

> k

, (2.16) then inequalities (2.14) and (2.15) imply 

∥XN +k+1 − x∗∥ ≤ ∥ T (XN +k) − x∗∥ + f (XN +k)α ∥ZN +k∥≤ C0a2 

> k

+ K0a2αk ∥ZN +k∥≤ C0a2 

> k

+ ak+1 − C0a2 

> k

= ak+1 .

By (2.13), 

ak+1 − C0a2 

> k

= θa k − C0a2 

> k

= ( θ − C0ak)ak ≥ (θ − C0r)ak ≥ θ

2 ak.

Therefore (2.16) is guaranteed if 

∥ZN +k∥ ≤ Rk := θ

2K0

a1−2αk . (2.17) Since α > 1/2, the exponent 1 − 2α is negative, and hence Rk → ∞ as k → ∞ . The rest of the proof proceeds similarly to Lemma 2.3. 

Remark 2.4. We observe from the proof of Lemma 2.5 that the threshold α > 1/2 is exactly the condition that the noise amplitude f (x)α decays faster than the distance scale ∥x − x∗∥ near the minimizer, since locally f (x) ∼ ∥ x − x∗∥2.10 A RANDOMIZED NEWTON METHOD IN NONCONVEX OPTIMIZATION 

3. Conclusion and Future Directions In this manuscript, we analyze a ran-domized Newton algorithm (2.2) for optimization and prove convergence for multimodal functions defined on the torus with a unique global minimizer. Our algorithm exploits knowledge of the global minimum value to prove quadratic path wise almost sure con-vergence. The focus is on a setting that allows for a clean result. Let us remark that our work can be extended in several directions. Instead of the classical Newton algorithm at the core, quasi-Newton methods like BFGS could be considered. This would be important when the Hessian is not available and in higher dimensions where evaluating the Hessian is computationally costly. Another potential generalization is to allow the minimum to be at a larger set and not just at a unique point. This is the generic case in overparametrized settings. The assumptions on the function near the minimum must then be adjusted and convergence measured in the distance to the minimizing set. Extension to problems where the minimum value is unknown requires major changes to the proofs and the convergence would probably be weaker than almost sure. While one can potentially replace fmin in (2.2) with some estimator of the global minimum, the scheme loses the strong Markov property which is essential in the proofs of Lemmas 2.2 and 2.4. 

Funding Information. This work was completed while the first author was funded under the National Defense Science and Engineering Graduate (NDSEG) Fellowship administered through the Department of the Air Force (AFRL/Space Force). 

REFERENCES [1] C. Boyer and A. Godichon-Baggioni, On the asymptotic rate of convergence of stochastic Newton algorithms and their Weighted Averaged versions , Computational Optimization and Applica-tions, 84:921–972, 2023. 1 [2] T.-S. Chiang, C.-R. Hwang, and S. J. Sheu, Diffusion for global optimization in Rn, SIAM Journal on Control and Optimization, 25:737–753, 1987. 1 [3] B. Engquist, K. Ren, and Y. Yang, Adaptive state-dependent diffusion for derivative-free optimiza-tion , Communications on Applied Mathematics and Computation, 6:1241–1269, 2024. 1 [4] S. Geman and C.-R. Hwang, Diffusions for global optimization , SIAM Journal on Control and Optimization, 5:1031–1043, 1986. 1 [5] X. Li and A. Milzarek, A unified convergence theorem for stochastic optimization methods , Advances in Neural Information Processing Systems 35 (NeurIPS 2022), 2022. 1 [6] J. Martin, L. C. Wilcox, C. Burstedde, and O. Ghattas, A stochastic Newton MCMC method for large-scale statistical inverse Problems with application to seismic inversion , SIAM Journal on Scientific Computing, 2012. 1 [7] J. Nocedal and S. Wright, Numerical optimization , Springer Nature Link, 44–45, 2006. 2 [8] H. Robbins and S. Munro, A stochastic approximation method , Ann. Math. Statist. 22:400–407, 1951. 1, 2 [9] F. Roosta-Khorasani and M. W. Mahoney, Sub-sampled Newton methods , Mathematical Program-ming: Series A and B, 2019. 1 [10] X. Wang, S. Ma, D. Goldfarb, and W. Liu. stochastic quasi-Newton methods for nonconvex stochas-tic optimization , Siam Journal on Optimization, 27:927–956, 2017. 1 [11] X. Wang and Y. Yuan, On the convergence of stochastic gradient descent with bandwidth-based step size , Journal of Machine Learning Research, 24:1–49, 2023. 2 [12] A. S. Wojtowytsch, Stochastic gradient descent with noise of machine learning type part i: discrete time analysis , Journal of Nonlinear Science, 3:33–45, 2023. 1