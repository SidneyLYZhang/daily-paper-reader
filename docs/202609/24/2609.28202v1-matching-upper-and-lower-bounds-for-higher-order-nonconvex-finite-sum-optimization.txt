Title: Matching Upper and Lower Bounds for Higher-Order Nonconvex Finite-Sum Optimization

URL Source: https://arxiv.org/pdf/2609.28202v1

Published Time: Thu, 24 Sep 2026 01:19:32 GMT

Number of Pages: 25

Markdown Content:
# Matching Upper and Lower Bounds for Higher-Order Nonconvex Finite-Sum Optimization 

Wendao Wu 1,∗ Haihan Zhang 1,∗ Chenheng Zhang 1,∗

Yanyi Li 1 Chunyuan Zheng 1

Cong Fang 1,† Haoxuan Li 1,† Zhouchen Lin 1,†

> 1

Peking University 

wuwendao@stu.pku.edu.cn zhanghaihan@stu.pku.edu.cn chenhengz@stu.pku.edu.cn liyanyi26@stu.pku.edu.cn cyzheng@stu.pku.edu.cn fangcong@pku.edu.cn hxli@pku.edu.cn ZLIN@pku.edu.cn 

> ∗

Equal contribution. †Corresponding authors. 

Abstract 

We establish tight randomized higher-order oracle complexity for finding first-order stationary points of nonconvex finite sums. Let n be the number of components, ∆ > 0 the initial objective-gap bound, Lp > 0 an individual pth-derivative Lipschitz bound, and ε > 0 the target gradient norm. For every fixed integer p ≥ 2, the minimax number of exact component queries returning the value and all derivatives through order p, with success probability at least 2 /3, is Θp

(

n + ∆ L1/p p n1−1/(2 p)ε−(p+1) /p )

,

where the constants depend only on p and the worst case ranges over all finite dimensions. The lower bound holds for unrestricted randomized adaptive algorithms and closes the √n gap between the previously known general-order upper and lower bounds in their dependence on n.We extend dense weak hiding to complete higher-order replies while keeping each component’s regularity independent of the chain length. The matching upper bound retains the known finite-sum exponent, requires only mean-squared pth-derivative increments, and removes the fixed-confidence logarithmic loss by verifying entire recursive-estimation epochs with exact function values. The characterization includes the additive n term for every positive parameter regime; it counts oracle calls with unrestricted internal computation. 

Keywords: Nonconvex finite-sum optimization; higher-order oracles; randomized lower bounds; variance reduction. 

AI Usage. Nearly the entire research pipeline for this paper was carried out by our laboratory’s internal auto-research system, powered by GPT-5.6 Sol. The system also conducted a Lean-backed article audit of the resulting manuscript. The authors subsequently reviewed and approved the mathematical claims, presentation, and formal artifacts, and take responsibility for the final manuscript. The complete Lean audit report and the system’s technical report will be made public at a later date. 

1arXiv:2609.28202v1 [math.OC] 23 Sep 2026 1 Introduction 

We study the number of higher-order component queries needed to find an ε-stationary point of a nonconvex finite sum 

F (x) = 1

n

> n∑
> i=1

fi(x), x ∈ Rd, ∥∇ F (x)∥ ≤ ε. 

Fix an integer p ≥ 2. Each query chooses a component and a point and returns its exact value and all derivatives through order p. We assume that each pth derivative is Lp-Lipschitz and that the initial objective gap is at most ∆, with ∆ , L p, ε > 0. Algorithms may choose both indices and points adaptively and use private randomness. Our question is how the component count n changes the best achievable query complexity under this information model. For a full objective, regularized Taylor methods attain the accuracy exponent ( p + 1) /p (Birgin et al., 2017), and matching lower bounds cover arbitrary randomized local-oracle algorithms (Carmon et al., 2020). Computing a full jet by querying every component at every iteration gives no savings from the finite-sum structure. Taylor control variates provide such savings: Zhou and Gu (2022) obtain a dimension-free upper bound with component-count exponent 1 −1/(2 p). Yet the randomized lower bound of Emmenegger et al. (2022) has exponent ( p − 1) /(2 p). Writing Kp = ∆ L1/p p ε−(p+1) /p ,the previous general-order comparison is Ωp

> (

Kpn(p−1) /(2 p))

versus ˜Op

> (

Kpn1−1/(2 p))

. (1) Here n ≤ cpK2p/ (p+1)  

> p

is the earlier lower bound’s accuracy range, and the tilde hides confidence-related logarithms. Constants with subscript p depend only on the fixed order. The two powers of 

n differ by exactly √n. It remains to determine whether adaptive component access can improve this upper exponent, or whether a stronger lower bound makes it necessary. We prove that the upper exponent is necessary. Let Mp denote the smallest deterministic query cap that guarantees an ε-stationary output with probability at least 2 /3 for every admissible instance, taking the worst case over all finite dimensions. Then 

Mp = Θ p

> (

n + Kpn1−1/(2 p))

. (2) The characterization holds for every n ≥ 1 and all positive ∆ , L p, ε . The lower bound permits arbitrary query points and outputs, without a span or zero-respecting restriction. The upper bound holds on the larger class defined by a mean-squared increment bound on the pth derivatives. Consequently, both regularity classes have the same minimax complexity (Theorem 3.2). The main extension is to fixed orders p ≥ 3, with p = 2 included. 

Why complete higher-order replies remain hard. The main obstacle is to conceal a direction from every tensor in a query reply while keeping each component individually smooth. We extend the dense weak hiding construction of Peng et al. (2026) and its exact-Hessian development in the unpublished companion (Anonymous Authors, 2026). A hidden direction is spread across a fixed table of weakly biased component rows. Its exact row average retains the direction, but learning it requires many distinct rows. An exactly flat gate conceals every derivative through order p until the preceding stage opens. The key analytic estimate bounds all mixed ( p + 1)st derivatives without a chain-length factor and with only the inverse first power of the hiding bias. A censored transcript then bounds the information available before the first revealing query, including for adaptive indices and unbounded query points. These two estimates yield the missing √n factor in the lower bound; Section 5 explains their balance. 2Table 1: Randomized component-query complexity for first-order stationarity at fixed p ≥ 2. Here Kp = ∆ L1/p p ε−(p+1) /p . Previous rows display the nontrivial term on the common range 

n ≤ cpK2p/ (p+1)  

> p

; the tilde hides logarithms. Their upper bound uses derivatives alone and is implementable with our value-inclusive oracle. Our bounds include initialization and hold for all positive parameters, with unrestricted adaptive algorithms and worst-case dimension.                  

> Reference Upper bound Lower bound Regularity Emmenegger et al. (2022) —Ωp(Kpn(p−1) /(2 p))Individual Dp
> Zhou and Gu (2022) ˜Op(Kpn1−1/(2 p))—Individual Dp
> This Paper
> (Theorem 3.2)
> Θp(n+Kpn1−1/(2 p))Individual Dpor mean-squared Dp

Matching the rate under second moments. The upper method uses the Taylor control variate of Zhou and Gu (2022); its finite-sum exponent is inherited. Our analysis instead controls an entire recursive-estimation epoch with constant conditional probability under second moments. An exact evaluation at the endpoint certifies either stationarity or sufficient decrease, so an inaccurate epoch can be rejected while preserving all accepted progress. Amortizing these full evaluations over Θ( √n)steps removes the logarithmic loss at fixed confidence. For failure probability δ ∈ (0 , 1), Theorem 3.4 gives the additional cost Op(n log(1 /δ )). Values, initialization, rejected trials, and verification are included in the query count. As in the underlying higher-order oracle model, global minimization of a returned Taylor model is permitted internal computation. The additive n term is necessary even for arbitrarily small positive gap: without a bound on lower-order smoothness, one unknown component can move the stationary region of a quadratic. This complements the chain lower bound and gives full parameter coverage. The exact oracle model and main statements follow in Section 3; the algorithm and lower-bound mechanism are developed in Sections 4 and 5, with complete proofs in the appendices. 

# 2 Related Works 

Full-function higher-order methods. Cubic regularization establishes the second-order foun-dation (Nesterov and Polyak, 2006); general-order regularized models attain the corresponding (p + 1) /p stationary-point exponent (Birgin et al., 2017). The lower bounds of Carmon et al. (2020) show that unrestricted randomized access to local derivatives cannot improve that exponent on the stated smoothness class. These full-function results determine the accuracy dependence, whereas incremental access introduces the component-count question studied here. Doikov and Grapiglia (2026) analyze lower-order finite-difference implementations of higher-order methods. Their dimension-dependent cost concerns recovering derivative information for a full objective, a different resource from querying one complete component jet. More recently, Zhou (2026) study deterministic first-order oracle lower bounds under higher-order smoothness. Their oracle returns first-order information for a full objective, whereas our bounds concern randomized incremental access to complete pth-order component jets. 

Variance reduction and finite-sum higher-order methods. SPIDER and PAGE use recursive gradient estimation to exploit mean-squared gradient smoothness (Fang et al., 2018; Li et al., 2021). Variance-reduced cubic and trust-region methods extend estimation ideas to second-order models 3and second-order stationarity (Zhou et al., 2019; Wang et al., 2019; Shen et al., 2019); Hessian-only sample counts in these settings differ from the total component-jet cost used here. The general-order Taylor estimator of Zhou and Gu (2022) provides our closest algorithmic baseline. Our polynomial-residual recursion is its algebraic reformulation, as shown in (21). Their Theorem 5.8 has success probability 1 − T δ 0 for a per-step failure parameter δ0; achieving fixed overall confidence introduces a logarithmic dependence on the horizon. Our epoch analysis avoids that simultaneous per-step guarantee. Unified analyses of stochastic cubic methods provide complementary choices of estimators and lazy updates (Chayti et al., 2024). For p = 2, Pasechnyuk-Vilensky et al. (2026) report an n + ˜O(n1/2ε−3/2) total oracle bound, suppressing other problem constants, under both mean-squared gradient smoothness and third-moment Hessian smoothness. The additional gradient-smoothness bound restricts a lower-order parameter that is unbounded in our class. Their rate therefore concerns a different minimax problem and is not a uniform upper bound under our higher-order assumption alone. 

Randomized finite-sum lower bounds. Emmenegger et al. (2022) establish higher-order lower bounds for randomized algorithms without a span restriction. Their individual-smoothness result is the quantitative baseline in (1); their third-moment Hessian condition defines a separate, larger class with a stronger second-order lower bound. Peng et al. (2026) close the first-order individual-smoothness gap using fixed-table dense weak hiding. Our construction preserves that mechanism and supplies the higher-order derivative estimate and complete-jet transcript reduction. The unpublished companion (Anonymous Authors, 2026) develops the exact-Hessian flat-gate and censored-revelation argument for p = 2. Relative to it, the present contribution is the fixed-p ≥ 3 extension and the second-moment, log-free upper analysis. We cite this unpublished manuscript for attribution; no theorem from it is used as a black box, and all required construction and information lemmas are proved here. 

Probabilistic models and exact acceptance tests. Trust-region and regularization methods can safeguard models that are accurate only with constant probability by testing actual objective decrease (Bandeira et al., 2014; Cartis and Scheinberg, 2018). Our verification mechanism applies this established principle to an entire finite-sum epoch. Its role is to make a full snapshot and a full endpoint evaluation support Θ( √n) recursive steps. A dimension-free second-moment estimate controls the epoch, while exact acceptance tests preserve objective decrease through failed trials. The resulting bound counts all these calls and imposes a deterministic total query cap. 

# 3 Oracle Model and Main Results 

By translation the initial point is 0. An instance in dimension d consists of n functions fi ∈ Cp(Rd)such that 

F = 1

n

> n∑
> i=1

fi, F (0) − inf  

> x∈Rd

F (x) ≤ ∆ < ∞. (3) For a k-linear tensor A, we use ∥A∥op = sup ∥h1∥,..., ∥hk ∥≤ 1 |A[h1, . . . , h k]|. The individual regularity class satisfies 

∥∇ pfi(x) − ∇ pfi(y)∥op ≤ Lp ∥x − y∥ for every i, x, y. (4) The mean-squared class instead satisfies  

> (

1

n

> n∑
> i=1

∥∇ pfi(x) − ∇ pfi(y)∥2op 

> )1/2

≤ Lp ∥x − y∥ for every x, y. (5) 4The first condition implies the second with the same Lp; for n ≥ 2 the inclusion is strict (Ap-pendix F.3). Neither condition bounds lower-order derivative magnitudes or their offsets across components. Lower boundedness is required only of F , although our lower-bound components are themselves bounded below. 

Definition 3.1 (Exact incremental p-jet complexity) . A query selects i ∈ [n] and x ∈ Rd and returns 

J pi (x) = (fi(x), ∇fi(x), . . . , ∇pfi(x)).

Each complete reply costs one query. An algorithm’s indices, points, stopping decisions, and output are arbitrary measurable functions of previous replies and an independent private random tape. Internal computation is unrestricted. For either regularity class, its minimax complexity is the smallest deterministic query budget that guarantees an output ̂ x with ∥∇ F (̂x)∥ ≤ ε with probability at least 2 /3, uniformly over all finite d and all admissible instances. Denote the two complexities by 

Mind  

> p

and Mms  

> p

.Set 

Kp = ∆ L1/p p ε−(p+1) /p , Sp = n + Kpn1−1/(2 p).

Theorem 3.2 (Fixed-order minimax characterization) . For every fixed integer p ≥ 2, there exist constants cp, C p > 0 depending only on p such that, for all n ≥ 1 and ∆, L p, ε > 0,

cpSp ≤ Mind  

> p

≤ Mms  

> p

≤ CpSp. (6) 

The lower bound holds for arbitrary randomized adaptive algorithms, including unbounded queries and unqueried outputs. The upper bound has success probability at least 31 /32 with a deterministic budget. If ∆ = 0 , zero queries suffice; if ∆ > 0 and Lp = 0 , both complexities are Θ( n).

For p = 3 and p = 4, the common rates are respectively Θ

> (

n + ∆ L1/33 n5/6ε−4/3)

, Θ

> (

n + ∆ L1/44 n7/8ε−5/4)

.

The constants are dimension-free, not uniform in an order p that grows with the other parameters. 

Corollary 3.3 (Higher moment classes) . For any fixed or variable q ∈ [2 , ∞], replace (5) by the normalized qth-moment bound with the same Lp (the maximum when q = ∞). Its fixed-confidence minimax complexity is Θp(Sp), with constants independent of q.Proof. Normalized moments are nondecreasing in their order. Every such class contains the individual class and is contained in the mean-squared class. Apply (6). This includes third-moment Hessian smoothness at p = 2, but does not assert that second moments are a necessary threshold. 

Theorem 3.4 (Confidence and lower-bound dimension) . Under (5) , for every δ ∈ (0 , 1) , success probability 1 − δ is achievable with 

Op

> (

Kpn1−1/(2 p) + n[1 + log(1 /δ )] 

> )

(7) 

queries on every path. The lower bound in Theorem 3.2 can be witnessed in dimension 

d ≤ Cp(1 + Kpn−1/(2 p)) log(2 + n + Kpn1−1/(2 p)). (8) 

No matching dependence on δ is claimed. 

Proofs of the main and refinement theorems appear in Appendices A–E. We next explain the algorithm and the two matching balances. 54 Verified Taylor-Residual Epochs 

At a snapshot z, one full pass stores the component Taylor polynomials 

Pi(y) = 

> p∑
> j=0

1

j! ∇j fi(z)[( y − z)⊗j ], P = 1

n

> ∑
> i

Pi.

Higher derivatives within an epoch are evaluated from P without further queries. Only the gradient is tracked recursively. After selecting a step st and putting xt+1 = xt + st, use fresh uniform indices in 

vt+1 = vt + ∇P (xt+1 ) − ∇ P (xt)+ 1

B

> B∑
> b=1
> [

∇fIt,b (xt+1 ) − ∇ fIt,b (xt) − ∇ PIt,b (xt+1 ) + ∇PIt,b (xt)]. (9) The sampled residuals use two queries each. Storing complete snapshot jets makes every Taylor correction available at no additional oracle cost. For all trial epochs choose 

m = ⌈√n⌉, B = 64 m, M = 4( p2 + 2 p+1 )Lpmp−1,rp = p!ε

4M , d0 = M r p+1 

2( p + 1)! , H = 

> ⌊

∆

md 0

> ⌋

+ 1 . (10) The step is a global minimizer, on the closed ball of radius r, of 

⟨vt, s ⟩ +

> p∑
> j=2

1

j! ∇j P (xt)[ s⊗j ] + M

(p + 1)! ∥s∥p+1 . (11) A measurable minimizer exists. Global model solution is permitted internal computation; this is where the oracle model deliberately differs from an arithmetic-time model. 

Why second moments suffice. With steps bounded by r and epoch length m, the residual’s conditional second moment is at most 

σ2, σ = Lpmp−1rp

(p − 1)! .

The error et = vt − ∇ F (xt) is a vector martingale starting at zero. Its terminal second moment is at most mσ 2/B , so a maximal inequality gives 

P

> (

max  

> t≤m

∥et∥ > σ | starting history 

> )

≤ mB = 164 .

On the complementary event, every boundary step decreases the objective by at least d0, and every interior step has an endpoint with gradient norm at most ε/ 2. These statements follow from deterministic Taylor error bounds; no random tensor concentration is used. 

Why errors do not accumulate across epochs. Every output from the stationarity test is exactly certified. Every accepted nonterminal epoch has real objective decrease at least md 0,whether or not its estimates were correct. Every good epoch either terminates or is accepted. Hence nontermination after 2 H trials requires at least H bad trials, although their expected number is at most 2 H/ 64. Markov’s inequality gives failure probability at most 1 /32. This argument uses conditional bounds, not independence of epoch outcomes. 6Algorithm 1 Verified Taylor-residual epochs (fixed confidence) 

Require: n, p, ∆, L p, ε > 0; parameters in (10)  

> 1:

z ← 0 

> 2:

for ℓ = 1 , . . . , 2H do  

> 3:

Query every component at z; form Pi, P , F (z), and v ← ∇ F (z).  

> 4:

x ← z; full ← true  

> 5:

for t = 0 , . . . , m − 1 do  

> 6:

Select a global minimizer s of (11); set y ← x + s. 

> 7:

if ∥s∥ < r then  

> 8:

full ← false ; break  

> 9:

end if  

> 10:

if t < m − 1 then  

> 11:

Sample B fresh uniform indices and update v by (9).  

> 12:

end if  

> 13:

x ← y 

> 14:

end for  

> 15:

Query every component at y; compute exact F (y) and ∇F (y).  

> 16:

if ∥∇ F (y)∥ ≤ ε then  

> 17:

return y 

> 18:

end if  

> 19:

if full and F (y) ≤ F (z) − md 0 then  

> 20:

z ← y ▷ Accept only verified progress.  

> 21:

end if 

Otherwise keep z; the next trial uses fresh samples.  

> 22:

end for  

> 23:

return z ▷ The cap is reached with probability at most 1 /32. 

The upper-bound balance. Each trial costs at most 2 n + 2 Bm = O(n) queries, counting endpoint verification. The number of successful epochs that the objective gap allows is 1 + ∆

md 0

= Op(1 + Kpm−1/p ).

Substituting m ≍ √n gives Op(n + Kpn1−1/(2 p)). Appendix A proves the deterministic query cap and the confidence refinement. 

# 5 Higher-Order Dense Weak Hiding 

The lower bound in Theorem 3.2 requires three properties: an unfinished stage must force a large gradient, opening a stage must require many distinct component rows, and the entire construction must remain individually pth-order smooth. We summarize how these requirements fit together; Lemma C.1 and Proposition D.5 establish the analytic and information bounds, and Section E combines them. 75.1 A fixed-table radial chain 

For each stage j ∈ [T ], choose an independent uniform hidden vector Θ (j) ∈ {± 1}D. Given this vector, sample a fixed sign table S(j) ∈ {± 1}n×D whose columns have exact sums nρ Θ(j) 

> k

. The bias 

ρ is chosen on the parity-compatible grid. Write Ψ( z) = z

√1 + ∥z∥2 , qj (z) = 

〈

Θ(j)/√D, Ψ( z)

〉

, ˜qij (z) = ρ−1 〈

S(j) 

> i:

/√D, Ψ( z)

〉

.

Thus n−1 ∑ 

> i

˜qij = qj exactly, not in expectation. Let a = 1 /8, b = 1 /4, and let Gp be a nondecreasing gate equal to zero on ( −∞ , a ], equal to one on [ b, ∞), and flat through order p + 1 at both interfaces. A normalized integral of tp+1 (1 − t)p+1 

gives an explicit degree-(2 p + 3) transition polynomial. Define 

Ri(x) = −˜qi1(x1) −

> T

∑

> j=2

Gp(qj−1(xj−1)) (˜qij (xj ) + 1 ) +

> T

∑

> j=1

∥xj ∥2. (12) The average R replaces each ˜qij by qj . Its three analytical properties are 

R(0) − inf R ≤ 2T, min  

> j

qj (xj ) < b =⇒ ∥∇ R(x)∥ > 1/3,

Lip( ∇pRi) ≤ Hp+1 /ρ, log Hp+1 = O(p log( p + 2)) .

(13) The gradient statement uses the first unfinished block. Its gradient is 2 xj − c∇qj (xj ) for some c ≥ 1, whose norm is bounded below uniformly whenever qj ≤ b.

5.2 Why the derivative estimate is dimension-free 

All fixed-order derivatives of a radial coordinate have dimension-free bounds. For a link involving blocks j − 1, j , the product rule gives, with k = p + 1, 

|Dk[Gp(qj−1)( ˜qij + 1)][ h1, . . . , h k]| ≤ Cp,k 

ρ

> k

∏

> ℓ=1

(∥hℓj−1∥ + ∥hℓj ∥).

A direct triangle bound would introduce T . Instead set aℓj = ∥hℓj−1∥ + ∥hℓj ∥. Finite overlap and 

k ≥ 2 imply 

∑

> jk

∏

> ℓ=1

aℓj ≤

> k

∏

> ℓ=1

∥aℓ∥ℓk ≤ 2kk∏

> ℓ=1

∥hℓ∥.

Only one weak factor appears in each link, so the bias cost remains 1 /ρ , not a higher power. These two facts give the last line of (13). 

5.3 Complete replies, censoring, and adaptive information 

If qj (xj ) ≤ a, the outgoing link has value and every derivative through order p exactly zero. Therefore, before stage j first opens, its public reply is a deterministic function of completed-stage data and the selected row of table j. Revealing that entire row only strengthens the algorithm. For a stage with at most r distinct rows exposed, the censored row transcript satisfies 

I(Θ; Tr) ≤ Drρ 2.

8Under an independent hidden direction, any transcript-measurable vector V with ∥V ∥ ≤ 1 has alignment exceeding a with probability at most e−D/ 128 . Relative-entropy data processing then shows that a stage is unlikely to open in fewer than 

rρ = 

> ⌊

14096 min {n, ρ −2}

> ⌋

assigned calls. Crucially, the censored transcript excludes the completion response: the response may reveal the direction, but it arrives only after the first alignment query has been chosen. A first-hit coupling makes this distinction rigorous. A truncated public simulator reveals at most one new stage per query. Its untouched future stages remain independent of its history, so a union bound controls all unintended future alignments in block dimension D = O(1 + log(( N + 1) T )). On the no-future-alignment event, exact gate flatness couples its public replies to those of the true oracle. Stage costs add without assuming independent durations. Reserving one untouched final stage also handles an arbitrary unqueried output. The resulting lower bound is 

N = Ω 

> (

T min {n, ρ −2}

> )

. (14) The bounded map Ψ makes this argument valid for every query radius. 

5.4 The matching bias balance and remaining regimes 

Scale by fi(y) = αR i(βy ) with 

βp = Lpρ

6Hp+1 ε , α = 6εβ .

This enforces individual smoothness and a gradient barrier above 2 ε. The gap budget permits 

T = Θ p(Kpρ1/p )whenever the right side is sufficiently large. Consequently (14) becomes Ωp

> (

Kp min {nρ 1/p , ρ −2+1 /p }

> )

.

The increasing and decreasing branches balance at ρ ≍ n−1/2, yielding Ω p(Kpn1−1/(2 p)). For the complementary regime, a single hidden component contains an unknown linear perturba-tion of a public quadratic. Until that component is queried, four disjoint possible stationary regions are indistinguishable, giving Ω( n). Small component counts use identical copies of a single hidden radial chain. Appendix E supplies the integer choices, probability constants, dimension bound, and boundary-parameter cases. 

# 6 Consequences and Limitations 

The fixed-order finite-sum price is n1−1/(2 p): the exponents 3 /4, 5/6, 7/8, . . . are intrinsic to exact incremental access under individual higher-order smoothness. The proof identifies the same exponent from two different balances. The algorithm amortizes full passes over √n-step verified epochs; the hard instance requires order n rows per weakly hidden stage while allowing order Kpn−1/(2 p) stages. Moment-class inclusion makes this law unchanged under any normalized moment assumption of order at least two. This theorem does not compare the computational price of forming tensors of different orders. It fixes the order of the oracle and the regularity class together; it therefore does not assert that richer 9information is intrinsically harmful on one fixed class. Nor does it provide a sharp second-order-stationarity complexity, a tensor-vector-product lower bound, a necessary second-moment threshold, or optimal dependence on a growing p. The high-confidence upper bound need not be minimax in δ.Exact function values and unrestricted model computation are substantive features of the result, not omitted implementation costs. 

# References 

Anonymous Authors. Tight incremental second-order complexity under third-moment Hessian smoothness. Unpublished companion manuscript, cited for attribution only. All required construc-tion and information lemmas are proved in the present paper, 2026. Afonso S. Bandeira, Katya Scheinberg, and Luís N. Vicente. Convergence of trust-region methods based on probabilistic models. SIAM Journal on Optimization , 24(3):1238–1264, 2014. doi: 

10.1137/130915984 .Ernesto G. Birgin, J. L. Gardenghi, José Mario Martínez, Sandra A. Santos, and Philippe L. Toint. Worst-case evaluation complexity for unconstrained nonlinear optimization using high-order regularized models. Mathematical Programming , 163(1–2):359–368, 2017. doi: 10.1007/ s10107-016-1065-8 .Yair Carmon, John C. Duchi, Oliver Hinder, and Aaron Sidford. Lower bounds for finding stationary points I. Mathematical Programming , 184:71–120, 2020. doi: 10.1007/s10107-019-01406-y .Coralia Cartis and Katya Scheinberg. Global convergence rate analysis of unconstrained optimization methods based on probabilistic models. Mathematical Programming , 169(2):337–375, 2018. doi: 

10.1007/s10107-017-1137-4 .El Mahdi Chayti, Martin Jaggi, and Nikita Doikov. Unified convergence theory of stochastic and variance-reduced cubic Newton methods. Transactions on Machine Learning Research , 2024. ISSN 2835-8856. URL https://openreview.net/forum?id=FCs5czlDTr .Nikita Doikov and Geovani Nunes Grapiglia. On the complexity of lower-order implementations of higher-order methods. Mathematical Programming , 2026. doi: 10.1007/s10107-026-02386-6 .Published online July 8, 2026. Nicolas Emmenegger, Rasmus Kyng, and Ahad N. Zehmakan. On the oracle complexity of higher-order smooth non-convex finite-sum optimization. In Proceedings of the 25th International Conference on Artificial Intelligence and Statistics , volume 151 of Proceedings of Machine Learning Research , pages 10718–10752. PMLR, 2022. URL https://proceedings.mlr.press/v151/ emmenegger22a.html .Cong Fang, Chris Junchi Li, Zhouchen Lin, and Tong Zhang. SPIDER: Near-optimal non-convex optimization via stochastic path-integrated differential estimator. In Advances in Neural Informa-tion Processing Systems , volume 31, 2018. URL https://papers.nips.cc/paper_files/paper/ 2018/hash/1543843a4723ed2ab08e18053ae6dc5b-Abstract.html .Zhize Li, Hongyan Bao, Xiangliang Zhang, and Peter Richtárik. PAGE: A simple and optimal prob-abilistic gradient estimator for nonconvex optimization. In Proceedings of the 38th International Conference on Machine Learning , volume 139 of Proceedings of Machine Learning Research , pages 6286–6295. PMLR, 2021. URL https://proceedings.mlr.press/v139/li21a.html .10 Yurii Nesterov and B. T. Polyak. Cubic regularization of Newton method and its global performance. 

Mathematical Programming , 108(1):177–205, 2006. doi: 10.1007/s10107-006-0706-8 .Dmitry Pasechnyuk-Vilensky, Dmitry Kamzolov, and Martin Takáč. Cubic regularized Newton method with variance reduction for finite-sum non-convex problems, 2026. URL https://arxiv. org/abs/2510.08714v2 . arXiv:2510.08714, version 2, revised April 27, 2026. Yuxing Peng, Zhiqing Tang, and Weijia Jia. Dense weak hiding: Closing complexity gaps in nonconvex and PL finite-sum optimization under individual smoothness, 2026. URL https: //arxiv.org/abs/2609.00045v2 . arXiv:2609.00045, version 2. Zebang Shen, Pan Zhou, Cong Fang, and Alejandro Ribeiro. A stochastic trust region method for non-convex minimization, 2019. URL https://arxiv.org/abs/1903.01540 . arXiv:1903.01540. Zhe Wang, Yi Zhou, Yingbin Liang, and Guanghui Lan. Stochastic variance-reduced cubic regu-larization for nonconvex optimization. In Proceedings of the 22nd International Conference on Artificial Intelligence and Statistics , volume 89 of Proceedings of Machine Learning Research ,pages 2731–2740. PMLR, 2019. URL https://proceedings.mlr.press/v89/wang19d.html .Dongruo Zhou. Sharp first-order lower bounds for higher-order smooth nonconvex optimization, 2026. URL https://arxiv.org/abs/2606.05438v2 . arXiv:2606.05438, version 2, revised September 21, 2026. Dongruo Zhou and Quanquan Gu. Dimension-free complexity bounds for high-order nonconvex finite-sum optimization. In Proceedings of the 39th International Conference on Machine Learning ,volume 162 of Proceedings of Machine Learning Research , pages 27143–27158. PMLR, 2022. URL 

https://proceedings.mlr.press/v162/zhou22a.html .Dongruo Zhou, Pan Xu, and Quanquan Gu. Stochastic variance-reduced cubic regularization methods. Journal of Machine Learning Research , 20(134):1–47, 2019. URL https://jmlr.org/ papers/v20/19-055.html .11 A Proof of the Upper Bound 

The proof first controls Taylor residuals over one adaptive epoch, then turns that event into verified progress and a total query cap. Only condition (5) is used. Let L = Lp. By Jensen’s inequality, 

∇pF is L-Lipschitz. At a snapshot z, query every component and store its entire p-jet. Define the scalar Taylor polynomials 

Pi(y) = 

> p

∑

> j=0

1

j! ∇j fi(z)[( y − z)⊗j ], P = 1

n

∑

> i

Pi. (15) Polynomial evaluations and tensor contractions require no new oracle calls. 

Lemma A.1 (Taylor errors and residual second moments) . For 2 ≤ j ≤ p,

∥∥∥∇j F (x) − ∇ j P (x)

∥∥∥op ≤ L ∥x − z∥p+1 −j

(p + 1 − j)! . (16) 

For ∥x − z∥ ≤ (m − 1) r and ∥s∥ ≤ r, set 

Yi(x, s ; z) = ∇fi(x + s) − ∇ fi(x) − ∇ Pi(x + s) + ∇Pi(x), (17) 

σ = Lm p−1rp

(p − 1)! . (18) 

Then 

Ei ∥Yi(x, s ; z)∥2 ≤ σ2, (19) 

where i is uniform on [n].Proof. Taylor’s formula applied to ∇j F gives (16). Applying the same integral remainder formula componentwise and using Minkowski’s inequality in the finite normalized ℓ2 space gives 

(

Ei

∥∥∥∇2fi(y) − ∇ 2Pi(y)

∥∥∥2op 

)1/2

≤ L ∥y − z∥p−1

(p − 1)! .

For p = 2 this is exactly (5). For p > 2, put h = y − z and use the identity 

∇2fi(y) − ∇ 2Pi(y) = 1(p − 3)! 

∫ 10

(1 − t)p−3[∇pfi(z + th ) − ∇ pfi(z)][ h⊗(p−2) ]dt. 

The normalized L2 norm of the integrand is at most Lt ∥h∥p−1. The integral of t(1 − t)p−3/(p − 3)! is 1 /(p − 1)!. Tensor contraction leaves a bilinear form in this display. Now 

Yi(x, s ; z) = 

∫ 10

[∇2fi(x + ts ) − ∇ 2Pi(x + ts )] s dt. 

Every point in this integral is within mr of z. A second application of Minkowski proves (19). An epoch starts at x0 = z, v0 = ∇F (z), and has at most m steps. After an adaptive step st has been chosen, use fresh independent uniform indices It, 1, . . . , I t,B and update 

vt+1 = vt + ∇P (xt + st) − ∇ P (xt) + 1

B

> B

∑

> b=1

YIt,b (xt, s t; z). (20) 12 Only two component queries per sampled residual are needed, since all derivatives at z are stored. Algebraically, this is the Taylor control-variate recursion of Zhou and Gu (2022, Algorithm 3): for any degree-p polynomial Pi,

∇Pi(x + s) − ∇ Pi(x) = 

> p−1∑
> k=1

1

k!

> p−k−1∑
> u=0

1

u! ∇k+u+1 fi(z)[( x − z)⊗u, s ⊗k, · ]. (21) The contribution here is the mean-squared analysis and verified-epoch wrapper, not the underlying Taylor control variate. 

Lemma A.2 (One-epoch success with a constant batch multiplier) . Suppose every step has norm at most r and B ≥ 64 m. Then, conditional on the complete history before the epoch, 

P

> (

max  

> 0≤t≤m

∥vt − ∇ F (xt)∥ > σ 

> )

≤ 164 . (22) 

The statement remains valid for an epoch that stops early. Proof. Put et = vt −∇ F (xt). Conditional on the history and the chosen step, the increment et+1 −et

has mean zero and conditional squared norm in expectation at most σ2/B . Thus et is a vector martingale and 

E ∥em∥2 ≤ mσ 2

B .

Pad an early-stopped epoch with zero increments. In Algorithm 1, only e0, . . . , e m−1 are needed to choose steps; the unperformed final estimator update is also padded with a zero martingale increment. The nonnegative submartingale ∥et∥2 satisfies the maximal inequality 

P

> (

max  

> t

∥et∥ > σ 

> )

≤ E ∥em∥2

σ2 ≤ 164 .

All statements hold conditionally on an arbitrary starting history. No coordinatewise tensor concentration, dimension factor, bounded-gradient assumption, or tail envelope is used. Set 

m = ⌈√n⌉, B = 64 m, A = Lm p−1, M = 4( p2 + 2 p+1 )A, rp = p!ε

4M . (23) At point xt, choose a global minimizer over ∥s∥ ≤ r of 

qt(s) = ⟨vt, s ⟩ +

> p∑
> j=2

1

j! ∇j P (xt)[ s⊗j ] + M

(p + 1)! ∥s∥p+1 . (24) A fixed Borel minimizer selector exists; Appendix F.1 gives a constructive measurability argument. This is a permissible internal computation in the stated oracle model, not a polynomial-time subroutine guarantee. 

Lemma A.3 (Deterministic model alternatives) . On the good event in (22) , every model minimizer satisfies 

∥st∥ = r =⇒ F (xt + st) ≤ F (xt) − d0, d0 = M r p+1 

2( p + 1)! , (25) 

∥st∥ < r =⇒ ∥∇ F (xt + st)∥ ≤ ε/ 2. (26) 13 Proof. Let E1 = ∥vt − ∇ F (xt)∥ and Ej = ∥∥∇j P (xt) − ∇ j F (xt)∥∥op for j ≥ 2. Throughout the epoch, 

E1 ≤ Ar p

(p − 1)! , Ej ≤ Ar p+1 −j

(p + 1 − j)! (2 ≤ j ≤ p).

Here mp+1 −j ≤ mp−1 for j ≥ 2. Since qt(st) ≤ qt(0) = 0, on the boundary Taylor’s formula gives 

F (xt + st) − F (xt) ≤ − M − L

(p + 1)! rp+1 +

> p∑
> j=1

Ej rj

j!

≤ − M − A(p2 + 2 p+1 − 2) (p + 1)! rp+1 ≤ − M

2( p + 1)! rp+1 .

We used the exact identity 1(p − 1)! +

> p∑
> j=2

1

j!( p + 1 − j)! = p2 + 2 p+1 − 3(p + 1)! .

In the interior, ∇qt(st) = 0. Comparing this equation with the Taylor expansion of ∇F (xt + st)yields 

∥∇ F (xt + st)∥ ≤ M r p

p! + Lr p

p! +

> p∑
> j=1

Ej rj−1

(j − 1)! 

≤ [M + A(p + 2 p − 1)] rp

p! ≤ 2M r p

p! = ε

2 .

A.1 Exact verification, rejection, and a deterministic total budget 

A trial epoch operates as follows. Query all components at its starting snapshot z. Run the preceding procedure until either an interior model step occurs or m boundary steps have been taken. Call the resulting candidate y. Query all components at y to obtain the exact values F (y) and ∇F (y). If ∥∇ F (y)∥ ≤ ε, return y. Otherwise accept y as the next snapshot only if the trial took m

boundary steps and 

F (y) ≤ F (z) − md 0. (27) In every other case reject the trial and keep the old snapshot z. Rejected trials use fresh samples on the next attempt. Reusing stored jets can save queries, but is unnecessary for the bound. Every output from the stationarity test is certified. Every accepted nonterminal trial decreases 

F by at least md 0, even when its gradient estimates were inaccurate. Every good trial either returns a certified point or is accepted, by Lemma A.3. Let 

H = 

> ⌊

∆

md 0

> ⌋

+ 1 , R = 2 H. (28) Stop after R trials if no point has been certified and return an arbitrary point in that exceptional case. 

Proposition A.4 (No logarithmic loss at constant success) . The verified-epoch procedure succeeds with probability at least 31 /32 and uses at most CpSp component queries on every execution path. 

14 Proof. Let a bad-trial indicator be one when the good event from Lemma A.2 fails. After termination pad remaining trials by zero bad indicators. Conditional bad probabilities are at most 1 /64, so the expected number of bad trials among the first R is at most R/ 64. If the algorithm has not terminated, it cannot have encountered H good trials: each would have generated an accepted decrease of at least md 0, contradicting Hmd 0 > ∆ ≥ F (0) − inf F .Thus nontermination after R = 2 H implies at least R/ 2 bad trials. Markov’s inequality bounds its probability by 1 /32. This does not require independence between trials. Each trial uses at most 2 n + 2 Bm queries, including both its snapshot and endpoint verification. Since m2 ≤ 4n and B = 64 m, this is at most 514 n. Moreover, the definitions of r, M, d 0 imply ∆

md 0

= C′

> p

Kpm−1/p , C′ 

> p

= 2( p + 1)!4 (p+1) /p [4( p2 + 2 p+1 )] 1/p 

(p!) (p+1) /p . (29) Therefore the deterministic query budget is 

O

> (

n

> [

1 + C′

> p

Kpm−1/p ]) 

= Op

> (

n + Kpn1−1/(2 p))

.

The failure event can only arise from the deterministic cap, not from accepting an uncertified point as stationary. 

Proposition A.5 (Arbitrary confidence) . For every δ ∈ (0 , 1) , replace the trial cap by 

Rδ = 2 H + ⌈4 log(1 /δ )⌉ .

The algorithm returns an ε-stationary point with probability at least 1 − δ, within 

Op

> (

Kpn1−1/(2 p) + n[1 + log(1 /δ )] 

> )

queries on every path. Proof. Let Zr denote the number of bad trials among the first r, using zero indicators after termi-nation. The conditional bad probability bound gives E[2 Zr | history before trial r] ≤ (65 /64)2 Zr−1 .Induction yields E2Zr ≤ (65 /64) r. For r ≥ 2H, nontermination implies Zr ≥ r/ 2. Therefore 

P(nontermination after r) ≤ 

> (

65 64 √2

> )r

≤ e−r/ 4.

The prescribed cap is at least 4 log(1 /δ ). The query count follows from the same per-trial cost as in Proposition A.4. 

Remark A.6 . This is an upper bound in δ, not a claim of confidence-optimal minimax complexity. Exact component values are used in (27); all corresponding calls are charged. The fixed-confidence theorem does not invoke a union bound over all iterates. 

# B Explicit Gates and Dimension-Free Derivative Bounds 

We now construct individually smooth hard instances. Use thresholds 

a = 18 , b = 14 , w = b − a = 18 .

15 For the fixed target order p, let 

Qp(t) = Zp 

> ∫t
> 0

up+1 (1 − u)p+1 du, Zp = (2 p + 3)! (( p + 1)!) 2 , (30) and set 

Gp(s) = 

> 

0, s ≤ a, Qp(( s − a)/w ), a < s < b, 

1, s ≥ b. 

(31) This is a nondecreasing Cp+1 ,1 function. It is flat through order p + 1 at both ends: the function and its derivatives through order p + 1 vanish on ( −∞ , a ], and Gp = 1 with all positive-order derivatives through p + 1 zero on [ b, ∞). One extra order of flatness avoids any interface differentiability issue when bounding Dp+1 .For example, when p = 3, 

Q3(t) = 126 t5 − 420 t6 + 540 t7 − 315 t8 + 70 t9.

For 1 ≤ k ≤ p + 1, an explicit derivative envelope is 

> ∥∥∥

G(k) 

> p
> ∥∥∥∞

≤ Ep,k := 8 kZpp+1 ∑

> ℓ=0
> (

p + 1 

ℓ 

> )

(p + 1 + ℓ)! (p + 2 + ℓ − k)! , Ep, 0 = 1 . (32) This follows by expanding (1 − t)p+1 and differentiating Q′

> p

; all denominators are factorials of nonnegative integers. For a unit vector u ∈ RD, write Ψ( z) = z

> √

1 + ∥z∥2 , qu(z) = ⟨u, Ψ( z)⟩.

Lemma B.1 (All fixed-order radial derivatives) . Let B0 = 1 and Bk = 2 · 4kkk for k ≥ 1. For every D, every unit u, and every k ≥ 0,

sup 

> z
> ∥∥∥

Dkqu(z) 

> ∥∥∥op

≤ Bk. (33) 

The order-zero norm means absolute value. Proof. The order-zero bound follows from ∥Ψ( z)∥ < 1. Fix real z and a real unit direction h, put 

s = 1 + ∥z∥2, and consider the holomorphic expression 

t 7 −→ ⟨u, z + th ⟩

> √

s + 2 t⟨z, h ⟩ + t2 .

For |t| ≤ √s/ 4, the perturbation of the denominator’s square is at most 9 s/ 16. There is an analytic square-root branch on this disk, with denominator modulus at least √7s/ 4. The numerator modulus is at most 5 √s/ 4. Thus the displayed function has modulus less than two. Cauchy’s inequality gives 

|Dkqu(z)[ h, . . . , h ]| ≤ 2 k! 4 k.

The real polarization identity for a symmetric k-linear form bounds its full multilinear norm by 

kk/k ! times its diagonal norm. This gives (33) for k ≥ 1, independently of D.16 We give explicit constants for the component regularity. Let Π k denote the set of partitions of [k], and define 

Ap, 0 = 1 , Ap,k = ∑

> π∈Πk

Ep, |π|

∏ 

> V∈π

B|V | (1 ≤ k ≤ p + 1) , (34) 

Cp,k = 2 max  

> 0≤h≤k

Ap,h Bk−h, Hp+1 = Bp+1 + 2 p+1 Cp,p +1 . (35) These constants depend only on p. The partition form of the chain rule gives 

∥∥∥Dk(Gp ◦ qu)

∥∥∥op ≤ Ap,k .The factorial sums and partition counts in these formulas also give log Hp+1 = O(p log( p + 2)); a derivation is given in Appendix F.2. Their growth is not optimized. 

# C The Hard Finite Sum and Its Analytic Properties 

Choose an integer s with the parity of n and 1 ≤ s ≤ n/ 8, and put ρ = s/n . At each stage j ∈ [T ], independently draw a uniform hidden direction Θ (j) ∈ {± 1}D. Conditional on it, draw a fixed table 

S(j) ∈ {± 1}n×D, independently across columns, with column ℓ uniform over sign vectors of sum 

sΘ(j) 

> ℓ

. Thus 1

n

> n

∑

> i=1

S(j) 

> i:

= ρΘ(j)

exactly, not just in expectation. Define 

qj (z) = qΘ(j)/√D(z), ˜qij (z) = ρ−1qS(j) 

> i:/√D

(z),

so that n−1 ∑ 

> i

˜qij = qj . With blocks x = ( x1, . . . , x T ), let 

Ri(x) = −˜qi1(x1) −

> T

∑

> j=2

Gp(qj−1(xj−1))( ˜qij (xj ) + 1) + 

> T

∑

> j=1

∥xj ∥2 . (36) Its average is 

R(x) = −q1(x1) −

> T

∑

> j=2

Gp(qj−1(xj−1))( qj (xj ) + 1) + 

> T

∑

> j=1

∥xj ∥2 . (37) All component functions are Cp+1 and are bounded below. The total dimension is d = T D .

Lemma C.1 (Gap, gradient barrier, and individual regularity) . For the above family, 

R(0) − inf R ≤ 2T, (38) min  

> j

qj (xj ) < b =⇒ ∥∇ R(x)∥ > 13 , (39) Lip( ∇pRi) ≤ Hp+1 

ρ (i ∈ [n]) . (40) 

In particular, the regularity constant has no T or D factor. Proof. Since |qj | < 1 and 0 ≤ Gp ≤ 1, R(0) = 0 and R ≥ − 2T , proving (38). For the gradient barrier, we first prove that for every unit u, c ≥ 1, and q = qu(x) ≤ b = 1 /4, 

∥2x − c∇qu(x)∥ > 13 . (41) 17 Let ζ = (1 + ∥x∥2)−1. Direct differentiation gives 

⟨x, ∇qu(x)⟩ = qζ, ∥∇ qu(x)∥2 = ζ(1 − q2 − q2ζ), 0 < ζ ≤ 1 − q2.

Consequently 

∥2x − c∇qu(x)∥2 = 4( ζ−1 − 1) − 4cqζ + c2ζ(1 − q2 − q2ζ).

If 0 ≤ q ≤ 1/4, its unconstrained minimizer in c is 2q

1 − q2 − q2ζ ≤ 2q

(1 − q2)2 < 1.

Hence c = 1 minimizes over c ≥ 1. The derivative of the resulting expression with respect to ζ is at most −4 + 1 = −3, so its minimum is at ζ = 1 − q2. The square root there is (1 − q2)2 − 2q

√1 − q2 ≥ (1 − b2)2 − 2b = 97 256 > 13 .

If q ≤ 0, c = 1 again minimizes. The negative cross term is now nonnegative, and the smallest singular value of DΨ( x) is (1 + ∥x∥2)−3/2. Thus the squared norm is at least 4 ∥x∥2 + (1 + ∥x∥2)−3 ≥ 1,

using (1 + t)−3 ≥ 1 − 3t. This proves (41). Let j be the first index with qj (xj ) < b . Its incoming coefficient is one, while its outgoing contribution gives 

∇xj R(x) = 2 xj − cj ∇qj (xj ), cj = 1 + 1{j<T }G′

> p

(qj (xj ))( qj+1 (xj+1 ) + 1) ≥ 1.

Apply (41). Finally, put k = p + 1 ≥ 3. For one link and arbitrary global directions h1, . . . , h k, the product rule and (34) imply 

|Dk[Gp(qj−1)( ˜qij + 1)][ h1, . . . , h k]| ≤ Cp,k 

ρ

> k

∏

> ℓ=1

(

∥∥∥hℓj−1

∥∥∥ +

∥∥∥hℓj

∥∥∥). (42) Indeed, a derivative of the weak factor of positive order r is bounded by Br/ρ , and its order-zero value plus one is bounded by 2 B0/ρ . Each subset in the product rule has coefficient bounded by 

Cp,k /ρ , and summing its block products gives the product on the right. For aℓj =

∥∥∥hℓj−1

∥∥∥ +

∥∥∥hℓj

∥∥∥, bounded overlap gives 

∥∥∥aℓ∥∥∥k ≤

∥∥∥aℓ∥∥∥2 ≤ 2

∥∥∥hℓ∥∥∥ .

Hölder’s inequality therefore yields 

> T

∑

> j=2
> k

∏

> ℓ=1

aℓj ≤ 2kk∏

> ℓ=1

∥∥∥hℓ∥∥∥ .

The initial term contributes Bk/ρ , and the quadratic contributes zero. Consequently, sup 

> x

∥∥∥Dp+1 Ri(x)

∥∥∥op ≤ Hp+1 /ρ. 

All these derivatives are continuous, including at the gate interfaces by the extra flatness. Integrating along the segment from x to y proves (40). 

Remark C.2 . Bounded-overlap derivative estimates already occur in higher-order zero-chain con-structions (Carmon et al., 2020; Emmenegger et al., 2022). Here the estimate is applied to radial blocks with a weak encoding: only one factor of each link carries 1 /ρ , and the blockwise Hölder argument preserves that cost uniformly in both T and D.18 D Exact-p-jet hiding and the row information bound 

The analytic construction is now admissible, but its gradient barrier is useful only if an algorithm cannot open many stages quickly. The argument has four steps: bound the information in a single table; establish stage freshness before its first row is exposed; censor replies after a stage's first alignment; and couple the public truncated transcript to the complete oracle. Extra disclosures are used only for information analysis, and the public algorithm ignores them. Future-stage independence is established without conditioning on the absence of earlier future alignments. The tables are fixed before any query, so repeated component queries at one stage reveal the same row, not fresh samples. 

D.1 A censored one-table experiment 

For one independent pair (Θ , S ) as above, let W be arbitrary independent side information including the learner’s random tape. The learner may receive one free initialization row whose index is chosen using W before the row is seen. At each of at most B∗ rounds, it chooses an adaptive index It

and an adaptive vector Vt of norm at most one, then observes row SIt:. It may also receive any deterministic function of the query and previously revealed rows. It receives no alignment flag and no hidden direction. Let Kt be the number of distinct rows known before round t, including the free initialization row. Define 

rρ = 

> ⌊

14096 min {n, ρ −2}

> ⌋

, Er =

> {

∃t ≤ B∗ : Kt < r, D −1/2⟨Θ, V t⟩ > a 

> }

.

Lemma D.1 (Censored adaptive row information) . Assume ρ ≤ 1/8 and 1 ≤ r ≤ n/ 4096 . Let Tr

be the transcript stopped immediately after the rth distinct row is revealed, or at the horizon if fewer are revealed. Then, conditionally on almost every W = w,

I(Θ; Tr | W = w) ≤ Drρ 2. (43) 

If r = rρ ≥ 2 and 

D ≥ 2048 + 512 log( B∗ + 2) , (44) 

then P(Er | W = w) < 1/8.Proof. Condition on W = w and pad the stopped transcript to fixed length by cemetery symbols. Query choices contribute no conditional information because they are functions of preceding observations. If the free initialization row is present, treat it as a zero-th exposure and include it in the transcript; it is charged to the row-information budget, though not to the stage’s query budget. Suppose d < r distinct rows have been seen and their sum in column ℓ is Σ d,ℓ . For a previously unqueried index, exchangeability gives 

P(SItℓ = 1 | Θℓ = θ, past) = n + sθ − d − Σd,ℓ 

2( n − d) =: pθ.

The two parameters differ by s/ (n − d) and lie in [1 /4, 3/4], since s ≤ n/ 8 and d ≤ n/ 4096. On this interval the negative second derivative of binary entropy is at most 16 /3. For any posterior probability 

π = P(Θ ℓ = 1 | retained history), the second-order Jensen gap is at most 12 (16 /3) π(1 − π)( p+ − p−)2,hence at most 23 

> (

sn − d

> )2

≤ ρ2.

19 Expose the new row coordinate by coordinate. Given the hidden vector and the revealed rows, the remaining columns are conditionally independent. To justify this under adaptive row selection, fix a possible retained history: each selected index is then fixed, and consistency with the policy imposes no additional constraint beyond the cells already observed. The likelihood of these cells factors across columns conditional on Θ. When revealing coordinates of the new row successively, the channel to its next cell consequently depends on the hidden vector only through Θ ℓ. The entropy bound above is uniform in its posterior probability; it does not assume that hidden coordinates are posterior-independent. Thus the preceding information bound applies to each of the D new cells. Repeated rows contribute no information. The conditional chain rule gives (43), including the optional first row. Now let r = rρ. The information is at most D/ 4096. Under the product law consisting of the uniform hidden vector and an independent transcript with its actual marginal distribution, every retained Vt is independent of Θ. A Rademacher tail bound and a union bound give 

q := Pproduct (Er) ≤ B∗ exp( −a2D/ 2) = B∗e−D/ 128 .

Writing u for the true probability of Er, relative-entropy data processing yields 

I(Θ; Tr | W = w) ≥ u log(1 /q ) − log 2 .

Condition (44) gives log(1 /q ) ≥ 3D/ 512 and, because D ≥ 2048, D/ 4096 + log 2 < 3D/ 4096. Hence 

u ≤ D/ 4096 + log 2 

D/ 128 − log B∗

< 18 .

The event is measurable from the retained transcript and Θ: the query selecting the rth row is retained before stopping. This is why the stopping convention is specified explicitly. 

D.2 A stronger truncated simulator and first-hit censoring 

For 0 ≤ h ≤ T , define R[h] 

> i

by retaining the first term in (36), the quadratic, and only links with 2 ≤ j ≤ min {h + 1 , T }. The completed prefix initially has length h = 0; the current stage is j = h + 1. At a query point x, the simulator checks the current alignment before giving its reply. If qj (xj ) > a ,it promotes exactly one stage, to h + 1; otherwise it leaves h unchanged. Its public reply is the exact p-jet of the truncated function at the updated prefix. Once h = T , keep the prefix fixed and return the full component p-jet on every remaining call; no further completion tests or disclosures are made. For information analysis only, enrich this simulator by revealing the selected row of the current table. On a completion response it additionally reveals the entire data of the completed stage and the selected row of the newly current stage. The latter is its one free initialization row. These disclosures make the simulator more informative, not less. 

Lemma D.2 (Exact jet truncation and reply factorization) . If every gate of a link omitted from 

R[h] 

> i

has input at most a, then its full p-jet is zero and 

J pRi(x) = J pR[h] 

> i

(x).

Before completion of the current stage j, the public truncated reply is a deterministic function of the completed-stage data, the query, and row S(j) 

> i:

. At completion it is a function of these data, Θ(j),and row S(j+1)  

> i:

if j < T .

20 Proof. An omitted link has form Gp(q(x))( ˜qi(y) + 1). Every derivative through order p is a sum of product-rule and chain-rule terms containing some G(k) 

> p

(q(x)) with 0 ≤ k ≤ p. All such factors vanish for q(x) ≤ a, including equality. For an active incoming link, every derivative of ˜qi is determined by its selected row. The outgoing link enters only on completion, when the current direction may be revealed. No later hidden table or direction enters the truncated reply. 

Lemma D.3 (Prefix measurability and stage freshness) . Let ξ be the algorithm’s private random tape, let Zj = (Θ (j), S (j)), and let ht be the simulator’s prefix length after response t. For g < T ,the event {ht = g} and the enriched history on that event are measurable with respect to 

Ag+1 = σ(ξ, Z1, . . . , Zg+1 ).

For g = T , AT suffices. In particular, conditional on the enriched history and ht = g < T , stages strictly beyond g + 1 have their independent prior distributions. Before the initialization row of a reached stage j is exposed, its table also has its independent prior conditional on the entire earlier-stage data and ξ.Proof. Induct on t. On a branch with pre-query prefix g, the query and completion test use only 

Ag+1 . A noncompletion reply uses earlier data and one row of stage g + 1. A completion reply may reveal the complete data of stage g + 1 and one row of stage g + 2, but nothing later. The event 

{ht = g} is the disjoint union of a noncompletion branch from prefix g and a completion branch from prefix g − 1; both have measurable representations in Ag+1 . This proves the first statement, including event measurability, rather than only measurability conditional on a possibly informative selection event. Independence of ξ, Z1, . . . , ZT gives the future-stage assertion. For the last assertion, the query completing stage j − 1, its completion test, and the selection of its component index are functions of ξ, Z1, . . . , Zj−1 before the response is generated. Order that response conceptually by revealing completed-stage data first and the stage-j initialization row second. Reaching this intermediate instant involves no stage-j data. Conditional on the earlier variables, the new table retains its prior and its initialization-row index is fixed. This conceptual ordering changes no observable response available before the next query. Fix a reached stage j and condition at the intermediate instant in Lemma D.3. Independent information may be supplied without charge in the one-table experiment, so conditioning on the complete earlier tables is legitimate. No conditioning on the absence of a future-alignment event is performed. Couple the real enriched stage to a counterfactual experiment that always uses the noncompletion reply map and always reports noncompletion, even after a first alignment would have occurred. This is a censored row learner of the preceding type: its reply maps depend only on rows and the independent old data. Up to the first completion query, both interactions have the same pre-query history and choose the same query. In particular, 

{completion with fewer than rρ previously exposed rows } ⊆ E rρ . (45) The completion reply, and any direction it reveals, occurs after the event in (45); it is not included in the information transcript. Noncompletion flags are forced constants in the censored experiment and contribute no information there. 

Corollary D.4 (Each reached stage is rarely short) . Under (44) , a reached stage completes in fewer than rρ calls assigned to it with conditional probability at most 1/8, before its initialization row is exposed. An unreached stage is declared not short. 

21 Proof. On its qth assigned call with q < r ρ, it has seen at most its initialization row and q − 1previous response rows, so the number of distinct pre-query rows is at most q < r ρ. Apply (45) and Lemma D.1. Conditioning on all earlier data is legitimate because they are independent of the new table. Averaging over that larger conditioning field gives the same bound conditional on the actually observed pre-initialization history. 

D.3 Adding stage costs and coupling to the true oracle 

Assign each call to its pre-query current stage. If N ≤ rρJ/ 2 and J stages complete, at least J/ 2 of those stages must be short, since every other stage consumes at least rρ disjoint assigned calls. By Corollary D.4, the expected number of short stages among the first J is at most J/ 8. Markov’s inequality gives 

P(hN ≥ J) ≤ 14 . (46) No independence of the stage durations is assumed. Let B be the event that at some queried point a block strictly beyond the pre-query current stage has alignment greater than a, also including blocks strictly beyond the final current stage at the unqueried output. At every history of the enriched truncated simulator, all such strictly future stages retain their independent prior. This is precisely the conditional independence in Lemma D.3; it is valid even when prefix lengths and component indices are adaptive. Therefore, regardless of query radii, 

P(B) ≤ (N + 1) T e −D/ 128 , (47) because ∥Ψ( x)∥ < 1. This argument is in the truncated simulator and does not condition on the absence of future alignments. To relate it to a true exact-oracle algorithm, run the same public policy and random tape in the true interaction and in the truncated simulator, ignoring the simulator’s extra disclosures. On Bc,their public transcripts agree inductively. On a noncompletion query all omitted gates, starting with the current outgoing gate, are flat. On a completion query the outgoing link is included, and every still-omitted gate is flat. Lemma D.2 applies in both cases. The public outputs agree as well. We do not assert equality between a public transcript and an enriched transcript; enrichment is used only for domination and information analysis. 

Proposition D.5 (Unfinished direction under arbitrary exact-p-jet access) . Let rρ ≥ 2, 1 ≤ J < T ,and N ≤ rρJ/ 2. If 

D ≥ max {2048 + 512 log( N + 2) , 128 log(128( N + 1) T )}, (48) 

then for every randomized adaptive true exact-p-jet algorithm, with probability at least 95 /128 its output has qk(̂ xk) ≤ a in some block. The same conclusion holds after arbitrary positive input scaling. Proof. Equations (46) and (47) imply probability at least 1 − 1/4 − 1/128 = 95 /128 for hN < J 

and Bc in the truncated run. Since J < T , a block strictly beyond its final current stage remains; its output alignment is at most a. Transfer the event to the true public interaction by the preceding coupling. For input scaling x 7 → βx , replace every alignment vector by Ψ( βx ), which is still bounded in norm by one. 22 E Scaling and Complete Parameter Coverage 

Let H∗ = Hp+1 . Scale the components by 

fi(y) = αR i(βy ), βp = Lpρ

6H∗ε , α = 6εβ . (49) Then Lip( ∇pfi) ≤ αβ p+1 H∗

ρ = Lp, (50) 

F (0) − inf F ≤ 2αT, (51) min  

> j

qj (βy j ) < b =⇒ ∥∇ F (y)∥ > 2ε. (52) Choose 

T =

⌊

cT,p Kpρ1/p ⌋

, cT,p = 12 · 61+1 /p H1/p 

> ∗

. (53) This guarantees the gap constraint exactly. If cT,p Kpρ1/p ≥ 4, reserve the last stage and set J = T −1. Proposition D.5 gives the lower term Ωp

(

Kpρ1/p min {n, ρ −2}

)

= Ω p

(

Kp min {nρ 1/p , ρ −2+1 /p }

)

. (54) The first branch increases in ρ and the second decreases. They balance at ρ ≍ n−1/2.For example, a universal threshold n0 = 2 16 is sufficient for the bias and row-count conditions below. For n ≥ n0, choose s with the parity of n in [ √n, √n + 2]. Then 

n−1/2 ≤ ρ ≤ 2n−1/2, rρ ≥ n/ 32768 , T = Θ p(Kpn−1/(2 p))whenever the last quantity is sufficiently large. Hence Ωp

(

Kpn1−1/(2 p))

(55) queries are necessary with constant success probability. The probability of an ε-stationary output under the hard distribution is at most 33 /128 < 1/3 for a sufficiently small multiple of this budget. The tables and directions are fixed before the algorithm runs. Averaging over this distribution and the algorithm’s private randomness, then fixing an instance, gives a worst-case deterministic finite sum for each randomized algorithm. 

E.1 The additive n term, including arbitrarily small positive gap 

Choose a hidden index I∗ uniformly from [ n] and an independent hidden scalar Z uniformly from 

{− 3, −1, 1, 3}. In dimension one set 

fi(x) = γ

2 x2 − 4nεZ 1{i=I∗}x, γ = 72 ε2

∆ . (56) Then 

F (x) = γ

2 x2 − 4εZx, F (0) − inf F = Z2∆9 ≤ ∆.

For every p ≥ 2 the pth-derivative Lipschitz constant is zero. Until the hidden index is queried, all replies are identical to those of the public quadratic γx 2/2 and reveal nothing about Z. Under the 23 all-quadratic transcript, at most N distinct indices are tried, so the probability of hitting I∗ is at most N/n , even under adaptive index selection. The four possible success intervals |γ̂ x − 4εZ | ≤ ε

are pairwise disjoint. Thus 

P(success) ≤ Nn + 14 ≤ 516 < 13 if N ≤ n/ 16 .

This proves Ω( n) for every ∆ > 0, including when the nontrivial chain cannot fit into the gap budget. If Kpn−1/(2 p) is below a sufficiently large p-dependent constant, Sp = Op(n), so this single hidden-index family already lower-bounds the whole target expression. 

E.2 Small component counts 

For completeness, the finite set of component counts below the universal large-n threshold does not require an external full-function lower bound. Set ρ = 1 and take every row in table j equal to Θ(j), so every component is the same radial-chain function. The analytic estimates and exact-jet coupling remain valid. This case uses only those estimates and the future-alignment bound (47), not Lemma D.1, whose small-bias hypothesis does not hold at ρ = 1. At most one stage is promoted per query. If N ≤ T − 2, at least one strictly future block remains at the output deterministically in the truncated interaction. Choose D to make (47) at most 1 /128. Scaling with (49)–(53) gives an Ω p(Kp) lower bound when Kp is large enough. Together with (56), this is Ω p(n + Kpn1−1/(2 p))for the finitely many remaining n, after decreasing the constant. Thus all n ≥ 1 are covered. 

Remark E.1 (Dimension of a hard instance) . The constructions can be taken in finite dimension 

d ≤ Cp

> (

1 + Kpn−1/(2 p)) log (2 + n + Kpn1−1/(2 p)).

For large n this follows directly from d = T D and (48); the small-n and hidden-index cases fit after adjusting Cp.

Proof of Theorem 3.2. The lower bounds in this section hold in the individual class. Proposition A.4 gives the upper bound in the mean-squared class. Since (4) implies (5), the sandwich in (6) follows. If ∆ = 0, the initial point is a global minimizer of a differentiable function and is stationary. If 

Lp = 0, every component is a polynomial of degree at most p, recovered in full by one p-jet query at the origin. Hence n calls determine F exactly. To see directly that an ε-stationary point exists, let 

η = ε2/(8∆) and minimize F (x) + η ∥x∥2 /2. This function is coercive because F is bounded below. At a global minimizer xη,

∇F (xη) = −ηx η, η

2 ∥xη∥2 ≤ F (0) − inf F ≤ ∆,

so ∥∇ F (xη)∥ ≤ ε/ 2. By continuity, a rational point with gradient norm below ε exists. For the known polynomial choose the first such point in a fixed enumeration of Qd. This is a measurable zero-additional-query computation. The hidden-index quadratic family proves the matching Ω( n)for ∆ > 0. 

# F Additional Technical Details 

F.1 Measurable model minimization 

Lemma F.1 (A Borel model selector) . For fixed r > 0, there is a Borel map from the finite-dimensional coefficient vector of (24) to a global minimizer on B(0 , r ).

24 Proof. Write the coefficient vector as c and the continuous objective as q(c, s ). Its minimum v(c)over the fixed compact ball is continuous: uniform convergence of q(ck, ·) to q(c, ·) on the ball holds whenever ck → c. Start with a closed cube containing the ball and subdivide it into finitely many closed cubes of half the side length. Select the first subcube whose intersection with the ball contains a minimizer, and repeat within that selected cube. For a fixed subcube Q, this selection test is Borel because min 

> s∈Q∩B(0 ,r )

q(c, s ) = v(c)is an equality of continuous functions when the intersection is nonempty; an empty intersection is always rejected. At each depth, there are finitely many possible cube histories, so every selection is Borel. The nested nonempty compact intersections with the minimizer set have diameters tending to zero. Their unique limit is a minimizer and a Borel function of c. This selector is an admissible exact-oracle decision rule. 

F.2 Growth of the order-dependent constants 

Let k ≤ p + 1. The envelope in (32) obeys 

Ep,k ≤ 8kZp 2p+1 (2 p + 2) k−1 (k ≥ 1) , Zp = (2 p + 3) 

> (

2p + 2 

p + 1 

> )

≤ (2 p + 3)2 2p+2 .

For a partition π of [ k] into h nonempty blocks,  

> ∏
> V∈π

B|V | ≤ 2h4kkk,

since ∏  

> V∈π

|V ||V | ≤ kk. There are at most kk partitions. Substitution into (34)–(35) gives log(2 + Hp+1 ) ≤ C(p + 1) log( p + 2) for a numerical C. These are explicit, dimension-free bounds; they are not optimized in p.

F.3 Strictness of the mean-squared assumption 

For n ≥ 2, the inclusion from individual to mean-squared smoothness is strict even among bounded-below one-dimensional examples with arbitrarily small positive initial-gap allowance. For ω > 0, set 

f1(x) = √n L pω−(p+1) sin( ωx ), fi(x) = 0 (i > 1) .

The Lipschitz constant of ∇pf1 is exactly √nL p, while the normalized mean-squared increment is bounded by Lp|x − y|. Thus (5) holds but (4) with the same parameter does not. Moreover, 

F (0) − inf F = Lpn−1/2ω−(p+1) , which is at most any prescribed ∆ > 0 for sufficiently large ω.

F.4 From a hard distribution to a deterministic instance 

All hidden signs, tables, and the hidden-index choices are sampled once before interaction. For every fixed randomized decision rule with the stated budget, the preceding lower-bound proofs bound its success probability averaged jointly over these data and its private tape. If every deterministic realization had success probability at least 2 /3 over that tape, the joint average would also be at least 2 /3. The proved upper bound on joint success is smaller. Hence a fixed realization defeats the randomized rule. No fresh stochastic oracle, adaptive change of the underlying function, or restriction to deterministic algorithms is used. 25