---
title: Matching Upper and Lower Bounds for Higher-Order Nonconvex Finite-Sum Optimization
title_zh: 高阶非凸有限和优化的匹配上界与下界
authors: "Wendao Wu, Haihan Zhang, Chenheng Zhang, Yanyi Li, Chunyuan Zheng, Cong Fang, Haoxuan Li, Zhouchen Lin"
date: 2026-09-23
pdf: "https://arxiv.org/pdf/2609.28202v1"
tags: ["query:ml-theory"]
score: 6.0
evidence: 非凸有限和优化的紧致预言机复杂度
tldr: 针对非凸有限和优化中寻找一阶稳定点的高阶随机预言复杂度，此前一般阶上下界在样本数 n 上相差 sqrt(n)。本文对固定整数 p≥2，在成功概率至少 2/3 下给出精确到常数的极小极大查询复杂度，下界覆盖无限制随机自适应算法。上界保留已知有限和指数，仅需 p 阶导数的均方增量，并通过整轮递归估计验证消除固定置信度对数损失。结果首次匹配高阶有限和上下界，并刻画了加性 n 项与参数区域。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.28202v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1713, \"height\": 340, \"label\": \"Table\"}]"
motivation: 非凸有限和优化中，高阶算法寻找一阶稳定点的随机预言复杂度在 n 依赖上长期存在 sqrt(n) 缺口，亟需紧致上下界。
method: 将稠密弱隐藏构造扩展到完整高阶回复，使各分量正则性不随链长增长；上界用精确函数值验证整轮递归估计，仅依赖 p 阶导数均方增量。
result: "结果为：对固定 p≥2，极小极大查询数为 Θ_p(n+ΔL_p^{1/p}n^{1-1/(2p)}ε^{-(p+1)/p})，成功概率≥2/3，且所有正参数区域均含加性 n 项。"
conclusion: 该结果闭合了高阶有限和优化在 n 依赖上的 sqrt(n) 缺口，给出匹配上下界，并允许无限制内部计算，只计分量查询次数。
---

## 摘要
我们为寻找非凸有限和的一阶稳定点建立了紧的随机高阶 oracle 复杂度。设 n 为分量个数，Δ>0 为初始目标间隙上界，L_p>0 为单个 p 阶导数的 Lipschitz 上界，ε>0 为目标梯度范数。对每个固定整数 p≥2，在成功概率至少为 2/3 时，返回函数值及直到 p 阶所有导数的精确分量查询的极小极大次数为 [ Θ_p( n+Δ L_p^{1/p} n^{1-1/(2p)} ε^{-(p+1)/p} ) ]，其中常数仅依赖于 p，最坏情况覆盖所有有限维数。该下界对无限制的随机自适应算法成立，并在对 n 的依赖上闭合了此前已知的一般阶上界与下界之间的 sqrt(n) 差距。我们将稠密弱隐藏扩展到完整的高阶回复，同时保持每个分量的正则性不依赖于链长。匹配的上界保留了已知的有限和指数，仅需要均方 p 阶导数增量，并通过用精确函数值验证整个递归估计轮次，消除了固定置信度下的对数损失。该刻画在所有正参数范围内都包含加性 n 项；它统计允许无限制内部计算的 oracle 调用次数。

## Abstract
We establish tight randomized higher-order oracle complexity for finding first-order stationary points of nonconvex finite sums. Let $n$ be the number of components, $Δ>0$ the initial objective-gap bound, $L_p>0$ an individual $p$-th derivative Lipschitz bound, and $ε>0$ the target gradient norm. For every fixed integer $p\ge 2$, the minimax number of exact component queries returning the value and all derivatives through order $p$, with success probability at least $2/3$, is \[ Θ_p\!\left( n+ΔL_p^{1/p}n^{1-1/(2p)} ε^{-(p+1)/p} \right), \] where the constants depend only on $p$ and the worst case ranges over all finite dimensions. The lower bound holds for unrestricted randomized adaptive algorithms and closes the $\sqrt{n}$ gap between the previously known general-order upper and lower bounds in their dependence on $n$. We extend dense weak hiding to complete higher-order replies while keeping each component's regularity independent of the chain length. The matching upper bound retains the known finite-sum exponent, requires only mean-squared $p$-th derivative increments, and removes the fixed-confidence logarithmic loss by verifying entire recursive-estimation epochs with exact function values. The characterization includes the additive $n$ term for every positive parameter regime; it counts oracle calls with unrestricted internal computation.