Title: Exact Rank-Space KL Projection for Shared-Marginal Low-Rank Factors: Application to Doubly Stochastic Clustering

URL Source: https://arxiv.org/pdf/2608.08642v1

Published Time: Tue, 11 Aug 2026 01:35:52 GMT

Number of Pages: 12

Markdown Content:
> 1

# Exact Rank-Space KL Projection for Shared-Marginal Low-Rank Factors: Application to Doubly Stochastic Clustering 

Enliang Hu ynel.hu@gmail.com 

Abstract—We study exact Kullback–Leibler (KL) projection for low-rank factorizations whose two nonnegative factors have prescribed row marginals and a shared, learned column marginal. For arbitrary positive row marginals of equal total mass, the joint KL projection reduces exactly to a strictly convex gauge-fixed dual with only r − 1 effective variables; its Hessian is a sum of categorical covariance terms and admits O(( n + m)r) matrix-free Hessian–vector products. The projection theorem is objective-independent. We then special-ize this geometry to doubly stochastic (DS) graph learning through W = U Diag( g)−1V ⊤, where row-simplex factors with a common column mass induce an exactly DS graph without materializing an n×n optimization variable. Combined with observed-edge sparse fitting, a stochastic anchor-reduced manifold regularizer, and Bregman backtracking, the resulting mirror-descent method preserves exact feasibility at every accepted step. Under a nonvanishing latent-mass condition, it satisfies sufficient decrease and an O(1 /N ) mirror-stationarity bound, while strictly positive accumulation points are KKT stationary. Matched clustering experiments show competitive accuracy, feasibility residuals near numerical precision, and favorable anytime behavior without a dense learned graph. Index Terms—KL projection, low-rank factorization, shared marginals, doubly stochastic clustering, mirror descent, sparse graph learning 

I. Introduction Graph-based clustering converts pairwise relations into a similarity graph and seeks a partition consistent with its geometry. Spectral methods established the importance of graph normalization and Laplacian structure [1]–[3]. A persistent difficulty is degree imbalance: a few high-degree samples can dominate the graph while weakly connected samples contribute little. Doubly stochastic (DS) similarities alleviate this imbalance by requiring a nonnegative graph to have unit row and column sums. Matrix scaling provides the classical computational ba-sis [4]–[6], and DS normalization has long been useful for spectral clustering [7]. The main obstacle is computational. Direct DS graph-learning models optimize an explicit n × n matrix, which gives a transparent representation but incurs quadratic state and repeated dense updates [12]. Low-rank formula-tions avoid this bottleneck. DCD, for example, represents the graph through a single nonnegative factor and achieves linear-in-n factor storage for fixed rank [11]. The price is structural: the same factor appears on both sides of the induced similarity, so representation and optimization are self-coupled. General two-factor factorizations remove that coupling, but U V ⊤ is not doubly stochastic merely because U, V ≥ 0. Penalty enforcement or post-hoc scaling can restore stochasticity approximately or after the update, but then feasibility is no longer built into the factor geometry. This leaves a specific design gap. We seek a low-rank clustering model that simultaneously provides four proper-ties: exact DS feasibility throughout optimization, freedom for two latent factors to differ, fitting restricted to observed sparse affinities, and scalable manifold regularization. No single property is difficult in isolation; the challenge is to obtain them together without reintroducing an n×n state or a large constrained subproblem. We address this gap with the shared-marginal factoriza-tion 

W = U Diag( g)−1V ⊤, (1) subject to 

U 1 = V 1 = 1, U ⊤1 = V ⊤1 = g, U, V ≥ 0. (2) The constraints in (2) are only linear equalities and nonnegativity, hence the factor feasible set is convex. For every feasible triplet with g > 0,

W 1 = U Diag( g)−1g = 1, W ⊤1 = V Diag( g)−1g = 1,

so exact DS feasibility follows directly from factor feasibil-ity. The shared mass g is learned rather than prescribed, while U and V remain distinct and can encode comple-mentary latent roles. The factorization itself has important antecedents in low-rank optimal transport. Entropic OT made Sinkhorn scaling a central primitive [14]; iterative KL/Bregman projections provide a general constrained-transport view-point [15], [16]; and factored couplings introduced nonneg-ative factors linked through a common marginal [17]–[19]. Most recently, Jawanpuria and Mishra study the same balanced factor constraints with prescribed row marginals as a Riemannian manifold [20]. Their balanced nonlinear KL retraction is computed by cyclic Bregman projections, whereas an (r −1) -dimensional Schur complement appears in a tangent-space Fisher–Rao projector. We therefore do not claim the shared-marginal factorization, mirror de-scent, or the mere existence of rank-sized linear systems as new. Our main geometric result concerns the nonlinear KL arXiv:2608.08642v1 [cs.LG] 9 Aug 2026 2

projection itself: after eliminating row multipliers and the shared column mass, the complete projection is one strictly convex (r − 1) -dimensional dual. We then specialize this objective-independent operator to sparse DS clustering. LoRD/B-LoRD form a close clustering line, using a class-probability parameter to obtain a convex linear one-factor DS constraint and projected optimization [13]. Our graph model instead retains two factors, learns their common latent mass, and preserves exact feasibility inside every accepted mirror step. Two additional choices make the model suitable for sparse graph data. First, absent edges in a k-NN graph should not automatically be treated as equally strong negative observations. We therefore fit only the observed support, following the general principle of observed-entry objectives in incomplete-matrix estimation [24]. Second, local affinities alone may not capture broader geometry. A sparse sample-to-anchor matrix Z defines 

La = I − ZD −1 

> a

Z⊤,

which is applied without materializing either the reduced similarity or the Laplacian. The row-stochastic anchor construction gives a nonnegative stochastic PSD similarity and the bound 0  La  I, while each action costs 

O(nqr ) when every sample uses at most q anchors [25], [26]. Nyström approximation is retained as an empirical alternative [27], [28]. The remaining difficulty is optimization. Although the feasible factor set is convex, the graph objective is noncon-vex because U , V , and g interact multiplicatively. We first step away from clustering and consider arbitrary positive prescribed row marginals a and b of equal total mass. The exact joint KL projection onto the corresponding shared-marginal factor polytope has only r − 1 effective dual variables after gauge fixing; its Hessian is a sum of weighted covariance terms and is positive definite on the reduced space. The same dual admits matrix-free Hessian–vector products in O(( n + m)r) time. This theorem depends only on the factor constraints and KL geometry, not on the clustering loss or anchor regularizer. The DS clustering feasible set is the special case a = b = 1,so the same operator preserves exact feasibility inside every accepted mirror step rather than repairing the graph afterward. The contributions are as follows.  

> •

Exact DS structure in a convex two-factor geome-try. Proposition 1 formalizes the model-level property: linear row-simplex/shared-marginal constraints define a convex factor polytope, and every feasible triplet with positive latent mass induces an exactly doubly stochastic graph without imposing U = V . The same representation admits an optional symmetric DS out-put of rank at most 2r. 

> •

An objective-independent rank-space KL projection. Theorem 1 treats arbitrary positive row marginals of equal total mass and shows that the complete joint KL projection reduces to an (r − 1) -dimensional strictly convex dual. The reduced Hessian is SPD after gauge fixing; explicit Newton costs O(( n + m)r2 + r3) per evaluation/solve, while a Hessian–vector product costs only O(( n + m)r). 

> •

Feasible descent with a quantitative stationarity guar-antee. Theorem 2 proves finite backtracking, exact feasibility, Bregman sufficient decrease, asymptotic regularity, and an O(1 /N ) mirror-stationarity bound. Corollary 1 then identifies every strictly positive ac-cumulation point as KKT stationary under the same nonvanishing-mass condition.  

> •

Sparse manifold modeling and controlled empirical comparison. Observed-edge fitting and a stochastic anchor-reduced Laplacian avoid dense learned graph states, while the experiments compare representative full-graph, one-factor low-rank, and shared-marginal alternatives under a unified clean/matched protocol. Ablations isolate the reduced geometry, factor-side choice, exact projection, and stationarity behavior. The main text gives complete theorem statements to-gether with concise proof sketches. Appendix A contains the full proof of the exact rank-space KL projection theorem; longer proofs of the auxiliary structural and convergence statements are omitted for brevity. II. Related Work A. Spectral and doubly stochastic graph clustering Spectral clustering links partitioning to the eigenspaces of graph Laplacians. Normalized cuts [1], the normalized spectral algorithm of Ng, Jordan, and Weiss [2], and later tutorials [3] established the main normalization principles used in graph clustering. DS normalization goes further by balancing both row and column masses. Its computational basis is matrix scaling [4], [5], for which convergence and numerical properties have been studied extensively [6]. Zass and Shashua used DS approximation directly as a spectral-clustering normalization [7]. Learned-graph for-mulations subsequently imposed DS and clustering struc-ture jointly; a KDD formulation learned a structured DS matrix with a low-rank Laplacian constraint [10], while the more recent SDSGC combines a full DS graph with spectral structure [12]. These direct models retain an interpretable graph variable but generally require O(n2)

state storage. B. Low-rank graph factorization for clustering Low-rank nonnegative factorizations are closely related to clustering and spectral objectives [8], [9]. DCD special-izes this idea to DS clustering through a Data–Cluster– Data random walk and a one-factor nonnegative low-rank decomposition [11]. Its storage is linear in n for fixed rank, but the repeated factor in the induced similarity creates self-coupling and leads naturally to multiplicative MM updates. More recently, LoRD and B-LoRD revisit low-rank DS clustering from a kernel k-means perspective and introduce a class-probability parameter that turns their one-factor DS constraint into a convex linear factor constraint [13]. Our setting is different: two factors share 3

a learned latent mass, the factors need not coincide, and feasibility is enforced by a joint KL projection whose effective dimension is r − 1.C. Low-rank transport, matrix scaling, and Bregman projections Entropic OT connects transport computation to Sinkhorn scaling [14], [16], while iterative Bregman projec-tions provide a general KL-projection framework for inter-sections of affine marginal constraints [15]. Factored cou-plings introduced a common intermediate marginal [17]; low-rank Sinkhorn later optimized such factors with LR-Dykstra-style KL projections and proved non-asymptotic stationarity [18]. FRLC uses a more flexible latent-coupling factorization and coordinate mirror descent [19]. The closest geometric antecedent is the 2026 Rieman-nian formulation of balanced low-rank OT [20]. It uses the same constraint pattern U 1 = a, V 1 = b, and U ⊤1 =

V ⊤1. Its balanced nonlinear KL retraction is computed through cyclic Bregman projections over the three affine constraint families; separately, its tangent-space Fisher– Rao projector reduces to an (r − 1) -dimensional Schur-complement system. Our novelty boundary is narrower and different: Theorem 1 eliminates the row multipliers and shared mass in the nonlinear KL projection itself, yielding one strictly convex (r−1) -dimensional dual whose minimizer is the exact joint projection. Thus we claim neither shared-marginal factors nor an (r − 1) tangent linear system in isolation. The clustering contribution is the use of this direct projection as the finite-iterate feasibility operator for a sparse nonconvex DS graph objective. D. Mirror descent and non-Euclidean first-order methods Mirror descent replaces Euclidean projection by a geom-etry induced by a distance-generating function; entropy is especially natural on simplices [21]. Bregman proximal-gradient analyses extend descent arguments beyond ordi-nary Euclidean Lipschitz-gradient settings [22], while rel-ative smoothness provides a general first-order framework for non-Euclidean descent [23]. Our convergence proof is self-contained and uses a problem-specific Bregman sufficient-decrease line search, but these works provide the optimization context for the KL mirror map and the use of non-Euclidean descent certificates. E. Sparse fitting and reduced manifold graphs Sparse input similarities naturally suggest losses that distinguish observed edges from unobserved pairs. The use of observed-support objectives has a broad precedent in incomplete-matrix estimation, including Soft-Impute-type methods [24]; we use this principle only as motivation and do not rely on nuclear-norm completion. For scalable manifold modeling, anchor graphs replace full pairwise relations by sparse sample-to-anchor weights and can yield nonnegative PSD graph operators with linear-scale storage [25]. Landmark-based spectral clustering similarly uses a small representative set to obtain linear scaling in the number of samples [26]. Nyström approximation is another classical route to low-rank kernel and spectral computations [27], [28]. Our main method uses the stochas-tic anchor construction because its reduced similarity is nonnegative, PSD, and exactly stochastic by construction; Nyström is retained as an accuracy-oriented alternative in the ablation study. The closest methods obtain stochastic structure in different ways and therefore pay different computational or modeling costs. Table II separates the main structural axes. The comparison is intentionally restricted to representa-tive DS formulations and to low-rank Sinkhorn, the closest shared-marginal antecedent; generic NMF and spectral baselines are retained for the empirical study rather than used to define the methodological gap. Table II highlights the intended niche of SMDSC. SDSGC keeps rich structure in an explicit graph; DCD and LoRD/B-LoRD obtain a compact symmetric graph through a single factor; low-rank Sinkhorn has two factors and a learned shared marginal but optimizes an OT cost. SMDSC combines the two-factor shared-marginal geometry with an observed-edge clustering loss and an anchor-reduced manifold term. The distinction is not that the other methods lack stochastic structure, but that they obtain it through a different representation or pay for it in a different part of the algorithm. III. Proposed Method A. Shared-marginal low-rank doubly stochastic factoriza-tion Shared-marginal factorizations are also used in low-rank optimal transport [17], [18]; here we use the geometry for sparse graph clustering rather than transport-cost mini-mization. Let U, V ∈ Rn×r 

> +

and g ∈ Rr

> +

. We parameterize the learned graph as 

W (U, V, g ) := U Diag( g)−1V ⊤, (3) subject to 

U 1r = V 1r = 1n, U ⊤1n = V ⊤1n = g. (4) Proposition 1 (Convex shared-marginal geometry and exact DS feasibility). Let 

F := {(U, V, g ) ≥ 0 : U 1r = V 1r = 1n,U ⊤1n = V ⊤1n = g}. (5) Then F is a convex polytope. For every (U, V, g ) ∈ F with 

g > 0, the matrix W = U Diag( g)−1V ⊤ is nonnegative and doubly stochastic, with rank( W ) ≤ r. Moreover, 

Ws := 12 (W + W ⊤) (6) is nonnegative, symmetric, and doubly stochastic, with 

rank( Ws) ≤ 2r.Proof. The defining constraints are affine equalities plus nonnegativity, so F is convex and polyhedral. The row-simplex constraints give 0 ≤ Uij , V ij ≤ 1, and hence 0 ≤4

TABLE I Closest shared-marginal projection mechanisms. The comparison concerns how marginal feasibility is restored, not the surrounding learning objective. 

Method factor constraints nonlinear KL feasibility step rank-space structure Low-rank Sinkhorn [18] prescribed transport marginals + shared g

LR-Dykstra / alternating KL projections 

g ∈ Rr 

> +

kept explic-itly Riemannian low-rank OT [20] 

U 1 = a, V 1 = b, shared column sums cyclic Bregman projection for balanced retraction 

(r − 1) Schur system in tangent projector This work same general balanced factor polytope; DS is a = b = 1

one exact joint KL projection via a strictly convex dual 

r − 1 nonlinear dual variables; HVP 

O(( n + m)r)

TABLE II Structural mechanisms in representative doubly stochastic and shared-marginal formulations. “Exact” refers to the represented marginal constraints and does not imply global optimality. Low-rank Sinkhorn is an optimal-transport antecedent rather than a clustering baseline.                                                                      

> Method Learned state DS / marginal mech-anism Symmetry by model Two-factor freedom Latent mass Sparse-edge objective Structure beyond stochasticity SDSGC [12] full Wand spec-tral factor explicit DS constraints optimized jointly by ALM Yes No –No structured Laplacian /graph connectivity DCD [11] one factor P∈
> Rn×r
> +
> DS by normalized DCD parameteriza-tion Yes No implicit cluster mass Yes sparse-similarity KL fit-ting LoRD [13] one factor V∈
> Rn×r
> +
> linear convex factor constraints + projec-tion Yes No prescribed class prior μ
> No †probabilistic clustering B-LoRD [13] one factor V∈
> Rn×r
> +
> linear convex factor constraints + projec-tion Yes No prescribed class prior μ
> No †adjustable block-diagonality regular-ization Low-rank Sinkhorn [18]
> Q, R, g exact transport marginals via KL/Dykstra projections No, in gen-eral Yes learned shared gNo transport-cost geometry SMDSC U, V, g plus sparse anchors exact DS at every accepted iterate via joint KL projection Not forced; optional sym-metriza-tion Yes learned shared gYes stochastic anchor-reduced manifold
> †

LoRD/B-LoRD can exploit a sparse input S computationally, but their Frobenius objective is not an observed-edge-only loss. For SMDSC, W need not be symmetric because the two factors are deliberately allowed to differ. If a symmetric graph is required, 

Ws = ( W + W ⊤)/2 remains nonnegative and doubly stochastic, with rank( Ws) ≤ 2r.

gj ≤ n; therefore F is bounded and is a convex polytope. For g > 0, nonnegativity of W is immediate and 

W 1n = U Diag( g)−1g = 1n,W ⊤1n = V Diag( g)−1g = 1n.

The rank bound rank( W ) ≤ r follows from the factoriza-tion. Averaging W with W ⊤ preserves nonnegativity and both stochastic marginals, and subadditivity of rank gives 

rank( Ws) ≤ 2r.For optimization it is convenient to eliminate g. Define 

C := {(U, V ) : U, V ∈ Rn×r 

> +

, U 1r = V 1r = 1n,U ⊤1n = V ⊤1n}. (7) and extend the shared latent mass smoothly to a neigh-borhood of C by 

¯g(U, V ) := 12

(U ⊤1n + V ⊤1n

) , D¯g := Diag(¯ g). (8) On C, ¯g = U ⊤1n = V ⊤1n.B. Anchor-reduced manifold regularization Following scalable anchor/landmark graph construc-tions [25], [26], let Z ∈ Rn×m 

> +

be a sparse sample-to-anchor matrix with normalized rows, 

Z1m = 1n. (9) Define 

da := Z⊤1n, Da := Diag( da),Sa := ZD −1 

> a

Z⊤, La := In − Sa. (10) where zero-mass anchors are removed so that da > 0. The matrix Sa is never formed explicitly. Proposition 2 (Structure of the anchor-reduced Laplacian). Suppose Z ≥ 0, Z1m = 1n, and da = Z⊤1n > 0. Then 

Sa = S⊤ 

> a

, Sa ≥ 0, Sa  0, Sa1n = 1n. (11) Moreover, 

0  Sa  In, 0  La  In, La1n = 0 . (12) 5

Consequently, for 

ϕ(V ) := μ

2 tr( V ⊤LaV ), (13) we have 

∇ϕ(V ) = μL aV, ‖∇ ϕ(V1)−∇ ϕ(V2)‖F ≤ μ‖V1 −V2‖F .

(14) The anchor action is evaluated without forming an n×n

matrix: 

LaV = V − Z [D−1 

> a

(Z⊤V )] . (15) If each sample connects to at most q anchors, then 

nnz( Z) ≤ nq , so one anchor action costs O(nqr ) operations and O(nq ) additional storage. C. Reduced sparse-fitting objective and gradients Let A = A⊤ ≥ 0 be a sparse affinity matrix with symmetric support Ω. Our main reduced objective is 

Φ( U, V ) := 12

∥∥PΩ

(U D −1¯g V ⊤ − A)∥ ∥2

> F

+ μ

2 tr( V ⊤LaV ), (U, V ) ∈ C .

(16) The factor V is designated as the manifold-aware factor. This does not privilege a particular mathematical side: Proposition 3 shows that regularizing U instead gives the factor-swapped problem. Let 

E := PΩ(W − A), g := ¯ g(U, V ), D := Diag( g),

(17) and define h ∈ Rr componentwise by 

hj := − [U ⊤EV ]jj 

g2

> j

. (18) A direct chain-rule calculation gives 

GU (U, V ) := ∇U Φ( U, V ) = EV D −1 + 12 1nh⊤,GV (U, V ) := ∇V Φ( U, V ) = E⊤U D −1 + μL aV + 12 1nh⊤.

(19) Thus the equal split of the g-derivative is exactly the chain rule induced by (8); it is not an auxiliary heuristic. Proposition 3 (Factor-swap equivalence). Assume A = A⊤

and Ω = Ω ⊤. Define 

ΦV (U, V ) := fΩ

(U D −1 

> g

V ⊤; A) + μ

2 tr( V ⊤LaV ), (20) 

ΦU (U, V ) := fΩ

(U D −1 

> g

V ⊤; A) + μ

2 tr( U ⊤LaU ), (21) where fΩ(W ; A) = 12 ‖P Ω(W − A)‖2 

> F

and (U, V, g ) satisfies (4). Then 

ΦV (U, V ) = Φ U (V, U ). (22) Consequently, the V -regularized and U -regularized prob-lems have the same optimal value, and their feasible stationary points correspond under (U, V ) 7 → (V, U ).Proposition 4 (Symmetric manifold regularization pre-serves the one-factor submanifold). Assume A = A⊤,

Ω = Ω ⊤, and La = L⊤ 

> a

. Consider 

Φsym (U, V ) = fΩ(W (U, V ); A)+ μ

4 tr( U ⊤LaU ) + μ

4 tr( V ⊤LaV ). (23) If an exact mirror-descent iteration is initialized with U0 =

V0, then 

Uk = Vk for all k. (24) Hence the iteration remains on the one-factor submanifold 

Wk = Uk Diag( U ⊤ 

> k

1n)−1U ⊤ 

> k

. (25) D. Exact rank-space KL projection Before specializing to clustering, consider two factors with general prescribed row marginals. Let a ∈ Rn 

> ++

and 

b ∈ Rm 

> ++

satisfy 

1⊤ 

> n

a = 1⊤

> m

b =: M, 

and define 

C(a, b ) = {(U, V ) ≥ 0 : U 1r = a, V 1r = b, U ⊤1n = V ⊤1m}. (26) The clustering feasible set in (7) is the special case m = n

and a = b = 1n.Given positive references ¯U ∈ Rn×r 

> ++

and ¯V ∈ Rm×r 

> ++

,consider 

min   

> (U,V )∈C (a,b )

DKL (U ‖ ¯U ) + DKL (V ‖ ¯V ). (27) For z ∈ Rr , define rowwise 

ui(z) = ai

¯ui ez

¯u⊤ 

> i

ez , vℓ(z) = bℓ

¯vℓ e−z

¯v⊤ 

> ℓ

e−z , (28) and let U (z), V (z) stack these rows. Set 

ψa,b (z) = 

> n

∑

> i=1

ai log(¯ u⊤ 

> i

ez ) + 

> m

∑

> ℓ=1

bℓ log(¯ v⊤ 

> ℓ

e−z ). (29) Theorem 1 (Exact shared-marginal KL projection). Let 

Aa = Diag( a), Bb = Diag( b), cU (z) = U (z)⊤1n, and 

cV (z) = V (z)⊤1m. Then 

∇ψa,b (z) = cU (z) − cV (z), (30) 

∇2ψa,b (z) = Diag( cU ) − U ⊤A−1 

> a

U

+ Diag( cV ) − V ⊤B−1 

> b

V  0. (31) The nullspace of (31) is exactly span {1r } and ψa,b (z +

c1r ) = ψa,b (z). After fixing the gauge zr = 0 , ψa,b is strictly convex and coercive, hence has a unique minimizer 

z⋆. The unique solution of (27) is (U (z⋆), V (z⋆)) . Thus the exact projection has only r − 1 effective unknowns; one Hessian evaluation costs O(( n + m)r2) and the reduced Newton solve costs O(r3). For the clustering case a = b =

1n, (31) reduces to 

Diag( cU ) − U ⊤U + Diag( cV ) − V ⊤V. 6

The dual also admits matrix-free curvature products. For any x ∈ Rr ,

∇2ψa,b (z)x = Diag( cU )x − U ⊤A−1 

> a

(U x )+ Diag( cV )x − V ⊤B−1 

> b

(V x ), (32) which costs O(( n + m)r) without forming the Hessian. Thus large-rank variants can use Newton–CG on the gauge-fixed system, whereas the direct Cholesky route in Theorem 1 is preferable at the small ranks typical of clustering. Proof sketch. The scaled row normalizations in (28) enforce the prescribed row marginals. Differentiating (29) gives (30); the Hessian is a positive weighted sum of row-wise categorical covariance matrices. Strict positivity makes their common nullspace exactly the all-ones direc-tion. Equality of the total row masses gives shift invariance, and gauge fixing removes the only null direction. The remaining KKT equation is cU = cV , precisely the shared-column constraint. Appendix A gives the complete proof. This result is objective-independent: any KL-mirror or Bregman method whose feasible factors have prescribed row marginals and a shared column marginal can use the same rank-space projection. It concerns the complete non-linear KL projection itself, in contrast to cyclic Bregman realization of the balanced retraction and the separate rank-sized Schur system in the tangent-space projector of [20]. In the remainder we specialize to a = b = 1n.E. Feasible mirror-descent algorithm Given a feasible positive pair (Uk, V k) and step size η > 

0, form the exponentiated references 

¯Uk = Uk exp( −ηG kU ), ¯Vk = Vk exp( −ηG kV ), (33) where GkU = GU (Uk, V k) and GkV = GV (Uk, V k). The next candidate is the exact KL projection of ( ¯Uk, ¯Vk) onto C,computed by Theorem 1. Equivalently, 

Tη (U, V ) := arg min    

> (˜U , ˜V)∈C

{

〈GU , ˜U − U 〉F

+ 〈GV , ˜V − V 〉F

+ 1

η DKL ( ˜U ‖U )+ 1

η DKL ( ˜V ‖V )

}

,

(34) where GU = GU (U, V ) and GV = GV (U, V ). Write 

(U +, V +) = Tη (U, V ). A backtracking step is accepted when Φ( U +, V +) ≤ Φ( U, V )+ 〈GU , U + − U 〉F

+ 〈GV , V + − V 〉F

+ 1 − ση DKL (U +‖U )+ 1 − ση DKL (V +‖V ),

(35) where σ ∈ (0 , 1) . The line search starts from a fixed ¯η > 0

at each outer iteration and multiplies it by ρ ∈ (0 , 1) until (35) holds. Lemma 1 (Mirror fixed points characterize first-order stationarity). Let (U, V ) ∈ C ∩ R2nr  

> ++

and η > 0. Then 

Tη (U, V ) = ( U, V ) ⇐⇒ 0 ∈ (GU (U, V ), G V (U, V )) + NC (U, V ).

(36) Equivalently, if (U +, V +) = Tη (U, V ), then 

Dsym (U, V ; U +, V +)

η2 , (37) where 

Dsym := DKL (U +‖U ) + DKL (U ‖U +)+ DKL (V +‖V ) + DKL (V ‖V +).

vanishes if and only if (U, V ) is first-order stationary. Theorem 2 (Feasible descent and mirror-stationarity). Assume: 1) La  0 and ‖La‖2 ≤ 1;2) (U0, V 0) ∈ C ∩ R2nr  

> ++

;3) the accepted iterates have nonvanishing latent masses: there exists g > 0 such that 

min  

> j

¯gj (Uk, V k) ≥ g for all k; (38) 4) every KL projection is solved exactly. Then the backtracking mirror iteration defined by (34)– (35) has the following properties. (i) Finite backtracking and a uniform positive step-size lower bound. There exists ηmin > 0 such that every accepted step satisfies 

0 < η min ≤ ηk ≤ ¯η. (39) (ii) Exact feasibility, positivity, and sufficient decrease. Every finite iterate belongs to C ∩ R2nr  

> ++

. Moreover, 

Φ( Uk, V k) − Φ( Uk+1 , V k+1 )

≥ σηk

DKL (Uk+1 ‖Uk) + σηk

DKL (Vk+1 ‖Vk)+ 1

ηk

DKL (Uk‖Uk+1 ) + 1

ηk

DKL (Vk‖Vk+1 ).

(40) (iii) Asymptotic regularity. The objective values con-verge and 

> ∞

∑

> k=0

(‖Uk+1 − Uk‖2 

> F

+ ‖Vk+1 − Vk‖2

> F

) < ∞. (41) Consequently, 

‖Uk+1 − Uk‖F + ‖Vk+1 − Vk‖F → 0. (42) (iv) Non-asymptotic mirror-stationarity rate. Define 

∆k := 1

η2

> k

[

DKL (Uk+1 ‖Uk) + DKL (Uk‖Uk+1 )+DKL (Vk+1 ‖Vk) + DKL (Vk‖Vk+1 )

]

.

(43) Then, for every N ≥ 1,

min  

> 0≤k<N

∆k ≤ Φ( U0, V 0) − Φinf 

ση min N , (44) where Φinf ≥ 0 is the infimum of Φ over its positive-mass feasible domain. In particular, ∆k → 0.7

Proof sketch: The nonvanishing-mass assumption places every accepted iterate in a compact subset of C on which the reduced gradient is uniformly Lipschitz. Com-paring the mirror subproblem with the current feasible point and using strong convexity of negative entropy shows that sufficiently small trials remain in a slightly larger positive-mass region; the descent lemma then guarantees acceptance, giving finite backtracking and a uniform pos-itive step-size lower bound. Mirror optimality combined with the acceptance test yields the symmetrized-KL de-crease inequality. Summation gives asymptotic regularity and the O(1 /N ) residual bound. The longer technical derivation is omitted for brevity. Corollary 1 (KKT stationarity of positive accumulation points). Under the assumptions of Theorem 2, every accumulation point (U ⋆, V ⋆) whose entries are strictly positive satisfies 

0 ∈ (GU (U ⋆, V ⋆), G V (U ⋆, V ⋆)) + NC (U ⋆, V ⋆). (45) Equivalently, there exist row multipliers α, β ∈ Rn and a shared-marginal multiplier λ ∈ Rr such that 

GU (U ⋆, V ⋆) + α1⊤ 

> r

+ 1nλ⊤ = 0 ,GV (U ⋆, V ⋆) + β1⊤ 

> r

− 1nλ⊤ = 0 , (46) together with the feasibility equations defining C.Remark 1 (Vanishing latent components). The nonvanishing-mass assumption is needed because D−1

> g

becomes singular when gj → 0. The present algorithm does not include a barrier that guarantees a uniform lower bound on every latent mass, so Theorem 2 is explicitly conditional on this property. If a latent mass tends to zero, the corresponding rank-one component carries vanishing total mass in the shared-marginal factorization and can be removed, after which the analysis applies to the active reduced-rank representation. In implementation, min j gj

should therefore be monitored explicitly. Remark 2 (Scope of novelty). Shared-marginal low-rank coupling factorizations and KL mirror descent have an-tecedents in low-rank optimal transport. The present con-tribution should therefore not claim the parameterization itself as new. The paper-specific theoretical contribution is the specialization to sparse doubly stochastic graph fitting with anchor-reduced manifold regularization, the (r − 1) -dimensional exact KL projection characterization, and the resulting feasible Bregman descent and stationarity analysis. F. Practical anchor construction and parameter scaling The theoretical results treat La and μ as fixed. In the implementation, m anchors are selected from the feature space, each sample is connected to a small number q of nearby anchors, and the resulting nonnegative weights are normalized row-wise to obtain Z1 = 1. This gives the operator in (10). Algorithm 1 Shared-Marginal Mirror Descent (SM-MD) Require: Sparse affinity A, anchor matrix Z, rank r,manifold weight μ, ¯η > 0, ρ ∈ (0 , 1) , σ ∈ (0 , 1) . 

> 1:

Initialize U0, V 0 > 0 with (U0, V 0) ∈ C and set g0 =

U ⊤ 

> 0

1n = V ⊤ 

> 0

1n. 

> 2:

for k = 0 , 1, . . . do  

> 3:

Compute Wk only on Ω and form the sparse residual Ek = PΩ(Wk − A). 

> 4:

Compute GkU , G kV from (19), using LaVk = Vk −

Z[D−1 

> a

(Z⊤Vk)] . 

> 5:

Set η ← ¯η. 

> 6:

repeat  

> 7:

¯U ← Uk exp( −ηG kU ), ¯V ← Vk exp( −ηG kV ). 

> 8:

Solve the (r − 1) -dimensional strictly convex dual in Theorem 1.  

> 9:

Obtain the exact feasible projection (U +, V +)

and set g+ = 12 (U +⊤1n + V +⊤1n). 

> 10:

if the Bregman sufficient-decrease test (35) fails then  

> 11:

η ← ρη . 

> 12:

end if  

> 13:

until (35) holds  

> 14:

(Uk+1 , V k+1 , g k+1 ) ← (U +, V +, g +). 

> 15:

if the stationarity measure (43) is below tolerance then  

> 16:

break  

> 17:

end if  

> 18:

end for  

> 19:

Return U, V, g and, when a symmetric graph is re-quired, Ws = ( W + W ⊤)/2.The strength of an anchor manifold should decrease when the anchor graph disagrees with the observed sparse graph. We therefore use the label-free agreement score 

κ(A, S a) = 〈A, PΩ(Sa)〉‖A‖F ‖P Ω(Sa)‖F

, (47) and scale 

μ = c κ (A, S a)2 ffit (U0, V 0)

fman (V0) . (48) The scalar c is selected on a held-out development set and then fixed. This normalization makes c dimensionless and reduces excessive smoothing when local and anchor graphs disagree. We use c = 10 in all reported experiments. KL mirror descent is sensitive to coordinates initialized extremely close to zero. Given a common initial hard partition H, we use an interior initialization 

U0 = V0 = (1 − ε)H + εr 1n1⊤ 

> r

, (49) with ε = 0 .05 selected on held-out development data. The manifold-aware factor V is a soft latent representation rather than a coordinate-aligned one-hot membership matrix. We therefore row-normalize V and apply k-means for the final discretization. This decoder is fixed before the reported comparisons; a direct row-wise argmax was evaluated separately and was substantially less accurate. 8

G. Complexity Let |E| = nnz( A) and let J denote the number of Newton evaluations used by the exact KL projection in one accepted mirror step. The graph is evaluated only on the sparse support Ω. Computing the residual and the products EV and E⊤U costs O(|E|r). The derivative through the shared marginal can be accumulated with r×r

products in O(nr 2) time. With at most q nonzero anchor weights per sample, the manifold action costs O(nqr ).Each Newton evaluation of the projection costs O(nr 2),followed by an O(r3) gauge-fixed solve. Thus one accepted outer iteration has the order 

O(|E|r + nqr + J(nr 2 + r3)) , (50) where the O(nr 2) gradient term is absorbed by the projection term for J ≥ 1. In practice J is small. The working storage, including the sparse input graph and anchor representation, is 

O(|E| + n(r + q)) . (51) Neither W , Sa, nor La is materialized as a dense n × n

matrix. When r is large enough that forming the r ×r Hes-sian is undesirable, (32) supports a matrix-free Newton– CG projection whose curvature products cost O(nr ) in the clustering specialization; we retain direct Cholesky in the reported experiments because the clustering ranks are small. Table III places this cost beside representative DS and shared-marginal alternatives. The comparison reports dominant optimization cost after graph construction. For LoRD/B-LoRD, b is the number of inner projection iterations; for low-rank Sinkhorn, T is the number of LR-Dykstra iterations and d is the rank of a factored cost matrix. Input storage is shown when it determines scalability. The table makes the scalability claim precise rather than absolute. DCD and LoRD/B-LoRD also avoid a dense learned graph and can be very efficient on sparse inputs; SMDSC does not improve their linear-in-n storage order. Its computational distinction is instead that exact two-factor DS feasibility and the anchor manifold are retained without materializing a dense graph, while the additional feasibility work is confined to a rank-sized Newton system. The direct full-graph SDSGC model pays quadratic memory, whereas low-rank Sinkhorn can become linear in sample size only when the transport cost itself admits a low-rank representation. IV. Experiments A. Evaluation protocol The model and optimization settings are fixed through-out the reported experiments. Unless otherwise stated, the numerical values in this section come from a single-thread reference implementation that mirrors the stated formulas. The experiments are intended to assess clustering quality, structural feasibility, ablation behavior, and the opti-mization diagnostics predicted by the theory. Wall-clock values are interpreted only within the common numerical environment used for the corresponding comparison and are not used to claim cross-software speedups. The main paper uses a clean matched protocol. On each trial, methods share the same standardized data, symmetric 10-NN affinity graph, paired random seed, and a common spectral partition mapped into method-compatible initial variables. Method-specific restart ad-vantages are disabled in the main comparison: DCD uses a single matched start, and LoRD/B-LoRD use a single matched feasible start. The matched SDSGC route uses the common sparse support and is therefore labeled SDSGC-Clean rather than presented as the original dense solver. A common decoder and matched iteration/time budgets are used whenever the method output permits them. Method-specific native procedures—including DCD prior-assisted starts, the dense/GLAN SDSGC route, and LoRD/B-LoRD multi-start selection—are retained only as supplementary fidelity checks and are excluded from the main ranking and primary scalability conclusions. The reporting protocol uses paired seeds, the exact shared-marginal Newton projection, and row-normalized 

k-means on V for SMDSC. The main accuracy table uses three paired seeds for the compared methods, while a separate ten-seed analysis evaluates the stability of the fixed SMDSC configuration. The medium bench-mark contains Digits1200, Blobs1500, Anisotropic1500, Varied1500, and Imbalanced1800. The large benchmark contains Blobs5000, Varied8000, and Imbalanced10000. We report adjusted Rand index (ARI), normalized mutual information (NMI), clustering accuracy (ACC), feasibility residuals, and anytime ARI. Clean matched and method-specific native procedures are kept separate so that restart policies and preprocessing choices do not enter the main ranking implicitly. B. Medium-scale comparison Table IV reports the matched comparison for the V -only shared-marginal model with exact Newton KL projection, adaptive anchor weighting, and row-normalized k-means decoding. The proposed method is denoted SMDSC. The comparison uses five datasets and three paired seeds; the separate ten-seed experiment below assesses sensitivity to initialization more directly. C. Ten-seed stability To check whether the fixed configuration depends on a few favorable initializations, we reran SMDSC on the same five medium graphs with ten paired spectral-initialization seeds. Table V reports the resulting ARI mean and stan-dard deviation. The synthetic Blobs and Anisotropic cases are essentially invariant to initialization, and Varied1500 is also highly stable. Imbalanced1800 shows a wider spread, identifying imbalanced graphs as an important stress case. Across all 50 runs, the maximum row-simplex residual is below 5 × 10 −16 and the maximum shared-marginal residual is below 10 −11 .9

TABLE III Dominant optimization cost and storage after graph construction. |E| denotes the number of nonzeros in a sparse sample graph. Costs are per outer iteration unless stated otherwise.                                                                         

> Method Dominant time Feasibility / inner step Working storage Dense learned
> n×nstate? SDSGC [12] O(( r+ 1) n2)spectral update +ALM graph update
> O(n2+nr )Yes DCD [11], [13] O(|E|r)for sparse similarity multiplicative MM; no sep-arate matrix projection
> O(|E|+nr )No LoRD [13] O(|E|r+nrb L)Euclidean/Dykstra projec-tion, bLinner iterations
> O(|E|+nr )No B-LoRD [13] O(|E|r+nrb B)modified projected step,
> bBinner iterations
> O(|E|+nr )No Low-rank Sinkhorn [18]
> O(n2r+T nr )for a general equal-size dense cost; O(ndr +T nr )for rank-dcost LR-Dykstra scal-ing/projection
> O(nr )factors + cost rep-resentation No SMDSC O(|E|r+nqr +J(nr 2+r3)) exact (r−1) -D Newton projection, Jevaluations
> O(|E|+n(r+q)) No

TABLE IV Medium-scale matched results averaged over three paired seeds. 

Method ARI ACC NMI RI SMDSC 0.8308 0.8443 0.9011 0.9493 DS-Sinkhorn 0.7014 0.7251 0.8285 0.9021 DCD 0.5840 0.6193 0.7479 0.8512 SC 0.5658 0.6090 0.7306 0.8438 B-LoRD 0.5462 0.6691 0.6845 0.8352 LoRD 0.5346 0.6515 0.6797 0.8400 SDSGC-Clean 0.2184 0.4234 0.3445 0.6474 

TABLE V Ten-seed stability of SMDSC. 

Dataset ARI mean ARI std. Digits1200 0.6348 0.0055 Blobs1500 1.0000 0.0000 Anisotropic1500 0.9984 0.0006 Varied1500 0.7221 0.0011 Imbalanced1800 0.7128 0.0800 Macro mean 0.8136 –

D. Large-scale anytime behavior Fig. 1 compares ARI under a common time budget in the same numerical environment. SMDSC reaches useful solutions quickly. Its 0–5 s ARI area under the curve (AUC) is 0.7505, compared with 0.7055 for LoRD and 0.5896 for DCD. The practical DS-Sinkhorn variant has the highest AUC (0.7696) and 5 s ARI (0.8186), but it is not the theoretical main algorithm because its post-step scaling can increase the optimized objective. SMDSC in-stead preserves exact feasibility and the sufficient-decrease theorem at every accepted step. Its ARI peaks around the 2 s checkpoint in this experiment and then decreases while the optimization objective continues to decrease, indicat-ing a remaining objective–clustering alignment issue at later iterations rather than a feasibility or descent failure. 0 1 2 3 4 5Time budget (s) 0.45 0.50 0.55 0.60 0.65 0.70 0.75 0.80 Macro ARI 

> LoRD-Clean B-LoRD-Clean DCD-Clean
> SMDSC

Fig. 1. Large-scale anytime ARI on three benchmark graphs, averaged over three paired trials in the same numerical environment. 

E. Anchor versus Nyström reduced geometry The anchor construction is the main method because it satisfies the graph properties in Proposition 2 by construction. Nyström can provide a richer low-rank global approximation and achieves higher mean ARI on several benchmark graphs, but its normalized approximation is more sensitive to landmarks and initialization. In a fo-cused Blobs5000 study with ten initializations, the anchor operator has no ARI values below 0.5, whereas the tested Nyström version fails on four of ten trials. Table VII summarizes the tradeoff. For Blobs5000 over ten initializations, the anchor and 10             

> TABLE VI Large-scale 0–5 s anytime summary.
> Method ARI AUC ARI at 5 s DS-Sinkhorn 0.7696 0.8186 SMDSC 0.7505 0.7516 LoRD 0.7055 0.7811 B-LoRD 0.6554 0.6859 DCD 0.5896 0.6095

Nyström ARI means are 0.7474 and 0.6474, respectively; the corresponding standard deviations are 0.0888 and 0.2039. Nyström has a higher median (0.7526 versus 0.7191) but a lower minimum (0.4259 versus 0.7189), illustrating a higher-ceiling/lower-stability regime. This motivates using anchor geometry in the theoretical main method while retaining Nyström as an extension. F. Which factor should carry the manifold term? For a symmetric affinity and symmetric support, Propo-sition 3 predicts that V -only and U -only regularization are the same problem after exchanging the factor names. The implementation confirms this exactly when the decoder is exchanged accordingly: their paired ARI values coincide to numerical precision. By contrast, the symmetric U + V

manifold model is a genuinely different model. Proposi-tion 4 shows that with U0 = V0 it remains on a one-factor submanifold. G. Projection and stationarity diagnostics The (r − 1) -dimensional SPD Newton solver reproduces the previous root-based KL projection to approximately 

2.4 × 10 −14 in the projected factors, while the shared-marginal residual is around 10 −12 in the validation prob-lem. Fig. 2 plots the stationarity measure in (43). On the Varied1500 diagnostic run, all 60 accepted mirror steps de-crease the original objective, and the DS/shared-marginal constraints remain at numerical precision. These diagnos-tics verify that the implemented line search matches the sufficient-decrease analysis rather than relying on a post-hoc feasibility correction. V. Discussion The comparison in Tables II and III clarifies where SMDSC differs from nearby methods. The advantage is not a uniformly smaller asymptotic order than every low-rank alternative. DCD and LoRD/B-LoRD also avoid a dense learned graph and can be cheaper per iteration. SMDSC instead targets a different combination of prop-erties: two-factor freedom, exact DS feasibility at each accepted iterate, observed-edge sparse fitting, and an explicit reduced manifold term. The additional feasibility cost is confined to a rank-sized Newton system rather than an n × n constrained update. This positioning also identifies when the method is most appropriate. If a symmetric one-factor representation is 0 10 20 30 40 50 60 Mirror iteration 10 4     

> 10 3
> 10 2
> 10 1
> 10 0
> 10 1
> 10 2
> Stationarity measure
> Fig. 2. Mirror-stationarity diagnostic on Varied1500 for the unified
> U, V, g implementation.

sufficient and manifold regularization is unnecessary, DCD or LoRD/B-LoRD may be simpler and computationally preferable. If the full learned graph itself is the object of interest and n2 storage is acceptable, a direct model such as SDSGC preserves that representation explicitly. SMDSC is aimed at the intermediate regime in which the graph is too large to materialize, the two latent roles should not be tied, and exact stochastic balance is still required during optimization rather than only after it. The asymmetric factorization deserves one further clari-fication. The model does not force W to be symmetric, be-cause enforcing U = V would remove the very two-factor flexibility being introduced. For applications requiring a symmetric graph, Ws = ( W + W ⊤)/2 is still nonnegative and doubly stochastic, with rank at most 2r. The theory and the main clustering decoder operate directly on the factor representation, so symmetrization is a downstream option rather than an optimization constraint. The current convergence analysis has two explicit boundaries. First, Theorem 2 assumes that latent masses do not vanish. This is natural because D−1 

> g

becomes singular as some gj → 0; a vanishing component can instead be removed and interpreted as automatic rank reduction. Second, Corollary 1 establishes the Euclidean KKT statement only for strictly positive accumulation points. Mirror stationarity remains valid globally over the positive iterates, while a complete boundary KKT result would require additional normal-cone analysis. These con-ditions should be viewed as the present scope of the theory rather than hidden regularity assumptions. The anchor construction also represents a deliberate tradeoff. Nyström reduction can attain higher clustering accuracy on favorable datasets, but the row-stochastic anchor operator is nonnegative, exactly stochastic, PSD, and admits a simple spectral bound and matrix-free action. The present method therefore chooses the anchor form for structural control and scalability, not because it dominates every low-rank approximation empirically. A natural extension is to design corrected Nyström operators 11 

> TABLE VII Anchor versus Nyström reduced operators under the fixed SMDSC configuration.

Scale Operator ARI NMI State (MB) Row resid. Shared resid. ARI rank Medium Anchor 0.7176 0.8457 0.186 4.2×10 −16 9.2×10 −13 1.80 Medium Nyström 0.7710 0.8375 0.432 4.6×10 −16 8.9×10 −13 1.20 Large Anchor 0.7265 0.8723 0.918 4.4×10 −16 1.0×10 −11 1.67 Large Nyström 0.8408 0.8676 2.029 4.4×10 −16 1.5×10 −11 1.33    

> TABLE VIII Manifold-side ablation. The symmetric scale s= 0 .2was selected on independent development data.

Scale Model Decoder ARI Medium V -only V 0.8314 Medium U -only U 0.8314 Medium symmetric, s = 0 .2 mean 0.7404 Large V -only V 0.7678 Large U -only U 0.7678 Large symmetric, s = 0 .2 mean 0.7151 

that preserve these graph properties while retaining their higher empirical ceiling. Finally, the structural comparison suggests two broader directions. The shared-marginal polytope can support other sparse graph losses beyond squared observed-edge fitting, provided their gradients can be evaluated without forming W . Likewise, the low-dimensional KL projection is independent of the particular anchor regularizer. Robust, multi-view, semi-supervised, or adaptive-rank variants can therefore reuse the same feasibility mechanism while changing the data-fit or geometry term. VI. Conclusion We derived an exact rank-space KL projection for shared-marginal low-rank factors with arbitrary positive row marginals of equal total mass. The complete nonlinear projection reduces to a strictly convex (r − 1) -dimensional dual, with explicit covariance curvature and matrix-free Hessian–vector products. Doubly stochastic clustering is a direct specialization: two nonnegative row-simplex factors share a learned latent mass without being forced to coin-cide, while sparse observed-edge fitting and a stochastic anchor-reduced Laplacian avoid dense graph construction. Embedded in mirror descent, the projection yields feasible iterates, quantitative Bregman descent, an O(1 /N ) mirror-stationarity guarantee, and KKT-stationary positive accu-mulation points under the stated mass condition. The method does not claim to dominate every low-rank DS solver in raw per-iteration cost. Its contribution is the combination of exact DS structure, two-factor flexibility, sparse graph fitting, and reduced manifold geometry at linear-in-n state for fixed rank, graph degree, and an-chor sparsity. The empirical study therefore emphasizes matched structural comparisons and within-environment anytime behavior rather than software-dependent absolute speed claims. Appendix A Proof of the exact shared-marginal KL projection theorem Proof of Theorem 1. Write pi(z) = ui(z)/a i and qℓ(z) = 

vℓ(z)/b ℓ. These are strictly positive probability vectors. The row-marginal identities in (28) are immediate. Differ-entiating (29) gives 

∇ψa,b (z) = ∑

> i

aipi(z) − ∑

> ℓ

bℓqℓ(z) = cU (z) − cV (z).

For a softmax probability vector, ∇pi = Diag( pi) − pip⊤ 

> i

,whereas differentiating qℓ with respect to z gives the negative of the analogous covariance matrix. Hence 

∇2ψa,b = ∑

> i

ai[Diag( pi) − pip⊤ 

> i

]+ ∑

> ℓ

bℓ[Diag( qℓ) − qℓq⊤ 

> ℓ

].

which is exactly (31). For any x ∈ Rr ,

x⊤[Diag( cU ) − U ⊤A−1 

> a

U ]x

= ∑

> i

ai

∑

> j

pij x2 

> j

−

∑

> j

pij xj



> 2

 ≥ 0.

Each summand is a variance under a strictly positive categorical distribution and vanishes iff all coordinates of 

x are equal. The same holds for the V term, so the Hessian nullspace is exactly span {1r }.Let M = 1⊤ 

> n

a = 1⊤

> m

b. Under z 7 → z + c1r , the first sum in (29) increases by M c and the second decreases by M c , proving shift invariance. Fixing zr = 0 therefore removes the only null direction. For existence, let zmax =max j zj and zmin = min j zj . Strict positivity of ¯U , ¯V gives constants CU , C V such that 

ψa,b (z) ≥ M (zmax − zmin ) + CU + CV .

On the gauge-fixed subspace, ‖z‖ → ∞ implies zmax −

zmin → ∞ , so ψa,b is coercive and has a unique minimizer 

z⋆.The feasible set of (27) contains a strictly positive point, for example Uij = ai/r and Vℓj = bℓ/r , whose shared column mass equals (M/r )1r . The unique KL minimizer is also strictly positive: if an entry were zero, moving toward this strictly positive feasible point would give one-sided directional derivative −∞ for that scalar KL term while all 12 

positive-coordinate derivatives remain finite, contradicting optimality. Therefore logarithmic KKT equations are valid. With row multipliers α ∈ Rn, β ∈ Rm, and shared-column multiplier λ ∈ Rr ,

log( U/ ¯U ) + α1⊤ 

> r

+ 1nλ⊤ = 0 ,

log( V / ¯V ) + β1⊤ 

> r

− 1mλ⊤ = 0 .

Setting z = −λ, the prescribed row constraints recover exactly (28). The remaining KKT condition is cU (z) = 

cV (z), equivalently ∇ψa,b (z) = 0 . Strict convexity of the primal KL objective gives uniqueness. References 

[1] J. Shi and J. Malik, “Normalized cuts and image segmentation,” IEEE Trans. Pattern Anal. Mach. Intell., vol. 22, no. 8, pp. 888– 905, 2000. [2] A. Y. Ng, M. I. Jordan, and Y. Weiss, “On spectral clustering: Analysis and an algorithm,” in Advances in Neural Information Processing Systems, vol. 14, 2001. [3] U. von Luxburg, “A tutorial on spectral clustering,” Statist. Comput., vol. 17, no. 4, pp. 395–416, 2007. [4] R. Sinkhorn, “A relationship between arbitrary positive matri-ces and doubly stochastic matrices,” Ann. Math. Statist., vol. 35, no. 2, pp. 876–879, 1964. [5] R. Sinkhorn and P. Knopp, “Concerning nonnegative matrices and doubly stochastic matrices,” Pacific J. Math., vol. 21, no. 2, pp. 343–348, 1967. [6] P. A. Knight, “The Sinkhorn–Knopp algorithm: Convergence and applications,” SIAM J. Matrix Anal. Appl., vol. 30, no. 1, pp. 261–275, 2008. [7] R. Zass and A. Shashua, “Doubly stochastic normalization for spectral clustering,” in Advances in Neural Information Processing Systems, vol. 19, pp. 1569–1576, 2006. [8] C. Ding, X. He, and H. D. Simon, “On the equivalence of nonnegative matrix factorization and spectral clustering,” in Proc. SIAM Int. Conf. Data Mining, pp. 606–610, 2005. [9] D. Kuang, C. Ding, and H. Park, “Symmetric nonnegative matrix factorization for graph clustering,” in Proc. SIAM Int. Conf. Data Mining, pp. 106–117, 2012. [10] X. Wang, F. Nie, and H. Huang, “Structured doubly stochas-tic matrix for graph based clustering,” in Proc. 22nd ACM SIGKDD Int. Conf. Knowl. Discovery Data Mining, pp. 1245– 1254, 2016. [11] Z. Yang, J. Corander, and E. Oja, “Low-rank doubly stochastic matrix decomposition for cluster analysis,” J. Mach. Learn. Res., vol. 17, no. 187, pp. 1–25, 2016. [12] N. Wang, Z. Cui, A. Li, Y. Lu, R. Wang, and F. Nie, “Structured doubly stochastic graph-based clustering,” IEEE Trans. Neural Netw. Learn. Syst., vol. 36, no. 6, pp. 11064–11077, 2025. [13] W. Lyu, Y. Jia, H. Liu, and J. Hou, “Graph-based clustering revisited: A relaxation of kernel k-means perspective,” J. Mach. Learn. Res., vol. 27, no. 131, pp. 1–44, 2026. [14] M. Cuturi, “Sinkhorn distances: Lightspeed computation of op-timal transport,” in Advances in Neural Information Processing Systems, vol. 26, pp. 2292–2300, 2013. [15] J.-D. Benamou, G. Carlier, M. Cuturi, L. Nenna, and G. Peyré, “Iterative Bregman projections for regularized transportation problems,” SIAM J. Sci. Comput., vol. 37, no. 2, pp. A1111– A1138, 2015. [16] G. Peyré and M. Cuturi, “Computational optimal transport,” Found. Trends Mach. Learn., vol. 11, nos. 5–6, pp. 355–602, 2019. [17] A. Forrow, J.-C. Hütter, M. Nitzan, P. Rigollet, G. Schiebinger, and J. Weed, “Statistical optimal transport via factored cou-plings,” in Proc. 22nd Int. Conf. Artif. Intell. Statist., PMLR, vol. 89, pp. 2454–2465, 2019. [18] M. Scetbon, M. Cuturi, and G. Peyré, “Low-rank Sinkhorn factorization,” in Proc. 38th Int. Conf. Mach. Learn., PMLR, vol. 139, pp. 9344–9354, 2021. [19] P. Halmos, X. Liu, J. Gold, and B. J. Raphael, “Low-rank opti-mal transport through factor relaxation with latent coupling,” in Advances in Neural Information Processing Systems, vol. 37, 2024. [20] P. Jawanpuria and B. Mishra, “A Riemannian approach to low-rank optimal transport,” arXiv preprint arXiv:2606.12120, 2026. [21] A. Beck and M. Teboulle, “Mirror descent and nonlinear pro-jected subgradient methods for convex optimization,” Oper. Res. Lett., vol. 31, no. 3, pp. 167–175, 2003. [22] H. H. Bauschke, J. Bolte, and M. Teboulle, “A descent lemma beyond Lipschitz gradient continuity: First-order methods re-visited and applications,” Math. Oper. Res., vol. 42, no. 2, pp. 330–348, 2017. [23] H. Lu, R. M. Freund, and Y. Nesterov, “Relatively smooth convex optimization by first-order methods, and applications,” SIAM J. Optim., vol. 28, no. 1, pp. 333–354, 2018. [24] R. Mazumder, T. Hastie, and R. Tibshirani, “Spectral regular-ization algorithms for learning large incomplete matrices,” J. Mach. Learn. Res., vol. 11, no. 80, pp. 2287–2322, 2010. [25] W. Liu, J. He, and S.-F. Chang, “Large graph construction for scalable semi-supervised learning,” in Proc. 27th Int. Conf. Mach. Learn., pp. 679–686, 2010. [26] X. Chen and D. Cai, “Large scale spectral clustering with landmark-based representation,” in Proc. AAAI Conf. Artif. Intell., vol. 25, no. 1, pp. 313–318, 2011. [27] C. K. I. Williams and M. Seeger, “Using the Nyström method to speed up kernel machines,” in Advances in Neural Information Processing Systems, vol. 13, pp. 682–688, 2001. [28] C. Fowlkes, S. Belongie, F. Chung, and J. Malik, “Spectral grouping using the Nyström method,” IEEE Trans. Pattern Anal. Mach. Intell., vol. 26, no. 2, pp. 214–225, 2004.