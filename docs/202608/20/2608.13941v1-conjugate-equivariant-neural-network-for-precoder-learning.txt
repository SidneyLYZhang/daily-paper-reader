Title: Conjugate Equivariant Neural Network for Precoder Learning

URL Source: https://arxiv.org/pdf/2608.13941v1

Published Time: Mon, 17 Aug 2026 00:23:53 GMT

Number of Pages: 5

Markdown Content:
> 1

# Conjugate Equivariant Neural Network for Precoder Learning 

Shiyong Chen, Student Member, IEEE, Mingyu Deng, Student Member, IEEE, and Shengqian Han, Senior Member, IEEE 

Abstract —Exploiting mathematical properties of wireless poli-cies in deep neural network (DNN) design can improve learning performance and generalizability while reducing training com-plexity. Permutation equivariance and permutation invariance have been incorporated into DNN architectures. In this paper, we investigate conjugation equivariance (CE) and propose a general conjugation-equivariant neural network (CENN) framework for precoder learning. We first establish that the optimal policies for a unified class of precoding problems satisfy CE, i.e., when the channel matrices are conjugated, the conjugate of an optimal precoder remains optimal. We then show that, for DNNs with linear processing functions, enforcing CE restricts their ability to learn optimal precoding policies. To overcome this limitation, we develop a general nonlinear construction and prove that it converts an arbitrary base function into a CE processing function while preserving the base function’s original equivariance properties. This construction enables ex-isting equivariant networks to incorporate CE without adding learnable parameters. Simulations for fully digital and RIS-aided precoding show that the resulting CE-enhanced networks improve learning and generalization performance while requiring fewer training samples and shorter training time than their original counterparts. 

Index Terms —Precoding, deep learning, conjugation equivari-anc, equivariant neural network. 

I. I NTRODUCTION 

# PRECODER optimization is essential for improving the spectral efficiency (SE) of wireless communication sys-tems. Conventional numerical optimization methods, including the weighted minimum mean square error (WMMSE) algo-rithm [1] and block coordinate descent (BCD) methods [2], require iterative updates and therefore incur substantial online computational cost. Deep learning offers an alternative: once trained offline, a deep neural network (DNN) can predict precoders at low online computational cost. Nevertheless, conventional DNNs often incur substantial training complex-ity, including large number of learnable parameters, high sample requirements, and long training times. Moreover, their limited generalizability may also necessitate retraining when the system configuration changes [3]. Incorporating mathematical properties of a precoding policy into the DNN architecture as inductive biases can improve learning performance and generalizability while reducing training complexity [4]. Existing studies have introduced such biases by exploiting the equivariance and invariance properties of wireless policies [5–8]. In [5], permutation equivariance (PE) was incorporated into a graph neural network (GNN) for learning fully digital beamforming. For hybrid precoding, a multidimensional GNN was designed in [6] to exploits multi-dimensional PE properties. In addition, permutation invariance (PI) was incorporated into GNNs for power allocation in [8]. By exploiting these mathematical properties, the resulting networks improve learning and generalization performance while reducing training complexity.  

> The authors are with the School of Electronics and Information Engineering, Beihang University, Beijing 100191, China (Email: {shiyongchen, mingyu-deng, sqhan }@buaa.edu.cn).

Beyond PE and PI, recent studies have investigated unitary equivariance (UE) and torus equivariance (TE) in precoder learning [9, 10]. The UE property means that applying a unitary transformation to the channel vectors induces the same transformation on the corresponding optimal precoding vectors. Since permutation matrices form a subset of unitary matrices, UE provides a stronger equivariance property than PE. Accordingly, exploiting UE in addition to PE can further reduce training complexity and improve generalizability. The TE property arises in reconfigurable intelligent surfac (RIS)-aided precoding and means that independently rotating the channel associated with each reflecting element induces a corresponding phase rotation of the optimal RIS precoder. By jointly exploiting UE, TE, and PE, a unitary-, torus-, and permutation-equivariant neural network was developed in [10]. Although various equivariance and invariance properties have been incorporated into the DNN design, one property exhibited in precoding policies has been overlooked: conjuga-tion equivariance (CE). The CE property means that when the channel matrices are conjugated, the conjugates of the corresponding optimal precoding matrices remain optimal. To the best of our knowledge, CE has not been explicitly identified for precoder learning or jointly exploited with PE, UE, and TE in existing DNNs. In this paper, we develop a general conjugation-equivariant neural network (CENN) framework for precoder learning. We first establish that the optimal policies for a unified class of precoding problems satisfy CE. We then show that, for DNNs with linear processing functions, enforcing CE restricts the weights and biases to real values and prevents cross-coupling between real and imaginary components, thereby limiting the DNNs’ ability to learn optimal precoding policies. To overcome this limitation, we develop a general nonlinear construction and prove that it converts an arbitrary base func-tion into a CE processing function while preserving the base function’s original equivariance properties. This construction enables existing equivariant networks to incorporate CE with-out adding learnable parameters. Simulations for fully digital and RIS-aided precoding show that the resulting CE-enhanced networks improve learning and generalization performance while requiring fewer training samples and shorter training time than their original counterparts. II. S YSTEM MODEL 

Consider a downlink system where a base station (BS) with Nt antennas serves K single-antenna users. Let H and 

W denote the collections of channel and precoding matrices, respectively. The sum-rate maximization problem under the system constraints can be uniformly formulated as 

max 

> W
> K

X

> k=1

log 2



1 + |fk,k (H, W)|2

PKj=1 ,j ̸ =k |fk,j (H, W)|2 + δ2



(1a) 

s.t. |φm(W)|2 ≤ Im, m = 1 , . . . , M, (1b) arXiv:2608.13941v1 [eess.SP] 14 Aug 2026 2

|ηn(W)|2 = En, n = 1 , . . . , N. (1c) where fk,j (H, W) denotes a desired complex received signal or interference term, φm(·) and ηn(·) define the inequality and equality constraints on precoders, respectively, and δ2 is the noise power. In typical precoding problems, the functions fk,j (H, W),

φm(W) and ηn(W) satisfy the following CE property: con-jugating their inputs conjugates the corresponding outputs, 

fk,j (H∗, W∗) = fk,j (H, W)∗, (2a) 

φm(W∗) = φm(W)∗, (2b) 

ηn(W∗) = ηn(W)∗. (2c) The following representative precoding problems instantiate the unified formulation, and their associated functions satisfy the CE relations in (2).  

> •

Fully digital precoding: H = H and W = W, where 

H = [ h1, . . . , hK ] ∈ CNt×K and W = [ w1, . . . , wK ] ∈

CNt×K denotes the channel and precoding matrices from the BS to the users, respectively. The desired signal and interference terms are fk,j (H, W) = hH 

> k

wj . Let 

Pmax denote the maximum transmit power. The power constraint follows by setting φ1(W) = vec( W) and 

I1 = Pmax . 

> •

RIS-aided precoding: H = ( H, A1, . . . , AK ) and W =(W, we), where Ak ∈ CNt×Ne denotes the equivalent channel matrix from the BS to the k-th user vis the RIS, 

we ∈ CNe×1 is the RIS precoder, and Ne is the number of reflecting elements. The desired signal or interference term is fk,j (H, W) = ( hd,k + Akw∗ 

> e

)H wj . The transmit power constraint follows by setting φ1(W) = vec( W)

and I1 = Pmax , whereas the RIS unit-modulus constraint follows by setting ηm(W) = [ we]m and em = 1 .III. CE P ROPERTY AND DESIGN OF CENN In this section, we first establish the CE property of the precoding policy. We then present a general construction that converts existing equivariant networks into CENNs. 

A. CE Property of the Precoding Policy 

The optimal precoding policy can be viewed as a mapping from the channel matrices H to the optimal precoding matrices 

ˆW, expressed as ˆW = F(H). The following proposition establishes that the optimal precoding policy satisfies the CE property. 

Proposition 1. If ˆW be an optimal solution to problem (1) 

for the channel matrices H, the corresponding conjugated precoder ˆW∗ is also an optimal solution to problem (1) for conjugated input ˆH∗, as expressed by 

ˆW∗ = F( ˆH∗). (3) 

Proof: See Appendix A. Proposition 1 motivates the design of CENN whose input-output mapping preserves the CE property. 

B. Design of CENN 

The equivariant property of DNN can be ensured by design-ing equivariant layers that preserve the desired equivariance property [11]. Thus, the design of CENN can be reduced to design each layer of it to satisfy the CE property. Let d ∈ CC×1 and d′ ∈ CC′×1 denote the input and output of one layer in CENN, where C and C′ are their hidden representation dimension. The update equation from d to d′

can be expressed as [9] 

d′ = σ ψ(d), (4) where ψ(·) is processing function with learnable parameters, and σ(·) is an complex-valued activation function. The layer (4) satisfying the CE property indicates that the following condition must hold: if the input d is conjugated as d∗, then the output is conjugated as (d′)∗, which is expressed as 

σ(ψ(d)) ∗ = σ ψ(d∗). (5) This equivariance condition require both the processing function ψ(·) and the activation function σ(·) are conjugation equivariant [11]. In the following, we first study the design of 

ψ(·), and then chose a conjugate equivariant σ(·).

1) Design of Linear Processing Function ψ(·): The linear processing function ψ(·) is defined as [9] 

ψ(d) = Wd + b (6) where W ∈ CC′×C and b ∈ CC′×1 denote the learnable weight matrix and bias, respectively. To preserve the CE property, the linear processing function in (6) should satisfy 

ψ(d∗) = ψ(d)∗. (7) Substituting (6) into (7) gives 

Wd ∗ + b = W∗d∗ + b∗. (8) Since (8) must hold for arbitrary d∗, the learnable parameters must satisfy 

W = W∗, b = b∗. (9) Therefore, W and b are restricted to be real-valued. When W and b are real-valued, the output z = ψ(d) in (6) can be decomposed as 

Re {z} = WRe {d} + b, (10a) 

Im {z} = WIm {d}. (10b) 

Remark 1. Equation (10) shows that the real and imag-inary parts of the output are independently determined by the corresponding components of the input, with no cross coupling between them. This decoupled structure is restrictive for precoder learning, because the real and imaginary parts of the optimal precoder depend on both the real and imaginary parts of the channel matrix. For example, in fully digital pre-coding, the optimal structure is related to (HH H +νINt )−1H,where ν is a Lagrange multiplier associated with the power constraint [12]. 

Thus, although linear processing functions are widely used for precoder learning, the real-valued parameter constraints re-quired by CE prevent these layers from capturing the coupling between the real and imaginary components. 3

2) Design of Nonlinear Processing Functions: To over-come the limitation of linear processing, nonlinear processing functions are needed to capture the coupling between the real and imaginary components. For linear processing functions, CE can be enforced by directly constraining the learnable weights W and b due to their fixed affine form in (6). In contrast, for general nonlinear processing functions, direct parameter constraints are less tractable. We therefore provide a general function-level construction that guarantees CE. 

Proposition 2. A function ψ(·) satisfies the CE property, i.e., 

ψ(d∗) = ψ(d)∗, (11) 

if and only if there exists a base function ϕ(·) such that 

ψ(d) = 12 [ϕ(d) + ϕ(d∗)∗] . (12) 

Proof: See Appendix B. Proposition 2 provides a general way that convert an arbitrary base function ϕ(·) into a CE processing function though (12). Consequently, existing nonlinear processing func-tions, including those in [9, 10, 13, 14], can serve as ϕ(·) to construct nonlinear CE processing functions. Existing nonlinear processing functions often preserve addi-tional equivariance properties, including PE, UE, and TE. The following proposition shows that applying the construction in Proposition 2 preserves the function’s original equivariance property while additionally enforcing CE. 

Proposition 3. Suppose that ϕ(·) satisfies the equivariance property 

ϕ Πd  = Πϕ(d), (13) 

where Π ∈ CC′×C′

is a valid transformation matrix. Assume that the conjugated matrices Π∗ is also valid transformation matrix of the same type. Then the function ψ(·) defined in (12) 

satisfies both CE and the equivariance property in (13) , i.e., 

ψ Πd ∗ = Πψ(d)∗. (14) 

Proof: See Appendix C. Based on Proposition 3, processing functions from existing equivariant networks can serve as the base function ϕ(·)

in (12). The resulting CE-enhanced processing functions retain their original equivariance properties while also satisfying CE 

3) Design of Activation Function σ(·): To ensure that each layer is conjugation equivariant, the activation function should satisfy 

σ(x∗) = σ(x)∗, ∀x ∈ C. (15) We therefore adopt the following activation function [9]: 

σ(x) = x

1 + |x| . (16) It is straightforward to verify that this activation function satisfies the CE property and compatible with PE, UE, and TE property. Finally, combining the proposed conjugation-equivariant processing function in (12) and activation functions in (16) yields the CENN layer update in (4). IV. S IMULATION RESULTS 

In this section, we evaluate the proposed CE construction for learning precoding policies. Specifically, we apply the CE construction to existing nonlinear processing functions and compare the resulting CE-enhanced CENNs with their original non-CE counterparts. In simulations, we use 100 ,000 chan-nel samples for training and another 1,000 channel samples for testing. 

A. Learning Fully Digital Precoder 

In this subsection, we evaluate the proposed CE construction for learning the fully digital precoding policy by comparing each baseline network with its CE-enhanced variants .  

> •

WMMSE : An iterative algorithm for sum-rate maximiza-tion in multiuser precoding systems [1], which can find at least a local optimal precoder.  

> •

UPNN : An equivariant neural network proposed in [9] that employs a nonlinear processing function satisfying both UE and PE.  

> •

MGNN : A model-based GNN proposed in [13] with a nonlinear processing function that follows a Taylor expansion of the matrix pseudoinverse and satisfies PE.  

> •

GAT : An attention-based GNN proposed in [14] that employs a nonlinear processing function designed to satisfy PE and incorporate an attention mechanism.  

> •

CUPNN, CMGNN, and CGAT : These are the CE-enhanced variants of UPNN, MGNN, and GAT, respec-tively. Each uses the corresponding processing function as the base function ϕ(·) in (12), thereby retaining its original equivariance properties while also satisfying CE. All learning-based methods are trained in an unsupervised manner using the negative of the objective function in (1a) as the loss, where fk,j (H, W) = hH 

> k

wj . The numbers of users, numbers of BS antennas, and the signal-to-noise ratio (SNR) are set to K = 16 , Nt = 32 , and 10 dB, respectively. Fig. 1 evaluates learning performance under different num-bers of training samples, where the performance metric is defined as the ratio of the sum rate achieved by each learning-based method to that achieved by WMMSE. Comparing UPNN, MGNN, and GAT with their CE-enhanced variants shows that enforcing CE consistently improves performance. The gain of CMGNN over MGNN is particularly pronounced when training samples are limited. CUPNN achieves the highest performance across all training-set sizes, which can be attributed to its joint exploitation of CE, PE, and UE. Table I evaluates generalization across different numbers of users and antennas. All methods are trained with K = 16 and 

Nt = 32 and then tested at different system sizes. Comparing UPNN, MGNN, and GAT with their CE-enhanced variants shows that enforcing CE generally improves generalization with respect to both the antenna dimension and the user dimension. The gain of CUPNN over UPNN is particularly pronounced when generalizing to systems with more antennas and users. Table II compares the inference latency and training com-plexity of the considered methods. The training complexity is measured by the number of samples, training time, and number of model parameters required to reach 90% of the sum rate 44 8 16 64 128 640 1280 40 60 80 100 

Fig. 1: Learning performance under different numbers of training samples. 

TABLE I: Generalization Performance (%)                                 

> (K, N t)(2 ,32) (32 ,32) (16 ,16) (16 ,48)
> UPNN 97.96 89.41 73.71 86.64
> CUPNN 99.87 95.15 82.42 94.84
> MGNN 64.77 68.58 41.45 86.15
> CMGNN 74.38 70.65 47.4 94.84
> GAT 67.39 38.20 11.22 28.64
> CGAT 71.76 42.56 24.83 29.05

attained by WMMSE. CUPNN requires the fewest samples, the shortest training time, and the lowest space complexity, which can be attributed to its joint exploitation of CE, UE, and PE. The CE-enhanced variants incur slightly higher infer-ence latency because of the additional computation in (12), but they reduce the sample and time complexities without increasing the space complexity. As neither GAT nor CGAT reaches the target performance, their training complexities are not reported. 

TABLE II: Inference Time and Training Complexity                              

> Name Inference time Training Complexity Sample Time Space
> UPNN 2.65 ms 43.32 s 2.73 K
> CUPNN 2.79 ms 22.67 s 2.73 K
> MGNN 2.42 ms 1.35 K 36.34 s 51.07 K
> CMGNN 2.61 ms 950 23.89 s 51.07 K
> GTA 1.57 ms –––
> CGTA 1.72ms –––
> Note: “K” and “M” represent thousand and million, respectively.

B. Learning RIS-Aided Precoding 

In this subsection, we evaluate the proposed CE construction for RIS-aided precoder learning by comparing each baseline network with its CE-enhanced variant.  

> •

BCD : A near-optimal numerical baseline that jointly optimizes precoders W and we using BCD methods [2].  

> •

UTPNN : An equivariant DNN proposed in [10] that uses a nonlinear processing function to jointly preserve unitary, torus, and permutation equivariance.  

> •

PNN : A GNN proposed in [7] that uses a nonlinear processing function only satisfying PE.  

> •

CUTPNN and CPNN : These are the CE-enhanced vari-ants of UTPNN and PNN, respectively. Each uses the corresponding processing function as the base function 

ϕ(·) in (12), thereby retaining its original equivariance properties while also satisfying CE All learning-based methods are trained in an unsupervised manner using the negative of the objective function in (1a) as the loss, where fk,j (H, W) = ( hd,k + Akw∗ 

> e

)H wj . The number of users, number of BS antennas, number of reflecting elements, and SNR are set to K = 8 , Nt = 16 , Ne = 100 ,and −5 dB respectively. Fig. 2 evaluates learning performance across different num-bers of training samples. The performance metric is defined as the ratio of the sum rate achieved by each learning-based method to that achieved by BCD. Comparing UTPNN and PNN with their CE-enhanced variants shows that enforcing CE consistently improves performance. CUTPNN achieves the highest performance across all training-set sizes, which can be attributed to its joint exploitation of CE, PE, UE, and TE. 64 128 640 1280 6400 12800 60 70 80 90 100 

Fig. 2: Learning performance under different numbers of training samples. Table III evaluates generalization across different numbers of users, BS antennas, and reflecting elements. All methods are trained with K = 8 , Nt = 16 and Ne = 100 , and then tested at different system sizes. Comparing UTPNN and PNN with their CE-enhanced variants shows that enforcing CE generally improves generalization along all three dimensions. 

TABLE III: Generalization Performance (%)                                         

> (K, N t, N e)UTPNN CUTPNN PNN CPNN
> (4 ,16 ,100) 86.72 92.16 73.85 77.78
> (12 ,16 ,100) 94.43 97.52 78.51 81.28
> (8 ,8,100) 96.18 97.09 87.52 91.06
> (8 ,24 ,100) 95.96 96.78 85.66 88.90
> (8 ,16 ,80) 96.46 98.40 85.96 89.12
> (8 ,16 ,120) 97.23 98.95 87.92 90.69

Table IV compares the inference latency and training com-plexity of the considered methods. The training complexity is measured by the number of training samples, training time, and number of model parameters required to reach 90% of the sum rate attained by BCD. CUTPNN requires the fewest samples, the shortest training time, and the lowest space 5

complexity, which can be attributed to its joint exploitation of CE, UE, TE, and PE. The CE-enhanced variants incur slightly higher inference latency because of the additional computation in (12), but they reduce the sample and time complexities without increasing the space complexity. 

TABLE IV: Inference Time and Training Complexity 

Name Inference time Training Complexity Sample Time Space 

UTPNN 6.17 ms 125 19.64 s 75.94 K 

CUTPNN 6.64 ms 85 14.81 s 75.94 K 

PNN 7.64 ms >12.8 K 1.52 h 4.56 M 

CPNN 10.73 ms 9.4 K 0.45 h 4.56 M 

V. C ONCLUSIONS 

This paper investigated CE in precoding policies and devel-oped a general CENN framework. We showed that directly enforcing CE on DNNs with linear processing functions restricts their ability to learn optimal precoding policies. To overcome this limitation, we developed a general nonlinear construction that converts an arbitrary base function into a CE processing function while preserving its original equivariance properties. Applying this construction to existing equivariant networks yielded CE-enhanced DNNs without introducing additional learnable parameters. Simulations demonstrated that these DNNs outperform their original counterparts in learning and generalization performance while requiring fewer training samples and shorter training time. APPENDIX APROOF OF PROPOSITION 1We prove the optimality by contradiction. Suppose that for problem (1), ˆW is an optimal solution to the channel matrix ˆH,whereas ˆW∗ is not optimal to the conjugated channel matrix 

ˆH∗. Then there exists a feasible precoder eV such that 

Rsum ( ˆH∗, fW) > R sum ( ˆH∗, ˆW∗), (A.1) where Rsum (H, W) denotes the objective function in (1a). Based on the CE property (2), we can find that Rsum (H, W)

is invariant under the joint conjugation of the channel and precoder. By conjugating the input of the left- and right-hand terms in (A.1), they can be equivalently rewritten as 

Rsum ( ˆH∗, fW) = Rsum ( ˆH, fW∗), (A.2a) 

Rsum ( ˆH∗, ˆW∗) = Rsum ( ˆH, ˆW). (A.2b) Combining (A.1) and (A.2) yields 

Rsum ( ˆH, fW∗) > R sum ( ˆH, ˆW), (A.3) which contradicts the optimality of ˆW for ˆH. Hence, ˆW∗ is an optimal for ˆH∗, which completes the proof. APPENDIX BPROOF OF PROPOSITION 2We first prove sufficiency. For any ϕ(·), define ψ(·) as in (12). Then 

ψ(d∗) = 12 [ϕ(d∗) + ϕ(d)∗] = ψ(d)∗, (B.1) which shows that ψ(·) is CE. We then prove necessity. Suppose that ψ(·) is CE and choose ϕ(·) = ψ(·). Then 

12 [ϕ(d) + ϕ(d∗)∗] = 12 [ψ(d) + ψ(d∗)∗] = ψ(d), (B.2) where the last equality follows from ψ(d∗)∗ = ψ(d). Hence, (12) holds for some ϕ(·). This completes the proof. APPENDIX CPROOF OF PROPOSITION 3Since Π∗ is also a valid transformation matrix of the same type, ϕ(·) satisfies 

ϕ(Π∗d) = Π∗ϕ(d). (C.1) Using (12), we have 

ψ(Πd ∗) = 12 [ϕ(Πd ∗) + ϕ(Π∗d)∗] (C.2) 

= 12

h

Πϕ(d∗) +  Π∗ϕ(d)∗i

(C.3) 

= Π 12 [ϕ(d∗) + ϕ(d)∗] = Πψ(d)∗, (C.4) where the second equality follows from (13) and (C.1). This proves (14) and completes the proof. REFERENCES [1] Q. Shi, M. Razaviyayn, Z.-Q. Luo, and C. He, “An iteratively weighted MMSE approach to distributed sum-utility maximization for a MIMO interfering broadcast channel,” IEEE Trans. Signal Process. , vol. 59, no. 9, pp. 4331–4340, Sep. 2011. [2] H. Guo, Y.-C. Liang, J. Chen, and E. G. Larsson, “Weighted sum-rate maximization for reconfigurable intelligent surface aided wireless networks,” IEEE Trans. Wireless Commun. , vol. 19, no. 5, pp. 3064– 3076, May 2020. [3] H. Sun, X. Chen, Q. Shi, M. Hong, X. Fu, and N. D. Sidiropoulos, “Learning to optimize: Training deep neural networks for interference management,” IEEE Trans. Signal Process. , vol. 66, no. 20, pp. 5438– 5453, Sep. 2018. [4] S. Ravanbakhsh, J. Schneider, and B. P´ oczos, “Equivariance through parameter-sharing,” in Proc. ICML , 2017. [5] B. Zhao, J. Guo, and C. Yang, “Understanding the performance of learn-ing precoding policies with graph and convolutional neural networks,” 

IEEE Trans. Commun. , vol. 72, no. 9, pp. 5657–5673, Sep. 2024. [6] S. Liu, J. Guo, and C. Yang, “Multidimensional graph neural networks for wireless communications,” IEEE Trans. Wireless Commun. , vol. 23, no. 4, pp. 3057–3073, Aug. 2024. [7] B. Zhao and C. Yang, “Learning beamforming for RIS-aided systems with permutation equivariant graph neural networks,” in Proc. IEEE VTC-Spring , 2023. [8] S. Chen, Y. Dai, and S. Han, “Learn to optimize resource allocation under QoS constraint of AR,” in Proc. IEEE GLOBECOM , 2025. [9] Y. Ge, S. Liao, S. Han, and C. Yang, “Precoder learning by leveraging unitary equivariance property,” in Proc. IEEE GLOBECOM , 2025. [10] M. Deng and S. Han, “Precoder learning in RIS-aided systems by leveraging torus equivariance property,” in Proc. IEEE GLOBECOM ,2026. [11] L.-H. Lim and B. J. Nelson, “What is an equivariant neural network?” 

arXiv:2205.07362 , 2022. [12] E. Bj¨ ornson, M. Bengtsson, and B. Ottersten, “Optimal multiuser trans-mit beamforming: A difficult problem with a simple solution structure,” 

IEEE Signal Process. Mag. , vol. 31, no. 4, pp. 142–148, Jul. 2014. [13] J. Guo and C. Yang, “A model-based GNN for learning precoding,” 

IEEE Trans. Wireless Commun. , vol. 23, no. 7, pp. 6983–6999, Dec. 2024. [14] ——, “A size-generalizable GNN for learning precoding,” in Proc. IEEE VTC-Fall , 2023.