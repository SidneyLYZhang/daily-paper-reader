---
title: Mitigating the Curse of Dimensionality in Uniform Convergence of Deep Neural Networks via Smooth Activations
title_zh: 通过平滑激活函数缓解深度神经网络一致收敛中的维度灾难
authors: "Yizhe Ding, Runze Li, Jia Liu, Lingzhou Xue"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05599v1"
tags: ["query:ml-theory"]
score: 9.0
evidence: 平滑激活DNN的非渐近界，解决非凸优化中的收敛问题
tldr: 标准ReLU深度网络在L2范数下达到最优，但在均匀收敛中遭受维度诅咒。本文研究光滑激活深度网络（前馈和残差结构），建立伪维数界、逼近保证和Hölder范数界，推导出Huber、最小二乘、分位数和逻辑回归中非渐近均匀收敛率。证明光滑DNN能自适应利用目标函数的低维层次组合结构，有效缓解维度诅咒。仿真和真实数据验证了其作为ReLU网络替代的理论可靠性和实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05599v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1370, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05599v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1631, \"height\": 331, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05599v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 964, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05599v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1702, \"height\": 963, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05599v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1708, \"height\": 1650, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05599v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1713, \"height\": 726, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05599v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1712, \"height\": 728, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05599v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1698, \"height\": 453, \"label\": \"Table\"}]"
motivation: ReLU网络在均匀收敛中存在维度诅咒，需寻求可在最坏情况下保证收敛的替代模型，因此引入光滑激活DNN。
method: 分析光滑激活DNN（前馈及残差结构），建立伪维数界、非渐近逼近定理和Hölder范数界，利用低维层次组合结构推导均匀收敛率。
result: 推导出光滑DNN在Huber、最小二乘、分位数及逻辑回归中的非渐近均匀收敛率，证明可避免维度诅咒。
conclusion: 光滑DNN在理论上可替代ReLU网络，提供均匀收敛保证，仿真与真实数据验证其有效性。
---

## 摘要
本文为具有平滑激活函数的深度神经网络（DNN）估计量的一致收敛建立了一个理论框架。虽然标准ReLU网络在各种非参数回归任务中能在$L^2(P)$范数下达到极小化最优速率，但我们建立了一个理论下界，表明最小二乘ReLU估计量在其一致收敛行为中可能遭受维度灾难。受下游任务中对最坏情况可靠性需要可靠一致保证的启发，我们通过分析平滑激活的DNN（平滑DNN），包括前馈和残差结构，来解决这一局限。我们为这些模型的近似器建立了新的伪维数界、非渐近逼近保证和Hölder范数界。利用这些结果，我们推导出平滑DNN估计量在多种统计上下文中的非渐近一致收敛速率，包括Huber回归、最小二乘回归、分位数回归和逻辑回归。我们证明平滑DNN能够通过自适应地利用目标函数的低维层次组合结构来缓解一致收敛中的维度灾难。在模拟研究和实际应用的支持下，我们的结果将平滑DNN定位为一种在理论上可行且在实际中可替代ReLU网络的统计学习方法，适用于需要一致保证的任务。

## Abstract
This paper establishes a theoretical framework for the uniform convergence of smoothly activated deep neural network (DNN) estimators. While standard ReLU networks achieve minimax-optimal rates in the $L^2(P)$ norm for various nonparametric regression tasks, we establish a theoretical lower bound demonstrating that least-squares ReLU estimators can suffer from the curse of dimensionality in their uniform convergence behavior. Motivated by the need for reliable uniform guarantees in downstream tasks requiring worst-case reliability, we address this limitation by analyzing smoothly activated DNNs (smooth DNNs), encompassing both feedforward and residual structures. We establish novel pseudo-dimension bounds, non-asymptotic approximation guarantees, and Hölder-norm bounds for the approximators of these models. Leveraging these results, we derive non-asymptotic uniform convergence rates for smooth DNN estimators across multiple statistical contexts, including Huber, least-squares, quantile, and logistic regression. We prove that smooth DNNs can mitigate the {curse of dimensionality} in uniform convergence by adaptively exploiting the low-dimensional hierarchical composition structure of the target function. Supported by both simulation studies and a real-world application, our results position smooth DNNs as a theoretically grounded and practically viable alternative to ReLU networks for statistical learning tasks requiring uniform guarantees.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：标准ReLU激活函数的深度神经网络在$L^2(P)$范数下可以达到极小化最优收敛速率，但在**一致收敛（uniform convergence）**中却面临**维度灾难**。即使目标函数高度光滑，ReLU网络的均匀收敛速率仍受限于$n^{-1/(d+1)}$（d为特征维度），这严重制约了其在需要最坏情况保证的下游任务（如置信区间构建、迁移学习）中的可靠性。
- **整体含义**：为克服这一瓶颈，论文转向现代深度学习广泛使用的**平滑激活函数**（如SiLU, GELU, Mish）及**残差架构**，建立了完整的理论框架，证明平滑DNN能够自适应地利用目标函数的**低维层次组合结构**，从而有效缓解维度灾难，并在Huber、最小二乘、分位数、逻辑回归等多种统计任务中提供非渐近的一致收敛保证。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：平滑激活函数（$C^\infty$光滑）使得DNN整体具有更高的正则性，从而在一致收敛分析中能够通过**Gagliardo–Nirenberg型插值不等式**将$L^\infty$误差与$L^2$误差关联，获得优于ReLU的速率；同时残差连接有助于优化和深度扩展。
- **关键技术细节**：
  - **定义平滑DNN类**：包括前馈和残差结构，激活函数为Pfaffian函数（如SiLU, GELU, Mish），满足拟衰减和局部$C^\infty$条件（Assumption 4.5）。
  - **伪维数（Pseudo-dimension）界**（Theorem 4.4）：对于Pfaffian激活的平滑DNN，伪维数$ \text{Pdim} \lesssim_\log D^4W^6 $（D深度，W宽度）。
  - **逼近误差界**：
    - Sobolev函数逼近（Theorem 4.6）：对$f \in W^{\alpha,\infty}$，存在平滑DNN使得$\|f-\phi\|_{W^{s,\infty}} \lesssim (WD)^{-2(\alpha-s)/d}$。
    - 层次组合模型逼近（Theorem 4.8）：对$f_0 \in \mathcal{H}(d,\ell,\mathcal{P},C)$，逼近误差$\|f_0-\phi_{f_0}\|_{L^\infty} \lesssim (WD)^{-2\gamma^*}$，其中$\gamma^*$为调整模型复杂度，只依赖内层函数的光滑度与输入维数之比。
  - **Hölder范数上界**（Theorem 4.10）：平滑DNN近似器在某子集上满足$\|\phi\|_{C^m} \leq (C m)^m$型因子界，有利于插值。
  - **一致收敛速率推导**：基于以上工具，结合经验过程理论，推导出Huber、最小二乘、分位数、逻辑回归估计量的$L^\infty$非渐近界。例如Huber估计（Theorem 5.4）误差分解为统计误差+逼近误差+Huber化偏差+亚高斯偏差项，且统计误差随有效样本大小$\tilde{n}=n/(D^4W^6)$衰减。最优速率可达到$n^{-0.99\gamma^*(1-1/m)/(2\gamma^*+2(1-1/m))}$，避免对d的指数依赖。
- **公式/算法流程**：文中使用经验风险最小化，定义平滑DNN估计量$\hat{f}_n$通过最小化对应损失函数得到，与ReLU网络结构相同但激活函数替换为平滑版本。

## 3. 实验设计：数据集 / 场景、benchmark、对比方法
- **模拟实验**：
  - **数据生成**：8维协变量$X_i$均匀取自$[0,1]^8$，目标函数$f_0$为多成分非线性函数，噪声尺度函数$g$也为非线性；噪声$\varepsilon_i$取自Student t分布（自由度2或4，即重尾和中等重尾）。
  - **对比方法**：ReLU FNN、SiLU FNN、SiLU ResNet（深度和宽度均设为4，参数规模匹配）。
  - **Benchmark**：估计$L^2$和$L^\infty$误差，并基于100次重复计算收敛指数。
- **真实数据实验**：
  - **场景**：美国本土（CONUS）臭氧浓度分析，研究温度对每日最大8小时平均臭氧的影响。
  - **数据来源**：U.S. EPA AQS监测站数据（约184万观测，1500站点），温度数据来自gridMET，时间跨度为2015-2024年暖季（5-9月）。
  - **任务**：分位数回归（$\tau=0.10,0.30,0.50,0.87$），反事实扰动（温度增加0.5/1.0/1.5°C）预测臭氧变化。
  - **对比方法**：SiLU ResNet vs. ReLU FNN（深度6，宽度128，Adam优化200个epoch）。
  - **评估指标**：空间粗糙度（SR，有限差分平均绝对值）和极端值分数（EVF，变化>3 ppb的占比）。

## 4. 资源与算力
- 论文明确提及实验环境：**Ubuntu 24.04.2 LTS**，**AMD EPYC 7532 CPU**，**512 GB RAM**，**NVIDIA RTX 3090 GPU**。
- **训练耗时**：未报告每个实验的具体训练时长或GPU使用数量，仅提及对臭氧数据使用Adam训练200个epoch。模拟实验100次重复，每次三个模型，但未给出总时间。

## 5. 实验数量与充分性
- **模拟实验**：共进行$2$种噪声分布 × $5$种Huber参数$\tau$ × $3$种模型 × $3$种样本量（512/1024/2048） = 90个设置，每个设置重复100次，报告均值和bootstrap标准差。此外估计了收敛指数，并绘制箱线图。实验覆盖了重尾噪声、不同样本量和稳健性参数，对比公平（参数规模匹配）。充分性良好。
- **真实数据实验**：仅在单一臭氧数据集上比较SiLU ResNet与ReLU FNN，使用两个诊断指标（SR、EVF）按月及整体报告，并给出bootstrap标准差。没有与其他传统非参数方法（如核方法、样条）或不同平滑激活函数对比。实验数量相对有限。
- **公平性**：模拟中通过设置L1和L3为单位映射确保参数规模大体一致。真实数据中未详细说明参数规模是否严格匹配，但提到“comparable parameter sizes”。总体客观。

## 6. 论文的主要结论与发现
- **理论结论**：
  1. ReLU最小二乘估计量的一致收敛速率下限为$n^{-1/(d+1)}$（定理2.1），这是由ReLU激活的低光滑性本质导致的维度灾难。
  2. 平滑DNN（SiLU, GELU等）能够显著缓解这一问题：其一致收敛速率仅依赖于层次组合结构的复杂度参数$\gamma^*$，而与输入维度d无关，从而实现了对低维结构的自适应。
  3. 在Huber、最小二乘、分位数、逻辑回归中均建立了非渐近$L^\infty$界，并证明了鲁棒性。
- **实验结论**：
  - 模拟中，SiLU FNN/ResNet在$L^2$和$L^\infty$误差上均优于ReLU FNN，且SiLU ResNet最佳；收敛指数估计也显示SiLU ResNet在$L^\infty$上优势更明显。
  - 真实臭氧数据分析中，SiLU ResNet产生的反事实变化场更平滑（SR更小）、极端值更少（EVF更低），且bootstrap标准差更小，表明其预测更稳定可靠。

## 7. 优点：方法或实验设计上的亮点
- **理论贡献突出**：
  - 首次揭示ReLU网络一致收敛的维度灾难下界。
  - 为平滑DNN（包括残差结构）建立了完整的理论工具箱（伪维数、逼近、Hölder范数界），填补了统计理论空白。
  - 推导了多种损失函数的非渐近一致收敛速率，且证明能自适应低维结构。
- **实验设计**：
  - 模拟中不仅比较了误差绝对值，还估计了收敛指数，直接验证了理论速率。
  - 真实应用选择了具有实际意义的分位数回归任务，并使用了反事实分析，可视化展示空间异质性，增强了说服力。
  - 使用bootstrap计算标准误差，给出了不确定性度量。

## 8. 不足与局限
- **实验覆盖**：
  - 仅对比了ReLU FNN，未包括其他非参数方法（如核平滑、随机森林）或其他平滑激活函数之间的比较（如SiLU vs GELU vs Mish）。
  - 真实数据仅一个案例（臭氧），且未在不同地区或气候条件下验证，泛化性有限。
  - 缺少对更复杂模型（如Transformer架构）或更大规模数据的测试。
- **理论假设较强**：
  - Assumption 5.2要求近似器具有阶乘型Hölder范数上界，这在实际中无法直接验证。
  - 结果依赖于目标函数属于层次组合模型，且需要已知结构参数（如$\gamma^*$）。
- **计算资源报告不完整**：未详细说明每个实验的GPU使用数量和耗时，不利于复现。
- **偏差风险**：在真实数据中，只报告了部分诊断指标（SR和EVF）的平均值，未进行严格的统计检验（如假设检验）证明SiLU ResNet的改进是否在统计上显著。

（完）
