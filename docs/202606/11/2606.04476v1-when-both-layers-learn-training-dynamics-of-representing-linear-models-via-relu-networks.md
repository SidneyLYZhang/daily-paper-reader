---
title: "When Both Layers Learn: Training Dynamics of Representing Linear Models via ReLU Networks"
title_zh: 当两层都学习时：通过ReLU网络表示线性模型的训练动力学
authors: "Berk Tinaz, Changzhi Xie, Mahdi Soltanolkotabi"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04476v1"
tags: ["query:ml-theory"]
score: 9.0
evidence: 研究ReLU网络训练中的梯度下降动态和非严格鞍点
tldr: 研究单隐层ReLU网络联合训练两层拟合线性目标函数的梯度下降动力学。损失景观存在非严格鞍点，但GD从随机初始化可靠收敛。分析揭示三阶段：隐层权重与目标方向对齐、两层范数共同增长、局部快速细化。证明线性收敛速率及阶最优样本复杂度，并建立整个轨迹上的均匀浓度界。理论解释了端到端训练中两层同时学习的成功机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04476v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1694, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04476v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1657, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04476v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1521, \"height\": 806, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04476v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1504, \"height\": 738, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04476v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1504, \"height\": 741, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04476v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 584, \"height\": 560, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04476v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 587, \"height\": 561, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04476v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 831, \"height\": 713, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04476v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 828, \"height\": 710, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04476v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 826, \"height\": 706, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04476v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 825, \"height\": 700, \"label\": \"Figure\"}]"
motivation: 两层同时训练时损失景观有非严格鞍点，为何GD能逃离并收敛到全局最小缺乏理论理解。
method: 通过轨迹级分析，刻画对齐、增长、局部细化三阶段动力学，并建立均匀浓度控制。
result: 证明从适度小随机初始化起，GD线性收敛到全局最小，样本复杂度阶最优。
conclusion: 为两层联合训练的动力学提供严格理论，证明非严格鞍点可被避免。
---

## 摘要
在本文中，我们研究了联合训练单隐层ReLU网络的两层以拟合线性目标函数的梯度下降动力学。具体来说，我们考虑一个可实现场景，其中输入独立同分布于高斯分布，标签遵循一个植入的线性模型。这个风格化的框架捕捉了逆问题和某些自编码器模型中端到端训练的显著特征。尽管其表面简单，该动力学仍然难以理解，部分原因是损失景观包含多个非严格鞍点，这使得不清楚为什么从随机初始化开始的梯度下降能可靠地逃离不良平稳区域。我们提供了优化景观的详细刻画，并证明了从适度小的随机初始化开始——同时训练两层——梯度下降以线性速率收敛到全局最小值，并具有阶次最优的样本复杂度。我们的分析通过三个阶段跟踪轨迹：对齐阶段，其中隐藏权重逐渐与植入方向对齐，同时输出权重保持正确的符号模式；增长阶段，其中两层的范数增加，同时保持对齐；以及局部细化阶段，其中对齐的神经元迅速收敛到植入方向，导致快速的局部收敛。为了严格证明梯度下降避免了非严格鞍点，我们为端到端动力学开发了轨迹级控制论证。此外，我们建立了沿整个轨迹成立的新颖的均匀集中结果，这对于获得阶次最优的样本复杂度至关重要。我们通过在一系列配置上的广泛实验来证实我们的理论。

## Abstract
In this paper, we study the gradient descent dynamics for jointly training both layers of a one-hidden-layer ReLU network to fit a linear target function. Concretely, we consider a realizable setting where inputs are drawn i.i.d. from a Gaussian distribution and labels follow a planted linear model. This stylized framework captures salient features of end-to-end training in inverse problems and certain auto-encoder models. Despite its apparent simplicity, the dynamics remain poorly understood, in part because the loss landscape contains multiple non-strict saddle points, making it unclear why gradient descent from random initialization reliably escapes bad stationary regions. We provide a detailed characterization of the optimization landscape and prove that gradient descent from a moderately small random initialization-simultaneously training both layers-converges to a global minimizer at a linear rate with order-wise optimal sample complexity. Our analysis tracks the trajectory through three phases: an alignment phase in which hidden weights progressively align with the planted direction while the output weights maintain the correct sign pattern; a growth phase in which the norms of both layers increase while preserving alignment; and a local refinement phase in which the aligned neurons rapidly converge to the planted direction, yielding fast local convergence. To rigorously show that GD avoids non-strict saddles, we develop trajectory-level control arguments for the end-to-end dynamics. In addition, we establish novel uniform concentration results that hold along the entire trajectory, and are essential for obtaining order-wise optimal sample complexity. We corroborate our theory with extensive experiments across a range of configurations.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义

- **研究动机**：端到端训练神经网络（如逆问题、自编码器）取得了显著经验成功，但其训练动力学缺乏理论理解。特别是，当网络的两层同时通过梯度下降（GD）学习时，损失景观中存在的非严格鞍点（non-strict saddle points）使得难以解释 GD 为何能可靠逃离不良平稳区域并收敛到全局最优。
- **整体含义**：本文选择最简单的可解释设定——单隐层 ReLU 网络拟合线性目标函数 \(y = a^T x\)（输入为高斯分布），旨在揭示两层联合训练的内在机制，为理解深度网络的特征学习提供基础理论支撑。

### 方法论

- **核心思想**：通过轨迹级分析将 GD 的训练过程划分为三个明确阶段，并证明每个阶段的可控性，从而确保算法避免非严格鞍点并最终收敛。
- **关键技术细节**：
    - **对齐阶段**（Alignment Phase）：从适度小的随机初始化开始，隐层权重 \(w_1, w_2\) 逐渐与目标方向 \(a\) 和 \(-a\) 对齐，同时输出权重 \(v_1, v_2\) 保持正确的符号模式。
    - **增长阶段**（Growth Phase）：在保持对齐的前提下，两层权重范数协同增长，将有效参数推至正确尺度，远离平坦鞍点区域。
    - **局部细化阶段**（Local Refinement Phase）：对齐后的神经元快速收敛到植入方向，有效参数以线性速率收敛到全局最小值（\(v_1 w_1 = a, v_2 w_2 = -a\)）。
    - **关键引理**：建立了不平衡项（imbalance term）的界、角度稳定性、范数稳定性，以及针对总体损失的 PL 不等式和光滑性。
    - **均匀浓度界**：针对整个 GD 轨迹证明了新的均匀浓度结果，使得经验梯度与总体梯度的偏差随误差减小而自动收紧，从而在局部细化阶段获得精确的梯度估计，并得到阶次最优的样本复杂度 \(n = \Omega(d)\)。

### 实验设计

- **数据集/场景**：合成数据，输入 \(x_i \sim \mathcal{N}(0, I_d)\)，标签 \(y_i = a^T x_i\)（实验中固定 \(a = e_1\)，\(\|a\|=1\)）。主要场景包括单输出（\(r=1\)）和多输出（\(r>1\)），以及不同初始化尺度（小初始化 \(\sigma=10^{-8}\) 与默认初始化 \(\sigma=1\)）。
- **基准**：理论预测结果（如线性收敛速率、角度对齐、权重配对行为），没有与其他现有方法进行对比（因为本文专注于理论分析而非性能比较）。
- **对比方法**：主要是不同隐层神经元数量（\(k=2\) 精确参数化 vs. \(k>2\) 过度参数化）以及不同初始化尺度的对比实验。
- **主要观察指标**：损失函数值、权重范数、角度（如 \(\theta_1 = \angle(w_1, a)\)）、输出层权重的符号演化、最终权重的方向恢复情况。

### 资源与算力

- 论文中明确说明实验使用 PyTorch，在搭载 **Intel Xeon Gold 5220R CPU** 的服务器上运行。
- 未提及 GPU 型号、数量或具体训练时长，推测实验规模较小，对算力需求不高。

### 实验数量与充分性

- **实验组数**：论文展示了多组典型实验，包括：
    - 单输出 \(k=2\) 时两种不同初始化的轨迹（收敛到全局最优 vs. 陷入非严格鞍点）。
    - 单输出 \(k>2\)（如 \(k=4\)）时权重配对恢复 ±a 方向的观测。
    - 多输出（\(r=3,5,10\)）时的配对模式可视化。
    - 不同随机种子下的重复性确认（文中说明结果稳定）。
- **充分性与客观性**：实验数量有限，但紧密围绕理论发现（三阶段、鞍点规避、配对行为），并提供了直观的可视化验证。未进行大规模消融或超参数扫描，但对于理论验证型论文而言足够。实验结果客观，未发现明显偏向。

### 主要结论与发现

1. **梯度下降的收敛性**：从适度小的随机初始化开始，同时训练两层，GD 能线性收敛到全局最小值，且样本复杂度 \(n = O(d)\) 是最优的。
2. **三阶段动力学**：训练过程自然分为对齐、增长和局部细化三个阶段，每个阶段都有明确的可控特性，确保算法避免非严格鞍点。
3. **非严格鞍点的规避**：尽管损失景观含有连续的非严格鞍点流形，GD 轨迹通过协同增长范数和保持对齐，不会被困在其中。
4. **过度参数化的行为**：当隐层神经元数 \(k>2\) 时（实验显示 \(k=4\) 等），虽然单个神经元不直接对齐 ±a，但将符号相同的神经元分组求和后能精确恢复目标方向，提示了扩展理论的可能性。
5. **均匀浓度界**：沿整个轨迹成立的新颖均匀浓度结果，其精度会随误差减小而自动提高，是实现最优样本复杂度的关键。

### 优点

- **理论深度**：首次对两层可训练的 ReLU 网络拟合线性目标函数提供了完整的轨迹级分析，处理了非严格鞍点这一核心困难。
- **技术创新**：提出了轨迹级控制论证和随误差自收紧的均匀浓度界，这些技术对于其他含非严格鞍点的非凸优化问题具有潜在推广价值。
- **实验验证**：通过简单而清晰的实验直观展示三阶段动力学和配对行为，有力地支撑了理论。
- **实用性**：使用标准的梯度下降（无特殊修改或预处理）和常见的随机初始化，结论直接适用于实际训练。

### 不足与局限

- **理论局限性**：
    - 主要理论仅针对 \(k=2\) 的精确参数化情况，过度参数化（\(k>2\)）仅有经验结果，缺乏正式理论保证。
    - 输出层初始化使用 \(\chi^2\) 分布（为了高概率成功），若使用标准高斯初始化会引入常数失败概率（对于 \(k=2\)）。论文虽提及可通过增大 \(k\) 来降低失败概率，但未完全统一。
    - 假设输入为各向同性高斯分布，目标为线性函数，限制了结论向更复杂数据分布或非线性目标函数的泛化。
- **实验局限性**：
    - 实验全部基于合成数据，未在真实数据集（如图像逆问题）上验证理论中的三阶段行为。
    - 未进行广泛的超参数敏感度分析（如不同学习率、不同 \(d\)、不同 \(n\) 下的收敛行为）。
    - 对于多输出场景的实验只展示了配对模式，未深入分析训练动态是否仍遵循三阶段。
    - 未与其他训练技巧（如学习率调度、正则化）结合测试。
- **应用限制**：当前结果仅适用于特定架构（单隐层、ReLU）、特定损失（平方误差）和特定训练方式（全批量 GD），向深度网络、其他激活、SGD 的扩展需要进一步工作。

（完）
