---
title: Learning Dynamics Reveal a Hierarchy of Weight-Induced Layerwise Gram Metrics
title_zh: 学习动力学揭示权重诱导的逐层Gram度量层次结构
authors: Claudio Nordio
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09744v1"
tags: ["query:ml-theory"]
score: 9.0
evidence: 学习动态中的层间Gram度量和权重诱导几何
tldr: 研究前馈ReLU网络在固定读出和二次损失下的学习动力学。通过将梯度下降重写为训练集空间上的集体动力学，单隐层可消除权重变量得到残差的封闭方程，其核分解为输入几何矩阵和动态协同激活矩阵。深度网络保持层状核结构，但从三层起需要权重诱导的Gram算子层次来闭合信息传输。该发现揭示了层间度量层级，为理解深层网络优化提供新视角。
source: arxiv
selection_source: fresh_fetch
motivation: 从集体动力学角度理解深层网络训练，消除权重变量以揭示结构化的层状Gram度量及其层级关系。
method: 针对前馈ReLU网络，将梯度下降重写为训练集上残差的封闭动力学，并通过引入权重诱导的Gram算子实现层间信息闭合。
result: 单隐层核分解为输入几何与协同激活矩阵；深度三层以上需要Gram算子层次结构才能闭合残差动力学。
conclusion: 揭示了权重诱导的层状Gram度量层级，为深层网络的动力学分析和优化提供了新框架。
---

## 摘要
我们研究具有固定读出和二次损失的前馈ReLU网络。目标是将梯度下降重写为一种不仅在权重空间中的动力学，而且是在训练集空间上定义的场中封闭的集体动力学。对于单隐藏层，可以从激活动力学中消除权重变量，得到由集体核控制的残差封闭方程，该集体核分解为输入几何矩阵和动态共激活矩阵。对于更深层的网络，残差动力学保留了清晰的逐层核结构。然而，从第三层开始，封闭需要一组权重诱导的Gram算子层次结构来中介跨层的信息传输。

## Abstract
We study feed-forward ReLU networks with fixed readout and quadratic loss. The aim is to rewrite gradient descent not primarily as a dynamics in weight space, but as a collective dynamics closed in terms of fields defined on the training-set space. For a single hidden layer, the weight variables can be eliminated from the activation dynamics, yielding a closed equation for the residuals governed by a collective kernel that factorizes into an input-geometric matrix and a dynamical co-activation matrix. For deeper networks, the residual dynamics retains a clean layer-wise kernel structure. However, from depth three onward, closure requires a hierarchy of weight-induced Gram operators that mediate information transport across layers.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：传统上，神经网络梯度下降被视为权重空间的动力学，但论文探索是否可以将学习过程重写为基于激活、残差等场变量的集体动力学，从而消除显式权重依赖，获得更直接的结构化描述。
- **背景**：受神经切线核（NTK）启发，但不同于无穷宽极限，该工作保持有限宽度，并关注激活模式诱导的几何结构。旨在揭示随网络深度增加，学习动力学如何自然产生一个权重诱导的Gram度量层次结构（层状核），用于跨层信息传输。
- **整体含义**：论文表明，对于固定读出、二次损失的ReLU网络，残差动力学可以保持逐层核形式，且从三层起需要引入新的算子（权重诱导的Gram度量）来封闭动力学；这些度量具有投影算子的几何解释，为理解深度网络训练中的特征线性化、谱性质（如重尾）提供了新视角。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将梯度下降的权重更新转化为激活场、共轭场和诱导Gram度量的耦合动力学。通过链式法则和梯度更新表达式，逐步消去权重变量，得到仅依赖场变量的封闭方程。
- **关键技术细节**：
  - **符号定义**：输入 \(u_0^\alpha\)，激活 \(u_\ell^\alpha = \phi(v_\ell^\alpha)\)，指示器 \(a_\ell^\alpha = 1(v_\ell^\alpha > 0)\)；输出 \(\hat{Y}^\alpha = \frac{1}{N}\sum_i u_{L,i}^\alpha\)，残差 \(r^\alpha = \hat{Y}^\alpha - Y^\alpha\)；损失 \(\mathcal{L} = \frac{1}{M}\sum_\alpha (r^\alpha)^2\)；梯度下降更新 \(\Delta W^{(\ell)} = -\eta \partial\mathcal{L}/\partial W^{(\ell)}\)。
  - **单隐层（L=1）**：得到残差封闭方程 \(r(t+1) = (I - \frac{2\eta}{M}J(t))r(t)\)，其中核 \(J^{\alpha\beta} = Q_{\alpha\beta}^{(0)} A_{\alpha\beta}^{(1)}\)，\(Q^{(0)}\) 为输入几何重叠，\(A^{(1)}\) 为共激活重叠（动态）。
  - **两隐层（L=2）**：引入共轭场 \(b_1^\alpha = (W^{(2)})^T a_2^\alpha\)；动力学需要 \(u_2, u_1, b_1\) 三个场；残差核分解为输出层贡献 \(Q^{(1)}A^{(2)}\) 和隐藏层贡献 \(Q^{(0)}S^{(1)}\)，其中 \(S^{(1)}\) 为四阶共轭场相关。
  - **三隐层（L=3）**：共轭场递归定义 \(b_\ell^\alpha = (W^{(\ell+1)})^T (a_{\ell+1}^\alpha \odot b_{\ell+1}^\alpha)\)；计算 \(b_1\) 的动力学时出现无法用已有场表示的量：\((W^{(2)})^T D_{\alpha\beta}^{(2)} W^{(2)}\)，由此定义第一个权重诱导Gram度量 \(G_1^{\alpha\beta} = (W^{(2)})^T \text{diag}(a_2^\alpha \odot a_2^\beta) W^{(2)}\)。
  - **四隐层（L=4）**：出现第二个度量 \(G_2^{\alpha\beta} = (W^{(3)})^T D_{\alpha\beta}^{(3)} W^{(3)}\)，且 \(b_1\) 动力学同时依赖 \(G_1\) 和 \(G_2\)，形成层次结构。
  - **任意深度**：归纳出一般形式：共轭场 \(b_L \equiv 1\)，\(b_\ell = (W^{(\ell+1)})^T (a_{\ell+1} \odot b_{\ell+1})\)；激活更新 \(\Delta u_\ell^\alpha \propto a_\ell^\alpha \sum_\beta Q_{\alpha\beta}^{(\ell-1)} a_\ell^\beta b_\ell^\beta r^\beta\)；共轭场更新包含来自更深层的贡献，产生Gram度量 \(G_\ell^{\alpha\beta} = (W^{(\ell+1)})^T D_{\ell}^{\alpha\beta} W^{(\ell+1)}\)，其中 \(D_\ell^{\alpha\beta} = \text{diag}(a_{\ell+1}^\alpha \odot a_{\ell+1}^\beta)\)。
  - **结构性质**：残差核保持逐层分解 \(K^{(L)} = \sum_{\ell=1}^L Q^{(\ell-1)} S^{(\ell)}\)，其中 \(S^{(\ell)} = \frac{1}{N}\sum_i a_\ell^\alpha a_\ell^\beta b_\ell^\alpha b_\ell^\beta\)（四阶相关）。显式权重依赖仅通过二次型Gram度量出现。
- **算法流程**：论文未提供算法伪代码，而是通过数学推导给出封闭的动力学方程组（如式(137)、式(144)-(162)等）。实际应用需同时演化 \(u_\ell, b_\ell, G_\ell\)。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **该论文为纯理论分析，未进行任何数值实验或仿真验证。** 因此没有使用数据集、benchmark或对比方法。论文在讨论部分仅进行了概念性关联（如NTK、WeightWatcher），无定量实验。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。
- **未提及任何计算资源或算力要求。** 论文全部为数学推导，不涉及实验计算。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。
- **无实验。** 论文推导了从L=1到L=4的显式方程，并推广至任意深度，但缺乏数值验证。从理论分析角度，推导逻辑自洽，但充分性受限于假设（固定读出、二次损失、忽略阈值穿越的一阶近似）。需要后续仿真或真实训练来检验封闭动力学的实际有效性。

### 6. 论文的主要结论与发现
- **核心结论**：对于固定读出、二次损失的ReLU网络，梯度下降可以重新表述为训练集空间上场的封闭动力学，且深度增加不会产生更高阶的集体统计量；残差核始终保持二阶激活重叠和四阶共轭场相关的形式。
- **关键发现**：从三层网络起，需要引入权重诱导的Gram度量 \(G_\ell^{\alpha\beta} = (W^{(\ell+1)})^T D_\ell^{\alpha\beta} W^{(\ell+1)}\) 才能封闭动力学；这些度量具有对称正定、幂等投影性质，可解释为由深层共激活几何拉回得到的度量；它们形成了层次结构，通过耦合共轭场方程逐层生成。
- **启示**：学习动力学的复杂度主要转移到场（激活、共轭、Gram度量）的演化而非核的统计阶数；Gram度量可能解释训练后权重矩阵的重尾谱现象（如WeightWatcher所观察）。

### 7. 优点：方法或实验设计上有哪些亮点
- **数学严谨性**：从简单单隐层出发，逐步构建至任意深度，推导步骤清晰，公式结构对称美观。
- **几何解释新颖**：将权重诱导的算子 \(G_\ell\) 识别为拉回Gram度量，赋予其清晰的几何意义（共激活子空间的投影下的度量），为理解深度网络中层间信息传输提供了新视角。
- **理论洞察**：揭示了“集体动力学”可以避免显式权重空间的高维演化，转而用低阶统计量（二阶、四阶）描述残差动力学，并自然出现层次化结构。这一视角可能有助于设计更高效的训练算法或正则化方法。
- **连接实际现象**：在讨论中与NTK、WeightWatcher的重尾谱现象建立了理论桥梁，尽管未实证，但给出了可检验的机制假设。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **缺乏实验验证**：论文全部为理论推导，没有在真实或合成数据上验证封闭动力学的预测精度、收敛性、与真实梯度下降的匹配程度。理论假设（如忽略阈值穿越事件 \(\Delta a_\ell=0\)）是否合理未经检验。
- **模型限制**：
  - 仅考虑固定读出（输出层权重固定为1/N平均），不适用于可训练输出层。
  - 仅适用ReLU激活，其他激活函数（如tanh、sigmoid）的类似结构尚未分析。
  - 仅使用二次损失，未涉及交叉熵等更常用的损失。
  - 梯度下降为纯离散更新（未考虑动量、自适应学习率）。
  - 假设宽度 \(N\) 有限但未讨论有限宽度效应；推导中未使用NTK无穷宽极限，但保留有限宽度，可能引入有限尺寸效应。
- **理论覆盖**：仅深入推导至L=4，更高层的形式仅通过归纳给出，未严格证明递归封闭性；对任意深度下Gram度量层次生成机制未给出完整递归公式证明。
- **偏差风险**：论文自述“独立于arXiv:2605.06258v1”，但未进行比较或讨论差异；同时缺少与已有神经正切核文献（如无限宽、有限宽修正）的详细对比。
- **应用限制**：无法直接用于现有深度学习框架的加速或解释，需要进一步构建数值实现，并评估其计算复杂度（演化所有 \(G_\ell\) 可能开销巨大）。

（完）
