---
title: "GraphPO: Graph-based Policy Optimization for Reasoning Models"
title_zh: GraphPO：面向推理模型的基于图的策略优化
authors: "Yuliang Zhan, Xinyu Tang, Jian Li, Dandan Zheng, Weilong Chai, Jingdong Chen, Jun Zhou, Ge Wu, Wenyue Tang, Hao Sun"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18954v1"
tags: ["query:rl-semantic"]
score: 9.0
evidence: 基于图的策略优化用于强化学习中的状态表示
tldr: 针对RLVR中独立采样导致中间步骤冗余、树方法局部比较带来高方差的问题，提出GraphPO框架。它将推理轨迹组织为有向无环图，合并语义等价路径并分配效率与正确性优势，从而降低优势估计方差、提升探索效率。在三个LLM上的推理和agentic搜索实验中，GraphPO以相同token或响应预算一致优于链式和树式基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18954v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 731, \"height\": 354, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1446, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1431, \"height\": 867, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1452, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1442, \"height\": 386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 513, \"height\": 377, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 513, \"height\": 341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 519, \"height\": 366, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18954v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 1051, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18954v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18954v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 357, \"label\": \"Table\"}]"
motivation: 现有RLVR方法中独立响应采样导致冗余探索，树方法仅在分支内局部比较，信息共享不足且优势估计方差高。
method: 将rollout表示为有向无环图，以语义状态为节点、推理步骤为边，合并等价路径，对入边分配效率优势、出边分配正确性优势。
result: 理论上可降低优势估计方差；在三个LLM的推理和agentic搜索基准上，以相同预算一致优于链式和树式基线。
conclusion: GraphPO通过图结构融合与细粒度优势分配，显著提升推理模型的训练效率和探索多样性。
---

## 摘要
基于可验证奖励的强化学习（RLVR）已成为增强大型推理模型能力的标准范式。RLVR通常独立采样响应，并使用最终答案的奖励来优化策略。这种范式有两个局限性。首先，独立响应通常包含相似的中间推理步骤，导致冗余探索和计算浪费。其次，稀疏的最终答案奖励使得难以识别有用的步骤。基于树的方法通过共享前缀并比较同一前缀下的分支来提供细粒度信号，从而部分解决了这一问题。然而，树的分支仍然是独立扩展的。当不同分支达到相似的推理状态时，它们无法共享信息并重复相似的探索。此外，基于树的方法忽略了这种分散性，仅在各分支内部进行局部比较，这可能导致优势估计的方差更高。为了解决这一挑战，我们提出了GraphPO（基于图的策略优化），一种新颖的RL框架，它将轨迹表示为一个有向无环图，其中推理步骤作为边，从推理路径中总结出的语义状态作为节点。GraphPO将语义等价的推理路径合并为等价类，允许它们共享后缀，并将预算从冗余扩展重新分配到多样化探索。此外，我们为入边分配效率优势，为出边分配正确性优势，从而在从结果中推导过程监督的同时提高推理效率。理论上表明，GraphPO降低了优势估计方差并提高了推理效率。在三个大语言模型上的推理和智能体搜索基准实验中，GraphPO在相同的token预算或响应预算下始终优于基于链和基于树的基线。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has become a standard paradigm for enhancing the capability of large reasoning models. RLVR typically samples responses independently and optimizes the policy using from final answers. This paradigm has two limitations. First, independently responses often contain similar intermediate reasoning steps, causing redundant exploration and wasted computation. Second, sparse final-answer rewards make it hard to identify useful steps. Tree-based methods partly address this problem by sharing prefixes and comparing branches from the same prefix to provide fine-grained signals. However, tree branches are still expanded independently. When different branches reach similar reasoning states, they cannot share information and repeat similar exploration. Moreover, tree-based methods ignore such dispersion and only perform local comparisons within separate branches, which can lead to higher variance in advantage estimation. To address this challenge, we propose GraphPO (Graph-based Policy Optimization), a novel RL framework that represents rollouts as a directed acyclic graph, with reasoning steps as edges and semantic states summarized from the reasoning paths as nodes. GraphPO merges semantically equivalent reasoning paths into equivalence classes, allowing them to share suffixes and reallocating budget away from redundant expansions to diverse exploration. Furthermore, we assign efficiency advantages to incoming edges and correctness advantages to outgoing edges, thereby improving inference efficiency while deriving process supervision from outcome. Theory shows that GraphPO reduces advantage-estimation variance and enhances reasoning efficiency. Experiments on three LLMs across reasoning and agentic search benchmarks show that GraphPO consistently outperforms chain- and tree-based baselines with the same token budgets or response budgets.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：可验证奖励强化学习（RLVR）是提升大型推理模型（LRM）能力的主流范式，但存在两个关键问题：
    - **独立采样导致冗余**：相同prompt下多条响应常包含重复的中间推理步骤，浪费计算资源。
    - **稀疏奖励**：仅最终答案的二进制奖励难以对中间步骤进行信用分配，限制模型学习。
- **现有方法的不足**：
    - 基于树的方法（如TreeRL、TreePO）通过共享前缀和分支比较提供部分过程信号，但分支间仍独立扩展，当不同分支达到相似语义状态时无法共享信息，且仅进行局部比较导致优势估计方差高。
- **本文目标**：提出图结构 rollout 框架，通过合并语义等价推理路径来减少冗余、提高资源利用率、降低优势估计方差，从而提升推理模型的训练效率与最终性能。

### 2. 论文提出的方法论：核心思想、技术细节与算法流程

- **核心思想**：将每次推理 rollout 组织为**有向无环图 (DAG)**——节点表示从初始prompt到当前步骤的语义状态，边表示生成的推理步骤。通过检测并合并语义等价的状态，形成**等价类**，实现跨路径的信息共享和预算重分配。
- **关键技术细节**：
    1. **图构建 (Graph Construction)**：
        - 从根节点 \( v_0 \) 开始逐层扩展，每一层从当前节点采样多个子步骤（边）。
        - 使用策略模型对每条路径进行结构化总结生成节点语义状态 \( s_v \)，并通过嵌入模型计算向量 \( z_v \)。
        - 余弦相似度 \( \text{sim}(u,v) \ge \kappa \) 的节点（非祖先-后代关系）合并为等价类 \( Q(v) \)。
        - 合并后，到达已发现等价类的后续分支预算减半，节省的计算重新分配给未探索的新状态，提高探索效率。
    2. **图奖励 (Graph Reward)**：
        - 对每个节点 \( v \)，从自身及等价类内其他节点的可达终端状态计算分数 \( S(v) \)，公式为加权平均：
          \[
          S(v) = \frac{c_{\text{own}}^v + w \sum_{q \in Q(v)\setminus\{v\}} c_{\text{own}}^q}{t_{\text{own}}^v + w \sum_{q \in Q(v)\setminus\{v\}} t_{\text{own}}^q}
          \]
        - 每条边 \( e(u,v) \) 的步骤奖励为分数增益与语义新颖性因子的乘积：
          \[
          r_{\text{step}}(u,v) = (S(v) - S(u)) \cdot (1 - \eta(u,v)), \quad \eta = \max(0, \text{sim}(u,v))
          \]
        - 通过共享等价类的下游结果，将稀疏的二元奖励转化为稠密的过程监督信号。
    3. **双组图优势 (Dual-Group Graph Advantage)**：
        - **正确性组 (Correctness Group)**：对同一源节点 \( u \) 的所有直接后继（包括等价类共享的后继），将各边 \( e \) 的步骤奖励进行标准化作为正确性优势 \( A_{\text{cor}}(e) \)。
        - **效率组 (Efficiency Group)**：对同一等价类 \( Q \) 内的路径，以路径长度（从最近共同祖先 \( a_Q \) 到节点 \( v \) 的边数）的负值为基础，乘以等价类平均分数 \( \overline{S}_Q \) 作为效率优势 \( A_{\text{eff}}(v) \)，鼓励更短的推理路径。
        - 最终双组优势：\( A_{\text{dual}}(e) = A_{\text{cor}}(e) + \lambda_{\text{eff}} A_{\text{eff}}(e) \)。
    4. **策略优化**：采用 PPO 风格目标，在每个 token 上应用重要性采样比与双组优势，并加入 KL 散度惩罚。
- **理论保证**：文中提供引理和定理证明：等价类池化降低优势估计方差；预算重分配提升探索效率；步骤奖励匹配 orcale PRM 的梯度方向；效率优势使策略偏好更短的等同正确路径。

### 3. 实验设计：数据集、基准与对比方法

- **推理任务**：
    - 数据集：AIME24、AIME25、MATH500、GPQA、LiveCodeBench。
    - 模型（三个）：Qwen2.5-7B-Math、Qwen3-8B-Base、Deepseek-R1-Distill-Qwen-7B。
    - 对比方法：
        - 链式：GRPO、DAPO
        - 树式：TreeRL、SPO、TREE-GRPO、PROS、TreePO
        - 本文方法：GraphPO
- **Agent 任务**：
    - 场景：深度搜索任务（General AI Assistant、WebWalkerQA、BrowseComp、XBench）。
    - 模型：Qwen2.5-7B-Instruct (ReAct 框架)
    - 对比方法：ReAct+GRPO、ReAct+DAPO、ReAct+Tree-GRPO、GraphPO
- **控制变量**：所有方法在相同 token 预算或响应预算下进行比较（链式64轨迹，树/图接近的token数）。

### 4. 资源与算力

- **训练硬件**：16 × NVIDIA H20 GPU（附录C明确说明）。
- **训练步数**：推理任务 350 步；agent 任务 40 步。
- **推理/轨迹生成效率对比**：在同一 H20 GPU 上（无并行化）测试生成速度。
- **框架**：基于 Verl 框架（推理任务）和 Search-R1 框架（agent 任务），使用 vLLM 进行批量推理。

### 5. 实验数量与充分性

- **主要结果**：表1（三模型 × 五基准）和表2（agent 任务 × 四基准）展示 GraphPO 在所有设置中均取得最佳平均准确率。
- **训练动态分析**：图4展示了准确率、熵、响应长度随训练步数的变化趋势。
- **额外对比**：
    - 图5a：GraphPO 与 PRM 方法（ReasonFlux-PRM）对比，无需标注数据仍更优。
    - 图5b：在相同轨迹数量（64条）下比较性能与训练时间，GraphPO 更快且更优。
    - 图5c：轨迹生成效率对比（气泡图），GraphPO 与树方法相当。
- **消融实验**：
    - 合并阈值 \( \kappa \) 的影响（图6）：中等值最优，过小或过大均下降。
    - 池化系数 \( w \)（图S.2）：\( w=0.7 \) 最佳。
    - 分支策略（表S.1）：逐步减少分支的模式最优。
    - 效率优势消融（图S.1）：去除效率优势后响应长度先降后升（类似树方法），证明其促进效率。
- **充分性评价**：实验覆盖多个模型、多个基准、多个消融变量，对比基线全面且公平（相同预算），理论分析（附录F）与实验互相印证，可信度高。

### 6. 论文的主要结论与发现

- GraphPO 在所有测试的 LLM 和基准上**一致优于**链式和树式 RLVR 方法，在相同 token 或响应预算下提升 3–7 个百分点。
- 图结构 rollout 通过合并等价状态、共享后缀、重分配冗余预算，**提高了探索效率和采样利用率**（熵衰减更慢，保留更多有效推理模式）。
- 双组优势中的效率优势**有效缩短了正确推理路径长度**，提升了推理效率（见响应长度曲线）。
- 从结果奖励中**自动生成出过程监督信号**，无需手动标注，且接近 oracle PRM 的梯度方向。
- 理论分析表明：等价类池化降低优势估计方差；预算重分配提升正确 token 效率；效率优势使策略偏好更短的等同正确路径。

### 7. 优点：方法与实验亮点

- **方法创新**：
    - 首个将 rollout 表示为有向无环图并合并语义等价状态的工作，突破树结构的局部比较限制。
    - 双组优势（正确性+效率）设计巧妙，既进行信用分配又鼓励高效推理。
    - 从结果奖励中自发生成过程监督，无需额外 PRM 标注。
- **实验设计**：
    - 多模型、多基准的广泛验证（含 agent 场景）。
    - 严格保持资源公平（token预算、响应预算一致）。
    - 充分的消融实验和理论分析（附录F-G）增强了可信度。
- **理论贡献**：提供方差、效率、梯度对齐、路径选择等方面的严格证明。

### 8. 不足与局限

- **实验覆盖局限性**：论文主要聚焦数学推理和简单 agent 搜索，未系统评估在多模态推理、代码生成（带执行反馈）、长时交互环境下效果（论文附录H也提及）。
- **对语义检测的依赖**：节点合并依赖语义嵌入和阈值 \( \kappa \)，虽经理论分析（附录G）和消融实验证明鲁棒，但在跨领域迁移时嵌入质量可能变化，需谨慎调参。
- **偏差风险**：合并阈值过低可能引入误合并（false positive），虽受 \( \delta_\kappa \) 控制，但需实践经验。
- **算力消耗**：16×H20 GPU 的训练配置相对较高，且嵌入模型在 rollout 中额外消耗（虽文中说明生成效率与树方法相近）。
- **未与更多图方法对比**：仅在树方法和链式方法上比较，未对比其他图结构 RL 工作（如基于 MCTS 的图方法）。

（完）
