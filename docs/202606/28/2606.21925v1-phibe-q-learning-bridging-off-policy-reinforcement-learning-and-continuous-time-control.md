---
title: "PhiBE-Q-Learning: Bridging Off-Policy Reinforcement Learning and Continuous-Time Control"
title_zh: PhiBE-Q-Learning：桥接离策略强化学习与连续时间控制
authors: "Yuhua Zhu, Yutong Ren"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21925v1"
tags: ["query:npl-bdl-saig"]
score: 8.0
evidence: 连续时间强化学习；随机微分方程；离线策略方法
tldr: 连续时间强化学习面临离策略学习中状态-动作价值函数缺失的挑战。本文提出一种新的Q函数定义及其控制方程，基于PhiBE近似设计迭代算法，在模型基和无模型设置下利用离散时间离策略数据学习最优Q函数。在线性函数近似下证明了收敛性并给出了显式收敛速率。该方法填补了连续时间控制中离策略学习的理论空白。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21925v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1694, \"height\": 590, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21925v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1691, \"height\": 572, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21925v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1689, \"height\": 586, \"label\": \"Figure\"}]"
motivation: 连续时间强化学习中经典Q函数不存在，限制了离策略学习方法的应用。
method: 提出新Q函数定义与PhiBE近似，设计模型基与无模型迭代算法。
result: 在线性函数近似下给出收敛性证明与显式收敛速率。
conclusion: 实现了连续时间控制中离策略学习的理论突破，具有实用潜力。
---

## 摘要
本文提出了一种用于连续时间强化学习（CTRL）的离策略方法，其中系统动力学由未知的随机微分方程（SDE）支配，且仅能获得离散时间轨迹数据。一个核心挑战在于，经典的状态-动作值函数Q(s,a)（该函数在离散时间强化学习中支持离策略学习）在CTRL中并不存在（Baird, 1994; Jia and Zhou, 2023; Tallec et al., 2019）。另一方面，连续时间控制提供了诸如即时优势函数q(s,a)等局部概念，但这些通常依赖于状态值函数V(s)。为解决这一问题，我们在CTRL中引入了状态-动作值函数的新定义，并推导出其控制方程。基于PhiBE近似（Zhu, 2024; Zhu et al., 2025），我们提出了迭代算法，仅利用离散时间离策略数据，在基于模型和无模型设置下近似最优Q函数。在线性函数近似下，我们建立了收敛保证，并导出了所提方法的显式收敛速率。

## Abstract
In this paper, we develop an off-policy method for continuous-time reinforcement learning (CTRL), where the system dynamics are governed by an unknown stochastic differential equation (SDE) and only discrete-time trajectory data are available. A central challenge is that the classical state-action value function $Q(s,a)$, which enables off-policy learning in discrete-time RL, does not exist in CTRL (Baird, 1994; Jia and Zhou, 2023; Tallec et al., 2019). On the other hand, continuous-time control provides local notions such as the instantaneous advantage function $q(s,a)$, but these typically rely on state value function $V(s)$. To address this, we introduce a new definition of the state-action value function in CTRL and derive its governing equation.   Building on the PhiBE approximation (Zhu, 2024; Zhu et al., 2025), we propose iterative algorithms to approximate the optimal $Q$-function in both model-based and model-free settings using only discrete-time off-policy data. Under linear function approximation, we establish convergence guarantees and derive explicit convergence rates for the proposed method.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：连续时间强化学习（CTRL）中，经典的状态-动作值函数 \(Q(s,a)\) 不存在（会退化为值函数 \(V(s)\)），导致无法直接利用离策略（off-policy）数据进行学习。同时，现有连续时间方法（如瞬时优势函数 \(q(s,a)\)）依赖状态值函数 \(V\)，不适合离策略场景。
- **研究动机**：物理世界系统（如医疗、自动驾驶）具有连续时间动态，但数据往往以离散时间片段形式收集于未知行为策略下。离策略学习可提升样本效率并利用现有数据集，但连续时间下的离策略框架缺失。
- **整体含义**：本文首次在CTRL中定义了一个新的非退化Q函数，并基于PhiBE近似设计迭代算法，使得离策略数据可直接用于连续时间最优控制，填补了该领域的理论空白。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  - 定义连续时间Q函数 \(Q^\pi(s,a)\)，满足：(i) 不退化到 \(V\)；(ii) 其控制方程仅依赖于动作 \(a\) 下的动力学，不依赖策略 \(\pi\)。
  - 导出连续时间最优贝尔曼方程：\(\beta Q^*(s,a) = r(s,a) + \mathcal{L}_{b,\Sigma}[\max_{a'} Q^*(s,a')]\)，其中 \(\mathcal{L}_{b,\Sigma}\) 为生成算子。
  - 基于PhiBE近似（利用离散时间数据估计漂移 \(\hat{b}\) 与扩散 \(\hat{\Sigma}\)），将连续时间方程离散化为PhiBE-Q方程。
- **关键技术细节**：
  1. **已知动力学情形**：采用Galerkin投影法，将Q函数用线性基函数 \(\Phi(s,a)\) 近似，推导参数迭代公式：
     \[
     \theta_{n+1} = \theta_n + \alpha_n \langle r + \mathcal{L}_{b,\Sigma}[\max_a \Phi^\top\theta_n] - \beta \Phi^\top\theta_n,\ \Phi \rangle
     \]
  2. **未知动力学情形（模型无关）**：算法 PhiBE-Q-Learning，利用离散数据 \((s_i, a_i, s'_i, r_i)\) 构造PhiBE残差：
     \[
     \delta = r_i + \frac{s'_i - s_i}{\Delta t} \cdot \nabla V(s_i) + \frac{1}{2\Delta t}(s'_i - s_i)(s'_i - s_i)^\top : \nabla^2 V(s_i) - \beta Q(s_i,a_i)
     \]
     然后更新参数 \(\theta\)（如式 (27)）。
- **理论保证**：在线性函数近似下，证明Galerkin解的误差上界（定理3.3），以及迭代算法的线性收敛速率（定理3.4）。收敛条件：折扣因子 \(\beta\) 足够大，基函数平滑且正交，行为策略均匀覆盖。

### 3. 实验设计

- **数据集/场景**：线性二次型调节器（LQR），状态 \(s \in \mathbb{R}\)，动作 \(a \in \mathbb{R}\)，动力学为 \(ds = (As + Ba)dt + \sigma dB\)，奖励 \(r = s^\top Q s + a^\top R a\)。数据由均匀分布采样生成 \(s \sim U(-1,1), a \sim U(-1,1)\)。
- **Benchmark**：对比标准离散时间Q-learning（Baseline）。
- **对比方法**：
  - 已知离散时间动力学下的PhiBE Q-learning（图1）；
  - 离策略数据驱动的PhiBE Q-learning（图2）；
  - 与标准Q-learning对比（图3）。
- **参数设置**：折扣因子 \(\beta = 1\)（确定性）或 \(\beta = 2\)（随机），时间步 \(\Delta t = 0.1\)，基函数 \(\Phi = (s^2, a^2, 1)\)，学习率 \(\alpha = 0.1\)，批大小 \(|\mathcal{B}_n|\) 取不同值（图2）。

### 4. 资源与算力

- **文中未明确说明**：实验为简单一维LQR数值示例，运行在常规CPU上即可完成。未提及GPU型号、数量或训练时长。该实验主要验证理论，算力需求极小。

### 5. 实验数量与充分性

- **实验数量**：共3组实验：
  1. 已知动力学下不同 \(\Delta t\) 的误差收敛曲线（确定性与随机性各一张图）；
  2. 离散数据驱动下不同批大小的误差收敛曲线（确定性与随机性各一张图）；
  3. 与标准Q-learning的对比（确定性与随机性各一张图）。
- **充分性评估**：实验较为有限，仅覆盖简单LQR场景，未在高维或非线性系统中验证，也未包含多组随机种子统计（图中只有单次运行或数值积分结果）。实验虽然验证了理论速率和稳定性，但缺乏充分的消融分析（如不同折扣因子、基函数选择等）。因此，实验充分性一般，但作为理论论文的数值示例已可接受。

### 6. 论文的主要结论与发现

- 新定义的非退化连续时间Q函数使得离策略学习可行，且其贝尔曼方程不依赖于目标策略。
- 基于PhiBE的迭代算法（既包括已知动力学也包括数据驱动）在线性函数近似下可线性收敛到Galerkin解，且收敛速率不随 \(\Delta t \to 0\) 变差。
- 数值实验表明，PhiBE Q-learning逼近精确离散时间迭代的效果随 \(\Delta t\) 减小而提升，且显著优于标准Q-learning（后者误差更大）。
- 在随机动力学下，算法也能稳定收敛，收敛行为受批大小影响。

### 7. 优点

- **理论创新**：首次在连续时间控制中定义支持离策略学习的Q函数，并建立完整的收敛理论。
- **框架统一**：模型基和无模型算法均统一在PhiBE框架下，无需显式估计动力学模型。
- **稳定性**：收敛条件与 \(\Delta t\) 无关，克服了标准离散化RL中随 \(\Delta t \to 0\) 条件恶化的问题。
- **数值验证**：简单LQR实验清晰展示了收敛趋势和与基线方法的差距。

### 8. 不足与局限

- **实验覆盖不足**：仅在一维LQR上测试，未在高维状态/动作空间、复杂非线性系统（如机器人、药物剂量）或实际离策略数据集上验证。
- **线性函数近似的限制**：理论证明依赖线性基函数，非线性近似（如神经网络）的收敛性未讨论。
- **行为策略假设**：均匀行为策略对c0最优，但实际中可能无法实现均匀覆盖；文中也未实验其他行为策略。
- **样本复杂度缺失**：论文明确指出“有限样本误差留待未来工作”，未提供样本复杂度界，在数据有限场景的应用性未知。
- **V的梯度计算**：算法要求计算 \(\nabla V\) 和 \(\nabla^2 V\)，对于离散动作空间或非平滑策略，文中仅建议近似（如softmax或有限差分），可能引入额外误差。
- **实验可重复性**：未给出随机种子或误差条，无法判断结果的统计显著性。

（完）
