---
title: "Ricci-Filtration: Boosting Retrieval-Augmented Generation Reranker to Query-Answer Tasks by Discrete Ricci Flow"
title_zh: 里奇滤波：通过离散里奇流提升检索增强生成重排序器以处理问答任务
authors: "Tian Qin, Wei-Min Huang"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15482v1"
tags: ["query:ml-theory"]
score: 8.0
evidence: 将离散里奇流用于RAG中几何感知的图优化
tldr: 针对检索增强生成中噪声文档干扰重排序的问题，提出基于离散Ricci流的Ricci-Filtration方法。将查询与初始检索块建模为加权图，利用离散曲率和Ricci流评估每个块的结构重要性，过滤掉具有负曲率和大权重的噪声块，再经重排序提升生成质量。理论证明了归一化离散Ricci流能检测社区结构。实验在多个指标上超越基线，消融研究验证了框架在不同架构下的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15482v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1784, \"height\": 432, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1787, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 640, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 855, \"height\": 620, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1757, \"height\": 252, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15482v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1761, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1762, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1773, \"height\": 648, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1761, \"height\": 614, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1767, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1125, \"height\": 206, \"label\": \"Table\"}]"
motivation: 解决RAG中检索块噪声导致生成质量下降的问题。
method: 构建查询与初始检索块的加权图，用离散Ricci流动态调整边权重，根据曲率符号过滤噪声块。
result: 在准确率、精确率、召回率和F1分数上超越多种基线重排序方法。
conclusion: 离散Ricci流能有效检测社区结构，去除噪声块，提升RAG系统鲁棒性。
---

## 摘要
里奇流是一种曲率引导的扩散过程，通过收缩高正曲率区域和扩张负曲率区域来变形空间。类似地，加权图上的离散里奇流通过收缩具有正里奇曲率的边和拉伸具有负里奇曲率的边来修改边权重，有效增加簇之间的分离。受这两项奠基性工作的启发，我们提出了一种基于几何的RAG重排序增强流程，称为里奇滤波。通过将输入查询和初始检索的块建模为一个网络，其中输入查询和块作为节点，基于嵌入的成对关系定义初始图，里奇滤波利用离散曲率和里奇流来评估每个块相对于用户查询的结构重要性。该系统首先根据块相对于查询的几何曲率过滤初始块；然后，重排序器处理剩余块以增强生成性能。我们从理论上证明，归一化离散里奇流可以通过识别边权重的不同渐近行为来检测社区结构。这支持移除那些相对于查询节点具有大权重和负里奇曲率的“噪声”文档块。大量实验证实，里奇滤波在准确率、精确率、召回率和F1分数上优于几种基线重排序方法。此外，消融研究表明，里奇滤波在各种设置下通常优于基线，突出了该框架在不同架构上的鲁棒性。

## Abstract
Ricci flow is a curvature-guided diffusion process that deforms space by shrinking regions of high positive curvature and expanding those with negative curvature. Similarly, discrete Ricci flow on weighted graphs modifies edge weights by shrinking edges with positive Ricci curvature and stretching those with negative Ricci curvature, effectively increasing the separation between clusters. Inspired by these two cornerstone works, we propose a geometry-based RAG reranker enhancement procedure called Ricci-Filtration. By modeling the input query and initial retrieved chunks as a network, where the input query and chunks serve as nodes and embedding-based pairwise relations define an initial graph, Ricci-Filtration leverages discrete curvature and Ricci flow to evaluate the structural importance of each chunk with respect to the user query. The system first filters the initial chunks based on their geometric curvature relative to the query; then, a reranker processes the remaining chunks to enhance generative performance. We theoretically prove that normalized discrete Ricci flow can detect community structures by identifying distinct asymptotic behaviors in edge weights. This supports the removal of ``noisy'' document chunks characterized by large weights and negative Ricci curvature relative to the query node. Extensive experiments confirm that Ricci-Filtration outperforms several baseline reranking methods in accuracy, precision, recall, and F1 scores. Furthermore, ablation studies demonstrate that the Ricci-Filtration generally outperforms the baseline under various settings, highlighting the framework's robustness across different architectures.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：检索增强生成（RAG）系统通过检索外部文档来辅助大型语言模型（LLM）生成回答，但初始检索到的文本块往往包含大量噪声（无关或干扰信息），这些噪声会误导 LLM 产生幻觉。现有的重排序器（如交叉编码器、LLM 重排序器）虽然能提升相关性，但存在固定截断参数 \(k\)（缺乏灵活性）、计算成本高、依赖微调等问题。
- **核心问题**：如何在不增加额外 LLM 调用的情况下，从几何角度自动识别并过滤掉与查询结构不相关的噪声块，从而提升 RAG 系统的生成质量。
- **整体含义**：受离散 Ricci 流在社区检测中“收缩正曲率边、拉伸负曲率边”的启发，本文提出 **Ricci-Filtration** 框架，将查询与初始检索块建模为加权图，利用离散 Ricci 曲率动态评估每个块的结构重要性，先过滤噪声块再由重排序器处理，实现更鲁棒的检索增强生成。

---

### 2. 论文提出的方法论：核心思想、关键技术细节

#### 2.1 核心思想
- 将用户查询和初始检索的候选块视为图中的节点，基于嵌入向量的余弦不相似度构建初始图拓扑。离散 Ricci 流迭代更新边的权重，使“与查询曲率正相关”（即语义相近）的块间的边权重趋近于 0，而“与查询曲率负相关”（即噪声）的块间的边权重大幅增大，从而实现结构分离。

#### 2.2 关键技术细节
- **图构建**：
  - 对每个节点（查询或块）计算嵌入向量 \(z_i\)，定义余弦不相似度 \(r_{ij} = 1 - \cos(z_i, z_j)\)。
  - 使用百分位阈值 \(\tau\)（实验设为 50% 分位数）将 \(r_{ij}\) 二值化为边邻接矩阵 \(B\)，其中 \(B_{ij}=1\) 表示边存在。
  - 强制查询节点与所有候选块节点相连（\(B_{qj}=1\)），初始所有边权重设为 1。
- **离散 Ricci 流迭代**（Algorithm 1）：
  - 使用 Ollivier-Ricci 曲率（式 (1)），概率分布采用 \(\alpha=0.5, p=2\) 的指数形式（式 (2)）。
  - 每一步先归一化边权重（使平均权重为 1），计算当前度量下的曲率 \(\kappa_{xy}^{(i)}\)，然后更新边权重：\(w_{xy}^{(i+1)} = (1 - \varepsilon \kappa_{xy}^{(i)}) w_{xy}^{(i)}\)，步长 \(\varepsilon \in (0,1]\)。
  - 迭代 M=20 次后，对查询节点相连的边，保留最终归一化权重 \(\le 1\)（即低于平均尺度）的块，移除权重 >1 的块（视为噪声）。
- **重排序**：过滤后的剩余块送入标准交叉编码器（如 bge-reranker-base）进行重排序，再送入 LLM 生成答案。
- **理论支撑**：
  - Theorem 3.1：在理想社区图 \(G(a,b)\) 上（均匀邻域 \(\alpha=0, p=0\)）证明归一化 Ricci 流可使社区内边指数收缩快于社区间边。
  - Proposition 3.2：对实验常用参数 \((\alpha=1/2, p=2)\)，在相同图族上证明边权排序维持且非网关社区内边相对收缩。

---

### 3. 实验设计：数据集、基准与对比方法

#### 3.1 数据集
| 数据集 | 类型 | 样本量（随机抽取） |
|--------|------|-------------------|
| SQuAD2.0 | 单跳 QA（含不可回答问题） | 1000 |
| TriviaQA | 单跳 “事实密集” QA | 100 |
| HotPotQA | 多跳推理（hard bridging） | 1000 |
| MultiHop-RAG | 多跳 QA（4 种查询类型：推理/比较/时间/空） | 共 2556 |
| MuSiQue | 多跳组合 QA | 1000（用于消融） |

#### 3.2 对比方法（baseline）
- **No Reranker**：仅取余弦相似度 Top-5 直接生成。
- **Cross-Encoder Reranker**：使用 bge-reranker-base 对 Top-20 候选块重排序后取 Top-5。
- **LLM-based Reranker**：使用 gpt-4o-mini 进行点式评分（prompt 见附录 G），同样取 Top-5。
- **Ricci-Filtration**：在相同 Top-20 候选池上先进行几何过滤，再使用与 Cross-Encoder 相同的重排序器（bge-reranker-base），以控制变量。

#### 3.3 评估指标
- SQuAD2.0、HotPotQA：准确率（Acc）、精确率（P）、召回率（R）、F1。
- TriviaQA、MultiHop-RAG、MuSiQue：仅准确率。
- 生成 LLM：Llama-3.1-8B-Instruct 和 gpt-4o-mini（部分实验）。

---

### 4. 资源与算力
- 论文**未明确说明**训练或推理所使用的 GPU 型号、数量、训练时长等具体硬件信息。
- 仅提到使用开源模型（Llama-3.1-8B-Instruct、bge-reranker-base、OpenAI text-embedding-3-small）和 gpt-4o-mini API，但未披露推理的 GPU 配置或算力消耗。
- 附录 H 提供了各方法在若干任务上的平均运行时间对比，显示 Ricci-Filtration 的延迟（约 8.6–12.4 秒/查询）高于交叉编码器（约 1.1–4.2 秒），低于 LLM 重排序（约 3.8–4.8 秒）但实际 API 等待未计入。

---

### 5. 实验数量与充分性
- **实验组数**：
  - 主线对比：在 3 个数据集（SQuAD2.0、HotPotQA、MultiHop-RAG）上进行 4 种方法 × 2 种生成 LLM 的比较，共约 24 组结果。
  - 消融实验（TriviaQA）：覆盖 14 种不同设置（参数 \(\alpha, p, \tau, M\)、嵌入模型、重排序模型），报告准确率。
  - 额外消融（MuSiQue）：9 种设置。
  - 额外基线（K-means 过滤，附录 F）：2 个数据集对比。
- **充分性与公平性**：
  - **优点**：控制初始检索池一致，对比重排序器时保持下游重排序模型相同；消融覆盖主要超参数和模型选择；包含与简单聚类方法（K-means）的对比。
  - **不足**：样本量较小（TriviaQA 仅 100 例），可能影响统计稳定性；在 HotPotQA 和 MuSiQue 上表现弱于基线，但仅用准确率报告，未深入分析原因；未与更先进的例如基于图神经网络的重排序方法对比；时间效率比较仅 10 个随机问题，代表性有限。

---

### 6. 论文的主要结论与发现
1. **Ricci-Filtration 在单跳 QA（SQuAD2.0）和多跳空查询（MultiHop-RAG Null）上显著优于所有基线**，准确率提升约 5–20%。例如 SQuAD2.0 上 F1 从 66.46%（无重排序）提升至 70.17%（Llama3.1），MultiHop-RAG Null 查询准确率从 60.80% 提升至 83.39%。
2. **在多跳推理（HotPotQA）上性能略有下降**，表明当前过滤机制可能过度保留噪声，需进一步优化以适配多跳场景。
3. **消融实验证实框架对参数 \(p\) 和重排序器模型不敏感**（MS-MARCO-MiniLM-L6-V2、BGE-reranker-base 均有效），但高度依赖嵌入质量（text-embedding-3-small 优于 text-embedding-ada-002）。
4. **理论分析**（Theorem 3.1 & Proposition 3.2）在理想社区图上证明了归一化 Ricci 流可实现边权分离，为过滤机制提供数学支撑。

---

### 7. 优点：方法或实验设计上的亮点
1. **创新性**：首次将离散 Ricci 流引入 RAG 重排序，将几何曲率作为噪声检测准则，无需额外 LLM 调用，提供了一种新的“几何去噪”范式。
2. **动态过滤**：不同于固定 \(k\) 的截断，过滤后剩余块数量由几何结构自动决定，更灵活。
3. **理论分析**：在理想图上证明了社区分离特性，增加了方法的可信度。
4. **实验公平性**：严格控制初始检索池、生成 LLM、评估指标，聚焦于重排序阶段的改进。
5. **充分的消融**：覆盖了节点分布参数、图构建阈值、迭代次数、嵌入模型、重排序模型等多个维度，有助于理解方法鲁棒性。
6. **与简单基线（K-means）对比**：排除了“仅聚类”即可达到类似效果的疑问，凸显 Ricci 流图几何的优势。

---

### 8. 不足与局限
1. **计算开销**：Ricci 流迭代需要求解多次最优传输（Wasserstein 距离），单次查询延迟比交叉编码器高约 8–10 倍（附录 H），不利于实时场景。
2. **多跳推理适应性弱**：在 HotPotQA、MuSiQue 上性能不如交叉编码器，论文归因于过滤后仍保留过多噪声，未给出具体改进策略。
3. **对嵌入质量高度敏感**：消融表明替换嵌入模型（text-embedding-ada-002）后准确率下降 6%，且图构建阈值缺乏自适应方法。
4. **阈值设定为经验值**：初始图构建的 \(\tau=50\%\) 和手术阈值 \(\eta=1\) 均为手工设定，缺乏理论指导，可能在不同分布的数据上失效。
5. **样本量有限**：TriviaQA 仅 100 例，MuSiQue 消融的统计显著性难以评估。
6. **未考虑偏差风险**：论文虽在影响声明中提及偏差放大问题，但未在实验中量化，也缺乏公平性审计。
7. **适用场景限制**：仅针对 QA 任务，未在摘要、对话等其他 RAG 场景验证；大规模知识库下的计算可行性尚未讨论。

--- 

（完）
