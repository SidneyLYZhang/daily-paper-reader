---
title: "SILAGE: Memory-Efficient, Full-Gradient-Free Nonconvex Optimization for Nested Finite Sums"
title_zh: SILAGE：面向嵌套有限和的内存高效、无全梯度非凸优化
authors: "Igor Sokolov, Laurent Condat, Peter Richtárik"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15832v1"
tags: ["query:ml-theory"]
score: 8.0
evidence: 深度学习嵌套有限和的非凸优化
tldr: 非凸嵌套有限和优化中，现有方差缩减方法存在计算或内存瓶颈：PAGE需周期性全局全梯度刷新，SILVER需要O(nm)内存。本文提出SILAGE算法，利用双求和结构主动避免全局刷新，每步最多评估一个局部组梯度，仅需O(n)内存。收敛分析通过组间异质性δ1和组内异质性δ2自适应数据几何，避免悲观Lipschitz常数。在多个实际场景下改进了现有最优界。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15832v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 953, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15832v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1451, \"height\": 954, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15832v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 640, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1621, \"height\": 607, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1267, \"height\": 886, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 954, \"height\": 407, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1416, \"height\": 1082, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1308, \"height\": 475, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1050, \"height\": 427, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1127, \"height\": 495, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1276, \"height\": 900, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1267, \"height\": 696, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 405, \"height\": 245, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 570, \"height\": 404, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 540, \"height\": 405, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15832v1/table-014.webp\", \"caption\": \"\", \"page\": 0, \"index\": 14, \"width\": 540, \"height\": 405, \"label\": \"Table\"}]"
motivation: 现有方差缩减方法在嵌套有限和优化中面临计算开销大（PAGE需全局刷新）或内存开销高（SILVER需O(nm)内存）的权衡。
method: 提出SILAGE算法，利用双求和结构，每步仅评估一个局部组梯度，避免全局全梯度刷新，且内存开销仅为O(n)。
result: 收敛分析表明，SILAGE的复杂度通过组间异质性δ1和组内异质性δ2自适应数据几何，在多个实际场景下优于现有最优界。
conclusion: SILAGE在内存高效的同时实现了全梯度自由，为嵌套有限和优化提供了计算与内存的帕累托最优解。
---

## 摘要
大规模数据集上的经验风险最小化自然地呈现出嵌套的双重有限和结构，其中N=nm个总样本被逻辑上或物理上划分为n个大小为m的块（例如，在汇集数据孤岛、外核学习或有意分层中）。虽然方差缩减方法对于非凸目标实现了最优的oracle复杂度，但它们在这种集中式场景中面临严重的扩展瓶颈。递归估计器，如PAGE，需要定期对所有nm个样本进行全局全梯度刷新，计算成本高昂。相反，单循环方法，如SILVER，避免了这种刷新，但需要不切实际的O(nm)内存来存储每个样本的控制变量。在本文中，我们提出了SILAGE，一种解决这种权衡的方差缩减算法。通过主动利用双和结构，SILAGE消除了对所有nm个组件的定期全局全梯度刷新（每次迭代最多评估一个局部组梯度），同时仅需要O(n)内存。此外，我们提供了严格的收敛分析，避免了悲观的最坏情况Lipschitz常数。相反，SILAGE的复杂度通过嵌套功能相似性自然地适应底层数据几何：组间（δ1）和组内（δ2）异质性。我们的结果在几个实际相关的情况下改进了现有的最先进界。

## Abstract
Empirical risk minimization on massive datasets naturally exhibits a nested double finite-sum structure, where $N=nm$ total samples are logically or physically partitioned into $n$ blocks of size $m$ (e.g., in pooled data silos, out-of-core learning, or deliberate stratification). While variance-reduced methods achieve optimal oracle complexities for nonconvex objectives, they suffer from severe scaling bottlenecks in this centralized regime. Recursive estimators, such as PAGE, require periodic global full-gradient refreshes over all $nm$ samples, which are computationally expensive. Conversely, single-loop methods, such as SILVER, avoid such refreshes but require an impractical $\mathcal{O}(nm)$ memory footprint to store a control variate for every sample. In this paper, we propose SILAGE, a variance-reduced algorithm that addresses this trade-off. By actively exploiting the double-sum structure, SILAGE eliminates periodic global full-gradient refreshes over all $nm$ components (evaluating at most one local group gradient per iteration) while requiring only $\mathcal{O}(n)$ memory. Furthermore, we provide a tight convergence analysis that avoids pessimistic worst-case Lipschitz constants. Instead, SILAGE's complexity natively adapts to the underlying data geometry via nested functional similarities: across-group ($δ_1$) and within-group ($δ_2$) heterogeneity. Our results improve existing state-of-the-art bounds in several practically relevant regimes.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：现代机器学习的大规模经验风险最小化（ERM）自然地呈现嵌套双求和结构：总样本数 \(N = nm\) 被划分为 \(n\) 个组（块/数据孤岛），每组包含 \(m\) 个样本。这种结构出现在数据孤岛聚合、外核学习、刻意分层等场景。
- **现有方法瓶颈**：
  - 递归型方差缩减方法（如 PAGE）需要定期对所有 \(nm\) 个样本做全局全梯度刷新，计算开销巨大。
  - 单循环型方法（如 SILVER）无需全局刷新，但需为每个样本存储一个控制变量，内存占用高达 \(\mathcal{O}(nm)\)，在大规模数据下不现实。
- **核心问题**：在集中式训练中，如何同时避免全局刷新并大幅降低内存占用，同时保持高效的收敛率？论文提出 SILAGE 来解决这一计算与内存的权衡。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用问题的嵌套双求和结构，设计一个**单循环方差缩减估计器**，在一组（\(n\) 个）层面上维护控制变量，每组内部采用类 PAGE 的递归更新。通过精心耦合全局硬币与组选择，确保**每次迭代最多只计算一个局部组梯度**，彻底消除全局全梯度刷新。
- **算法流程（文字说明）**：
  - 维护 \(n\) 个组估计器 \(g_i^t\) 和它们的平均值 \(g^t\)；迭代 \(x^{t+1}=x^t-\gamma g^t\)。
  - **情形一：\(m \ge n\)**（Algorithm 1）
    - 以概率 \(p=n/m\) 抛一枚全局硬币。若硬币正面，则均匀选择一组 \(i_t\)，对该组执行**锚点重置**（计算完整局部梯度 \(\nabla f_{i_t}(x^{t+1})\)）；其余组用递归更新（每个组随机采样一个样本，计算差分）。
    - 若硬币反面，所有组均递归更新。
  - **情形二：\(n > m\)**（Algorithm 2）
    - 每组重置一次（锚点），但仅对 \(b_{\text{grp}}\) 个活动组执行递归更新；对剩余未采样组，使用由活动组计算的**共享漂移** \(d^t\) 统一更新，避免逐组计算。
    - 通过存储偏移量实现高效实现（Algorithm 3），实际每次迭代 \(O(m+b_{\text{grp}})\) 梯度计算。
- **关键技术细节**：
  - **收敛分析**：引入**嵌套相似性**常数：组间异质性 \(\delta_1\)、组内异质性 \(\delta_2\)，避免悲观的最坏情况 Lipschitz 常数 \(L_{\max}\)。
  - 在 \(m\ge n\) 时复杂度仅依赖 \(\delta_2\)（忽略 \(\delta_1\)）；在 \(n>m\) 时两项均有贡献。
  - 统一了 PAGE（当 \(n=1\)）和 SILVER（当 \(m=1\)）。
- **公式概览**（仅示意）：
  - 所需迭代次数复杂度：\( \mathcal{O}\left( (nL+\sqrt{nm}\,\delta_2)\frac{\Psi_0}{\epsilon} \right) \)（\(m\ge n\)，含初始化成本）。

### 3. 实验设计：数据集、Benchmark 与对比方法

- **数据集**：合成生成的分组逻辑回归数据。标签通过教师模型生成，特征由分层潜变量模型构造。共 8 个数据集，覆盖两种长宽比（\(m\ge n\) 和 \(n>m\)），每种长宽比包含四种相似性象限（\(\delta_1\) 小/大，\(\delta_2\) 小/大）。
- **基准（Benchmark）**：采用相同非凸正则化逻辑回归目标函数（\(\lambda=200\)），在所有方法上使用理论上允许的最大恒定步长（基于经验估计的常数）。
- **对比方法**：
  - ZeroSARAH（扁平化）
  - SILVER（扁平化）
  - D-ZeroSARAH（嵌套分布式变体）
  - 故意省略了 PAGE/D-PAGE（因其依赖全局刷新，作为理论对比而非实验对比）。
- **衡量标准**：绘制梯度范数平方 \(\|\nabla f(x^t)\|^2\) 随 epoch 变化的收敛曲线，直至降至 \(10^{-12}\) 或达到 40 epoch 预算。

### 4. 资源与算力

- **硬件**：双 Intel Xeon Gold 6246 CPU（24 核/48 线程）+ Nvidia GeForce RTX 3090（24 GB）和 RTX 2080 Ti（12 GB）。
- **训练时长**：文中未明确报告每次实验的具体运行时间，仅说明整体实验在单台工作站上完成。
- **算力说明**：由于数据为合成小规模（\(n=50\) 或 \(250\)，\(d=1000\)），实验计算量不大，无需大规模集群。

### 5. 实验数量与充分性

- **实验组数**：8 个数据集（2 种长宽比 × 4 种相似性象限）× 4 种对比方法 + SILAGE 两种变体 = 约 40 条收敛曲线。此外包含超参数网格搜索（batch size 调优）。
- **充分性**：
  - 实验设计**聚焦于验证理论预测**：通过控制 \(\delta_1,\delta_2\) 的相对大小，检查 SILAGE 是否如理论所示分别对 \(\delta_2\)（\(m\ge n\) 时）和两者（\(n>m\)）敏感。
  - 对比方法均为同类无全局刷新方法，公平性较好（使用相同步长公式、相同网格搜索规则）。
  - **局限**：实验为合成数据，未在真实大规模数据集（如 ImageNet、C4）上测试，也未测量端到端壁钟时间，因此对实用性的证明较弱。

### 6. 论文的主要结论与发现

- **理论优势**：SILAGE 在保持 \(\mathcal{O}(n)\) 内存的同时，完全避免了周期性全局全梯度刷新，并利用嵌套相似性（\(\delta_1,\delta_2\)）获得了比基于 \(L_{\max}\) 或扁平相似性更紧的复杂度界。
- **实验验证**：
  - 在 \(m\ge n\) 时，SILAGE 的收敛速度对 \(\delta_1\) 不敏感，即使 \(\delta_1\) 很大也能快速收敛；而 \(\delta_2\) 增加时难度增加。
  - 在 \(n>m\) 时，SILAGE 在所有四种相似性配置下均优于或与最佳基线相当。
  - 对比 ZeroSARAH（依赖 \(L_{\max}\)）和 D-ZeroSARAH，SILAGE 优势尤其明显。
- **统一性**：SILAGE 在边界条件下退化为 SILVER（\(m=1\)）和 PAGE（\(n=1\)）。

### 7. 优点

- **内存高效**：仅需 \(\mathcal{O}(n)\) 内存（存一个控制变量/组），远小于 SILVER 的 \(\mathcal{O}(nm)\)。
- **计算高效**：无需全局刷新，每次迭代最多计算一个完整组梯度（\(O(m)\)）。
- **理论紧致**：收敛界自然地适应数据的嵌套几何，避免悲观 Lipschitz 常数，在多种数据异质性场景下优于现有界。
- **结构通用**：适用于数据孤岛、外核学习、刻意分层等多种实际设置，并可扩展至 Polyak–Łojasiewicz 条件下的线性收敛。
- **算法普适**：可接受任意初始估计（无需初始全梯度计算）。

### 8. 不足与局限

- **实验规模**：仅使用合成的逻辑回归数据集（维度 \(d=1000\)），未在 ImageNet、C4 等真实大规模数据集或深度神经网络上验证。
- **效率评估**：仅报告 epoch 数，未报告壁钟时间或实际内存消耗，因此无法直接评估系统级加速。
- **常数依赖**：虽然算法本身不依赖未知常数，但步长选取需要估计 \(\delta_1,\delta_2\)，文中采用探针集 + 理论公式，实际使用可能需要近似或网格搜索。
- **对比缺失**：未与同样避免全局刷新的 SAGA/SVRG 变体（如 SAGA 及其分布式版本）比较（可能因为其内存占用同样为 O(nm)）。
- **分析复杂性**：双相似性参数的引入虽使理论更精细，但也增加了对非专业读者的理解门槛。
- **工业适用性**：共享漂移在 \(n>m\) 时的更新策略在组高度异质时可能效率降低（需选择合适 \(b_{\text{grp}}\)）。

（完）
