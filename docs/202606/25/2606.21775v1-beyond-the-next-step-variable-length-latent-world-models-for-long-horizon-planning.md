---
title: "Beyond the Next Step: Variable-Length Latent World Models for Long-Horizon Planning"
title_zh: 超越下一步：面向长时域规划的变长潜在世界模型
authors: "Tianqi Du, Qi Zhang, Yifei Wang, Yisen Wang"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21775v1"
tags: ["query:rl-semantic"]
score: 9.0
evidence: 用于强化学习规划的变长潜世界模型
tldr: "现有潜在世界模型通常依赖单步预测并递归展开，导致长程规划时累积误差严重。本文提出变长潜在世界模型(VLWMs)，直接学习动作序列的变长潜在状态预测，并采用课程训练逐步扩展预测长度。实验表明，VLWMs在长程控制任务上平均提升13%，尤其在长程规划任务上增益显著。该方法为潜在空间的长程预测与规划提供了简单有效的改进范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21775v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1350, \"height\": 807, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21775v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 355, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21775v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1329, \"height\": 678, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21775v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1161, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21775v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1198, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21775v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1019, \"height\": 224, \"label\": \"Table\"}]"
motivation: 现有潜在世界模型仅训练单步预测，递归展开导致累积误差，且训练目标与长程规划不匹配。
method: 提出VLWMs，学习变长动作序列的直接潜在状态预测，并采用课程训练从短程到长程逐步优化。
result: "在长程控制任务上平均提升13%，尤其在需要长程规划的任务上增益显著。"
conclusion: VLWMs为潜在世界模型的长程预测与规划提供了简单有效的改进范式。
---

## 摘要
近年来，世界模型通过学习基于观测和动作预测未来环境状态的预测模型，已成为构建智能代理的有前途范式。特别是，JEPA风格的潜在世界模型通过在紧凑表示空间中学习基于动作的动力学，提供了像素空间预测的有效替代方案。然而，现有的潜在世界模型通常依赖于单步预测，并且必须递归展开以实现长时域规划，这导致误差累积以及训练目标与下游规划任务之间的不匹配。为解决这一局限性，我们提出了变长潜在世界模型（VLWMs），该框架学习预测基于变长动作序列的未来潜在状态。VLWMs 不仅训练于单步转移，还直接建模时间扩展的动力学，使得同一预测器能够评估不同时域的动作计划。我们进一步引入课程训练策略，逐步扩展动作时域，从短程动力学优化到长程预测。在测试时，我们设计了针对 VLWMs 的规划方法，以更好地利用其变长预测能力。在长时域控制任务上的实验表明，VLWMs 显著改进了潜在空间世界模型，在不同数据集上相比当前最先进的 LeWM 平均提升 13%，尤其在需要扩展规划的任务上取得了更大收益。这些结果表明，VLWM 为改进潜在世界模型中的长时域预测与规划提供了一种简单而有效的范式。

## Abstract
Recently, world models have emerged as a promising paradigm for building intelligent agents by learning predictive models that estimate future environment states conditioned on observations and actions. In particular, JEPA-style latent world models provide an efficient alternative to pixel space prediction by learning action-conditioned dynamics in compact representation spaces. However, existing latent world models typically rely on one-step prediction and must be recursively rolled out for long-horizon planning, which leads to compounding errors and a mismatch between training objectives and downstream planning tasks. To address this limitation, we propose Variable-length Latent World Models (VLWMs), a framework that learns to predict future latent states conditioned on action sequences of variable lengths. Instead of training only on one-step transitions, VLWMs directly model temporally extended dynamics, allowing the same predictor to evaluate action plans over different horizons. We further introduce a curriculum training strategy that progressively expands the action horizon, stabilizing optimization from short-range dynamics to long-range prediction. At test time, we design planning methods tailored to VLWMs to better exploit their variable-length predictive capabilities. Experiments on long-horizon control tasks show that VLWMs significantly improve latent space world models, achieving 13\% average improvement over the state-of-the-art LeWM across different datasets, with especially large gains on tasks requiring extended planning. These results suggest that VLWM provides a simple yet effective paradigm for improving long-horizon prediction and planning in latent world models.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有潜在世界模型（如JEPA、DINO-WM等）通常采用**单步预测**训练，即每次只预测下一时刻的潜在状态。在长时域规划时，这类模型必须递归展开大量单步预测，导致**累积误差**迅速增长，规划质量严重下降。此外，单步训练目标与长程规划任务之间存在**目标不匹配**，且相邻帧在时间上高度冗余，使得模型容易学习到短期的插值或近恒等映射，而非高级动力学。
- **整体含义**：论文旨在打破单步预测的限制，提出一种**直接学习变长时序动力学**的潜在世界模型框架（VLWM），使得同一模型既能处理短时域细粒度控制，又能通过一次“跳跃”预测多个时间步后的状态，从而**减少递归步数、缓解误差累积**，并为长时域规划提供更灵活的接口。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：训练一个单一的预测器，使其能够**基于任意长度的动作段**，直接预测未来对应时距的潜在状态。即，输入当前潜在状态 $s_t$ 和长度为 $k$ 的动作序列 $a_{t:t+k-1}$，输出 $\hat{s}_{t+k}$。
- **关键技术细节**：
  - **变长监督训练目标**：
    \[
    \mathcal{L}_{\text{VLWM}} = \mathbb{E}_{t, k \sim p(k)} \left\| g_\phi(s_t, a_{t:t+k-1}) - \text{sg}\big( \bar{f}_{\bar{\theta}}(o_{t+k}) \big) \right\|_2^2
    \]
    其中 $p(k)$ 是时距采样分布（由课程训练控制）。传统模型是 $p(k)=\delta_{k=1}$ 的特例。
  - **动作即令牌（Action-as-Token）架构**：将每个动作嵌入视为一个独立的Transformer令牌，与状态令牌拼接成单一序列：`[s_{t-L+1}, ..., s_t, e_t, e_{t+1}, ..., e_{t+k-1}]`。因果Transformer读取该序列，直接输出最后一个令牌对应的 $\hat{s}_{t+k}$。无需专门的[PRED]或掩码令牌，通过添加类型嵌入和共享绝对位置嵌入实现任意长度支持。
  - **课程训练策略**：将训练过程分为 $K_{\max}$ 个阶段，每个阶段逐步扩展允许的最大预测时距。第 $j$ 阶段从 $\{1,\dots,j\}$ 中均匀采样 $k$。避免直接从头训练长距离预测的不稳定性。
  - **规划方法**：在MPC框架内，将规划时域 $H$ 划分为多个变长块 $H = k_1 + k_2 + \cdots + k_M$，每次用VLWM直接预测 $k_i$ 步后的状态。设计了三种规划器：
    - P1：固定块大小（任务自适应选择 $k^\star$）
    - P2：随机采样块大小
    - P3：长到短（开始用大块，逐步缩小块大小）

### 3. 实验设计：数据集、场景、基准、对比方法

- **数据与环境**：使用三个规划基准环境：
  - **PushT**：2D推动操作任务，20000条专家轨迹，平均196步。
  - **OGBench-Cube**：3D机器人操作（抓取并放置方块），10000条轨迹，每段200步。
  - **TwoRoom**：2D导航（两个房间通过门连接），10000条轨迹，平均92步。
- **评估协议**：目标条件规划，给定初始观测 $o_0$ 和目标观测 $o_g$（来自同一轨迹，偏移 $\Delta\in\{25,50,75,100\}$ 步后），使用CEM求解器在潜在空间中执行MPC。成功由环境特定判据确定。评价指标为成功率。
- **对比方法**：主要对比当前最先进的潜在世界模型 **LeWM**（单步预测基线）。同时对比VLWM内部不同规划策略（P1/P2/P3）以及有无课程训练。

### 4. 资源与算力

- 论文中**未明确说明**使用的GPU型号、数量及训练时长。仅在实现细节中提到使用bfloat16混合精度、batch size 128、训练10个epoch等。可以推断实验在常见GPU集群（如A100/V100）上运行，但具体算力信息未披露。

### 5. 实验数量与充分性

- **实验数量**：
  - 主实验：3个环境 × 4个目标偏移（25/50/75/100），报告成功率（图2）。
  - 规划策略消融：表1详细给出了LeWM和三种VLWM规划变体（P1固定k=3/5、P2随机、P3长到短k=3/5）在全部12个设置上的结果。
  - 课程训练消融：表2对比有无课程策略在12个设置上的效果。
  - 潜在表示质量分析：表3在PushT上使用线性探针预测三个物理属性（agent位置、块位置、块角度），报告MSE和Pearson相关系数。
  - 可视化：图3展示TwoRoom上VLWM与LeWM的滚动轨迹对比。
- **充分性与公平性**：
  - 所有环境、数据、评估协议、CEM求解器超参数与LeWM保持一致（除VLWM特有改动外），确保了直接可比性。
  - 报告了不同偏移下的性能，覆盖短、中、长程规划，具有代表性。
  - 消融实验全面：证明了课程训练的必要性和不同规划策略的适用场景。
  - 潜在表示探针实验进一步支持VLWM编码器学到了更丰富的语义结构。
  - 不足：仅在三个环境上验证，未涉及更复杂的真实机器人任务或大规模视频数据；OGBench-Cube上VLWM提升较小，说明对某些任务长程监督增益有限。

### 6. 论文的主要结论与发现

- VLWM在所有三个数据集、所有目标偏移上**一致优于**单步预测基线LeWM，尤其在长时域（$\Delta=75,100$）提升显著（例如TwoRoom-100成功率从36%升至68%）。
- **课程训练**对长时域关键任务（PushT、TwoRoom）至关重要，直接训练全范围导致性能下降；但在OGBench-Cube上效果不明显（该任务本身长程难度不随偏移增长）。
- **变长规划策略**（P1/P2/P3）均优于固定单步规划，但没有一种策略普遍最优，表明需要在线自适应选择块大小。
- VLWM的潜在表示在线性探针评估中优于LeWM，尤其在需要捕捉慢语义变化（如块角度）的属性上优势最大。
- 整体而言，VLWM为潜在世界模型的长程预测与规划提供了一种**简单、有效、可扩展的改进范式**。

### 7. 优点

- **方法创新性**：首次在潜在世界模型中系统地引入变长预测监督，将单步预测扩展为任意步预测，从根本上缓解累积误差。
- **架构简洁**：通过动作令牌化统一处理变长动作序列，避免了复杂的动作融合机制，架构通用性强（兼容JEPA/DINO等编码器）。
- **训练策略合理**：课程训练从短到长逐步扩展，保证了优化稳定性。
- **规划灵活性**：允许在同一模型内实现粗到细、随机等多种规划模式，为层次化规划提供了新视角。
- **实验全面**：覆盖不同领域（2D/3D、导航/操控），且有消融和探针分析，证据链完整。

### 8. 不足与局限

- **环境多样性有限**：仅在三个模拟环境上验证，缺少真实机器人平台、大规模视频数据或复杂开放世界场景的测试。
- **计算资源未报告**：未说明具体GPU型号、数量及训练时间，不利于可重复性和资源预算参考。
- **OGBench-Cube上提升不明显**：说明变长监督并非对所有任务都有效，其收益依赖于任务的长程难度。
- **规划策略缺乏自适应机制**：目前P1/P2/P3需手工选择或随机采样，论文未提出根据当前状态在线调整块大小的动态方法。
- **理论分析欠缺**：未从理论上解释为何变长监督能改善长程预测，或误差累积减小的上界。
- **与更复杂的世界模型（如Think-JEPA、Causal-JEPA）未作比较**：对比仅限LeWM，更全面的baseline对比可增强说服力。
- **长期时间范围限制**：最大预测时距 $K_{\max}=5$（对应25环境步），对于超长时域（数百步）规划是否仍有效需进一步验证。

（完）
