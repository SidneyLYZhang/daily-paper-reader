---
title: "Generalization in Deep Neural Networks: Minimax Rates for Gradient Methods"
title_zh: 深度神经网络的泛化：梯度方法的极小化最优速率
authors: "Junyu Zhou, Puyu Wang, Yunwen Lei, Marius Kloft, Yiming Ying"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06772v1"
tags: ["query:ml-theory"]
score: 8.0
evidence: 使用梯度方法的深度神经网络泛化分析
tldr: 深层神经网络的泛化性能是深度学习理论的核心问题，但回归任务的理论仍不完善。本文建立了光滑激活的深度网络经梯度方法与核方法学习动力学的联系，首次推导出GD和SGD在过参数化情况下的极小化最优过剩风险速率。结果表明，足够宽度下DNN可达到与核方法相当的泛化性能，为理解深度学习的统计性质提供了重要进展。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06772v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1481, \"height\": 507, \"label\": \"Table\"}]"
motivation: 深层神经网络的统计泛化性质在回归任务中仍缺乏理论理解，亟需建立其与梯度方法训练的关系。
method: 建立光滑激活的DNN经梯度下降/随机梯度下降训练与核方法学习动力学的等效性，从而继承核方法的最优性。
result: 首次导出GD和SGD在过参数化DNN上的极小化最优过剩风险速率，且网络宽度与样本量多项式相关。
conclusion: 足够宽度下，梯度训练DNN的泛化性能可媲美核方法，为理论解释提供关键连接。
---

## 摘要
理解过参数化神经网络的泛化性能已成为深度学习理论的核心课题。尽管近期进展（尤其是神经正切核（NTK）框架下的研究）揭示了浅层架构的行为，但深度神经网络（DNN）的统计泛化性质，特别是在回归任务中，仍远未得到充分理解。在本文中，我们通过提供使用梯度方法训练的DNN的全面泛化分析，在缩小这一差距方面取得了重大进展。首先，我们首次建立了使用梯度方法训练的具有光滑激活函数的DNN的学习动力学与核方法的学习动力学之间的关键联系，表明过参数化DNN上的梯度方法可以完全继承其对应核方法有利的学习动力学。基于这一联系以及核方法已确立的最优性，我们推导出了在假设网络宽度与样本规模呈多项式缩放关系下，梯度下降（GD）和随机梯度下降（SGD）的超出总体风险的首个已知极小化最优速率。我们的结果表明，在足够宽度下，通过GD或SGD训练的DNN能够实现与基于核的方法相当的泛化性能。

## Abstract
Understanding the generalization performance of over-parameterized neural networks has become a central topic in deep learning theory. While recent advances, particularly works under the Neural Tangent Kernel (NTK) regime, have shed light on the behavior of shallow architectures, the statistical generalization properties of deep neural networks (DNNs), especially in regression tasks, remain far less understood. In this paper, we make significant progress toward closing this gap by providing a comprehensive generalization analysis of DNNs trained using gradient-based methods. First, we establish, for the first time, a crucial connection between the learning dynamics of a DNN with smooth activation functions trained via gradient-based methods and those of kernel methods, showing that gradient-based methods on over-parameterized DNNs can fully inherit the favorable learning dynamics of their kernel counterparts. Building on this connection and the well-established optimality of kernel methods, we derive the first known minimax-optimal rates for the excess population risk of both gradient descent (GD) and stochastic gradient descent (SGD), under the assumption that network width scales polynomially with the sample size. Our results demonstrate that, with sufficient width, DNNs trained by GD or SGD can achieve generalization performance comparable to kernel-based methods.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的详细中文总结，已严格按照您要求的要点和格式组织。

### 论文核心问题与整体含义（研究动机和背景）

深度学习的核心谜题之一是过参数化的神经网络如何能在实现接近零的训练误差的同时，还能对未见过的数据具有良好的泛化能力。虽然神经正切核（NTK）理论为理解浅层神经网络的训练动态和泛化性能提供了重要工具，并揭示了梯度方法训练的网络与核方法之间的深刻联系，但对于更深层结构的神经网络（DNN），尤其在**回归任务**中的统计泛化性质，理论上仍远未得到充分理解。现有研究主要局限于浅层网络，它们为浅层网络提供了极小化最优的过剩风险速率，但对深度网络能否达到同样优越的性能，此前是悬而未决的问题。

本文的核心研究动机即在于回答这一关键问题：**使用梯度下降（GD）和随机梯度下降（SGD）训练的深度神经网络，是否能够实现与其核方法对应物（associated kernel methods）可比的泛化性能？** 本文对此给出了肯定的答案，从而显著加深了对深度架构中梯度方法泛化行为的理解。

### 方法论：核心思想、关键技术细节

#### 核心思想

1.  **建立深度网络与核方法学习动力学的连接**：本文首次建立了使用GD/SGD训练的、具有光滑激活函数的过参数化DNN，与由**无限宽NTK**定义的再生核希尔伯特空间（RKHS）中核梯度下降迭代 `g_T` 之间的关键联系。通过这种联系，DNN可以完全继承核方法有利的学习动力学特性。
2.  **新颖的误差分解**：针对深层网络，作者并未采用浅层网络中常用的、直接控制有限宽NTK对应的RKHS内学习动态与目标函数距离的分解方式（该方式难以扩展到深层网络），而是提出了一种新的分解：
    `ε_risk(f_W(T)) ≲ ∥f_W(T) - Sg_T∥²_ρ + ∥Sg_T - f_ρ∥²_ρ`
    其中，第一项衡量DNN学习动态与**无限宽NTK核GD迭代**的差距；第二项衡量核GD迭代与目标函数`f_ρ`的误差。这种分解使得作者可以分别利用对过参数化DNN轨迹的精细控制和已有的核方法最优性结果。

#### 关键技术细节

1.  **NTK均匀浓度的精细分析**：与先前工作（如Jacob等人的工作）相比，本文改进了NTK的均匀收敛率，从之前的`O(1/m^{1/6})`提升至`O(1/√m)`。这一更快的收敛率对于获得极小化最优的过剩风险速率至关重要。
2.  **GD/SGD轨迹的精细控制**：通过精心设计的双归纳法（double induction），对GD/SGD离开初始化的距离提供了更精确的分析，得到了一个与网络宽度 `m` 无关的更紧的上界，从而更准确地估计了DNN学习动态与核方法学习动态之间的差距。
3.  **关键命题：连接DNN与核方法**：
    *   **对于GD（命题1）**：在过参数化条件下（宽度 `m` 足够大），以高概率成立：
        `∥f_W(T) - Sg_T∥²_ρ ≲ C_L σ d (ηT)^4 log²(m) / m`
        这表明随着网络宽度 `m` 增加，DNN的输出非常接近由无限宽NTK驱动的核GD输出。
    *   **对于SGD（命题3）**：在类似条件下，以高概率成立：
        `E_A[∥f_W(T) - Sg_T∥²_ρ] ≲ C_L σ M d (ηT)^4 log²(m) / m + Mη log(T)`
        其中 `E_A` 表示对SGD随机挑选样本过程的期望。这表明DNN的SGD动力学同样能很好地被核GD迭代逼近。

### 实验设计

本文是**纯理论论文**，**不包含任何实验**。文中没有使用任何数据集、benchmark或与其他方法进行实验对比。所有结论均通过严格的数学推导证明得出。

### 资源与算力

无。本文为纯理论工作，不涉及计算资源的使用。

### 实验数量与充分性

不适用。由于论文不包含实验，因此不存在实验数量或充分性问题。其结论的可靠性完全依赖于理论证明的严谨性。

### 主要结论与发现

1.  **首次建立了深度网络与无限宽NTK核方法的关键联系**：证明了过参数化DNN（光滑激活函数）经GD/SGD训练后，其学习动态可以完全由核GD（在无限宽NTK对应的RKHS中）的迭代行为所刻画。
2.  **首次推导了GD和SGD的极小化最优速率**：
    *   在回归问题下，当网络宽度 `m` 与样本量 `n` 和输入维度 `d` 呈多项式关系时，GD和SGD都能达到与经典核方法相同的**极小化最优过剩风险速率 `O(n^{-2β / (2β+γ)})`**（当 `2β+γ > 1` 时）。其中 `β` 是目标函数的平滑度，`γ` 测量RKHS容量。
    *   对于GD，要求 `m ≳ C_L σ d n^{(2β+4)/(2β+γ)}`，步数 `T = n^{1/(2β+γ)}`。
    *   对于SGD，要求 `m ≳ C_L σ d n^{(2β+4)/(2β+γ)}`，步数 `T = n^{(2β+1)/(2β+γ)}` 和特定的衰减步长。
3.  **结果具有很强的灵活性**：建立连接后，研究结果可以很容易地扩展到更广泛的学习场景（如 `2β+γ ≤ 1` 的困难问题），只需利用不同假设下已有的核方法结果即可。

### 优点与亮点

1.  **理论贡献重要**：填补了对深度神经网络（而不仅仅是浅层网络）泛化性能理论理解的空白，回答了领域内的一个关键开放性问题。
2.  **技术原创性强**：
    *   提出的**新颖误差分解**巧妙地绕过了将浅层网络分析技术（如处理 `H_m` 和 `H_K` 的有效维度）扩展到深层网络时遇到的困难（层间耦合问题）。
    *   将NTK的**均匀收敛率从 `m^{-1/6}` 提升到 `m^{-1/2}`**，这一更优的速率是获得最终最优速率的关键。
    *   对GD/SGD轨迹的精细控制，使其上界独立于网络宽度 `m`，这对于理论推导非常重要。
3.  **结论清晰有力**：证明了过参数化的DNN在梯度方法下可以获得与统计学习理论中核方法同等的最优泛化性能，为深度学习在实际中的有效性提供了更强的理论支撑。

### 不足与局限

1.  **NTK框架的局限性**：论文本身承认了基于NTK方法的一个潜在局限——它依赖于线性近似来评估网络在训练期间的性能。在实际应用中，当训练迭代次数很大或网络宽度不够充分时，局部线性性可能不成立。本文的结论依赖于适度的迭代次数。
2.  **网络深度的影响**：理论结果中，泛化误差界限中包含一个随深度 `L` 指数增长的因子 `C_L σ`，这暗示网络越深，理论保证越弱。这与实际中更深模型训练更难的观察一致，但指数依赖可能并非最优，作者也指出了这是未来工作的方向。
3.  **激活函数限制**：分析严格依赖于激活函数是光滑的（二阶可导且有界），对于像ReLU这样的非光滑激活函数，结论不能直接适用。
4.  **架构限制**：分析主要集中在全连接前馈网络上，尚未扩展到卷积网络或残差网络等更复杂的架构。
5.  **实验缺失**：作为纯理论工作，没有实验验证。虽然数学证明严谨，但缺少对理论结论（例如，在有限宽网络和有限样本下实际泛化性能与理论界是否接近）的实证支持。

（完）
