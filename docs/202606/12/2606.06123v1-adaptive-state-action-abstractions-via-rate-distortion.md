---
title: Adaptive state-action abstractions via rate-distortion
title_zh: 基于率失真的自适应状态-动作抽象
authors: Fernando E. Rosas
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06123v1"
tags: ["query:rl-semantic"]
score: 8.0
evidence: 基于率失真理论的强化学习自适应状态-动作抽象
tldr: 强化学习缺乏动态调整状态-动作抽象粒度的通用原则。本文基于率失真理论提出自适应抽象原则，当学习误差与抽象误差相当时细化抽象。通过性能证书分解值误差，实现软状态-动作抽象。在表格环境中验证，即使大量压缩也能达到近最优性能。贡献在于提供了动态调整抽象级的理论原则与实现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06123v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1435, \"height\": 1186, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06123v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1382, \"height\": 427, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06123v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06123v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1395, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06123v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1348, \"height\": 491, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06123v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1362, \"height\": 1167, \"label\": \"Table\"}]"
motivation: 强化学习缺少在训练中根据学习进展动态调整问题抽象粒度的通用原则。
method: 基于性能证书将值误差分解为Bellman残差和bisimulation度量，并利用率失真原理构建可连续调整分辨率的软状态-动作抽象。
result: 在表格环境中，所提自适应抽象策略能在高压缩率下保持近最优性能。
conclusion: 通过率失真原理实现自适应抽象粒度切换，为构建高效的层级强化学习提供了新思路。
---

## 摘要
当学习走路时，婴儿似乎先解决一个粗略版本的问题——保持直立，接近照顾者——只有当在该分辨率下进一步练习不再有收益时，才会对其进行细化。强化学习提供了多种构建复杂任务简化版本的技术，但缺乏在动态学习过程中调整这些抽象粒度的通用原则。本文提出了这样一个原则：一旦抽象内部的学习误差与抽象本身引起的误差相当，就立即细化抽象。我们通过一个性能证书来研究形式化这一原则的一种方法，该证书将价值误差分解为两项：由贝尔曼残差捕捉的学习误差界，以及由双模拟度量给出的抽象误差界。由此产生的切换策略通过基于率失真原理构建的软状态-动作抽象来实现，其状态和动作轴上的分辨率可以连续调整。我们在多种表格化设置中验证了这种构建，表明在状态和动作信息有损压缩显著的情况下，仍能实现接近最优的性能。

## Abstract
When learning to walk, infants seem to address a coarse version of the problem first - stay upright, reach the caregiver - and refine it only when further practice at that resolution stops paying off. Reinforcement learning offers multiple techniques for building simple versions of complex tasks, but lacks general principles for how to dynamically adjust the granularity of these abstractions during learning. This paper proposes one such principle: refine the abstraction as soon as the learning error within it becomes comparable to the error induced by the abstraction itself. Here, we investigate one way of formalising this principle via a performance certificate that decomposes value error into two terms: a learning error bound captured by a Bellman residual, and an abstraction error bound given by a bisimulation metric. The resulting switching strategy is implemented by soft state-action abstractions built from rate-distortion principles, whose resolution along state and action axes can be continuously adjusted. We validate this construction in a range of tabular settings, showing that near-optimal performance can be achieved under substantial lossy compression of state and action information.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：强化学习（RL）缺乏在训练过程中根据学习进展动态调整状态‑动作抽象粒度的通用原则。现有的抽象方法（如双模拟、MDP同态）通常是静态的（硬分配），或仅在状态侧进行细化，而无法联合调整状态和动作抽象的分辨率。
- **研究动机**：生物学习（如婴儿学走路）遵循“先解决粗略版本，再在必要时细化”的机制。该工作希望为RL提供类似的原理，使抽象粒度随学习进程自适应地变化。
- **整体含义**：通过将价值误差分解为学习误差（Bellman残差）和抽象误差（双模拟失真），提出一个简单的切换规则——当学习误差下降到抽象误差水平时，立即细化抽象。这一原则通过率失真理论构建的**软状态‑动作抽象**族实现，可连续调节分辨率，并在多个表格控制任务中验证了有效性。

### 2. 论文提出的方法论

- **核心思想**：
  - **学习‑抽象分解**（Theorem 1）：任何由抽象策略导出的价值函数与最优价值函数之间的误差上界可分解为两项：
    - 学习误差：抽象MDP内的Bellman残差 \(\|\bar{T}^\dagger \bar{V} - \bar{V}\|_\infty\)；
    - 抽象误差：由状态往返核（round‑trip kernel）诱导的Hausdorff失真 \(\Delta_H(\kappa_S; d_V)\)。
  - **自适应原则**：当学习误差降至与当前抽象误差相当时，提高抽象分辨率。
- **关键技术细节**：
  - **软状态‑动作抽象**（Definition 1）：用随机编码器 \(\nu_S(s|\bar{s})\)、\(\nu_A(\bar{a}|s,a)\) 和解码器 \(\eta\) 构建抽象MDP \(\langle \bar{S},\bar{A},\bar{P},\bar{r},\gamma\rangle\)，是MDP同态的随机推广。
  - **率‑失真构建**（Eq. 18）：在状态信息率 \(I(\bar{S};S)\) 和动作条件信息率 \(I(\bar{A};S,A|\bar{S})\) 约束下，最小化平均失真 \(\Delta_\mu\)；引入 \(\beta\)（温度参数）控制分辨率，\(\lambda\) 平衡状态/动作压缩。
  - **自适应算法**（Sec. 5.2）：从低 \(\beta\)（粗粒度）开始，逐步增加 \(\beta\)，在每一步计划时监测Bellman残差；当残差低于当前失真 \(\bar{\Delta}_\beta\) 时，切换到下一更细粒度抽象。采用Blahut‑Arimoto交替优化求解编码器和解码器。
- **公式/算法流程**（文字说明）：给定MDP和参考分布 \(\mu\)，先估计双模拟失真 \(d\)；固定抽象字母表 \(\bar{S},\bar{A}\)，初始化 \(\beta_0\)；求解率失真问题得到当前编码器和解码器；在抽象MDP上进行Bellman迭代，计算残差并与失真比较；若残差≤失真，则增加 \(\beta\) 继续；否则保持当前抽象继续学习。

### 3. 实验设计

- **使用的数据集/场景**：
  - **Four Rooms**（11×11，4个目标，416状态，4动作）：测试状态压缩。
  - **Taxi**（5×5，501状态，6动作）：测试动作压缩。
  - **DoorKey**（MiniGrid启发，233状态，5动作）：测试状态+动作联合压缩。
  - **SysAdmin**（环形结构，2~7台机器）：测试扩展性和局部结构利用。
- **Benchmark**：无标准基准训练集，采用自身的全分辨率最优策略作为性能上限。
- **对比方法**：
  - 精确状态双模拟（State bisimulation）
  - 精确MDP同态（MDP homomorphism，即状态‑动作双模拟）
  - 固定分辨率抽象（不同β的静态软抽象）
  - 自适应策略自身在不同β下的轨迹
- **评价指标**：策略回报（精确值）、压缩率（信息等价尺寸 normalized）、失真（平均二次抽样距离）。

### 4. 资源与算力

论文未明确说明使用的计算资源（GPU型号、数量、训练时长）。由于所有实验均为表格环境，MDP已知，且采用精确Bellman更新和Blahut‑Arimoto算法，可以推断所有实验均在CPU上完成，计算量较小（每域几分钟至数小时量级）。文中未提及任何GPU资源。

### 5. 实验数量与充分性

- **实验数量**：共四个域，每个域内使用不同β阶梯（3~5个固定β + 自适应序列）。SysAdmin上扩展至N=2~7共6种规模。此外，表1给出了三个域（Four Rooms, Taxi, DoorKey）的压缩诊断对比（三个基准对照）；表2给出了SysAdmin的因子探针（10个候选因子）。
- **充分性评价**：
  - **优势**：覆盖了多种压缩类型（状态主导、动作主导、联合），并增加SysAdmin的规模扩展实验，验证了方法的扩展性和结构化特征提取能力。
  - **局限**：
    - 所有实验均为**表格设定**，未涉及函数近似或深度强化学习，限制了结论在复杂连续控制问题中的推广。
    - 未提供**消融实验**（如改变λ值、不同初始化、不同失真度量、不同细化阈值的影响）。
    - 对比方法仅包括**精确**双模拟/同态（它们在最优价值下无误差），未与**近似自适应**方法（如Ortner 2013、CEGAR等）直接比较，导致对比不够公平（静态精确方法计算量更大且不动态）。
    - 每个域只给出了代表性结果曲线（图1），未报告多次随机种子下的统计量（如均值、方差），但考虑到问题是确定性的（MDP已知），一个运行可代表确定性结果。
- **总体**：实验设计聚焦于概念验证，能支撑主要论点，但充分性一般，缺乏更广泛的消融和统计验证。

### 6. 论文的主要结论与发现

- 所提出的**学习‑抽象分解**（Theorem 1）有效地区分了两种误差来源，并提供了直观的自适应原则。
- **率失真构建**产生的软抽象族存在清晰的压缩‑失真前沿（Pareto frontier），且通过调节β可连续控制分辨率。
- **自适应规则**成功地在Four Rooms、DoorKey中在中等分辨率下恢复近最优性能；在Taxi中则需要较高分辨率（动作压缩是关键瓶颈）。
- **压缩诊断**（表1）表明：
  - Four Rooms主要压缩状态信息；
  - Taxi主要压缩动作信息；
  - DoorKey均衡压缩状态和动作。
- **SysAdmin**实验显示，随问题规模增大，近最优策略可在更低有效信息率下实现，且学到的编码器可解释为局部故障模式（triplet histogram、pair histogram等）和相关干预动作。
- 总之，该方法能**量化任务的内在信息需求**，在大量压缩下仍保持高性能。

### 7. 优点

- **理论创新**：提供了首个联合状态‑动作的自适应抽象形式化框架，将率失真理论引入RL规划，并给出简洁的误差分解和切换规则。
- **灵活性**：软抽象允许连续调节分辨率，且编码器/解码器可分离控制状态和动作信息，支持诊断性分析（哪部分信息是关键瓶颈）。
- **实验设计**：选择的三个经典控制任务和SysAdmin能清晰展示不同压缩类型，SysAdmin的结构化因子分析增加了可解释性。
- **对比透彻**：表1将自适应方法的压缩率与精确双模拟/同态严格对比，突显了自适应方法在保持性能的同时实现更高压缩。
- **代码公开**：论文承诺代码将在github上发布（参见附录G），有助于复现和扩展。

### 8. 不足与局限

- **仅表格环境**：所有实验基于已知模型、有限状态/动作的MDP，未拓展到连续状态/动作、模型未知或函数近似的情况，限制了实际部署。
- **失真度量依赖模型知识**：双模拟失真的计算需要使用MDP转移矩阵和奖励，这在真实RL设置中通常需从样本估计，文中未探讨估计误差对自适应规则的影响。
- **自适应规则简单**：切换准则仅基于残差与失真的大小比较，未考虑多步预测或鲁棒性，可能对噪声敏感。
- **对比方法有限**：未与现代自适应抽象方法（如Ortner 2013的在线聚合、CEGAR）进行直接对比，也未讨论计算复杂度差异（精确双模拟计算开销大，而近似方法可更快）。
- **缺乏统计严谨性**：未报告多次独立实验的统计量（如均值、标准差、置信区间），虽在确定性设定下可接受，但降低了generalization的说服力。
- **可扩展性挑战**：Blahut‑Arimoto算法在全表上运行，当状态‑动作空间庞大或使用深度网络代理时，计算瓶颈可能显著。
- **未涉及在线/样本效率**：论文假定MDP已知，未分析在从样本学习过程中如何联合估计失真和值函数，且未讨论抽象切换在样本采样中的稳定性。

（完）
