---
title: "A Fletcher's Augmented Lagrangian-Based Stochastic First-Order Method for Nonconvex Equality-Constrained Optimization"
title_zh: 基于Fletcher增广拉格朗日的随机一阶方法用于非凸等式约束优化
authors: "Yawen Cui, Qiankun Shi, Xiao Wang, Xiantao Xiao"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28230v1"
tags: ["query:ml-theory"]
score: 8.0
evidence: 面向深度学习训练的非凸优化方法
tldr: "针对非凸等式约束优化问题，现有随机一阶方法因目标和约束的随机性导致高oracle复杂度。本文提出一种基于分解随机搜索方向的Fletcher增广拉格朗日方法，通过最小奇异值的事件分解控制扰动，在二阶导数Lipschitz假设下，达到ε-KKT点的期望总oracle复杂度为O(ε^{-3})，数值实验验证了性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28230v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1697, \"height\": 1155, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28230v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1711, \"height\": 617, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28230v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1716, \"height\": 618, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28230v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1339, \"height\": 416, \"label\": \"Table\"}]"
motivation: 现有随机一阶方法在非凸等式约束优化中因随机性导致高oracle复杂度，尤其约束函数评估成本高。
method: 基于分解随机搜索方向，采用Fletcher增广拉格朗日作为平滑价值函数进行步长选择，并通过最小奇异值的事件分解控制随机雅可比的影响。
result: "在二阶导数Lipschitz假设下，算法达到ε-KKT点的期望总oracle复杂度为O(ε^{-3})（包含随机梯度和约束评估）。"
conclusion: 提出了一种高效随机一阶方法，显著降低了非凸等式约束优化中的oracle复杂度，数值实验验证了有效性。
---

## 摘要
本文研究了非凸等式约束优化问题，其中仅能获得目标函数和约束函数的随机一阶近似。由于目标和约束均存在随机性，现有的大多数随机一阶方法具有较高的预言复杂度，特别是在随机约束函数评估方面。为了解决这个问题，我们提出了一种基于分解随机搜索方向的随机一阶方法，并采用Fletcher增广拉格朗日作为光滑评价函数来选取步长。为了应对随机雅可比矩阵可能失去一致非退化性的情况，我们引入了一种基于最小奇异值的事件分解方法，从而能够控制随机搜索方向中的扰动。在目标和约束函数的二阶导数满足额外利普希茨连续性假设的条件下，我们证明了所提算法在随机梯度和随机约束函数评估方面，以期望总预言复杂度\(\mathcal O(ε^{-3})\)达到随机\(ε\)-KKT点。最后，我们通过数值实验展示了所提方法的性能。

## Abstract
In this paper, we study nonconvex equality-constrained optimization problems in which only stochastic first-order approximations of the objective and constraint functions are available. Owing to the stochasticity in both objective and constraints, most existing stochastic first-order methods incur relatively high oracle complexity, particularly in terms of stochastic constraint function evaluations. To address this issue, we develop a stochastic first-order method based on a decomposed stochastic search direction, and employ Fletcher's augmented Lagrangian as a smooth merit function for step-size selection. To cope with the possible loss of uniform nondegeneracy of the stochastic Jacobian, we introduce an event decomposition based on the smallest singular value, which enables us to control perturbations in the stochastic search direction. Under an additional Lipschitz continuity assumption on the second-order derivatives of the objective and constraint functions, we show that the proposed algorithm attains a stochastic \(ε\)-KKT point with an expected total oracle complexity of \(\mathcal O(ε^{-3})\) in terms of stochastic gradient and stochastic constraint function evaluations. Finally, we present numerical experiments to demonstrate the performance of the proposed method.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机和背景）
- **研究问题**：针对非凸等式约束优化问题，其中目标函数和约束函数只能通过随机采样近似（称为“全随机”约束优化）。现有随机一阶方法在随机约束函数评估方面oracle复杂度较高（通常为\(\mathcal O(ε^{-5})\)或\(\mathcal O(ε^{-6})\)），限制实际应用。
- **动机**：期望找到一种兼具光滑性与精确性的评价函数来降低约束评估复杂度。Fletcher增广拉格朗日函数通过将乘子替换为关于原始变量的连续函数，同时具有光滑性和精确性，适合设计随机算法。
- **整体意义**：提出一种单循环随机一阶方法，在二阶导数Lipschitz假设和强LICQ条件下，实现期望总oracle复杂度\(\mathcal O(ε^{-3})\)（同时涵盖随机梯度与约束函数评估），显著优于现有全随机方法。

### 2. 方法论：核心思想、关键技术细节、算法流程
- **核心思想**：
  - 将搜索方向分解为切向分量（用于目标下降）和法向分量（用于可行性改进）。
  - 使用截断SPIDER型递归估计器构建随机梯度、随机约束雅可比和随机约束值。
  - 以Fletcher增广拉格朗日作为光滑价值函数，用于步长选择和参数自适应更新。
  - 通过基于最小奇异值的事件分解（好事件 vs 坏事件）控制随机雅可比退化带来的扰动。

- **关键技术细节**：
  - **搜索方向**：\(\tilde{s}_k = -P_{\tilde{V}_k}\tilde{\nabla}f_k - w \tilde{\nabla}c_k \tilde{c}_k\)，其中\(P_{\tilde{V}_k}\)是向随机雅可比零空间的投影，\(w\in(0,1)\)。
  - **步长**：\(\eta_k = 1/(4L_k)\)，其中\(L_k\)依赖于自适应更新的惩罚参数\(\rho_k\)。
  - **惩罚参数更新**：\(\rho_k = \max\{ (4L_\lambda^2 + 2w^2 L_c^2 + 2)/(w\chi_k), \rho_{k-1} \}\)，其中\(\chi_k\)根据\(\mu_{\min}(\tilde{\nabla}c_k)\)与\(\nu/2\)的比较取不同值。
  - **事件分解**：根据\(\mu_{\min}(\tilde{\nabla}c_k) \geq \nu/2\)定义好事件\(G_k\)，否则为坏事件。在坏事件上利用概率界控制误差。
  - **SPIDER估计器**：每个epoch开始用大batch，后续用递归更新，并投影到有界球内保证矩有界。

- **算法流程**（Algorithm 2.1）：
  1. 初始化\(x_1\)，惩罚参数\(\rho_0\)，迭代次数\(K\)，epoch长度\(\tau\)。
  2. 对\(k=1..K\)：
     - 若\((k-1)\mod\tau=0\)，通过(2.7)计算\(\tilde{\nabla}f_k,\tilde{\nabla}c_k,\tilde{c}_k\)（大batch）；否则通过(2.8)递归更新。
     - 计算搜索方向\(\tilde{s}_k\) (2.2)。
     - 根据(2.6)更新\(\rho_k\)，再计算\(\eta_k = 1/(4L_k)\)。
     - 更新\(x_{k+1} = x_k + \eta_k\tilde{s}_k\)。
  3. 输出随机均匀选中的\(x_R\)。

### 3. 实验设计
- **数据集/场景**：
  - **CUTEst问题集**：138个等式约束优化问题（维度\(n+m \leq 1000\)），添加零均值高斯噪声模拟随机样本，噪声水平设为\(10^{-2}\)和\(10^{-4}\)。
  - **非凸公平约束分类问题**：基于a9a数据集（LIBSVM，n=123）和loan数据集（LendingClub，n=250），引入等式约束（通过松弛变量将不等式转为等式）和公平性参数。
- **基准与对比方法**：
  - Stoc-iALM [18]（随机不精确增广拉格朗日）
  - SLQPM [1]（单循环动量方法）
  - SSQP [27]（随机序列二次规划）
- **参数设置**：每个方法分配75个参数候选进行调优，基于可行性优先准则选择最佳参数。总样本预算：CUTEst实验为3000个随机样本，公平分类实验为4×10⁴（a9a）和2×10⁴（loan）。

### 4. 资源与算力
- 论文**未明确提及**使用的GPU型号、数量或训练时长。实验主要基于CPU上的小规模问题（CUTEst问题n+m≤1000，公平分类n≤250），且样本预算相对较小，推断未使用大规模算力。

### 5. 实验数量与充分性
- **CUTEst实验**：138个问题，两个噪声水平，共276组比较。报告胜场数、中位数KKT残差、中位数平稳性误差和中位数可行性误差。提供了箱线图展示分布。
- **公平分类实验**：两个数据集（a9a, loan），每个方法独立运行5次（不同随机种子），报告均值与标准差。展示了随时间变化的objective、constraint value、KKT residuals曲线。
- **消融实验**：未进行专门的消融实验（如不进行事件分解、不同\(w\)值等）。
- **公平性**：对比方法均使用相同随机种子和样本预算，参数调优方案明确。但CUTEst实验仅比较一种噪声水平和一种样本预算（3000），样本预算较小，可能不足以反映所有方法在更大预算下的表现。公平分类实验中，每个方法只调了一套固定参数，未进行多组参数比较，可能存在偏差。

### 6. 主要结论与发现
- 提出算法（FSFO）在理论上达到\(\mathcal O(ε^{-3})\)的期望总oracle复杂度（同时优化目标梯度和约束函数评估），这是全随机非凸等式约束问题中的最优结果。
- 在CUTEst实验中，FSFO在胜场数、中位KKT残差等指标上优于SSQP、SLQPM、Stoc-iALM。
- 在公平分类实验中，FSFO在a9a上获得最低目标值和KKT残差，在loan上表现与对比方法相当且略优。
- 验证了Fletcher增广拉格朗日作为光滑价值函数的有效性，以及事件分解处理随机雅可比退化的可行性。

### 7. 优点
- **理论贡献**：首次在全随机非凸等式约束优化中实现\(\mathcal O(ε^{-3})\)的约束函数评估复杂度，匹配了半随机情景下的最好结果。
- **方法设计**：将Fletcher增广拉格朗日引入随机优化，同时利用其光滑性和精确性，避免传统惩罚函数对惩罚参数\(\sim ε^{-1}\)的依赖。
- **事件分解**：通过最小奇异值条件分解好/坏事件，有效处理随机雅可比退化，并给出概率界，使分析更细致。
- **实验验证**：在两种不同类型的问题（优化问题集和机器学习公平性任务）上进行了比较，结果具有说服力。

### 8. 不足与局限
- **额外假设**：需要二阶导数Lipschitz连续（Assumption 1.3），比一般随机一阶方法要求更高，限制了应用范围。
- **实验覆盖有限**：
  - CUTEst实验样本预算仅3000，可能不足以收敛到稳定解；且仅测试两个噪声水平，未对更大噪声或不同分布进行实验。
  - 未进行大规模深度学习实验（如ImageNet、语言模型等），仅在小规模机器学习任务上测试。
  - 未提供消融实验（如无事件分解、不同\(w\)取值、不同epoch长度\(\tau\)的影响）。
- **调参与复现性**：公平分类实验中各方法参数由作者手动调优，可能未能公平对比；未提供开源的完整代码或超参数搜索范围，影响可复现性。
- **复杂度分析**：复杂度界为期望形式，非高概率保证，且依赖于条件二阶矩（非仅平均误差），采样策略需根据局部增量调整，实现稍复杂。
- **应用限制**：仅针对等式约束，对不等式约束需通过松弛变量转化，可能增加问题规模；强LICQ假设（最小奇异值一致下界）在退化问题中不成立。

（完）
