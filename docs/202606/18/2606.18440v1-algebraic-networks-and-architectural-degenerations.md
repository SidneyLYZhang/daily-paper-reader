---
title: Algebraic Networks and Architectural Degenerations
title_zh: 代数网络与架构退化
authors: Giacomo Graziani
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.18440v1"
tags: ["query:ml-theory"]
score: 9.0
evidence: 多项式神经网络的代数几何和神经变体
tldr: 本文研究了无偏置、单项式激活函数的代数神经网络几何。通过引入神经簇框架，将网络架构退化与神经簇奇点关联。对于全连接网络，定义了架构退化轨迹，并证明在层宽非增和标量输出条件下，全参数点光滑，奇异点必为退化参数。
source: arxiv
selection_source: fresh_fetch
motivation: 揭示多项式神经网络中参数空间与函数空间的几何结构，理解神经簇奇点的成因。
method: 构建代数网络框架，定义神经簇、态射与退化轨迹，利用代数几何分析全连接网络。
result: 非递增宽度标量输出全连接网络中，强正则参数对应神经簇光滑点，奇异点蕴含架构退化。
conclusion: 神经簇的奇点由层秩亏或神经元失活导致，为网络可识别性与约简提供几何判据。
---

## 摘要
我们研究了具有单项式激活函数和无偏置的多项式神经网络的几何性质。我们引入了一个代数网络的通用框架，包括其实现映射和相关的仿射神经簇。在此设定下，我们定义了态射、子网络、对称群和商参数空间，并讨论了可辨识性和可约性的几何概念。我们的主要目标是将神经簇的奇点与底层架构的退化联系起来。对于全连接网络，我们将架构退化轨迹定义为允许由具有秩亏损层或非活跃隐藏神经元的参数表示的函数轨迹。我们证明了，对于宽度非递增且输出为标量的全连接网络，在显式的逐层正则性假设下，完全参数给出了相应神经簇的光滑点。特别地，对于这些架构，奇点轨迹包含在架构退化轨迹中。

## Abstract
We study the geometry of polynomial neural networks with monomial activation functions and no bias. We introduce a general framework of algebraic networks, together with their realization maps and associated affine neurovarieties. In this setting we define morphisms, subnetworks, symmetry groups and quotient parameter spaces and we discuss geometric notions of identifiability and reducibility.   Our main goal is to relate the singularities of neurovarieties to degenerations of the underlying architecture. For fully connected networks, we define the architectural degeneracy locus as the locus of functions admitting a representation by parameters with a rank-deficient layer or an inactive hidden neuron. We prove that, for fully connected networks with non-increasing widths and scalar output, full parameters give smooth points of the corresponding neurovariety under explicit layerwise regularity assumptions. In particular, for these architectures, the singular locus is contained in the architectural degeneracy locus.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多项式神经网络（PNN）的参数化函数空间（神经簇）的几何结构，特别是其奇点（singularities）与网络架构退化（architectural degenerations）之间的内在关联。
- **背景与动机**：近年来，神经代数几何（neuroalgebraic geometry）领域试图用代数几何工具理解神经网络的表达能力和优化行为。已有工作表明，神经簇的维度、奇点等几何不变量反映了模型的可识别性和隐式偏置。本文试图将“奇点源于架构退化”这一直观原则形式化，为全连接单项式激活网络建立严谨的几何框架。

### 2. 论文提出的方法论
- **核心思想**：
  - 将无偏置、单项式激活的神经网络建模为**代数网络**（algebraic network），其参数空间为线性层矩阵的乘积，激活为逐坐标幂映射。
  - 定义**实现映射** (realization map) 将参数映射到齐次多项式函数，其Zariski闭包即为**仿射神经簇** (affine neurovariety)。
  - 引入**态射**和**子网络**概念，其中子网络通过包含隐藏神经元子空间自然嵌入。
  - 定义**完全参数** (full parameters) 为所有矩阵满秩且无零列的参数，并定义**架构退化轨迹** (architectural degeneracy locus) 为允许至少一个退化表示的函数集合。
- **关键技术细节**：
  - **对称性与商参数空间**：对于全连接网络，存在由重标度和隐藏层神经元置换构成的对称群G(X)。通过几何不变理论（GIT）构造商参数空间W(X)，实现映射经商诱导出 `φ_X: W(X) → F(X)`。
  - **正则三元组** (regular triple)：引入辅助参数空间Q_{a,B,r}(E) 参数化向量系数幂和的分解，并定义映射的局部环同构条件（正则性）。该条件本质上是**局部Warings可识别性**。
  - **逐层形式重建**：构造中间商空间P_t，利用每层的正则性证明相邻商空间的形式完备局部环同构，最终得到 `\hat{O}_{F(X),Φ(w)} ≅ \hat{O}_{W(X),[w]}`。
  - **主要定理（定理27和35）**：在宽度非增、标量输出且每层正则性满足（r≥3或r=2且宽度相等）时，完全参数的像点光滑。因此奇点必来自秩亏损或零列的退化参数。并推论网络是约简的（reduced）且一般有限可识别。
- **算法/公式**：无实验算法，核心为代数几何构造和证明。

### 3. 实验设计
- **数据集/场景**：无。本文为纯理论数学论文，不包含任何数值模拟或数据集实验。
- **基准（benchmark）**：不适用。文中未涉及与其他方法的对比。
- **对比方法**：无。

### 4. 资源与算力
- **明确说明**：论文未提及任何计算资源、GPU型号、训练时长等。全文无实验部分，因此不涉及算力需求。

### 5. 实验数量与充分性
- **实验数量**：零。论文完全由定义、引理、定理和证明构成，属于理论推导。
- **充分性评价**：从数学角度，证明完整严谨，覆盖了主要目标。但从实证角度，缺乏对理论结论（如奇点包含于退化轨迹）的具体示例或数值验证。因此“实验充分性”不适用，只能评价理论深度。

### 6. 论文的主要结论与发现
- **主要结论**：
  1. 对于满足特定条件的全连接单项式网络（非递增宽度、标量输出、每层正则性），**完全参数点对应的函数必定是神经簇的光滑点**。
  2. **奇点一定对应含秩亏损矩阵或零列的退化参数**，即奇点轨迹包含于架构退化轨迹。
  3. 此类网络是**约简的**（不能被真秩约束子网络完全实现）且**一般有限可识别**（商参数空间到神经簇的映射一般纤维有限）。
- **理论意义**：首次严格证明了神经簇奇点源于架构退化这一原则，为理解深度网络的几何和统计性质提供了代数几何基础。

### 7. 优点
- **框架统一性**：提出的代数网络、子网络、商参数空间等概念适用于更一般架构（卷积、张量网络等），具有潜在推广性。
- **严谨性**：使用完备局部环同构和形式变形理论，给出了光滑性的充分条件，数学证明严密。
- **几何洞察**：将网络可识别性、约简性与奇点分类统一在商参数空间下，揭示了参数对称性、退化与函数空间几何的深层联系。
- **简洁性**：通过引入“正则三元组”将深层网络逐层迭代分析，化繁为简。

### 8. 不足与局限
- **假设较强**：仅限于单项式激活（无偏置）、全连接、宽度非增、标量输出。实际常用网络往往有偏置、ReLU等非线性，且宽度可能是递增的。
- **覆盖不全**：对于r=2但宽度不等的情况，正则性不成立，结果不覆盖；未讨论卷积、注意力等架构。
- **缺乏实验验证**：纯理论结果，未提供数值示例或实际攻击案例来展示奇点与退化的对应，应用价值待挖掘。
- **局限性应用**：标量输出限制较大，多输出情况未涉及；对深度学习实践中广泛存在的过参数化、隐式正则化等现象的几何解释仍待补充。
- **复杂度**：语言高度代数化，对不具备代数几何背景的读者门槛高。

（完）
